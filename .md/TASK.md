# TASK.md — ERP Financeiro (C#)

> **APROVADO v0.1 (usuário, 22/09/2026).** Autor: Coordenador (chapéu Tech Lead). Base: `SDD.md`, `UX-SPEC.md`, ADR 001-008, `PRD.md`, `PRD-TECNICO.md`, `VISAO-PRODUTO.md` (O-xx/S-xx, cronograma), `CTO-REVIEW.md`.
> SDD.md e UX-SPEC.md **não foram alterados**. Nenhuma tarefa exige decisão estrutural nova (ver Seção 6).
> **Legenda de tier:** **[A]** obrigatório · **[B]** só se sobrar tempo (ordem de corte: de baixo para cima no Lote 14) · **[C]** fora do escopo (lista "estacionada" ao final da Seção 3).
> **Status:** `A fazer` / `Em andamento` / `Bloqueada` / `Concluída` (atualizado pelo Executor). **Dono** = chapéu do Executor: `DB` (banco/SQL), `BE` (backend C#), `FE` (Desktop/DevExpress/FastReport), `DOC/OPS` (docs, build, empacotamento), `QA` (teste manual/integração).
> **Premissas [A VALIDAR]** (D-03, D-04, D-05, D-06, D-08, D-10, D-11, contrato v1.1 P-1…P-9) estão explícitas na coluna "Premissa" e resumidas na Seção 4.3.

---

## 1. Diretrizes de Implementação

Regras práticas derivadas do SDD, ADRs, UX-SPEC e GUARDRAILS.md. Vale para toda tarefa.

**Arquitetura e código**
1. Camadas do SDD 2.1; dependência sempre para dentro. Domain sem NuGet. Application sem EF/Web API/DevExpress. Api não referencia Infrastructure. Desktop referencia Infrastructure **somente** no composition root (Autofac).
2. Controller fino: DTO -> comando -> serviço -> DTO de resposta. Toda regra (estado, motivo, validação, total) em Domain/Application. DTO da API nunca é entidade EF. Mapeamento manual (sem AutoMapper).
3. C# 7.3, .NET Framework 4.8. `PlatformTarget` fixo (x86 ou x64 conforme a DLL do Firebird decidida em T-01), nunca AnyCPU.
4. Dinheiro sempre `decimal` (nunca `double`/`float`), inclusive nos DTOs. Datas em UTC no banco/API (`IClock`, nunca `DateTime.Now` no Domain/Application); tela converte para hora local.
5. Sem `async void`; consultas da tela em `Task`, UI thread nunca bloqueada. `DbContext` **por operação/requisição** (escopo Autofac), nunca compartilhado entre API e UI (RT-08).
6. Consultas só via EF/parâmetros; nunca concatenar SQL. `ExecuteSqlCommand` só se T-01 mostrar que o provider falha em ponto específico (SDD 2.5) e sempre parametrizado.
7. Erro ao cliente sempre no envelope `{erro:{codigo,mensagem}}`, sem stack trace/mensagem de exceção. Detalhe só no `IAppLogger` com `correlationId`. `ApiKey` nunca em log nem versionada (só `App.config.example` no repositório).
8. Histórico (`FIN_VENDA_HISTORICO`) só por `INSERT`; repositório expõe apenas `Adicionar`. Venda + itens + histórico na **mesma transação**; `VERSAO` incrementada pela **aplicação**, sem trigger de versão.
9. Idempotência (P-2): repetição devolve o resultado original **sem novo histórico**. Conflito de concorrência: rollback, reler e reavaliar, máximo 3 tentativas, depois 409 `CONFLITO_CONCORRENCIA`.

**Banco (Firebird 3.0)**
10. DDL manual em `database/01-schema.sql` + `02-seed.sql`; sem migrations. Identificadores <= 31 chars (nomear constraints/índices explicitamente), DECIMAL <= 18, `TINYINT` vira `SMALLINT`, `DATETIME2` vira `TIMESTAMP` (UTC). `CLIENTE_ID` e `VALOR_TOTAL` **anuláveis** (ADR-007, D-08 assumida).
11. Um único processo abre o `.fdb` (o Desktop). Nada de segundo processo/ferramenta de teste abrindo o mesmo arquivo com o app aberto (ver T-20: usar o mesmo processo/harness com o app fechado).

**Tela (UX-SPEC)**
12. Só controles DevExpress padrão; único componente novo permitido: `CustomDrawEmptyForeground` (UX-SPEC 3). Qualquer outro componente novo exige marcação "NOVO" no UX-SPEC via Coordenador.
13. Formatação centralizada em `Formatadores` (moeda `N2`, data local `dd/MM/yyyy HH:mm`, status). Cultura pt-BR fixa. Status sempre **ícone + texto** (cor só reforça). Fonte >= 9 pt.
14. Toda tela de [A] entrega os **4 estados** (vazio, carregando, erro, sucesso) ou a justificativa do UX-SPEC 4. Tela é somente leitura (sem quitar/cancelar).
15. A tela **não chama a API por HTTP**: usa `ConsultaService`/`IHealthService` em processo (ADR-008). O relatório usa o **mesmo filtro** e **sem o limite de 5.000**.

**Testes e evidência**
16. Critério de aceite de cada tarefa é verificável por comando/roteiro descrito na própria linha; o Executor registra a evidência (saída/print) na coluna Notas. Bibliotecas de teste: xUnit + Moq (D-11 a validar; se rejeitada, trocar sem impacto em Domain/Application).
17. Pacotes NuGet apenas oficiais; registrar versão + licença de cada um (insumo do README, T-54).

**Bibliotecas obrigatórias/proibidas:** obrigatórias — EF 6.4, `FirebirdSql.Data.FirebirdClient`, `EntityFramework.Firebird`, Web API 2 + `Microsoft.Owin.Host.HttpListener`, Autofac(+WebApi2), Newtonsoft.Json, DevExpress WinForms (trial), FastReport (trial/Open Source). Proibidas — EF Core, AutoMapper, qualquer pacote pago, Serilog em [A] (S-10 é Tier C; usar `TraceLogger` atrás de `IAppLogger`).

**Ordem de corte e regra de precedência:** se o prazo apertar, cortar do fim (Lote 14 de baixo para cima). **Nunca cortar** Lotes 1-13 (Tier A). Dentro de O-09/O-10, o que cai primeiro é acabamento de layout, nunca persistência ou integração (VISAO 6.2). Nada de Tier B antes de T-40 (O-11) concluída.

---

## 2. Spikes Técnicos

| ID | Incerteza | Timebox | Go / No-go | Impacto se falhar |
|---|---|---|---|---|
| **T-01** | EF6 + Firebird 3.0 embarcado: gravar em `FIN_VENDA`, ler DECIMAL(18,2)/(18,4), identity/generator, `Take/Skip`, conflito de concorrência por `VERSAO` incrementada pela aplicação, bitness das DLLs, bloqueio de `.fdb` por segundo processo | ~2h (+1h máximo de extensão) | **Go:** gravação, leitura de DECIMAL e conflito de `VERSAO` funcionam. **No-go:** qualquer falha sem solução em +1h -> fallback SQL Server Express/LocalDB (ADR-002) | Reescrever T-05, T-11, T-12 (e connection string em T-26); Domain/Application/Api/Desktop/Reports intactos (ADR-002/008) |
| T-02 (parte) | FastReport: trial tem preview WinForms? Open Source gera só PDF/HTML? DevExpress: duração e marca d'água do trial | dentro das 1,5h de T-02 | Define ADR-006/008: com preview ou fallback PDF | T-49/T-50 (relatório) mudam de fluxo (já previsto em UX-SPEC 2.3) |

Tarefas sem spike próprio, mas com risco técnico alto marcado: T-27 (bind OWIN), T-38/T-39 (redefinidas em 23/09/2026, ADR-010, como suíte de integração **simulada** — o risco original "sem acesso ao código do Vendas" se concretizou; ver `BLOCKERS.md` Bloqueio 002).

---

## 3. Lista de Tarefas

Estimativa em horas-pessoa do dono. Nenhuma tarefa passa de 3,5h (< 1 dia-pessoa; limite ~8h). "Parale­lizável-com" lista tarefas do **mesmo lote** que podem rodar em paralelo (detalhe na Seção 4). Referências O-xx/S-xx apontam o item original da VISAO-PRODUTO.

### Lote 1 — Spike e preparação (Dia 1 manhã) [A]

| ID | Tarefa | Dono | Tier | Est. | Dep. | Paralelizável-com | Premissa | Critério de aceite | Status |
|---|---|---|---|---|---|---|---|---|---|
| T-01 | **Spike Firebird go/no-go** (O-01 parte 1). Console/projeto descartável: EF6+Firebird embarcado grava e lê `FIN_VENDA` (DECIMAL 18,2), atualização concorrente detectada por `VERSAO`, define bitness, testa segundo processo no `.fdb`. Registrar resultado como **ADR-009** (rascunho do Executor; Coordenador ratifica) e, se no-go, abrir entrada em `BLOCKERS.md` | DB/BE | A | 2h (+1h) | — | T-02, T-03 | ADR-002 | Roteiro executado com evidência: (1) INSERT+SELECT de decimal 1250.50 volta idêntico; (2) dois contextos atualizam a mesma linha, o segundo recebe conflito; (3) bitness registrada; (4) ADR-009 com decisão **GO** ou **NO-GO** explícita. **Bloqueante:** T-04, T-05, T-11, T-12 só iniciam após GO | Concluída — **GO**. Projeto descartável em `spikes/T-01-firebird-spike/` (net48/EF6/`EntityFramework.Firebird`/`FirebirdSql.Data.FirebirdClient`, `PlatformTarget=x64`); evidência em `spikes/T-01-firebird-spike/evidencia-execucao.txt` (4/4 PASS: criação `.fdb` embarcado, CA-1 decimal 1250.50 ida-e-volta, CA-2 conflito de concorrência via `VERSAO` com `DbUpdateConcurrencyException`, CA-3 bitness x64). Decisão registrada em `.md/adr/009-spike-firebird-resultado-go.md` (rascunho do Executor, ratificação pendente do Coordenador) e nota de resultado adicionada em `.md/adr/002-firebird-embarcado-spike-e-fallback.md`. Limitação documentada: critério "segundo processo" simulado com 2 contextos/conexões no mesmo processo (regra 11); violação de `UNIQUE` e `Skip/Take` não exercitados isoladamente, sem indício de risco — revalidar em T-14/T-15/T-23. T-04, T-05, T-11, T-12 liberadas. |
| T-02 | Instalar trials DevExpress/FastReport e confirmar: duração, marca d'água, existência de preview WinForms no FastReport; registrar versões e tipo de licença em `docs/licencas.md` | DOC/OPS | A | 1,5h | — | T-01, T-03 | D-09 | Ambos abrem num projeto WinForms vazio; `docs/licencas.md` lista versão, licença, duração e resposta "tem preview? sim/não"; ADR-006/008 marcado com o caso (preview ou PDF) | Concluída — `docs/licencas.md` criado. **Instalação não realizada** (sandbox sem GUI/instalador interativo); resposta baseada em pesquisa documental oficial (WebSearch/WebFetch, docs.devexpress.com, supportcenter.devexpress.com, fastreports.github.io/FastReport.Documentation, NuGet), limitação declarada explicitamente no documento com ação pendente de confirmação visual quando houver ambiente com GUI. DevExpress WinForms: trial 30 dias, com splash de aviso e watermark em relatórios/exportação. FastReport: Open Source (MIT) não tem preview nativo WinForms e PDF só via plugin; FastReport .NET Trial tem preview "In Application" e PDF nativo, com marca d'água esperada durante avaliação. Decisão registrada para T-49/T-50: preview via FastReport .NET Trial como caminho preferencial, com fallback automático para PDF — já coerente com o texto atual do ADR-008 (não exigiu reescrita do ADR; fica como insumo para o Coordenador ratificar). |
| T-03 | Contrato v1.1 (O-01 parte 2): `docs/contrato-v1.1.md` com endpoints, envelope, códigos, exemplos JSON reais (200/400/401/404/409), campos aditivos, e lista dos pontos **A VALIDAR** (D-03, D-05, D-06, D-08, P-1…P-9); enviar ao lado Vendas e registrar data/resposta. **Não** preencher a linha v1.1 do VISAO 2.4 sem aceite | DOC/OPS | A | 1,5h | — | T-01, T-02 | contrato v1.1, D-03, D-05, D-06, D-08 | Documento existe com >= 1 exemplo JSON por código de erro; registro de envio ao Vendas (data) e de resposta ponto a ponto (aceita/rejeitada); pontos rejeitados viram entrada em `BLOCKERS.md` | Concluída — `docs/contrato-v1.1.md` criado: 3 endpoints v1.0 + 3 aditivos (`POST /api/vendas`, `GET /api/health`, alias `/api/v1`), envelope `{erro:{codigo,mensagem}}`, exemplos JSON 200 (quitação/cancelamento/status) e 400/401/404/409 por endpoint relevante, seção "Pontos A VALIDAR" com D-03/D-05/D-06/D-08/P-1…P-9 (premissa + impacto se rejeitado). Envio ao lado Vendas e aceite ponto a ponto **não realizados** (sem acesso ao Vendas neste ambiente) — registrados no documento como pendência do usuário/orquestrador; linha v1.1 da `VISAO-PRODUTO.md` 2.4 não preenchida (G-5.20) |

> **Lote 1 — Validado (com ressalvas)** em 22/09/2026 pelo Validador
> (`QA-REPORT.md`, `SECURITY-REVIEW.md`). QA: aprovado com ressalvas (achados
> simples, sem reprovação crítica). DevSecOps: aprovado, sem débito de
> segurança. Checagem estrutural: consistente, sem inconsistência que exija
> redesenho. 3 achados simples viraram tarefas em `Refatoração Lote-1`
> (abaixo, fim da Seção 3) — as tarefas T-01/T-02/T-03 permanecem `Concluída`.

### Lote 2 — Esqueleto e Domínio (Dia 1) [A]

| ID | Tarefa | Dono | Tier | Est. | Dep. | Paralelizável-com | Premissa | Critério de aceite | Status |
|---|---|---|---|---|---|---|---|---|---|
| T-04 | Esqueleto da solução (O-02): `ERPFinanceiro.sln`, 7 projetos do SDD 2.1, referências conforme tabela, NuGet, `PlatformTarget` fixo, `.gitignore`, `App.config.example` | BE | A | 2h | T-01 (GO) | T-05 (outro lote) | D-11 (xUnit) | `msbuild` compila a solução vazia sem warning de referência; nenhum projeto viola a tabela de dependências (Domain sem referências; Api sem referência a Infrastructure) | Concluída — `ERPFinanceiro.sln` na raiz (formato clássico `.sln`, gerado com `dotnet new sln -f sln`) com os 7 projetos SDK-style (`net48`, `LangVersion=7.3`, `PlatformTarget=x64` fixo, nunca AnyCPU — `ActiveCfg`/`Build.0` do `.sln` mapeiam Any CPU/x86/x64 sempre para `x64`) em `src/`: `ERPFinanceiro.Domain` (zero `ProjectReference`/`PackageReference`), `ERPFinanceiro.Application` (só refs `Domain`), `ERPFinanceiro.Infrastructure` (refs `Domain`+`Application`; pacotes `EntityFramework` 6.5.1, `EntityFramework.Firebird` 10.1.0, `FirebirdSql.Data.FirebirdClient` 10.3.4, mesmas versões do spike T-01/ADR-009), `ERPFinanceiro.Api` (refs `Domain`+`Application`, **não** referencia Infrastructure; pacotes `Microsoft.AspNet.WebApi.Owin`/`.Core` 5.3.0, `Microsoft.Owin.Host.HttpListener` 4.2.2, `Autofac`/`Autofac.WebApi2`, `Newtonsoft.Json`), `ERPFinanceiro.Reports` (refs só `Application`; `FastReport.OpenSource` 2023.3.13 do NuGet público como base do esqueleto — pacote comercial FastReport .NET Trial não tem feed público instalável neste sandbox, mesma limitação de T-02/RL1-03, documentada no `.csproj`), `ERPFinanceiro.Desktop` (`WinExe`+`UseWindowsForms`; refs `Application`, `Reports`, `Api` e `Infrastructure` — esta última só permitida por ser o composition root Autofac, T-26; `Program.cs` com `Main` mínimo placeholder para permitir compilar o `Exe` vazio; `App.config.example` com `DbProviderFactories`/`entityFramework/providers` manuais e placeholders de porta/connection string/ApiKey/credenciais, conforme "Nota de infraestrutura" do ADR-009 — sem segredo real), `ERPFinanceiro.Tests` (refs só `Domain`+`Application`; `xunit`, `xunit.runner.visualstudio`, `Moq`, `Microsoft.NET.Test.Sdk`, D-11). DevExpress WinForms trial **não** incluído como `PackageReference` em nenhum projeto (feed licenciado `nuget.devexpress.com`, não instalável neste ambiente sandbox — mesma limitação de T-02; nota deixada no `.csproj` do Desktop). `.gitignore` na raiz: `bin/`/`obj/`/`*.fdb` reais e `App.config` real ignorados, mas `!App.config.example` e `!database/*.sql` explicitamente preservados. **Compilação real confirmada** (não só inspeção estrutural): `dotnet build ERPFinanceiro.sln -c Debug` — SDK `dotnet 10.0.400` presente no ambiente — resultado `Compilação com êxito. 0 Aviso(s). 0 Erro(s)` (restore contra NuGet público funcionou, rede disponível). Verificação estrutural adicional da tabela de dependências: `grep` confirma `ERPFinanceiro.Domain.csproj` sem nenhum `ProjectReference`/`PackageReference`, e `ERPFinanceiro.Api.csproj` sem `ProjectReference` a `Infrastructure`. Build artifacts (`bin/`/`obj/`) removidos após a verificação, nada versionado. Limitação de ambiente: **não** foi usado `msbuild` clássico (Developer Command Prompt/VS Build Tools ausentes neste sandbox) — usado `dotnet build` sobre projetos SDK-style `net48`, equivalente ao pedido pela nota de ambiente da tarefa. Nota para Coordenador: `database/01-schema.sql` (T-05, outro dono/lote) já apareceu no worktree durante esta execução (outra instância do Executor rodando em paralelo no mesmo lote) — não foi tocado por esta tarefa. |
| T-07 | Domain: entidades `Venda`, `VendaItem`, `VendaHistorico`; enums `StatusVenda`, `Operacao`; exceções de domínio; `CriarPorQuitacao/CriarPendente` (sem transições) | BE | A | 1,5h | T-04 | — | ADR-007 (campos anuláveis) | Compila; `Venda` com `ClienteId`/`ValorTotal` anuláveis; enums espelham 0/1/2 do SDD 5; nenhuma dependência externa em Domain | Concluída — `src/ERPFinanceiro.Domain/`: `Enums/StatusVenda.cs` (0/1/2), `Enums/Operacao.cs` (0/1/2, espelham CK_FIN_VENDA_STATUS/CK_FIN_VENDA_HIST_OPER do `database/01-schema.sql`); `Exceptions/DomainException.cs`, `VendaJaCanceladaException.cs`, `MotivoObrigatorioException.cs` (classe pronta, regra em si é T-09); `Entities/VendaItem.cs`, `VendaHistorico.cs`, `Venda.cs` com `ClienteId` (`string`) e `ValorTotal` (`decimal?`) anuláveis (ADR-007/D-08) e fábricas estáticas `CriarPendente` (nasce Pendente, 1 registro de histórico Recebida) e `CriarPorQuitacao` (nasce Quitada — fluxo "venda desconhecida" do SDD 2.2 — com histórico Recebida+Quitação); sem `Quitar()`/`Cancelar()` (T-08). `ERPFinanceiro.Domain.csproj` inalterado (zero `PackageReference`/`ProjectReference`, confirmado por leitura do arquivo). `dotnet build src/ERPFinanceiro.Domain`: sucesso, 0 avisos/0 erros. Testes em `src/ERPFinanceiro.Tests/Domain/VendaTests.cs` (xUnit, projeto já referenciava Domain+Application desde T-04): 6 casos cobrindo as duas fábricas (status/datas/histórico gerados) e validação de `VendaItem`; `dotnet test src/ERPFinanceiro.Tests` → 6 aprovados, 0 falhas. Nota de ambiente: o test host `net48` só carrega o adaptador xUnit com `COMPlus_LoadFromRemoteSources=1` (o worktree fica sob um caminho OneDrive, que o .NET Framework 4.8 trata como local remoto/CAS); sem essa variável o `dotnet test` reporta "0 testes" apesar do build ok — registrado aqui para as próximas tarefas BE que rodem `dotnet test` neste ambiente. |
| T-08 | Domain: máquina de estados `Quitar()`/`Cancelar()` (Pendente->Quitada, Pendente->Cancelada, Quitada->Cancelada; Cancelada terminal; repetição idempotente sinalizada) **sem** a regra de motivo | BE | A | 2h | T-07 | — | RN-03/RN-04 | Verificação por console/teste: cada transição válida muda estado e gera item de histórico pendente; Quitar em Cancelada lança `VendaJaCanceladaException`; repetição devolve "sem mudança" | Concluída — `Venda.Quitar(DateTime, correlationId)` e `Venda.Cancelar(DateTime, correlationId)` adicionados (retorno `bool`: true = transicionou, false = idempotente/sem mudança); Cancelada é terminal (Quitar em Cancelada lança `VendaJaCanceladaException`; Cancelar em Cancelada não lança, idempotente); cada transição válida gera `VendaHistorico` (Quitacao/Cancelamento); motivo ainda não implementado (fica para T-09). 8 testes novos em `VendaTests.cs` (15 no total); `dotnet vstest` fora do OneDrive (contorno de T-12): 15 Aprovado, 0 falha. |
| T-09 | Domain: regra S-05 — cancelar venda **Quitada** exige `motivo` não vazio, senão `MotivoObrigatorioException` | BE | A | 1h | T-08 | T-10 | D-06 | Quitada+cancelar sem motivo lança a exceção sem alterar estado; com motivo cancela e guarda motivo; Pendente sem motivo cancela normalmente | Concluída — `Venda.Cancelar(DateTime, string motivo = null, string correlationId = null)` (`src/ERPFinanceiro.Domain/Entities/Venda.cs`): Quitada+motivo nulo/vazio (`IsNullOrWhiteSpace`) lança `MotivoObrigatorioException` sem alterar Status/DataCancelamento/MotivoCancelamento/Versao/histórico; Quitada+motivo guarda em `MotivoCancelamento` e no `VendaHistorico.Motivo`; Pendente continua aceitando motivo nulo (opcional). Assinatura/comportamento de T-08 preservados (idempotência em Cancelada, `VendaJaCanceladaException` em Quitar sobre Cancelada) — só o `Cancelar` mudou, com novo parâmetro `motivo` inserido antes de `correlationId` (chamadas existentes por posição/nome continuam compilando). 4 testes novos em `VendaTests.cs` (Quitada sem motivo lança sem alterar estado; Quitada com motivo em branco lança; Quitada com motivo cancela e guarda; Pendente sem motivo cancela normal) + 1 teste existente (`Cancelar_VendaQuitada_...`) renomeado/ajustado para passar motivo, já que a regra antiga (motivo sempre opcional) deixou de valer para Quitada. Build (`dotnet build src/ERPFinanceiro.Tests`) ok, 0 avisos/erros. Execução real: copiado `src/ERPFinanceiro.Tests/bin/Debug/net48` para `C:\temp_erp_tests\net48` (fora do OneDrive) e rodado `dotnet vstest ERPFinanceiro.Tests.dll` de lá — **sem** reprodução do Bloqueio 001 nesta chamada (`Domain`-filtered: 16 Aprovado, 0 falha, incluindo os 5 casos de Cancelar). Pasta temporária apagada ao final. |
| T-10 | Domain: `Venda.CriarCancelada(vendaId, motivo)` para venda desconhecida (histórico Recebida->Cancelada; sem cliente/valor/itens) | BE | A | 1h | T-08 | T-09 | D-08, ADR-007 | Venda criada com status Cancelada, `ValorTotal`/`ClienteId` nulos, 1 histórico; se D-08 for rejeitada esta função fica sem uso (não apagar até decisão) | Concluída — `Venda.CriarCancelada` implementado (`src/ERPFinanceiro.Domain/Entities/Venda.cs`) + teste xUnit `CriarCancelada_VendaDesconhecida_...` em `VendaTests.cs` cobrindo os 3 critérios (Status Cancelada + ValorTotal/ClienteId nulos, 1 histórico Cancelamento sem StatusAnterior, 0 itens); histórico segue a interpretação já ratificada em `database/02-seed.sql`/V-1003 (só Cancelamento, sem Recebida separada). Execução real confirmada nesta sessão: `dotnet build src/ERPFinanceiro.Tests` ok (0 avisos/erros); binários copiados para `C:\temp_erp_tests\net48` (fora do OneDrive, mesmo contorno de T-09/Bloqueio 001) e `dotnet vstest ERPFinanceiro.Tests.dll --TestCaseFilter:"FullyQualifiedName~Domain"` rodado de lá — **16 Aprovado, 0 falha** de primeira tentativa (sem reprodução do erro do adapter). Pasta temporária apagada ao final. |

> **Lote 2 — Validado** em 22/09/2026 pelo Validador (`QA-REPORT.md`,
> `SECURITY-REVIEW.md`). QA: aprovado, sem ressalvas (0 reprovação, matriz de
> transições Pendente/Quitada/Cancelada x Quitar/Cancelar íntegra contra
> RN-03/RN-04/S-05/D-08). DevSecOps: aprovado, sem débito de segurança
> (`ItensEf`/`HistoricoEf` confirmados `internal`, `InternalsVisibleTo` restrito
> a Infrastructure/Tests, sem vazamento para Api/Desktop/Reports). Checagem
> estrutural: consistente, sem inconsistência que exija redesenho, nenhuma
> tarefa `Bloqueada`. Zero achados — nenhuma tarefa criada em
> `Refatoração Lote-2` (não criado; critério documentado no `SECURITY-REVIEW.md`
> para uso em lote futuro caso surja achado).

### Lote 3 — Banco (Dia 1) [A]

| ID | Tarefa | Dono | Tier | Est. | Dep. | Paralelizável-com | Premissa | Critério de aceite | Status |
|---|---|---|---|---|---|---|---|---|---|
| T-05 | `database/01-schema.sql`: 3 tabelas, PK identity/generator (conforme T-01), UNIQUE(`VENDA_ID`), índices nomeados (`IDX_FIN_VENDA_CLIENTE`, `_STATUS`, `_DATA_REC`), CHECKs de item, FKs (CASCADE nos itens), opcional triggers `BEFORE UPDATE/DELETE` no histórico. **Só SQL** | DB | A | 2h | T-01 (GO) | T-04, T-07 | D-08 (colunas NULL), ADR-005 | Script roda num `.fdb` novo sem erro; todo identificador <= 31 chars (conferido por consulta em `RDB$`); `VALOR_TOTAL` DECIMAL(18,2), `PRECO_UNITARIO` DECIMAL(18,4); sem trigger de `VERSAO` | Concluída — `database/01-schema.sql` criado: `FIN_VENDA` (PK `ID GENERATED BY DEFAULT AS IDENTITY`, `UNIQUE(VENDA_ID)`, `CLIENTE_ID`/`VALOR_TOTAL` anuláveis conforme ADR-007/D-08, `CHECK STATUS IN (0,1,2)`, `VERSAO INTEGER DEFAULT 0` **sem trigger**), `FIN_VENDA_ITEM` (FK `VENDA_REF` `ON DELETE CASCADE`, `CHECK QUANTIDADE>0`, `CHECK PRECO_UNITARIO>=0`), `FIN_VENDA_HISTORICO` (append-only reforçado por triggers `BEFORE UPDATE/DELETE` que lançam `EX_HIST_APPEND_ONLY`); índices `IDX_FIN_VENDA_CLIENTE`/`_STATUS`/`_DATA_REC` + 2 de apoio a FK (`IDX_FIN_VENDA_ITEM_VENDA`, `IDX_FIN_VENDA_HIST_VENDA`); script em formato isql padrão (`SET TERM ^`). **Execução real** (não só inspeção): harness de console descartável (DLLs nativas reaproveitadas de `spikes/T-01-firebird-spike/`) criou um `.fdb` novo, aplicou o script via `FirebirdSql.Data.Isql.FbScript`/`FbBatchExecution` (mesmo mecanismo previsto para o `DbInitializer` de T-12) e validou por consulta `RDB$` + INSERT/UPDATE/DELETE reais — **21/21 PASS** (0 FAIL), evidência em `database/evidencia-execucao-T-05.txt`: script roda sem erro; maior identificador 25 chars (`TRG_FIN_VENDA_HIST_NO_UPD`) <= 31; `VALOR_TOTAL DECIMAL(18,2)` e `PRECO_UNITARIO DECIMAL(18,4)` confirmados via `RDB$FIELDS`; zero triggers em `FIN_VENDA` (sem trigger de `VERSAO`); `UNIQUE(VENDA_ID)` confirmado; `CLIENTE_ID`/`VALOR_TOTAL` anuláveis confirmados; CHECKs de item rejeitam quantidade=0 e preço negativo, aceitam preço=0; FK CASCADE remove itens ao deletar a venda; triggers de histórico bloqueiam UPDATE/DELETE. Harness de teste não versionado (ficou em scratchpad de sessão, análogo ao projeto descartável de T-01) |
| T-06 | `database/02-seed.sql`: massa conhecida (>= 3 vendas: Quitada, Pendente, Cancelada sem quitação, com itens e histórico coerentes) para demo e teste do relatório | DB | A | 1h | T-05 | T-11, T-12 | ADR-007 | Rodar seed após schema não gera erro; soma dos valores da massa documentada em comentário (insumo do CA-07.3) | Concluída — `database/02-seed.sql` criado: V-1001 Quitada (CLI-001, itens PROD-001 2x150.0000 + PROD-002 1x200.5000, VALOR_TOTAL 500.50, histórico Recebida->Quitação), V-1002 Pendente (CLI-002, itens PROD-003 3x100.0000 + PROD-004 2x75.2500, VALOR_TOTAL 450.50, histórico só Recebida), V-1003 Cancelada sem quitação prévia (D-08/ADR-007 "venda desconhecida": CLIENTE_ID/VALOR_TOTAL NULL, **sem itens** por decisão do ADR-007, histórico só Cancelamento com STATUS_ANTERIOR NULL). Soma da massa documentada no cabeçalho do script (CA-07.3, nulos=0): 500.50+450.50+0.00 = **951.00**. Interpretação registrada (desvio pequeno, resolvido pelo Executor): o exemplo ilustrativo "Recebida->Cancelamento" da própria linha da tarefa cede à decisão já ratificada do ADR-007 para o cenário D-08 concreto pedido aqui (sem itens, sem Recebida prévia) — detalhe no cabeçalho do seed. **Execução real confirmada**: harness de console descartável (não versionado, análogo a T-05) reaproveitando as DLLs nativas do spike T-01 (`spikes/T-01-firebird-spike/FirebirdSpike/bin/Debug/net48/`) e `FbScript`/`FbBatchExecution`; criou `.fdb` novo, aplicou `01-schema.sql` + `02-seed.sql` sem exceção, e validou por SELECT — 23/23 PASS (3 vendas, 4 itens, 4 históricos, totais de itens batendo com `VALOR_TOTAL` de V-1001/V-1002, V-1003 nula/sem itens, soma total 951.00). Evidência em `database/evidencia-execucao-T-06.txt`. |
| T-11 | EF6: `FinanceiroDbContext` + mapeamento Fluent das 3 entidades, `VERSAO` como `IsConcurrencyToken` incrementada pela aplicação, nomes de colunas iguais ao DDL | BE | A | 3h | T-05, T-07 | T-06, T-12 | ADR-005 | Contra `.fdb` criado por T-05: inserir venda com 2 itens + histórico e reler sem perda de decimal; atualização com `VERSAO` velha lança conflito | Concluída — `FinanceiroDbContext` criado em `src/ERPFinanceiro.Infrastructure/Persistencia/FinanceiroDbContext.cs`: mapeamento Fluent das 3 entidades batendo com `database/01-schema.sql` (`FIN_VENDA`/`FIN_VENDA_ITEM`/`FIN_VENDA_HISTORICO`, todas as colunas); `VERSAO` com `.IsConcurrencyToken()`; `PRECO_UNITARIO` com `.HasPrecision(18,4)` e `VALOR_TOTAL` `.HasPrecision(18,2)`; `Database.SetInitializer<FinanceiroDbContext>(null)` (sem migrations, regra 10); construtor recebe `DbConnection` já pronta, mesmo padrão do `SpikeContext` do spike T-01/ADR-009. Decisão de encapsulamento (coleções privadas `_itens`/`_historico` de `Venda`, T-07/T-08): EF6 clássico exige navegação `ICollection<T>` mutável, incompatível com a API pública `IReadOnlyList` (Itens/Historico); adicionadas 2 propriedades **internal** só para o EF (`ItensEf`/`HistoricoEf` em `Venda.cs`, apontando para o mesmo backing field), visíveis só para `ERPFinanceiro.Infrastructure`/`ERPFinanceiro.Tests` via `InternalsVisibleTo` (`ERPFinanceiro.Domain.csproj`) — API pública do domínio inalterada, sem setter novo exposto. Mapeadas como associação independente (`HasMany(...).WithRequired().Map(m => m.MapKey("VENDA_REF"))`), sem precisar de propriedade de FK nas entidades filhas. Teste de integração real em `src/ERPFinanceiro.Tests/Infrastructure/Persistencia/FinanceiroDbContextTests.cs` (2 casos, cobrindo os 2 critérios de aceite: round-trip de venda+2itens+histórico sem perda de decimal incluindo `PRECO_UNITARIO DECIMAL(18,4)`; e `DbUpdateConcurrencyException` com `VERSAO` velha) + `App.config` novo em `src/ERPFinanceiro.Tests/` (registro `entityFramework`/`DbProviderFactories`). **Build 100% verde** e **execução real confirmada nesta sessão**: `dotnet build` de `ERPFinanceiro.Infrastructure`/`ERPFinanceiro.Tests` 0 avisos/0 erros; após o usuário desativar o Smart App Control, `dotnet vstest` direto na worktree (OneDrive) ainda falhou ao carregar `xunit.runner.visualstudio.testadapter.dll` — mas com **erro diferente do bloqueio de SAC anterior**: `FileLoadException` HRESULT 0x80131515 ("Operação sem suporte"), típico de restrição de zona/Mark-of-the-Web em pasta sincronizada do OneDrive, não do Controle de Aplicativo. Aplicado o contorno já documentado por T-12 (copiar `bin/Debug/net48` para `C:\temp_erp_tests`, fora do OneDrive, e rodar `dotnet vstest` de lá): **2/2 testes verdes** (round-trip decimal + conflito de concorrência via `DbUpdateConcurrencyException`). Pasta temporária removida ao final. Conclusão registrada para o Coordenador: a causa raiz do Bloqueio 001 era mista — o Smart App Control bloqueava com HRESULT 0x800711C7 e, mesmo desativado, a pasta OneDrive ainda impõe uma restrição de zona separada (0x80131515) que exige o contorno de copiar para fora do OneDrive; o contorno documentado por T-12 permanece necessário e válido para as próximas tarefas BE com teste de integração real. |
| T-12 | `DbInitializer` (Infrastructure): cria `.fdb` se ausente (SYSDBA/senha por configuração), aplica `01-schema.sql` embutido/copiado, sem passo manual (CA-05.5) | BE | A | 2h | T-04, T-05 | T-06, T-11 | ADR-002 | Apagar `.fdb`, rodar inicializador: banco criado com 3 tabelas; segunda execução é no-op; `.fdb` bloqueado gera exceção com mensagem clara (RT-05) | Concluída — `DbInitializer` em `src/ERPFinanceiro.Infrastructure/Persistencia/` (+ `IConfiguracaoBanco` em Application, `ConfiguracaoBancoAppConfig`/`BancoIndisponivelException` em Infrastructure). Credenciais via `IConfiguracaoBanco` injetado (nunca hardcoded); `01-schema.sql` embutido como Embedded Resource (fonte única em `database/01-schema.sql`, decisão documentada no código, robusto para T-53); aplica via `FbScript`/`FbBatchExecution` (mesmo mecanismo de T-05/T-06). 3 cenários do CA executados de fato com Firebird real em teste de integração (`src/ERPFinanceiro.Tests/Infrastructure/Persistencia/DbInitializerTests.cs`, 9/9 testes verdes via `dotnet vstest`): (1) `.fdb` ausente → criado com as 3 tabelas; (2) segunda execução → no-op, sem exceção; (3) `.fdb` existente bloqueado por outro handle (`FileShare.None`) → `BancoIndisponivelException` com mensagem clara citando bloqueio (RT-05). Nota de ambiente: `dotnet test`/`dotnet vstest` direto na worktree (OneDrive) falha ao carregar o test adapter net48 (HRESULT 0x80131515, política de pasta sincronizada) — contorno documentado no cabeçalho do teste (copiar `bin/Debug/net48` para pasta local fora do OneDrive e rodar `dotnet vstest` de lá); não é falha do `DbInitializer`. |

> **Lote 3 — Validado** em 22/09/2026 pelo Validador (`QA-REPORT.md`,
> `SECURITY-REVIEW.md`). QA: aprovado, sem ressalvas (0 reprovação; T-05/T-06
> confirmados por execução real contra Firebird embarcado — 21/21 e 23/23 PASS;
> T-11 confirmado com 2/2 testes de integração reais após a resolução parcial
> do Bloqueio 001, decimal round-trip incluindo DECIMAL(18,4) e conflito de
> concorrência via `DbUpdateConcurrencyException`; T-12 confirmado com 9/9
> testes reais, idempotência e mensagem clara de RT-05). DevSecOps: aprovado,
> sem débito de segurança (credenciais só via `IConfiguracaoBanco`, nunca
> hardcoded em código de produção; `sysdba`/`masterkey` confirmados restritos
> a testes/spike). Checagem estrutural: consistente, sem inconsistência que
> exija redesenho; T-14, T-22 e demais tarefas dos Lotes 4/5 que dependiam de
> T-11/T-12 confirmadas liberadas. Zero achados — nenhuma tarefa criada em
> `Refatoração Lote-3`.

### Lote 4 — Persistência e base da Application (Dia 2) [A]

| ID | Tarefa | Dono | Tier | Est. | Dep. | Paralelizável-com | Premissa | Critério de aceite | Status |
|---|---|---|---|---|---|---|---|---|---|
| T-13 | Application: interfaces `IVendaRepository`, `IUnitOfWork`, `IClock`, `IAppLogger`, `ICorrelationContext`; comandos/resultados (`QuitarVendaCommand` etc.); `FiltroVendas` (tipo, ainda sem lógica) | BE | A | 1,5h | T-07 | — | — | Compila sem referência a EF/Web API; `IVendaRepository` expõe só `Adicionar` para histórico | Concluída — `src/ERPFinanceiro.Application/Interfaces/{IVendaRepository,IUnitOfWork,IClock,IAppLogger,ICorrelationContext}.cs`, `Commands/{QuitarVendaCommand,CancelarVendaCommand,RegistrarVendaCommand,ItemVendaCommand}.cs`, `Consultas/FiltroVendas.cs`. `IVendaRepository` só `ObterPorVendaId`+`Adicionar` (sem update genérico, confirmado por reflexão em teste). `IUnitOfWork.SalvarAlteracoes()` síncrono (decisão documentada no XML doc: I/O assíncrono fica em Infrastructure). Comandos com campos conforme `docs/contrato-v1.1.md` 3.1/3.2/3.4. `dotnet build src/ERPFinanceiro.Application`: 0 erro/aviso, nenhum `PackageReference` novo (csproj inalterado, só `ProjectReference` a Domain). Testes: `src/ERPFinanceiro.Tests/Application/ApplicationContratosTests.cs` (10 casos, xUnit+Moq, sem dependência de Firebird/Infrastructure) — `dotnet vstest` após copiar `bin/Debug/net48` para fora do OneDrive (mesmo contorno do Bloqueio 001): **10/10 verde na 1ª tentativa**, sem reincidência do bloqueio de Smart App Control desta vez. |
| T-14 | `VendaRepository` EF6 (buscar por `VendaId` com itens, adicionar, consulta de leitura `AsNoTracking`) | BE | A | 2h | T-11, T-13 | T-17, T-21 | ADR-005 | Contra `.fdb` real: buscar existente/inexistente; listagem sem tracking; sem SQL concatenado | Concluída — `VendaRepository` (`src/ERPFinanceiro.Infrastructure/Persistencia/VendaRepository.cs`) implementa `ObterPorVendaId`/`Adicionar` (`IVendaRepository`, T-13) com `.Include("ItensEf")`/`.Include("HistoricoEf")` por nome de string; adicionado método auxiliar `ObterParaConsultaSomenteLeitura` (AsNoTracking), fora do contrato, para reaproveito futuro por `ConsultaService` (T-23). 3 testes de integração reais contra `.fdb` (Firebird embarcado, schema via `DbInitializer`/T-12) em `VendaRepositoryTests.cs`: venda existente retorna com itens+histórico; inexistente retorna null; leitura AsNoTracking confirma `EntityState.Detached`. Suíte completa (78 testes) verde via `dotnet vstest` (bin copiado para fora do OneDrive, contorno de rotina). Sem SQL concatenado — só LINQ/EF. |
| T-15 | `UnitOfWork`/transação (venda+itens+histórico atômicos) e tradução de conflito (0 linhas/violação UNIQUE) para `ConcorrenciaException` | BE | A | 2h | T-14 | — | ADR-005 | Falha injetada no histórico faz rollback da venda; conflito de `VERSAO` e violação UNIQUE viram `ConcorrenciaException` | Concluída — `UnitOfWork : IUnitOfWork` em `src/ERPFinanceiro.Infrastructure/Persistencia/UnitOfWork.cs`: `SalvarAlteracoes()` chama `_contexto.SaveChanges()` uma única vez (sem `TransactionScope` manual — o EF6 já agrupa toda a árvore rastreada, venda+itens+histórico, numa transação implícita por chamada; XML doc documenta a regra de uso "uma única `SalvarAlteracoes()` por operação de negócio" para não quebrar essa garantia). `ConcorrenciaException` criada em `src/ERPFinanceiro.Application/Exceptions/ConcorrenciaException.cs` (decisão de local documentada no XML doc da classe: Application, não Domain nem Infrastructure — não é regra de negócio do agregado, e T-16/`ExecutarComRetry` precisa capturá-la de um lado que Infrastructure referencia). Tradução: `DbUpdateConcurrencyException` (0 linhas afetadas, `VERSAO` obsoleta) → `ConcorrenciaException` direto; `DbUpdateException` só quando a cadeia de `InnerException` contém uma `FbException` cuja mensagem bate com violação de PRIMARY/UNIQUE KEY **e** cita a constraint `UQ_FIN_VENDA_VENDA_ID` (database/01-schema.sql) — verificado empiricamente contra Firebird real, não só por suposição: `FbException`/`IscCodes` (10.3.4) não expõe constante pública estável de "unique violation" (confirmado por reflexão contra o assembly do NuGet), a mensagem do engine é o sinal usado. Outras `DbUpdateException` (ex.: violação de CHECK) propagam como estão, sem virar `ConcorrenciaException`. 3 testes de integração reais em `src/ERPFinanceiro.Tests/Infrastructure/Persistencia/UnitOfWorkTests.cs` contra Firebird embarcado (schema via `DbInitializer`/T-12): (1) falha injetada no histórico — `Operacao` fora de 0/1/2 (viola `CK_FIN_VENDA_HIST_OPER`, rejeitada pelo servidor, não pelo cliente EF) — confirma rollback: venda, item e histórico ausentes do banco após a falha (tentativa inicial com Motivo >500 chars foi descartada: o provider Firebird trunca o parâmetro no cliente em vez de lançar, então não exercitava o servidor; documentado no comentário do teste); (2) conflito de `VERSAO` (dois contextos, mesma venda, segundo `SaveChanges` obsoleto) → `ConcorrenciaException`; (3) dois agregados novos com o mesmo `VendaId` em `SaveChanges` separados → segundo lança `ConcorrenciaException` por violação de `UQ_FIN_VENDA_VENDA_ID`. Execução real: `dotnet build src/ERPFinanceiro.Tests` 0 aviso/0 erro; `bin/Debug/net48` copiado para fora do OneDrive (contorno de rotina do Bloqueio 001) e `dotnet vstest` rodado de lá — **3/3 verde** (filtro `UnitOfWorkTests`) e **suíte completa 90/90 verde** (sem regressão nas 87 pré-existentes). Nota para o Coordenador: `ExceptionParaRespostaMapper` (T-28, Api) ainda não mapeia `ConcorrenciaException` para o envelope 409 — por design: a tradução final para o código público `CONFLITO_CONCORRENCIA` é responsabilidade de T-16 (após esgotar as 3 tentativas de retry, regra 9 do TASK.md Seção 1), não desta tarefa. |
| T-16 | Helper Application `ExecutarComRetry` (releitura/reavaliação, máx. 3 tentativas, depois `CONFLITO_CONCORRENCIA`) | BE | A | 1h | T-15 | — | ADR-005 | Teste com repositório falso que conflita 2x e passa na 3ª; 4ª falha propaga | Concluída — `ExecutorComRetry.Executar<T>`/`Executar(Action)` em `src/ERPFinanceiro.Application/Servicos/ExecutorComRetry.cs`: laço captura `ConcorrenciaException`, no máx. 3 tentativas, propaga na 3ª falha (quem traduz para HTTP 409 é a Api, T-28, não alterada). Testes em `ExecutorComRetryTests` (3/3 verde, rodado fora do OneDrive por `BLOCKERS.md` Bloqueio 001): conflita 2x e passa na 3ª (contagem exata = 3), conflita sempre e propaga após exatamente 3 tentativas, e variante `Action`. |
| T-17 | Application: validação de payload e total (CA-01.5 IDs vazios/`itens` vazio/`quantidade`<=0/`precoUnitario`<0; CA-01.6 tolerância 0,01; limites 50/500/1000 itens) | BE | A | 2h | T-13 | T-14, T-21 | P-7, RN-02 | Tabela de casos: cada violação -> `PAYLOAD_INVALIDO` ou `VALOR_TOTAL_DIVERGENTE`; diferença 0,01 aceita, 0,02 rejeitada | Concluída — `src/ERPFinanceiro.Application/Validacao/{ErroValidacao,ResultadoValidacao,ValidadorVendaCommand}.cs`: `ValidadorVendaCommand.Validar(QuitarVendaCommand)`/`Validar(RegistrarVendaCommand)` (mesmo payload, contrato-v1.1.md 3.1/3.4) devolvem `ResultadoValidacao` tipado (nunca lança exceção), cobrindo CA-01.5 (`vendaId`/`clienteId` ausente/vazio, `itens` vazio/nulo, `quantidade`<=0, `precoUnitario`<0, `produtoId` ausente/vazio -> `PAYLOAD_INVALIDO`) e CA-01.6 (`|valorTotal - Σ(quantidade×precoUnitario)| > 0,01` -> `VALOR_TOTAL_DIVERGENTE`, aritmética 100% `decimal`, regra 4). Interpretação adotada para "limites 50/500/1000" (desvio pequeno, documentado no XML doc da classe): a linha da tarefa não detalha o campo; fonte primária `SDD.md` Seção 7 — 50 chars para IDs (`vendaId`/`clienteId`/`produtoId`, validado aqui), 500 para `motivo` (só existe em `CancelarVendaCommand`, fora do escopo de itens/total desta tarefa, não validado aqui) e itens <= 1000 (validado aqui); "corpo <= 1 MB" é camada HTTP (Api), não Application. Testes em `src/ERPFinanceiro.Tests/Application/ValidadorVendaCommandTests.cs` (21 casos: sucesso; cada violação de CA-01.5 isolada incluindo limite de 50 chars e de 1000 itens; diferença de 0,01 aceita e de 0,02 rejeitada com `VALOR_TOTAL_DIVERGENTE`; `RegistrarVendaCommand` com as mesmas regras; comando nulo). `dotnet build` de `Application` e `Tests`: 0 aviso/0 erro. Execução real (Application pura, sem Firebird): copiado `bin/Debug/net48` para fora do OneDrive (mesmo contorno do Bloqueio 001) e `dotnet vstest` rodado de lá na 1ª tentativa — **21/21 Aprovado** (filtro `ValidadorVendaCommand`) e **31/31 Aprovado** (filtro `Application`, incluindo os 10 testes já existentes de T-13), sem reincidência do bloqueio. Pasta temporária apagada ao final. |
| T-21 | `TraceLogger` (Infrastructure) implementando `IAppLogger` (arquivo texto simples via `Trace`); S-10 Serilog **fica Tier C** | BE | A | 1h | T-13 | T-14, T-17 | — | Log grava linha com `correlationId`; teste confirma que `ApiKey` não é logada | Concluída — `TraceLogger` em `src/ERPFinanceiro.Infrastructure/TraceLogger.cs`: `TextWriterTraceListener` registrado em `Trace.Listeners`, caminho do arquivo recebido via construtor (nunca hardcoded; composition root T-26 lê `Log:CaminhoArquivo` do `App.config`, chave já documentada em `App.config.example`). Sem Serilog/lib externa. 4 testes novos em `src/ERPFinanceiro.Tests/Infrastructure/TraceLoggerTests.cs` (grava e lê linha com correlationId; cria diretório ausente; valida caminho obrigatório; documenta que o logger grava a mensagem tal como recebida — "nunca logar ApiKey" é disciplina de quem chama, não do logger). Rodados de fato: 56/56 verdes via `dotnet vstest`, contorno do Bloqueio 001 aplicado (bin/Debug/net48 copiado para fora do OneDrive) na 1ª tentativa. |

> **Lote 4 — Validado (com ressalvas)** em 22/09/2026 pelo Validador
> (`QA-REPORT.md`, `SECURITY-REVIEW.md`). QA: aprovado, sem ressalvas quanto
> ao critério de aceite literal das 6 tarefas (T-13…T-17, T-21), confirmado
> por execução independente da suíte (`dotnet build` + `dotnet vstest` fora
> do OneDrive, rodados pelo próprio Validador, não só a nota do Executor).
> DevSecOps: aprovado com débito de severidade baixa (RL4-01 — tradução de
> conflito de concorrência via mensagem em inglês da `FbException`, sem usar
> `ErrorCode`/`SQLSTATE`; não bloqueia deploy). Checagem estrutural:
> consistente, sem inconsistência que exija redesenho; T-18 e T-19 (Lote 5)
> confirmadas liberadas; T-20 corretamente ainda não liberada (depende de
> T-18/T-19, não deste lote). 2 achados simples viraram tarefas em
> `Refatoração Lote-4` (RL4-01, RL4-02 — abaixo, fim da Seção 3); RL4-02
> (ausência real de `xunit.runner.json`, apesar da nota de T-23 afirmar o
> contrário) é um **risco real para a execução confiável de T-20** e deve ser
> resolvida antes de T-20 iniciar (reproduzido pelo Validador: 2/2 execuções
> sem o arquivo falharam por `AccessViolationException` intermitente do
> Firebird Embedded sob paralelização; 2/2 com o arquivo passaram limpas,
> 93/93 testes).

### Lote 5 — Serviços de negócio (Dia 2 tarde) [A]

| ID | Tarefa | Dono | Tier | Est. | Dep. | Paralelizável-com | Premissa | Critério de aceite | Status |
|---|---|---|---|---|---|---|---|---|---|
| T-18 | `QuitacaoService` (O-05 + S-04): valida (T-17), busca; inexistente cria+quita (upsert RN-06), Pendente quita, Quitada devolve original sem histórico, Cancelada -> 409; usa UoW+retry; histórico com `correlationId` | BE | A | 3,5h | T-08, T-14, T-15, T-16, T-17, T-21 | T-19, T-22, T-23, T-24 | P-2, D-03 (ramo Pendente), P-7 | Roteiro contra `.fdb` real cobre CA-01.1, 01.2, 01.3, 01.5, 01.6 (CA-01.4 verificável no domínio; via API só com T-62); 1 linha de histórico por transição real | Concluída — `src/ERPFinanceiro.Application/Servicos/{QuitacaoService,ResultadoQuitacao}.cs`: valida com `ValidadorVendaCommand` (T-17) e lança nova `ERPFinanceiro.Application.Exceptions.ValidacaoException` (primeiro erro da lista — decisão documentada no XML doc da classe); busca via `IVendaRepository.ObterPorVendaId`; inexistente -> `Venda.CriarPorQuitacao` (T-07, upsert RN-06) com `DataRecebimento`=`DataQuitacao`=`IClock.UtcNow`; Pendente -> `venda.Quitar(...)` (T-08), com checagem de divergência (clienteId exato, valorTotal com a mesma tolerância 0,01 de T-17, itens por conjunto produtoId/quantidade/precoUnitario) que lança nova `DadosDivergentesException` (409 `DADOS_DIVERGENTES`, D-03/I-04) antes de quitar; já Quitada -> `Quitar()` já devolve `false` (T-08), sem chamar `SalvarAlteracoes` (P-2, sem histórico novo); Cancelada -> `Venda.Quitar` já lança `VendaJaCanceladaException` (T-08/T-28, 409). Todo o ciclo buscar+decidir+salvar roda dentro de `ExecutorComRetry.Executar` (T-16). **Decisão de arquitetura (desvio pequeno, documentado no XML doc de `Application.Exceptions.ValidacaoException`):** a instrução pedia lançar "`ValidacaoException` (já existe, criada em T-28)", mas essa classe vive em `ERPFinanceiro.Api.Erros` e `ERPFinanceiro.Application.csproj` só referencia `ERPFinanceiro.Domain` (SDD 2.1) — logo Application não pode lançar um tipo da Api (não compila). Criada uma `ValidacaoException` equivalente em `ERPFinanceiro.Application.Exceptions` (mesma forma: carrega `ErroValidacao`); `ExceptionParaRespostaMapper` (T-28, `src/ERPFinanceiro.Api/Erros/ExceptionParaRespostaMapper.cs`) foi **atualizado** com 2 novos `case` (`Application.Exceptions.ValidacaoException` -> 400 e `DadosDivergentesException` -> 409 `DADOS_DIVERGENTES`), com 3 testes novos em `ExceptionParaRespostaMapperTests` — a classe `ValidacaoException` original da Api permanece intacta, sem uso ainda (reservada para T-30/T-32). **Risco conhecido documentado (não resolvido nesta tarefa, sinalizado para T-20/Coordenador):** no ramo "criar" (upsert), se a 1ª tentativa falhar por violação de `UNIQUE(VENDA_ID)` (corrida de criação concorrente), a entidade `Added` zumbi continua rastreada pelo mesmo `DbContext` e uma releitura na 2ª tentativa materializa uma 2ª instância da mesma linha — o retry poderia falhar repetidamente (falso 409 `CONFLITO_CONCORRENCIA` em vez de sucesso idempotente) nesse cenário específico de corrida; ver comentário detalhado no `<remarks>` de `QuitacaoService`. Corrigir exigiria uma fábrica de `DbContext` por tentativa de retry (decisão de composição, T-26), fora do escopo de T-18. Testes (Firebird real) em `src/ERPFinanceiro.Tests/Application/QuitacaoServiceTests.cs` (7 casos): inexistente cria+quita (2 linhas de histórico, Recebida+Quitacao, `correlationId` gravado); Pendente quita (2 linhas de histórico); já Quitada devolve `dataQuitacao` original sem novo histórico; Cancelada lança `VendaJaCanceladaException` sem alterar dados; payload inválido (`itens` vazio) lança `ValidacaoException` `PAYLOAD_INVALIDO` sem persistir; valorTotal divergente lança `ValidacaoException` `VALOR_TOTAL_DIVERGENTE` sem persistir; Pendente com `clienteId` divergente lança `DadosDivergentesException` sem alterar dados (CA-01.4 coberto no domínio, como já previsto na premissa da própria linha). `dotnet build` da solução: 0 erro, só os 4 avisos pré-existentes de estilo xUnit1031 em `GlobalExceptionHandlerTests`. Execução real (contorno do Bloqueio 001, bin copiado para fora do OneDrive): filtro `QuitacaoServiceTests` 7/7 e filtro `ExceptionParaRespostaMapperTests` 10/10 na 1ª tentativa; suíte completa 108 testes — 1ª e 2ª rodadas com paralelização padrão tiveram 1 falha cada (`AccessViolationException` do Firebird Embedded, em testes *diferentes* a cada rodada — `QuitacaoServiceTests`/`UnitOfWorkTests` —, RL4-02 conhecido, `xunit.runner.json` ainda ausente no projeto); com `xunit.runner.json` temporário (`parallelizeTestCollections/Assembly: false`) na pasta de execução, **108/108 Aprovado**, confirmando que a falha intermitente não é regressão desta tarefa. |
| T-19 | `CancelamentoService` (O-06): Pendente cancela, Quitada exige motivo (T-09), Cancelada idempotente sem histórico novo, desconhecida cria Cancelada (T-10) | BE | A | 3h | T-09, T-10, T-14, T-15, T-16 | T-18, T-22, T-23, T-24 | D-06, D-08, P-2 | Roteiro cobre CA-02.1…02.6; venda desconhecida grava 1 histórico e `ValorTotal` nulo | Concluída — `CancelamentoService` em `src/ERPFinanceiro.Application/Servicos/CancelamentoService.cs` (+ `ResultadoCancelamento.cs`, mesmo padrão de `VendaStatusDto`/T-24: `Status` como enum, conversão PascalCase fica a cargo da Api/T-32). Fluxo único `Cancelar(CancelarVendaCommand)` roda dentro de `ExecutorComRetry` (T-16): busca por `VendaId` (`IVendaRepository.ObterPorVendaId`, T-14) e reavalia tudo a cada tentativa (releitura nunca reaproveita o agregado de uma tentativa anterior, para enxergar corretamente uma venda concorrente já criada/cancelada por outra requisição). Pendente cancela via `venda.Cancelar(...)` com motivo opcional (T-09); Quitada exige motivo — ausência propaga `MotivoObrigatorioException` (T-09) sem alterar nada, sem interceptação aqui (mapeada a 409 `MOTIVO_OBRIGATORIO` por T-28); já Cancelada é idempotente via o próprio `venda.Cancelar` (retorna `false`, sem novo histórico, T-08/T-09); desconhecida (`ObterPorVendaId` retorna null) cria via `Venda.CriarCancelada` (T-10, D-08/ADR-007). `IUnitOfWork.SalvarAlteracoes()` chamado uma única vez por tentativa; `correlationId` do comando repassado ao histórico (via `venda.Cancelar`/`CriarCancelada`) e a um log opcional (`IAppLogger`, parâmetro de construtor nulável — ausência de logger nunca bloqueia a operação). Validação de payload (`vendaId` vazio etc.) deliberadamente fora desta tarefa — fica com a Api/controller (T-32); aqui só `ArgumentNullException`/`ArgumentException` para uso incorreto do próprio serviço. `dotnet build` de Application e Tests: 0 erro/0 aviso novo. Testes de integração reais em `src/ERPFinanceiro.Tests/Infrastructure/Persistencia/CancelamentoServiceTests.cs` (fica em Infrastructure.Persistencia, não Application, porque exercita `VendaRepository`/`UnitOfWork`/Firebird real — Application não pode referenciar EF, SDD 2.1) — 5 casos cobrindo CA-02.1…02.6: Pendente cancela sem motivo; Quitada com motivo cancela (histórico com motivo); Quitada sem motivo lança `MotivoObrigatorioException` sem alterar Status/MotivoCancelamento/quantidade de histórico; já Cancelada idempotente (2º cancelamento não duplica histórico, `Assert.Single` no `Operacao.Cancelamento`); desconhecida cria Cancelada com `ClienteId`/`ValorTotal` nulos, 0 itens e exatamente 1 histórico (`StatusAnterior` nulo) — todas as asserções por releitura em uma conexão/contexto novos, contra o `.fdb` real (não o objeto em memória da chamada). Execução real confirmada nesta sessão: `bin/Debug/net48` copiado para fora do OneDrive (contorno de rotina do Bloqueio 001) e `dotnet vstest` rodado de lá — **5/5 Aprovado** (filtro `CancelamentoServiceTests`) e **suíte completa 98/98 Aprovado** (sem regressão), ambos na 1ª tentativa, sem reprodução do `AccessViolationException` intermitente (RL4-02). Pasta temporária apagada ao final. |
| T-22 | `IHealthService` (`SELECT 1`) retornando `ok/falha` + mensagem resumida | BE | A | 1h | T-11, T-13 | T-18, T-19, T-23, T-24 | P-5 (SDD 2.6) | Com `.fdb` acessível retorna ok; com caminho inválido retorna falha sem lançar | Concluída — `IHealthService`/`ResultadoHealth` em `src/ERPFinanceiro.Application/Interfaces/{IHealthService,ResultadoHealth}.cs`; implementação `HealthService` (`src/ERPFinanceiro.Infrastructure/Persistencia/HealthService.cs`) abre conexão Firebird própria (Embedded, curta, via `IConfiguracaoBanco`) e executa `SELECT 1 FROM RDB$DATABASE`; `ObterStatus()` nunca lança — qualquer exceção vira `ResultadoHealth.ComFalha(ex.Message)`, sem stack trace. 2 testes em `src/ERPFinanceiro.Tests/Infrastructure/Persistencia/HealthServiceTests.cs`: `.fdb` real (via `DbInitializer`) retorna `Ok=true`; caminho com diretório inexistente retorna `Ok=false` sem lançar. Rodados de fato (contorno do Bloqueio 001, bin copiado para fora do OneDrive): suíte completa 80/80 verde via `dotnet vstest` (1 falha transitória de `FinanceiroDbContextTests` na 1ª rodada — `AccessViolationException` conhecida de concorrência do Firebird Embedded nativo, já presente antes desta tarefa, não relacionada a T-22 — confirmada não-reincidente na 2ª rodada, 80/80). `dotnet build` de Application/Infrastructure/Tests: 0 erro/0 aviso novo. |
| T-23 | `ConsultaService.Listar(FiltroVendas)`: leitura sem tracking, ordenação `DataRecebimento` desc, limite 5.000 com flag de truncamento, DTO de listagem com nulos preservados (tela mostra "—"); filtros **ainda não aplicados** (T-57, Tier B) | BE | A | 2h | T-14, T-13 | T-18, T-19, T-22, T-24 | D-10 | Com seed (T-06): retorna todas em ordem correta; com 5.001 linhas de teste retorna 5.000 e flag `Truncado=true` | Concluída — `Listar` adicionado a `ConsultaService` (`src/ERPFinanceiro.Application/Consultas/ConsultaService.cs`, mesma classe de T-24 — arquivo compartilhado, coordenado por leitura prévia). Leitura via nova interface `IVendaConsultaLeitura` (`Application/Interfaces`), separada de `IVendaRepository` para não violar o contrato de 2 métodos já travado por `ApplicationContratosTests` (T-13); implementada por `VendaRepository` (`ListarMaisRecentes`, AsNoTracking, `OrderByDescending(DataRecebimento).Take(n)`, sem Include de Itens/Historico — não usados pela grade). `ConsultaService` ganhou parâmetro de construtor opcional `IVendaConsultaLeitura leitura = null` (construtor de T-24 preservado sem quebra). Pede `LimiteListagem+1` (5001) para detectar truncamento sem 2ª query. DTO novo `VendaListagemDto` + wrapper `ResultadoListagemVendas` (Itens/Truncado), nulos de `ClienteId`/`ValorTotal` preservados. Testes de integração (Firebird real) em `src/ERPFinanceiro.Tests/Infrastructure/Persistencia/ConsultaServiceListarTests.cs`: massa equivalente ao seed T-06 (3 vendas, ordem desc confirmada, nulos da Cancelada-desconhecida preservados) e massa de 5.001 linhas (insert em lote via ADO.NET) confirmando 5.000 + `Truncado=true`. Evidência: suite completa 87/87 verde (`dotnet vstest`, copiado para fora do OneDrive; parallelização xUnit desligada via `xunit.runner.json` — com parallelização padrão, 3 testes de Firebird embarcado falham por `AccessViolationException` sob concorrência nativa, falha pré-existente do harness de teste não introduzida por esta tarefa, revalidar se afetar CI). |
| T-24 | `ConsultaService.ObterDetalhe(vendaId)` (cabeçalho + itens; histórico carregado mas **fora da tela**, Tier C) e `ObterStatus(vendaId)` (insumo do GET status) | BE | A | 2h | T-14, T-13 | T-18, T-19, T-22, T-23 | RF-03 | Detalhe traz itens com subtotal; venda inexistente retorna nulo/exceção `VendaNaoEncontrada`; venda cancelada sem itens retorna lista vazia | Concluída — `src/ERPFinanceiro.Application/Consultas/{ConsultaService,VendaDetalheDto,VendaItemDetalheDto,VendaStatusDto}.cs`: `ConsultaService.ObterDetalhe`/`ObterStatus` usam `IVendaRepository.ObterPorVendaId` (T-14, já inclui itens+histórico); histórico carregado no agregado mas não copiado para `VendaDetalheDto` (RF-03/Tier C). Decisão de padrão (ambos os lados do critério de aceite são válidos): venda inexistente **lança `VendaNaoEncontradaException`** (reaproveitada de T-28, sem criar tipo novo) em vez de nulo, por consistência com o `GlobalExceptionHandler` (T-28, já mapeia essa exceção -> 404 `VENDA_NAO_ENCONTRADA`) e com o critério de aceite de T-31 (GET status, mesmo padrão). Subtotal por item = `Quantidade * PrecoUnitario`; `TotalItens` = soma dos subtotais (0 para lista vazia). Venda cancelada sem itens (D-08/ADR-007, ex. V-1003 do seed T-06) retorna `Itens` vazia sem erro — comportamento normal, não exceção. Arquivo `ConsultaService.cs` criado por esta instância (T-23, paralela, ainda não tinha criado o arquivo até este ponto — só `FiltroVendas.cs` existia); comentário no topo da classe documenta que T-23 adiciona `Listar` à mesma classe depois, sem remover estes métodos. Testes em `src/ERPFinanceiro.Tests/Application/ConsultaServiceTests.cs` (5 casos, `IVendaRepository` mockado com Moq — lógica pura, sem depender de `.fdb` real, que já é coberta por `VendaRepositoryTests`/T-14): detalhe com itens espelhando V-1001 do seed (subtotal/total corretos), detalhe de venda cancelada sem itens espelhando V-1003 (lista vazia, sem erro), detalhe/status de venda inexistente (ambos lançam `VendaNaoEncontradaException` com `VendaId` correto), status de venda existente. `dotnet build` de Application e Tests: 0 erro (Tests com os mesmos 4 avisos de estilo xUnit1031 pré-existentes, sem relação com esta tarefa). Execução real: bin/Debug/net48 do Tests copiado para fora do OneDrive (mesmo contorno do Bloqueio 001) e `dotnet vstest` rodado de lá — **5/5 Aprovado** (filtro `ConsultaServiceTests`) na 1ª tentativa. Pasta temporária apagada ao final. |
| T-20 | Teste de integração de **concorrência e idempotência** (S-04) contra Firebird real: 2 quitações simultâneas mesma `vendaId` (Tasks), repetição sequencial, quitar/cancelar concorrentes. Harness xUnit/console, app fechado | QA/BE | A | 2,5h | T-18, T-19 | — | ADR-005 | 20 execuções: sempre 1 venda, 1 histórico de quitação, nenhuma exceção não tratada; ambas respostas sucesso idempotente ou 409; evidência salva | Concluída — Pré-requisito RL4-02 resolvido primeiro (ver linha própria em `Refatoração Lote-4`). Harness em `src/ERPFinanceiro.Tests/Infrastructure/Persistencia/ConcorrenciaIdempotenciaTests.cs` (1 `[Fact]`, app fechado, só `Task`/threads dentro do processo de teste, regra 11): **Cenário 1** (critério de aceite literal) — 20 execuções de 2 `QuitacaoService.Executar` concorrentes (`Task.WhenAll`) para a mesma `vendaId` inexistente (corrida de criação/upsert RN-06), cada tentativa com seu próprio `FinanceiroDbContext`/escopo de operação (réplica fiel de RT-08/regra 5/T-26 — um `DbContext` por operação; dentro de cada operação, o próprio `ExecutorComRetry`, T-16, reaproveita esse mesmo contexto entre as até 3 tentativas de retry, que é o cenário exato do risco documentado). **Cenário 2**: repetição sequencial (idempotência, P-2). **Cenário 3**: quitação e cancelamento concorrentes na mesma venda Pendente (5 execuções adicionais). Critério de aceite verificado nas 20 execuções do Cenário 1: sempre exatamente 1 venda e 1 histórico de `Operacao.Quitacao` ao final (`Assert` por releitura real do `.fdb`, não do objeto em memória); toda exceção fora de `ConcorrenciaException`/`VendaJaCanceladaException`/`MotivoObrigatorioException` propaga e falharia o teste (nenhuma ocorreu). **Risco documentado no `<remarks>` de `QuitacaoService.cs` (T-18): não reproduzido nesta execução** — nas 3 rodadas completas realizadas (1 isolada + 2 dentro da suíte completa), as 20 corridas de criação concorrente sempre resolveram com sucesso duplo idempotente (nenhuma `ConcorrenciaException` observada); registrado como resultado real observado, não como garantia de que o risco não existe (achado permanece válido/sinalizado para T-26, é uma corrida de timing que pode não se manifestar em toda execução). Evidência completa (log com resultado de cada uma das 20+1+5 execuções) salva em `src/ERPFinanceiro.Tests/Infrastructure/Persistencia/evidencia-execucao-T-20.txt`. Execução real: `dotnet build` 0 erro/aviso novo; bin copiado para fora do OneDrive (contorno do Bloqueio 001) — filtro `ConcorrenciaIdempotenciaTests` verde em 3 execuções isoladas seguidas (nenhuma flakiness), e suíte completa **109/109 Aprovado em 2 rodadas seguidas** (108 pré-existentes + este harness), confirmando também que RL4-02 segue resolvido com o teste novo incluído. |

> **Lote 5 — Validado (com ressalvas)** em 22/09/2026 pelo Validador
> (`QA-REPORT.md`, `SECURITY-REVIEW.md`). QA: aprovado com ressalvas (0
> reprovação crítica/simples contra critério de aceite literal; 1 achado de
> correção funcional sob concorrência, não de critério de aceite — ver T-18/
> T-19/T-20). DevSecOps: aprovado, sem débito de segurança bloqueante (o
> achado acima não é de segurança; 1 nota de atenção preventiva para a
> auditoria futura de T-36, sem tarefa agora). RL4-02 (débito do Lote 4)
> confirmado **definitivamente resolvido** por execução independente do
> Validador (não só nota do Executor): suíte completa 109/109 em 2 rodadas
> seguidas + harness `ConcorrenciaIdempotenciaTests` isolado 3x seguidas, sem
> `AccessViolationException`. Checagem estrutural: consistente, sem
> inconsistência que exija redesenho; T-30/T-31/T-32/T-36/T-42/T-44/T-48
> (lotes futuros que dependem deste) confirmadas apontando para dependências
> `Concluída`, sem referência quebrada. 1 achado vira tarefa em
> `Refatoração Lote-5` (RL5-01 — abaixo, fim da Seção 3): risco documentado
> no `<remarks>` de `QuitacaoService.cs` (entidade "zumbi" rastreada pelo
> mesmo `DbContext` entre tentativas de `ExecutorComRetry`, na corrida de
> **criação** concorrente da mesma `vendaId` — upsert RN-06), não reproduzido
> em >= 26 execuções (20 de T-20 + 3 do Validador + rodadas de suíte
> completa), mas estruturalmente real; mesmo risco também no ramo "criar" de
> `CancelamentoService` (não coberto pelo harness de T-20, que só testa
> criação concorrente via quitação). Prioridade **alta** (não crítica — não
> reproduzida, não bloqueia este lote), prazo **antes de T-26 fechar**
> (composition root — ponto certo de dar um `DbContext` por tentativa de
> retry).

### Lote 6 — API base e hospedagem (Dia 3) [A]

| ID | Tarefa | Dono | Tier | Est. | Dep. | Paralelizável-com | Premissa | Critério de aceite | Status |
|---|---|---|---|---|---|---|---|---|---|
| T-25 | Api: `Startup` OWIN + `HttpConfiguration` (attribute routing, JSON: UTF-8, camelCase, decimal como número, datas ISO 8601 UTC), `ICorrelationContext` da Api, integração `Autofac.WebApi2` | BE | A | 2,5h | T-04, T-13 | — | P-6, D-04 | Endpoint de eco temporário responde JSON com decimal `1250.5` e data terminando em `Z`; removido antes de fechar | Concluída — `src/ERPFinanceiro.Api/Startup.cs` (`Configuration(IAppBuilder)`: `MapHttpAttributeRoutes`, JSON UTF-8/camelCase/`DateTimeZoneHandling.Utc`, container Autofac vazio documentado p/ T-26) + `CorrelationContext.cs` (`ICorrelationContext` via `CallContext.LogicalSetData/GetData`, `Definir()` p/ uso futuro do `CorrelationIdHandler` de T-29); sem endpoint de eco manual — critério de aceite coberto por `StartupTests` (4 testes: decimal como número, data `Z`, conversão p/ UTC, camelCase) em `src/ERPFinanceiro.Tests/Api/StartupTests.cs`, 4/4 verde via contorno do Bloqueio 001 (build fora do OneDrive + `dotnet vstest`, 1ª tentativa) — nota em `BLOCKERS.md` |
| T-26 | Desktop: **composition root Autofac** (registro de todas as interfaces, escopo por requisição/operação) + `App.config` (porta, connection string, `ApiKey`, credenciais do banco) | BE | A | 2h | T-14, T-15, T-21, T-25 | T-28, T-29 | D-04 | App sobe e resolve `QuitacaoService` sem exceção; `App.config.example` no repo, `App.config` real ignorado no git | Concluída — `src/ERPFinanceiro.Desktop/CompositionRoot.cs`: registra `IConfiguracaoBanco`->`ConfiguracaoBancoAppConfig`, `IClock`->`RelogioSistema` (nova, `src/ERPFinanceiro.Infrastructure/RelogioSistema.cs`, `DateTime.UtcNow`), `ICorrelationContext`->`ERPFinanceiro.Api.CorrelationContext` (Desktop já referenciava Api), `IAppLogger`->`TraceLogger` (caminho via `Log:CaminhoArquivo`), `IHealthService`->`HealthService`, `IVendaRepository`/`IVendaConsultaLeitura`->`VendaRepository`, `IUnitOfWork`->`UnitOfWork` — estes 3 últimos e o `FinanceiroDbContext` por trás deles com **`InstancePerLifetimeScope`** (nunca `SingleInstance`, regra 5 da Seção 1/RT-08); `QuitacaoService`/`CancelamentoService`/`ConsultaService` também `InstancePerLifetimeScope`. Registro de controllers da Api (`Autofac.WebApi2`) e o bind do container ao host OWIN ficam para T-27 (ainda não implementada) — fora do escopo aqui, documentado no XML doc da classe. `App.config.example` (`src/ERPFinanceiro.Desktop/`) conferido completo: porta (`Api:Porta`), connection string (`connectionStrings/FinanceiroDbContext`), `ApiKey` placeholder (`Api:ApiKey`), credenciais do banco (`Banco:Usuario`/`Banco:Senha`/`Banco:CaminhoFdb`) e caminho de log (`Log:CaminhoArquivo`) — já completo desde T-04/T-12/T-21/T-25, nenhuma chave faltando; `.gitignore` da raiz confirmado cobrindo `App.config` real com exceção explícita `!App.config.example`. **RL5-01 resolvida aqui** (ver linha própria abaixo, mesma decisão de composição): `QuitacaoService`/`CancelamentoService` deixaram de receber `IVendaRepository`/`IUnitOfWork` diretos e passaram a receber `IFabricaEscopoOperacao` (novas interfaces `IEscopoOperacao`/`IFabricaEscopoOperacao` em `Application/Interfaces/`, implementação `FabricaEscopoOperacaoEf` em Infrastructure) — abordagem (a) do enunciado: cada tentativa de `ExecutorComRetry` (T-16) chama `_fabricaEscopo.Abrir()` dentro do próprio callback, abrindo um `DbContext` novo por tentativa (`using`), nunca reaproveitado. Testes de T-18/T-19/T-20 adaptados (9 pontos de construção de serviço trocados de `repositorio+unitOfWork` para `new FabricaEscopoOperacaoEf(_configuracao)`) sem perda de cobertura — todos os cenários originais continuam verdes. Critério de aceite verificado por teste de integração real (não só inspeção): `src/ERPFinanceiro.Tests/Desktop/CompositionRootTests.cs` (4 casos) — `builder.Build()`+`scope.Resolve<QuitacaoService>()` não lança; resolve também `CancelamentoService`/`ConsultaService`/`IHealthService`; confirma `InstancePerLifetimeScope` (mesma instância de `IVendaRepository` dentro do mesmo escopo, instância nova em escopo novo) e que `IFabricaEscopoOperacao.Abrir()` sempre devolve um escopo/repositório novo. Execução real (contorno de rotina do Bloqueio 001, bin copiado para fora do OneDrive): suíte completa **114/114 Aprovado em 2 rodadas seguidas** (108 pré-existentes + 4 `CompositionRootTests` + 1 teste determinístico de RL5-01 + ajuste líquido dos testes adaptados), e o subconjunto `ConcorrenciaIdempotenciaTests`+`CompositionRootTests` isolado **3x seguidas sem falha** (6/6 cada vez). |
| T-27 | Host OWIN no Desktop (O-08): `ApiHost.Start/Stop`, bind `http://localhost:{porta}/`, falha de bind captura e expõe estado (`Iniciando/Ativa/Inativa - porta em uso`) e último erro | BE | A | 2h | T-25, T-26 | — | D-04 (ADR-004) | Subir em porta livre responde 200 num GET de teste; porta ocupada não derruba o app e reporta `Inativa - porta em uso`; `Stop` libera a porta | Concluída — `src/ERPFinanceiro.Desktop/ApiHost.cs`: `enum EstadoApiHost{Inativa,Iniciando,Ativa,InativaPortaEmUso}` + `ApiHost.Start(int porta)`/`Stop()` sobre `Microsoft.Owin.Hosting.WebApp.Start<Startup>("http://localhost:{porta}/")` (pacote novo `Microsoft.Owin.Hosting` 4.2.2 em `ERPFinanceiro.Desktop.csproj`, mesma versão do `Microsoft.Owin.Host.HttpListener` já usado pela Api). Falha de bind (`HttpListenerException`, porta em uso) é capturada num `catch (Exception ex) when (...)` que percorre a cadeia de `InnerException` (cobre tanto a exceção crua quanto embrulhada pelo carregador de startup do Katana) — nunca deixa subir; `Estado` vira `InativaPortaEmUso` e `UltimoErro` guarda a exceção original; `Stop()` faz `Dispose()` do `IDisposable` devolvido por `WebApp.Start` (libera a porta de fato, confirmado por teste) e volta a `Inativa` (a menos que já estivesse `InativaPortaEmUso`, preservando o último erro para o diálogo de status de T-45). **Conexão do composition root Autofac (T-26) ao container que `Startup`/`Autofac.WebApi2` usa**: `Startup.cs` ganhou `public static Autofac.IContainer Container { get; set; }` (substitui o container vazio hardcoded da nota de T-25) — `ApiHost.Start(int)` atribui `Startup.Container = _container` (o `IContainer` recebido no construtor do `ApiHost`, o mesmo montado por `CompositionRoot.Construir()`) antes de `WebApp.Start<Startup>(url)`; documentado no XML doc de `Startup.Container` por que é estático (o host OWIN instancia `Startup` por reflexão via `Activator.CreateInstance`, sem como injetar por construtor nesse caminho especificamente — seguro porque só existe 1 processo/1 host por vez, ADR-004) e por que isso não conflita com `StartupTests` de T-25 (container `null` cai no fallback vazio já existente, só usado para `CriarConfiguracaoJson`, sem host de verdade). **Decisão de composição (desvio pequeno, documentado no XML doc de `ApiHost`):** `ApiHost` ganhou uma 2ª sobrecarga `internal Start(int, Action<IAppBuilder>)`, visível ao assembly de teste via `InternalsVisibleTo` (`ERPFinanceiro.Desktop.csproj`) — usada só para o teste de bind/estado com um pipeline OWIN mínimo (`app.Run(...)`) próprio, porque nenhum controller Web API real existe ainda (T-30/T-31/T-32 são tarefas futuras) e o critério de aceite pede GET->200 real via HTTP, não simulado; o caminho de produção continua sendo só `Start(int)` (sem sobrecarga), sempre via `Startup`+container real. Testes novos em `src/ERPFinanceiro.Tests/Desktop/ApiHostTests.cs` (4 casos, sem controller/endpoint manual deixado no código de produção — nenhum endpoint de teste sobrou no `Api`/`Desktop`, mesma disciplina do endpoint de eco removido em T-25): (1) porta livre — `TcpListener(IPAddress.Loopback,0)` para achar porta livre, `Start` com pipeline mínimo, `HttpClient.GetAsync` real confirma 200 e corpo `"ok"`; (2) porta ocupada — abre `HttpListener` manual na mesma porta antes, confirma `Start` não lança (`Record.Exception` nulo), `Estado==InativaPortaEmUso`, `UltimoErro` é `HttpListenerException`; (3) `Stop` libera a porta — depois de `Stop()`, reabre um `HttpListener` novo na mesma porta e confirma que não lança; (4) conexão com o container real — `Start(int)` (caminho de produção, sem sobrecarga de teste) com `CompositionRoot.Construir()`, GET a uma rota não mapeada responde 404 (prova que `Startup`+`AutofacWebApiDependencyResolver` com o container real sobem sem exceção, já que nenhum controller existe ainda para dar 200 nesse caminho). `dotnet build` da solução: 0 erro, mesmos 4 avisos pré-existentes de estilo xUnit1031 em `GlobalExceptionHandlerTests` (não relacionados a esta tarefa). Execução real (contorno do Bloqueio 001, bin copiado para fora do OneDrive): filtro `ApiHostTests` 4/4 Aprovado na 1ª tentativa; suíte completa **118/118 Aprovado em 2 rodadas seguidas** (114 pré-existentes + 4 novos), sem flakiness observada. |
| T-28 | Api: `ExceptionHandler` global + envelope `{erro:{codigo,mensagem}}`, tabela exceção -> HTTP/código do SDD 2.4 (400/404/409/500), 500 sem stack trace | BE | A | 2h | T-25 | T-26, T-29 | P-1 | Exceção de cada tipo mapeada ao código correto; corpo 500 não contém texto da exceção; detalhe vai ao log | Concluída — `src/ERPFinanceiro.Api/Erros/{ErroEnvelopeDto,ExceptionParaRespostaMapper,GlobalExceptionHandler,ValidacaoException}.cs`: `GlobalExceptionHandler : System.Web.Http.ExceptionHandling.ExceptionHandler` registrado em `Startup.ConfigurarExceptionHandler` via `config.Services.Replace(typeof(IExceptionHandler), ...)`; lógica pura e testável em `ExceptionParaRespostaMapper.Mapear`/`GlobalExceptionHandler.Processar` (sem host OWIN). Tabela: `VendaJaCanceladaException`->409 `VENDA_JA_CANCELADA`; `MotivoObrigatorioException`->409 `MOTIVO_OBRIGATORIO`; `VendaNaoEncontradaException`->404 `VENDA_NAO_ENCONTRADA` (classe nova em `Domain/Exceptions/`, criada por antecipação — T-18/T-19/T-24 ainda não implementadas, documentado no XML doc); `ValidacaoException`->400 com o `codigo` específico do `ErroValidacao` que carrega (mecanismo escolhido: controller de T-30/T-32, ao ver `ResultadoValidacao.Sucesso == false` de T-17, lança `ValidacaoException(erro)` — Application continua nunca lançando para validação esperada); qualquer outra exceção->500 `ERRO_INTERNO` com mensagem genérica ao cliente, nunca a mensagem/stack trace original (só em `DetalheLog`). `IAppLogger`/`ICorrelationContext` resolvidos por requisição via `HttpRequestMessage.GetDependencyScope()` (não construtor), para não acoplar à ordem de registro do composition root Autofac (T-26, paralela); se a resolução falhar, a resposta ao cliente ainda sai correta, só o log fica ausente. `Startup.cs` editado com `old_string` específico (T-29 já tinha adicionado `ConfigurarCorrelationId` nesta sessão paralela; sem conflito). Testes em `src/ERPFinanceiro.Tests/Api/{ExceptionParaRespostaMapperTests,GlobalExceptionHandlerTests}.cs` (12 casos): cada tipo de exceção -> HTTP/código certos; 500 comprovado sem a mensagem sensível/nome da exceção original no corpo (`Assert.DoesNotContain`), com o mesmo detalhe + `correlationId` chegando ao `IAppLogger` mockado (`Assert.Contains`/`Moq.Verify`). `dotnet build` de Api e Tests: 0 aviso/0 erro (Api) e 4 avisos só de estilo xUnit1031 (`.Result` em teste síncrono, sem impacto funcional) em Tests. Execução real: bin/Debug/net48 do Tests copiado para fora do OneDrive (mesmo contorno do Bloqueio 001) e `dotnet vstest` rodado de lá na 1ª tentativa — **12/12 Aprovado** (filtro `GlobalExceptionHandlerTests\|ExceptionParaRespostaMapperTests`) e **19/19 Aprovado** (filtro `Api`, incluindo `StartupTests` de T-25 e os testes de `CorrelationIdHandler` de T-29), sem reincidência do bloqueio. Pasta temporária apagada ao final. |
| T-29 | Api: `CorrelationIdHandler` (gera/propaga id por requisição, header `X-Correlation-Id` de resposta) alimenta `ICorrelationContext` | BE | A | 1h | T-25 | T-26, T-28 | — | Toda resposta traz `X-Correlation-Id`; o mesmo id aparece no histórico da venda gravada | Concluída — `CorrelationIdHandler` (`DelegatingHandler`) em `src/ERPFinanceiro.Api/CorrelationIdHandler.cs`, registrado em `Startup.ConfigurarCorrelationId` (`config.MessageHandlers`). Lê `X-Correlation-Id` do request ou gera `Guid.NewGuid()`; chama `CorrelationContext.Definir(...)` (T-25); ecoa o valor no header da resposta. 3/3 testes verdes (`CorrelationIdHandlerTests`, contorno do Bloqueio 001: build publicado fora do OneDrive, `dotnet vstest` direto ali — ver nota em BLOCKERS.md). Gravação do `CORRELATION_ID` no histórico da venda é antecipação de T-18+, não coberta aqui por não existir ainda |

> **Lote 6 — Validado** em 22/09/2026 pelo Validador (`QA-REPORT.md`,
> `SECURITY-REVIEW.md`). QA: aprovado, sem ressalvas (0 reprovação crítica ou
> simples; suíte completa reexecutada pelo próprio Validador — 118/118
> Aprovado em 2 rodadas seguidas, fora do que o Executor já havia informado).
> DevSecOps: aprovado, sem débito de segurança (envelope 500 confirmado sem
> stack trace/mensagem original no corpo; `App.config.example` sem segredo
> real; `DbContext` por operação/requisição — RT-08/regra 5 — confirmado
> intacto após RL5-01). Ponto de atenção pedido nesta rodada
> (`Startup.Container` estático): avaliado a fundo nos dois chapéus e aceito
> como decisão de design dentro da garantia de processo único do ADR-004,
> sem janela de corrida detectada — não gerou achado nem tarefa. **RL5-01
> (`Refatoração Lote-5`) confirmada `Resolvida` por execução independente do
> Validador** (teste determinístico
> `Executar_ColisaoDeUniqueForcadaNaPrimeiraTentativa_...` reexecutado à
> parte, verde na 1ª tentativa; requisito operacional de RT-08 confirmado
> intacto). Checagem estrutural: consistente, sem inconsistência que exija
> redesenho; T-30/T-31/T-32 (Lote 7) e T-35/T-36 (Lote 8) confirmadas
> apontando para dependências `Concluída`, sem referência quebrada. Zero
> achados novos — `Refatoração Lote-6` não criado.

### Lote 7 — Endpoints (Dia 3, **marco: API utilizável pelo Vendas**) [A]

> Antecipar T-30 e T-31 para o meio do Dia 3 (quitação e status primeiro).

| ID | Tarefa | Dono | Tier | Est. | Dep. | Paralelizável-com | Premissa | Critério de aceite | Status |
|---|---|---|---|---|---|---|---|---|---|
| T-30 | `POST /api/vendas/quitacao`: DTOs + controller fino | BE | A | 2h | T-18, T-25, T-26, T-28, T-29 | T-31, T-32 | contrato v1.1, P-2 | curl: 200 `{status:"Quitada",dataQuitacao}`; repetição 200 com mesma data; corpo inválido 400; venda cancelada 409 `VENDA_JA_CANCELADA` | Concluída — `src/ERPFinanceiro.Api/Dtos/QuitacaoRequestDto.cs`/`QuitacaoResponseDto.cs` (espelham `docs/contrato-v1.1.md` Seção 3.1) + `src/ERPFinanceiro.Api/Controllers/VendasController.cs` (`[RoutePrefix("api/vendas")]`/`[Route("quitacao")]`, controller fino: DTO -> `QuitarVendaCommand` -> `QuitacaoService.Executar` -> `QuitacaoResponseDto`; decisão documentada no controller: T-31/T-32 adicionam actions a este mesmo controller, não criam um novo). Pré-requisito descoberto nesta tarefa (não coberto por T-26/T-27): `CompositionRoot.Construir()` não registrava os controllers Web API no Autofac — sem isso, `VendasController` (construtor com `QuitacaoService`) não resolvia via `AutofacWebApiDependencyResolver`; adicionado `RegistrarControllersApi` (`builder.RegisterApiControllers`) + `PackageReference Autofac.WebApi2` em `ERPFinanceiro.Desktop.csproj`. Teste de integração real do pipeline HTTP completo em `src/ERPFinanceiro.Tests/Desktop/ApiHostVendasControllerTests.cs` (reaproveita o padrão de `ApiHostTests.cs`/T-27: `ApiHost`+`Startup`+`CompositionRoot.Construir()` reais, Firebird embarcado real, sem chamar o controller em memória) — 1 `[Fact]` sequencial (mesmo `.fdb`, regra 11) cobrindo os 4 cenários do critério de aceite: venda nova 200 `{status:"Quitada",dataQuitacao}`; repetição 200 com mesma `dataQuitacao` (idempotência P-2, tolerância <1ms por arredondamento de `TIMESTAMP` do Firebird); corpo com `itens` vazio 400 `PAYLOAD_INVALIDO`; venda pré-semeada `Cancelada` 409 `VENDA_JA_CANCELADA`. Execução real confirmada: `dotnet build` (Api/Desktop/Tests) 0 avisos/0 erros (4 avisos pré-existentes `xUnit1031` em `GlobalExceptionHandlerTests.cs`, não desta tarefa); `bin/Debug/net48` copiado para fora do OneDrive (mesmo contorno de T-11/T-12/T-18) e `dotnet vstest ERPFinanceiro.Tests.dll` rodado de lá — suíte completa **119 Aprovado, 0 falha** (1 novo teste + 118 pré-existentes, sem regressão). Sem autenticação nesta tarefa (X-Api-Key é T-35, não é dependência de T-30). |
| T-31 | `GET /api/vendas/{vendaId}/status` | BE | A | 1h | T-24, T-25, T-26, T-28 | T-30, T-32 | RF-03 | 200 `{vendaId,status}` com string PascalCase; inexistente 404 `VENDA_NAO_ENCONTRADA` | Concluída — `src/ERPFinanceiro.Api/Dtos/VendaStatusResponseDto.cs` (`{vendaId,status}`) + nova action `Status(string vendaId)` (`[HttpGet][Route("{vendaId}/status")]`) adicionada ao `VendasController` já existente (T-30), reaproveitando `ConsultaService.ObterStatus` (T-24, injetado por construtor) e `GlobalExceptionHandler`/`ExceptionParaRespostaMapper` (T-28, já mapeia `VendaNaoEncontradaException` -> 404 `VENDA_NAO_ENCONTRADA`, sem tratamento novo). `status` convertido de `StatusVenda` (enum) para string PascalCase via `Enum.ToString()` na Api (mesma decisão de T-19/T-24 de manter a conversão fora de Domain/Application; valores do enum já são PascalCase: Pendente/Quitada/Cancelada). Teste de integração real em `src/ERPFinanceiro.Tests/Desktop/ApiHostVendasStatusControllerTests.cs` (mesmo padrão de `ApiHostVendasControllerTests.cs`/T-30: `ApiHost`+`Startup`+`CompositionRoot.Construir()` reais, Firebird embarcado real) cobrindo os 2 cenários do critério de aceite: venda Pendente pré-semeada -> 200 `{vendaId,status:"Pendente"}`; venda inexistente -> 404 `{erro:{codigo:"VENDA_NAO_ENCONTRADA",mensagem}}`. Execução real confirmada: `dotnet build` (solução completa) 0 erros; `bin/Debug/net48` copiado para fora do OneDrive (mesmo contorno de T-11/T-12/T-18/T-30) e `dotnet vstest ERPFinanceiro.Tests.dll` rodado de lá — teste novo passou isolado e, junto com os demais testes de `VendasController`/`ConsultaService` (9 testes: T-30 + T-31 + unitários de T-24), **9 Aprovado, 0 falha**, sem regressão. Pré-requisito de ambiente desta tarefa (não coberto por tarefa anterior): `App.config` local (gitignorado) do projeto `ERPFinanceiro.Tests` e as DLLs nativas do Firebird embarcado (`fbembed.dll`/`fbclient.dll`+dependências, ver `spikes/T-01-firebird-spike/README.md`) não estavam presentes nesta worktree recém-criada — resolvido localmente (fora do controle de versão) apontando `Banco:CaminhoFdb`/`Log:CaminhoArquivo` para um caminho isolado e copiando as DLLs x64 do Firebird embarcado para a pasta de execução dos testes; não é uma mudança de código do projeto. |
| T-32 | `POST /api/vendas/cancelamento`: DTOs + controller fino | BE | A | 2h | T-19, T-25, T-26, T-28, T-29 | T-30, T-31 | D-06, D-08, contrato v1.1 | curl: Pendente/desconhecida 200; Quitada sem motivo 409 `MOTIVO_OBRIGATORIO`; repetição 200 | Concluída — `src/ERPFinanceiro.Api/Dtos/CancelamentoRequestDto.cs`/`CancelamentoResponseDto.cs` (espelham `docs/contrato-v1.1.md` Seção 3.2: entrada `{vendaId,motivo}`, saída `{status}`) + nova action `Cancelamento` em `src/ERPFinanceiro.Api/Controllers/VendasController.cs` (mesmo controller de T-30/T-31, `[Route("cancelamento")]`; controller fino: DTO -> `CancelarVendaCommand` -> `CancelamentoService.Cancelar` (T-19) -> `CancelamentoResponseDto`, `Status` via `StatusVenda.ToString()` já PascalCase, P-3). Validação de `vendaId` ausente/vazio feita no controller (400 `PAYLOAD_INVALIDO` via `Application.Exceptions.ValidacaoException`) porque `ValidadorVendaCommand` (T-17) só cobre `QuitarVendaCommand`/`RegistrarVendaCommand`, não `CancelarVendaCommand` — decisão documentada em comentário no controller; `CancelamentoService.Cancelar` só garante a pré-condição via `ArgumentException` (erro de programação do chamador, não cenário de negócio). 409 `MOTIVO_OBRIGATORIO` (D-06) reaproveita o mapeamento de `MotivoObrigatorioException` já existente em `ExceptionParaRespostaMapper` (T-28), sem alteração. Controller passou a receber também `CancelamentoService` e `ICorrelationContext` (ambos já registrados no Autofac por T-26/T-30) — construtor ampliado sem remover os parâmetros de T-30/T-31. Teste de integração real em `src/ERPFinanceiro.Tests/Desktop/ApiHostVendasCancelamentoControllerTests.cs` (arquivo separado de `ApiHostVendasControllerTests.cs`/`ApiHostVendasStatusControllerTests.cs` para não colidir com T-30/T-31 rodando em paralelo sobre o mesmo controller), mesmo padrão `ApiHost`+`Startup`+`CompositionRoot.Construir()` real + Firebird embarcado real, 1 `[Fact]` sequencial cobrindo: Pendente cancela 200; desconhecida cria+cancela 200 (D-08); Quitada sem `motivo` 409 `MOTIVO_OBRIGATORIO` (nada alterado) e a mesma venda com `motivo` 200; repetição sobre já Cancelada 200 (idempotência, P-2); payload sem `vendaId` 400 `PAYLOAD_INVALIDO`. Nota de ambiente (achado desta tarefa, além do já documentado em `BLOCKERS.md` Bloqueio 001): nesta worktree, além do contorno de zona do OneDrive, o `App.config` local do `ERPFinanceiro.Tests` (gitignorado) e as DLLs nativas do Firebird embarcado (`fbclient.dll`/`fbembed.dll`/`security3.fdb`/plugins — normalmente extraídas manualmente do zip Firebird 3.0.12 x64 conforme `spikes/T-01-firebird-spike/README.md`) não estavam presentes; recriados localmente (App.config com `Banco:CaminhoFdb` dedicado `financeiro_tests_t32.fdb`, para não colidir com o `.fdb` usado por T-30/T-31 em paralelo) e copiados para `bin/Debug/net48`, com um ajuste adicional necessário não documentado no README do spike: `firebird.conf` precisa de `SecurityDatabase = security3.fdb` descomentado (senão `FbConnection.CreateDatabase` falha com "Your user name and password are not defined", pois o padrão comentado não resolve `$(dir_secDb)` num diretório sem instalação real do Firebird). Execução real confirmada: `dotnet build` (Api/Desktop/Tests) 0 avisos/0 erros; `bin/Debug/net48` copiado para fora do OneDrive (`dotnet vstest`) — suíte completa **121 Aprovado, 0 falha** (2 novos testes desta tarefa + T-31, sem regressão sobre os 119 de T-30). |
| T-33 | Alias `/api/v1/vendas/...` por dupla atribuição de rota nos 3 controllers (S-02) | BE | A | 1h | T-30, T-31, T-32 | T-34 | D-05 | Os 3 endpoints respondem idênticos nas duas rotas (mesma coleção, 2 bases); se D-05 rejeitada, desativar sem apagar controllers | Concluída — não há "3 controllers": há 1 único `VendasController` (`src/ERPFinanceiro.Api/Controllers/VendasController.cs`, decisão já documentada em T-30) com as 3 actions (Quitacao/Status/Cancelamento); tratado como "os 3 endpoints deste controller", conforme a redação da tarefa antecipava um design que não se concretizou. Mecanismo: "dupla atribuição de rota" implementada como um segundo `[Route(...)]` **absoluto** por action, ex. `[Route("quitacao")]` + `[Route("~/api/v1/vendas/quitacao")]` (prefixo `~/` ignora o `[RoutePrefix("api/vendas")]` do controller) — `RouteAttribute` do Web API 2 permite múltiplas ocorrências na mesma action (`AllowMultiple = true`), então as duas rotas roteiam para exatamente o mesmo método, sem duplicar controller/DTO/lógica; `Startup.ConfigurarRoteamento` (T-25, `MapHttpAttributeRoutes`) não precisou de nenhuma mudança. Aplicado às 3 actions: `Quitacao` (`~/api/v1/vendas/quitacao`), `Status` (`~/api/v1/vendas/{vendaId}/status`), `Cancelamento` (`~/api/v1/vendas/cancelamento`). D-05 é premissa "A VALIDAR" (`docs/contrato-v1.1.md` Seção 4) — assumida aceita nesta tarefa, mesma convenção já usada em premissas anteriores do projeto; critério de aceite já cobre o cenário de rejeição: **desativar sem apagar controllers** é literalmente remover/comentar a linha `[Route("~/api/v1/vendas/...")]` de cada uma das 3 actions (1 linha por endpoint, como a Matriz de Decisões do TASK.md já antecipa para D-05/P-8: "Desativar rota alias (1 linha)") — o `VendasController` e as rotas `/api/vendas/...` continuam intactos. Teste de integração real novo em `src/ERPFinanceiro.Tests/Desktop/ApiHostVendasAliasV1ControllerTests.cs` (mesmo padrão `ApiHost`+`Startup`+`CompositionRoot.Construir()` real + Firebird embarcado real de T-30/T-31/T-32): semeia 1 venda Pendente e chama `GET .../status` nas duas bases (`/api/vendas/{vendaId}/status` e `/api/v1/vendas/{vendaId}/status`) contra a mesma venda, comparando status HTTP e corpo JSON (`JToken.DeepEquals`) — escopo documentado no próprio teste: não exercita os 3 endpoints x 2 rotas exaustivamente (o caminho feliz de cada endpoint na rota base já está coberto pelos testes de T-30/T-31/T-32); o alias em si é o que este teste prova, usando o endpoint mais simples/idempotente (status) como representante. Execução real confirmada: `dotnet build` (solução completa) 0 erros; `bin/Debug/net48` copiado para fora do OneDrive (mesmo contorno de T-11/T-12/T-18/T-30/T-31/T-32, DLLs nativas do Firebird embarcado e `App.config` de T-32 reaproveitados sem recriação) e `dotnet vstest ERPFinanceiro.Tests.dll` rodado de lá — teste novo passou isolado, junto com os 3 testes de T-30/T-31/T-32 (**4 Aprovado, 0 falha**) e na suíte completa (**122 Aprovado, 0 falha**, sem regressão sobre os 121 de T-32). Nenhum conflito com T-34 (rodando em paralelo, só lê endpoints via HTTP/curl, não editou C#). |
| T-34 | Coleção Postman/curl de fumaça em `docs/` (marco do Dia 3): caminho feliz + 400/404/409 por endpoint | QA | A | 1,5h | T-30, T-31, T-32 | T-33 | contrato v1.1 | Coleção roda 100% verde contra o app local; salva em `docs/postman/`; **critério do marco: disponível ao Vendas até o fim do Dia 3** | Concluída — `docs/postman/smoke-vendas.sh` (curl real, formato escolhido em vez de `.postman_collection.json` — ver justificativa em `docs/postman/README.md`: sandbox sem GUI/Postman, curl é o único formato executável/verificável nesta sessão, Diretriz 16 da Seção 1) cobre os 7 cenários do critério de aceite (2-3 por endpoint): quitação 200 caminho feliz / 400 `PAYLOAD_INVALIDO` (itens vazio) / 409 `VENDA_JA_CANCELADA`; cancelamento 200 caminho feliz (D-08, venda desconhecida) / 409 `MOTIVO_OBRIGATORIO` (Quitada sem motivo); status 200 caminho feliz / 404 `VENDA_NAO_ENCONTRADA`. Script **auto-contido**: não depende de estado pré-semeado no banco — os cenários 409 são produzidos encadeando os próprios endpoints do contrato (cancelar venda desconhecida cria+cancela, D-08; depois quitar essa mesma venda dá 409; e quitar venda nova, depois cancelar sem motivo dá 409), com `vendaId` único por execução (sufixo timestamp), repetível sem colidir com execuções anteriores. Sem cenário `401`: `ApiKeyHandler` é T-35 (Lote 8), ainda não implementada — incluir 401 agora testaria um caminho sempre-200 (vazio/quebrado); nota explícita no script e no README apontando **T-37** como responsável por adicionar `X-Api-Key`/401 quando T-35 estiver pronta (conforme instrução da tarefa). **Execução real confirmada** (não só documentação estática): harness descartável novo em `tools/T-34-smoke-harness/` (mesma disciplina de `spikes/T-01-firebird-spike/`, fora da `.sln`) sobe a API pelo caminho de produção real (`ApiHost`+`Startup`+`CompositionRoot.Construir()`, T-26/T-27) contra Firebird embarcado real (DLLs nativas reaproveitadas de `src/ERPFinanceiro.Tests/bin/Debug/net48`, já resolvidas nesta worktree por T-31/T-32); `dotnet build` do harness 0 avisos/0 erros; rodado fora do OneDrive (`C:\temp_erp_t34`, mesmo contorno do Bloqueio 001) na porta 5034; `smoke-vendas.sh` executado 2 vezes seguidas contra essa instância real — **7/7 cenários verdes nas duas rodadas** (`exit 0`), evidência bruta da 1ª rodada em `docs/postman/evidencia-execucao-T-34.txt`. Harness parado ao final (arquivo `parar.txt`), `.fdb`/logs de teste apagados, nada de artefato temporário sobrou no repo. `App.config` do harness segue a convenção global do `.gitignore` (T-04): só `tools/T-34-smoke-harness/App.config.example` é versionado, instruções de uso no `docs/postman/README.md`. Marco do Dia 3 cumprido (documento disponível em `docs/postman/`); envio efetivo ao lado Vendas continua pendência do usuário/orquestrador, mesma situação já registrada para `docs/contrato-v1.1.md` (T-03). Nenhum código de produção tocado por esta tarefa (guardrail do dispatch: T-33, rodando em paralelo sobre `VendasController.cs`, não teve conflito). |

> **Lote 7 — Validado** em 22/09/2026 pelo Validador (`QA-REPORT.md`,
> `SECURITY-REVIEW.md`). QA: aprovado, sem ressalvas (0 reprovação; as 5
> tarefas — T-30, incluída nesta rodada pela primeira vez, e T-31…T-34 —
> confirmadas por execução independente da suíte completa: `dotnet build
> ERPFinanceiro.sln` 0 aviso/0 erro + `dotnet vstest` fora do OneDrive,
> **122/122 Aprovado na 1ª tentativa**, sem reprodução do Bloqueio 001 nesta
> rodada). DevSecOps: aprovado, sem débito de segurança (envelope de erro
> reaproveitado sem alteração, confirmado por `git diff`; validação de
> `vendaId` no controller de `Cancelamento` avaliada e aceita como decisão
> pontual, não achado; `App.config`/DLLs nativas recriados localmente
> confirmados fora do controle de versão via `git check-ignore`). Checagem
> estrutural: consistente, sem inconsistência que exija redesenho; T-35,
> T-36, T-37, T-38 (Lote 8) confirmadas apontando para dependências
> `Concluída` deste lote (T-30/T-31/T-32/T-34) sem referência quebrada.
> Zero achados — nenhuma tarefa criada em `Refatoração Lote-7`. Marco "API
> utilizável pelo Vendas" cumprido.

### Lote 8 — Segurança da API e integração real (Dia 4) [A]

> **T-38/T-39 redefinidas (Coordenador, 23/09/2026, ADR-010, `BLOCKERS.md`
> Bloqueio 002):** integração real com o Vendas (Delphi) é inviável neste
> sandbox (sem aceite formal do contrato pelo Vendas — T-03 — e sem acesso ao
> sistema Delphi real). T-38/T-39 passam a produzir uma **suíte de integração
> simulada** (harness HTTP real, não teste em memória, reaproveitando o
> padrão de T-34/T-37), cobrindo o mesmo critério técnico original
> (idempotência, 400/401/409, timeout/reenvio) — já era o fallback
> pré-aprovado no `TASK.md` Seção 4.3 para "contrato não aceito", estendido
> aqui também à ausência de acesso ao Delphi em si (mesma técnica cobre os
> dois casos). **Isto não é integração real com o Vendas** e deve ser
> declarado como tal em toda evidência/README. A pendência de integração
> real permanece aberta, registrada em `BLOCKERS.md` Bloqueio 002 e na Seção
> 6 abaixo, para ser retomada fora deste sandbox. Mudança de escopo do marco
> "O-11" sinalizada ao usuário — ver relatório do Coordenador; execução dos
> novos T-38/T-39 pelo Executor aguarda aprovação explícita.

| ID | Tarefa | Dono | Tier | Est. | Dep. | Paralelizável-com | Premissa | Critério de aceite | Status |
|---|---|---|---|---|---|---|---|---|---|
| T-35 | `ApiKeyHandler` (S-08): `X-Api-Key`, comparação em tempo constante, isenta `GET /api/health`; ausente/inválida -> 401 `NAO_AUTORIZADO` sem revelar qual | BE | A | 1,5h | T-25, T-28, T-26 | T-36 | P-4/P-5 | Sem header e com chave errada retornam 401 idêntico; com chave correta 200; chave não aparece em log | Concluída — `src/ERPFinanceiro.Api/ApiKeyHandler.cs`: message handler registrado em `Startup.ConfigurarApiKey` (novo método, chamado logo após `ConfigurarCorrelationId` e antes de `ConfigurarExceptionHandler`, mesmo padrão de `CorrelationIdHandler`/T-29 — nota de `SECURITY-REVIEW.md` sobre esta tarefa confirmada: roda antes do roteamento de controller, nenhuma rota autenticável escapa). Isenção de `GET /api/health` por `AbsolutePath` (`/api/health`, comparação case-insensitive, trim de `/` final) — por convenção de rota, não por checar se o endpoint responde (T-36, rodando em paralelo sobre `HealthController`/`Startup.cs`; `Startup.cs` lido antes de editar, só acrescentado `ConfigurarApiKey` sem tocar nada de T-36). Chave lida direto de `App.config` (`ConfigurationManager.AppSettings["Api:ApiKey"]`) — `Reference System.Configuration` adicionada a `ERPFinanceiro.Api.csproj` (mesma exceção de camada já documentada em `ERPFinanceiro.Desktop.csproj` para `Api:Porta`/`Log:CaminhoArquivo`: a chave é preocupação da própria Api, não passa por `IConfiguracaoBanco` nem por uma interface nova só para 1 valor). Comparação em tempo constante implementada manualmente (`ComparacaoEmTempoConstante`): `System.Security.Cryptography.CryptographicOperations.FixedTimeEquals` não existe no net48 (só a partir de .NET Core 2.1/.NET Standard 2.1) — XOR byte a byte sempre percorrendo o maior dos dois arrays (nunca retorna cedo por tamanho), acumulando a diferença num único `int`, decisão documentada no XML doc da classe. 401 sempre com o mesmo envelope `{erro:{codigo:"NAO_AUTORIZADO",mensagem:"Credencial de acesso ausente ou inválida."}}` (`docs/contrato-v1.1.md` Seção 2/3.1, byte a byte) tanto para header ausente quanto para chave errada — nenhuma distinção entre os dois casos. Chave nunca logada: `RegistrarTentativaNaoAutorizada` grava só uma mensagem genérica via `IAppLogger`/`ICorrelationContext` resolvidos por `GetDependencyScope` (mesmo padrão de `GlobalExceptionHandler`/T-28), nunca o header recebido nem o valor configurado. Efeito colateral necessário desta tarefa: os testes de integração real de T-27/T-30/T-31/T-32/T-33 (`ApiHostTests.cs`, `ApiHostVendasControllerTests.cs`, `ApiHostVendasStatusControllerTests.cs`, `ApiHostVendasCancelamentoControllerTests.cs`, `ApiHostVendasAliasV1ControllerTests.cs`) não enviavam `X-Api-Key` e passariam a receber 401 em vez do cenário original — atualizados para incluir `client.DefaultRequestHeaders.Add(ApiKeyHandler.HeaderName, "CHAVE_LOCAL_TESTE_T32")` (mesmo valor do `App.config` local gitignorado de `ERPFinanceiro.Tests`), sem alterar nenhuma asserção de negócio desses testes. Teste de integração real novo e dedicado desta tarefa em `src/ERPFinanceiro.Tests/Desktop/ApiHostVendasApiKeyControllerTests.cs` (arquivo separado dos demais para não colidir com a instância do Executor rodando em paralelo sobre T-36) — mesmo padrão `ApiHost`+`Startup`+`CompositionRoot.Construir()` real + Firebird embarcado real, 1 `[Fact]` cobrindo os 3 cenários do critério de aceite contra `GET .../status`: sem header 401 `NAO_AUTORIZADO`; chave errada 401 com corpo idêntico ao caso anterior (`JToken.DeepEquals`); chave correta 200 `{vendaId,status:"Pendente"}`; e uma 4ª verificação lendo o arquivo real de log (`Log:CaminhoArquivo`, escrito por `TraceLogger`/`IAppLogger`) confirmando que nem a chave correta nem a errada aparecem em nenhuma linha. Execução real confirmada: `dotnet build` (solução completa) 0 erros (4 avisos pré-existentes `xUnit1031`, não desta tarefa); `bin/x64/Debug/net48` (build desta tarefa) completado com as DLLs nativas do Firebird/`App.config`/`firebird.conf`/`security3.fdb` reaproveitados de `bin/Debug/net48` (já resolvidos nesta worktree pelo Lote 7) e copiado para fora do OneDrive (mesmo contorno do Bloqueio 001) — `dotnet vstest ERPFinanceiro.Tests.dll` rodado de lá: teste novo isolado **1 Aprovado, 0 falha**; todos os testes de `Desktop` (14, incluindo T-27/T-30…T-33 atualizados e T-36 em paralelo) **14 Aprovado, 0 falha**; suíte completa **124 Aprovado, 0 falha**, sem regressão. Nota para T-37 (próxima tarefa do lote, depende de T-35): `docs/postman/smoke-vendas.sh` (T-34) ainda não envia `X-Api-Key` — com `ApiKeyHandler` agora ativo, os cenários do script passam a receber 401 em vez do comportamento original; T-34 já previa isso explicitamente ("T-37 é responsável por adicionar X-Api-Key/401 quando T-35 estiver pronta"), nenhuma ação tomada aqui sobre o script/README para não invadir o escopo de T-37. |
| T-36 | `GET /api/health` (S-09): 200 `{status:"ok",banco:"ok"}`; falha de banco 503 `{status:"degradado",banco:"falha"}`; sem autenticação | BE | A | 1h | T-22, T-25, T-26 | T-35 | P-5 | Com banco ok 200; com `.fdb` inacessível 503; sem `X-Api-Key` responde | Concluída — `src/ERPFinanceiro.Api/Dtos/HealthResponseDto.cs` (`{status,banco}`) + novo `src/ERPFinanceiro.Api/Controllers/HealthController.cs` dedicado (`[RoutePrefix("api")]`/`[Route("health")]`, sem relação com `VendasController`/vendas — endpoint de infraestrutura), controller fino: delega integralmente a `IHealthService.ObterStatus()` (T-22, reaproveitada sem alteração, nunca lança) e traduz `ResultadoHealth.Ok` em `{status:"ok",banco:"ok"}`/200 ou `{status:"degradado",banco:"falha"}`/503, via `ApiController.Content<T>(HttpStatusCode, T)` (não `Ok()`, que só cobre 200) para passar pela mesma negociação de conteúdo/formatter camelCase de `Startup` (T-25). Nenhuma mudança em `Startup.cs`/`CompositionRoot.cs`: `RegisterApiControllers` (T-30) já registra qualquer controller da assembly automaticamente, `HealthController` resolveu sem registro adicional; nada de autenticação implementado aqui — isenção de `X-Api-Key` é responsabilidade exclusiva do `ApiKeyHandler` (T-35, rodando em paralelo no mesmo `Startup.cs`), este controller simplesmente não lê nenhum header. Teste de integração real novo em `src/ERPFinanceiro.Tests/Desktop/ApiHostHealthControllerTests.cs` (mesmo padrão `ApiHost`+`Startup`+`CompositionRoot.Construir()` real + Firebird embarcado real de T-30…T-33) cobrindo os 3 cenários do critério de aceite: banco acessível -> 200 `{status:"ok",banco:"ok"}`; banco inacessível (`.fdb` apagado depois de inicializado, mesmo princípio de `HealthServiceTests`/T-22 para simular falha de conexão sem lançar) -> 503 `{status:"degradado",banco:"falha"}`; requisição sem `X-Api-Key` responde 200 normalmente (não 401) — checado na mesma chamada do cenário 1, já que `GetAsync` não envia o header. Nota explícita no teste e aqui: esta rodada específica já rodou **com** o `ApiKeyHandler` de T-35 registrado em `Startup.cs` (a outra instância do Executor terminou de registrá-lo em paralelo antes da minha execução do teste) e mesmo assim `GET /api/health` respondeu sem 401 nos 2 cenários — confirma na prática que a isenção de T-35 cobre a rota, sem qualquer mudança necessária neste controller. Execução real confirmada: `dotnet build` (solução completa) 0 erros; `bin/x64/Debug/net48` copiado para fora do OneDrive (mesmo contorno de T-11/T-12/T-18/T-30…T-33), complementado com as DLLs nativas/plugins/`security3.fdb`/`firebird.conf` de Firebird embarcado e o `App.config` local já deixados pelo Lote 7 (reaproveitados sem recriação) — `dotnet vstest ERPFinanceiro.Tests.dll` rodado de lá: teste novo passou isolado (**1 Aprovado, 0 falha**). Suíte filtrada `ApiHost*` rodou **7 Aprovado, 2 falha** — as 2 falhas (`ApiHostVendasAliasV1ControllerTests`, `ApiHostVendasCancelamentoControllerTests`, ambas 401 inesperado) são efeito direto do `ApiKeyHandler` de T-35 (em andamento em paralelo nesta mesma worktree) exigindo `X-Api-Key` em rotas de venda que os testes de T-30…T-33 (já `Concluída`) não enviam — não são regressão desta tarefa nem escopo de T-36 (T-37 é quem ajusta os testes/exemplos após T-35 terminar); nada em `VendasController.cs`/testes de venda foi tocado por T-36. |
| T-37 | Validar exemplos JSON do contrato v1.1 contra a API (amostras de T-03 executadas, incluindo 401): ajustar `docs/contrato-v1.1.md` se houver divergência | QA | A | 1,5h | T-34, T-35 | T-38 | contrato v1.1, P-6 | Todos os exemplos de `docs/contrato-v1.1.md` reproduzidos byte a byte na resposta (exceto valores dinâmicos) | Concluída — reaproveitado o harness descartável de `tools/T-34-smoke-harness/` (`dotnet build` limpo de dentro do worktree; execução do binário fora do OneDrive, `C:\temp_erp_t37`, mesmo contorno do Bloqueio 001, DLLs nativas do Firebird copiadas de `src/ERPFinanceiro.Tests/bin/Debug/net48`) para subir a API real (`ApiHost`+`Startup`+`CompositionRoot.Construir()`) e validar campo a campo/código a código todos os exemplos JSON de `docs/contrato-v1.1.md` Seções 2, 3.1, 3.2, 3.3 e 3.5 contra a resposta real (`POST /quitacao`, `POST /cancelamento`, `GET .../status`, `GET /api/health`, alias `/api/v1/vendas/...`, os 2 casos de 401), exceto valores dinâmicos (`dataQuitacao`, `vendaId` gerado, timestamps) — evidência bruta completa em `docs/postman/evidencia-execucao-T-37.txt`. **4 divergências de texto de `mensagem` encontradas e corrigidas em `docs/contrato-v1.1.md`** (código de erro e HTTP status já batiam, só o texto da proposta original de T-03 divergia da implementação real — API tratada como fonte de verdade, conforme a diretriz da tarefa): `409 VENDA_JA_CANCELADA` (Seção 3.1, mensagem real com aspas no `vendaId` e frase diferente), `409 DADOS_DIVERGENTES` (Seção 3.1, aspas no `vendaId`), `404 VENDA_NAO_ENCONTRADA` (Seções 3.2 e 3.3, aspas no `vendaId`), `409 MOTIVO_OBRIGATORIO` (Seção 3.2, mensagem real mais curta). Os demais exemplos (200 quitação/cancelamento/status, `400 PAYLOAD_INVALIDO` dos dois endpoints, `401 NAO_AUTORIZADO`, `200`/`503` de `GET /api/health`, alias v1) já batiam byte a byte, sem ajuste necessário. **1 observação registrada sem alterar código** (fora do escopo desta tarefa, que só valida/ajusta o documento): a mensagem de `400 VALOR_TOTAL_DIVERGENTE` (`ValidadorVendaCommand.cs`) formata os números com a *culture* corrente do processo (não `InvariantCulture`) — no host desta sessão (pt-BR) saiu com vírgula decimal em vez do ponto do exemplo original; nota explicativa adicionada ao contrato (Seção 3.1) alertando que o lado Vendas não deve fazer parsing dessa mensagem (só do campo `codigo`), e sinalizando o ajuste de código como follow-up pontual, não tomado aqui. `POST /api/vendas` (Seção 3.4, D-03/P-9) não foi validado — endpoint ainda não implementado (T-62, Tier B condicional), registrado como tal na nova Seção 3.7 do contrato. Nova Seção "3.7 Validação dos exemplos contra a API real (T-37)" adicionada a `docs/contrato-v1.1.md` resumindo o resultado. `docs/postman/smoke-vendas.sh`/`README.md` (T-34) atualizados conforme a nota que a própria T-34 já previa: header `X-Api-Key` (variável `API_KEY`, default `CHAVE_LOCAL_SMOKE_T34`) adicionado a todas as chamadas de sucesso/400/404/409 (senão todas passariam a dar 401 pós-T-35) e 2 novos cenários `8`/`9` cobrindo `401 NAO_AUTORIZADO` (chave ausente / chave inválida, contrato Seção 2) — coleção agora com 9 cenários, confirmada **9/9 verde em 2 execuções seguidas** contra a API real (evidência em `docs/postman/evidencia-execucao-T-37.txt`); log do `ApiKeyHandler` durante os 2 cenários 401 confirmado sem vazar a chave (só mensagem genérica). Harness/App.config/DLLs nativas não alterados (reaproveitados como estavam). Pasta temporária `C:\temp_erp_t37` e o `.fdb` de trabalho removidos ao final da sessão. |
| T-38 | **O-11a (redefinida, ADR-010)** Suíte de integração **simulada**: harness HTTP real (reaproveita `tools/T-34-smoke-harness`, `ApiHost`+`Startup`+`CompositionRoot.Construir()` reais + Firebird embarcado real) faz o papel do Vendas disparando quitação e consulta de status contra a API local; registrar request/response como evidência. **Não é integração real com o Delphi** — rótulo explícito exigido em toda evidência/README/BLOCKERS | QA | A | 2h | T-30, T-31, T-35, T-36 | T-37 | contrato v1.1 (rascunho, sem aceite do Vendas), D-04 | Cenários (venda nova quitada 200; repetição idempotente sem novo histórico P-2; `GET status` reflete o resultado) executados via HTTP real contra a API local com evidência de request/response registrada; documento/evidência declara explicitamente "simulação — não é integração real com o Vendas (Delphi)" | Concluída — harness dedicado novo (não o mesmo processo/porta de T-34/T-37, mas mesmo padrão) em `tools/T-38-integracao-simulada/` (`T38IntegracaoSimulada.csproj`/`Program.cs`, mesma disciplina de `tools/T-34-smoke-harness`: não faz parte de `ERPFinanceiro.sln`, `App.config` local gitignorado com `.example` versionado, DLLs nativas do Firebird reaproveitadas de `src/ERPFinanceiro.Tests/bin/Debug/net48`) que sobe a API real (`ApiHost`+`Startup`+`CompositionRoot.Construir()`, porta dedicada `5038`) contra Firebird embarcado real e novo, e — diferente de T-34/T-37, que ficam no ar para curl externo — dispara ele mesmo, via `HttpClient` real (papel do Vendas), os 3 cenários do critério de aceite, sem interação. Rótulo "SIMULAÇÃO — NÃO é integração real com o Vendas (Delphi)" impresso em todo início/fim de execução do harness e repetido em `docs/integracao-simulada/README.md`/`evidencia-execucao-T-38.txt` (ADR-010). **Cenário 1** (quitação de venda nova): POST `/api/vendas/quitacao` com `X-Api-Key`, HTTP 200, `status:"Quitada"`. **Cenário 2** (repetição idempotente, P-2): mesmo payload reenviado; verificado por dois caminhos complementares — (a) resposta HTTP: `dataQuitacao` comparada por instante (não string bruta) com tolerância de 1 ms, confirmando o mesmo timestamp sem overwrite (`Venda.Quitar()` retornando `false`, RN-04); (b) consulta direta ao banco via `VendaRepository.ObterPorVendaId`/`FinanceiroDbContext` (mesmo caminho de acesso usado pela aplicação, sem SQL solto) contando `VendaHistorico` da venda antes e depois da repetição — 2 registros em ambas as leituras (`Recebida`+`Quitacao`), confirmando objetivamente "sem novo histórico" (a API HTTP não expõe histórico, então essa é a única verificação definitiva do critério). **Achado registrado, não bloqueante:** a comparação inicial por string bruta de `dataQuitacao` dava falso-negativo — a 1ª resposta reflete o valor calculado em memória (precisão de tick do .NET, ~7 dígitos de fração), a 2ª resposta reflete o mesmo valor após round-trip pela coluna `TIMESTAMP` do Firebird (só 4 dígitos de fração), então os dígitos finais divergem mesmo sendo o mesmo instante gravado (diferença < 0,1 ms nas 2 execuções) — corrigido para comparação semântica por `DateTime` com tolerância de 1 ms (também exigiu parsear a resposta sem o auto-parse de datas do Newtonsoft.Json, que por padrão trunca para segundo inteiro e mascararia esse achado); documentado em `evidencia-execucao-T-38.txt` como observação, sem alteração de código de produção (mesma disciplina de T-37 para achados fora do escopo da tarefa). **Cenário 3** (GET status): `GET /api/vendas/{vendaId}/status` com `X-Api-Key`, HTTP 200, `vendaId`/`status:"Quitada"` batendo com o resultado da quitação. Execução real confirmada: `dotnet build` (harness) 0 erros; binário copiado para fora do OneDrive (`C:\temp_erp_t38\net48`, mesmo contorno do Bloqueio 001), DLLs nativas do Firebird copiadas de `src/ERPFinanceiro.Tests/bin/Debug/net48` — **2 execuções seguidas, cada uma com banco novo e `vendaId` novo (timestamp), ambas 4/4 verificações OK** (evidência bruta completa de request/response em `docs/integracao-simulada/evidencia-execucao-T-38.txt`, roteiro/explicação em `docs/integracao-simulada/README.md`). Pasta temporária `C:\temp_erp_t38` e os `.fdb`/logs de trabalho (`C:\ERPFinanceiro\data\financeiro_t38_simulado.fdb` e correlatos) removidos ao final da sessão. Nenhum código de produção alterado por esta tarefa (só o harness/evidência de QA) — pendência de integração real com o Vendas (Delphi) permanece em aberto, sem alteração por esta tarefa (ADR-010, `BLOCKERS.md` Bloqueio 002). |
| T-39 | **O-11b (redefinida, ADR-010)** Suíte de integração **simulada**: mesmo harness de T-38 cobre cancelamento, reenvio após timeout (idempotência), erro 400/401/409 — cenários técnicos do contrato, não o sistema Vendas real. **Não é integração real com o Delphi** | QA | A | 1,5h | T-38, T-32 | — | D-06, D-08, P-2 | Cada cenário (cancelamento Pendente/desconhecida/Quitada sem motivo, reenvio pós-timeout idempotente, 400/401/409) executado via HTTP real contra a API local com resultado registrado; divergências viram itens de T-40; evidência rotulada como simulação | Concluída — harness dedicado novo (mesma disciplina de T-38, não faz parte de `ERPFinanceiro.sln`) em `tools/T-39-integracao-simulada/` (`T39IntegracaoSimulada.csproj`/`Program.cs`, `App.config` local gitignorado com `.example` versionado, DLLs nativas do Firebird reaproveitadas de `src/ERPFinanceiro.Tests/bin/Debug/net48`) que sobe a API real (`ApiHost`+`Startup`+`CompositionRoot.Construir()`, porta dedicada `5039`, distinta de `5038`/T-38) contra Firebird embarcado real e novo, e dispara ele mesmo, via `HttpClient` real (papel do Vendas), os cenários do critério de aceite, sem interação. Rótulo "SIMULAÇÃO — NÃO é integração real com o Vendas (Delphi)" impresso em todo início/fim de execução do harness e repetido em `docs/integracao-simulada/README.md` (seção T-39 acrescentada)/`evidencia-execucao-T-39.txt` (ADR-010). Vendas Pendente e Quitada semeadas diretamente no banco real antes de subir a API (mesma técnica de `ApiHostVendasCancelamentoControllerTests`, T-32 — não há endpoint público de "registrar Pendente" implementado nesta sessão, `POST /api/vendas` é T-62/Tier B condicional); venda desconhecida (cenário 2) não é semeada. **Cenário 1** (Pendente): POST `/api/vendas/cancelamento` com `X-Api-Key`, HTTP 200 `{status:"Cancelada"}`, motivo ausente (opcional para Pendente). **Cenário 2** (desconhecida, D-08): mesmo endpoint com `vendaId` nunca semeado, HTTP 200 `{status:"Cancelada"}` (cria já Cancelada). **Cenário 3** (Quitada sem motivo, D-06): HTTP 409 `{erro:{codigo:"MOTIVO_OBRIGATORIO"}}`, corpo/mensagem batendo com `docs/contrato-v1.1.md` Seção 3.2 (já ajustada por T-37). **Cenário 4** (reenvio pós-timeout, idempotência P-2): mesmo payload do cenário 1 reenviado sobre a venda já Cancelada, simulando o "Vendas" reenviando a mesma requisição por timeout sem saber se ela processou — verificado por dois caminhos complementares, mesmo padrão de T-38: (a) resposta HTTP idêntica (`{status:"Cancelada"}`); (b) consulta direta ao banco via `VendaRepository.ObterPorVendaId`/`FinanceiroDbContext` contando `VendaHistorico` antes e depois do reenvio — 2 registros em ambas as leituras (`Recebida`+`Cancelamento`), confirmando objetivamente "sem duplicar histórico" (a API HTTP não expõe histórico, então essa é a única verificação definitiva do critério). **Cenários 5a/5b/5c** (erros): 5a payload inválido (`vendaId` ausente) -> HTTP 400 `PAYLOAD_INVALIDO`; 5b sem `X-Api-Key` -> HTTP 401 `NAO_AUTORIZADO`; 5c `X-Api-Key` errada -> HTTP 401 `NAO_AUTORIZADO` com corpo **idêntico** ao de 5b (`JToken`/string comparados, confirmando que T-35 não distingue "ausente" de "inválida" para o chamador). Execução real confirmada: `dotnet build` (harness) 0 erros; binário copiado para fora do OneDrive (`C:\temp_erp_t39\net48`, mesmo contorno do Bloqueio 001), DLLs nativas do Firebird copiadas de `src/ERPFinanceiro.Tests/bin/Debug/net48` — **2 execuções seguidas, cada uma com banco novo e `vendaId`s novos (timestamp), ambas 8/8 verificações OK** (evidência bruta completa de request/response em `docs/integracao-simulada/evidencia-execucao-T-39.txt`, roteiro/explicação em `docs/integracao-simulada/README.md`, seção T-39). **Achado corrigido, não bloqueante:** a saída padrão do console (`Console.WriteLine`) usa por padrão o codepage OEM do Windows, divergente do UTF-8 do restante do código/JSON da API — ao redirecionar a execução para arquivo de evidência, os acentos das mensagens (`é`, `ã`, `ç`...) apareciam corrompidos (`�`); corrigido definindo `Console.OutputEncoding = new UTF8Encoding(false)` logo no início de `Main()` antes de qualquer `Console.WriteLine`, sem alterar nenhum comportamento de negócio — mesma disciplina de T-37/T-38 para achados fora do escopo de código de produção (documentado aqui por afetar a legibilidade da própria evidência desta tarefa). **Nenhuma divergência de comportamento encontrada** em relação a `docs/contrato-v1.1.md` Seção 3.2 nem ao já coberto por `ApiHostVendasCancelamentoControllerTests` (T-32, `Concluída`) — nada a registrar em T-40 por esta tarefa. Pasta temporária `C:\temp_erp_t39` e o `.fdb`/log de trabalho (`C:\ERPFinanceiro\data\financeiro_t39_simulado.fdb`, `C:\ERPFinanceiro\data\logs\app_t39_simulado.log`) removidos ao final da sessão. Nenhum código de produção alterado por esta tarefa (só o harness/evidência de QA) — pendência de integração real com o Vendas (Delphi) permanece em aberto, sem alteração por esta tarefa (ADR-010, `BLOCKERS.md` Bloqueio 002). |
| T-40 | **O-11c (redefinida)** Correções da integração simulada (reserva de tempo; só divergências de T-38/T-39) **+ registro formal da pendência de integração real com o Vendas** (não fechável nesta sessão — ver `BLOCKERS.md` Bloqueio 002/ADR-010). Marco: **fim do Dia 4 — suíte simulada verde; integração real com o Delphi permanece pendência externa explícita, não um "concluído"** | BE | A | 1,5h | T-39 | — | contrato v1.1 | Todas as divergências abertas em T-38/T-39 (simulado) fechadas ou registradas em `BLOCKERS.md` com motivo; re-teste dos cenários afetados verde; `BLOCKERS.md`/README (T-54) documentam a pendência de integração real como item em aberto, não resolvido por este lote | Concluída — releitura das notas de T-38 e T-39 (Seção 3) confirmou **zero divergências** de comportamento nos cenários simulados: T-38 registrou só um achado não bloqueante já corrigido no próprio harness de evidência (comparação de `dataQuitacao` por string bruta vs. semântica por `DateTime`, sem tocar código de produção); T-39 registrou explicitamente "nenhuma divergência de comportamento encontrada... nada a registrar em T-40 por esta tarefa". Ou seja, a parte "correções" desta tarefa é um no-op confirmado por leitura (nenhum código de produção alterado, nenhum re-teste de correção necessário além da suíte completa abaixo). Foco da tarefa: registro formal da pendência de integração real. (1) `BLOCKERS.md` Bloqueio 002 complementado com uma atualização datada (23/09/2026, T-40) confirmando o resultado de T-38/T-39 e linkando o novo documento dedicado, **sem reabrir o bloqueio** (status seguiu "Resolvido (redesenho de escopo)"). (2) Criado `docs/integracao-simulada/PENDENCIA-INTEGRACAO-REAL.md` (novo, dedicado): lista objetiva do que falta para a integração real acontecer — aceite formal de `docs/contrato-v1.1.md` pelo time Vendas (T-03, envio/aceite ainda não realizados), acesso a um ambiente com o Vendas/Delphi real ou homologação conjunta (nenhum existe neste sandbox), responsáveis conhecidos (lado ERP Financeiro) e desconhecidos (lado Vendas, não identificado nos artefatos do projeto) e próximos passos em ordem; documento pensado para ser linkado/incorporado por `T-54` (README final, Lote 13, ainda não criado nesta sessão) quando essa tarefa rodar. Suíte de testes completa reexecutada para confirmar que nada regrediu: `dotnet build ERPFinanceiro.sln` (0 erro/0 aviso, 7 projetos) + `dotnet vstest` com o contorno documentado do Bloqueio 001 (build copiado para `C:\temp_erp_t40`, fora do OneDrive, DLLs nativas do Firebird presentes em `bin/x64/Debug/net48`) — **124/124 testes aprovados** (Domain, Api, Infrastructure/Firebird real, Desktop/ApiHost pipeline HTTP completo), sem falhas nem skips. Pasta temporária removida ao final. Marco do Dia 4 (O-11) fechado nos termos do ADR-010: suíte simulada verde; integração real com o Delphi permanece pendência externa explícita e documentada, não apresentada como concluída | Concluída |

> **Lote 8 — Validado** em 23/09/2026 pelo Validador (`QA-REPORT.md`,
> `SECURITY-REVIEW.md`). QA: aprovado, sem ressalvas (0 reprovação crítica ou
> simples nas 6 tarefas; suíte completa reexecutada pelo próprio Validador —
> `dotnet build ERPFinanceiro.sln` 0 aviso/0 erro + `dotnet vstest` fora do
> OneDrive, **124/124 Aprovado em 2 rodadas seguidas**, sem flakiness).
> Redefinição de escopo de T-38/T-39 (ADR-010, `BLOCKERS.md` Bloqueio 002,
> já aprovada pelo usuário antes desta validação) **não reaberta** —
> confirmado só que a simulação foi bem executada e que o rótulo
> "SIMULAÇÃO — NÃO é integração real com o Vendas (Delphi)" está presente em
> toda evidência/README/harness relevante, sem nenhum artefato insinuando
> integração real concluída. DevSecOps: aprovado, sem débito de segurança
> (comparação em tempo constante de `ApiKeyHandler` revisada e aceita como
> criptograficamente adequada para net48; fail-closed sem chave configurada
> confirmado; chave nunca logada; 401 idêntico ausente/inválida; nenhuma rota
> de venda — incluindo as 6 do alias `/api/v1`, T-33 — escapa da checagem;
> `App.config`/DLLs reais dos novos harnesses confirmados fora do controle de
> versão). Checagem estrutural: consistente, sem inconsistência que exija
> redesenho; T-38 confirmada **não mais `Bloqueada`** (redecomposta pelo
> Coordenador e re-executada); `BLOCKERS.md` Bloqueio 002 confirmado coerente
> (Resolvido, pendência residual clara, não reaberto por esta validação);
> Lotes 9-12 confirmados sem dependência quebrada em T-38/T-39/T-40; T-53
> (Lote 13) aponta corretamente para T-40 `Concluída`. Zero achados —
> nenhuma tarefa criada em `Refatoração Lote-8`. Marco "O-11" (Dia 4)
> cumprido nos termos do ADR-010: suíte de integração simulada verde;
> integração real com o Vendas (Delphi) permanece pendência externa
> explícita, documentada em `BLOCKERS.md` Bloqueio 002 e
> `docs/integracao-simulada/PENDENCIA-INTEGRACAO-REAL.md`, não apresentada
> como concluída.

### Lote 9 — Tela de consulta: grid e detalhe (Dia 4, paralelo ao Lote 8) [A]

| ID | Tarefa | Dono | Tier | Est. | Dep. | Paralelizável-com | Premissa | Critério de aceite | Status |
|---|---|---|---|---|---|---|---|---|---|
| T-41 | Base visual: `Formatadores` (moeda, data local, status+ícone), skin única em `DefaultLookAndFeel`, ícones SVG 16 px (check/relógio/x), cultura pt-BR | FE | A | 1,5h | T-04 | T-42 (após contrato de `Formatadores`) | UX-SPEC 3 | Testes de `Formatadores` (moeda `1.250,00`, data UTC->local, status com texto+ícone); nenhuma cor hardcoded | Concluída — `src/ERPFinanceiro.Desktop/Formatadores.cs`: `FormatarMoeda` (N2 pt-BR), `FormatarDataHoraLocal` (UTC->`TimeZoneInfo.Local`, `dd/MM/yyyy HH:mm`, overload nullable->"—"), `FormatarStatus` (texto+`IconeStatus`, sem cor). 13 testes novos em `FormatadoresTests.cs` (137 no total) passam (workaround OneDrive/Bloqueio 001: copiar bin p/ fora do OneDrive antes de `dotnet vstest`). Skin `DefaultLookAndFeel`/SVG real do DevExpress **não implementada nem testada**: pacote DevExpress WinForms trial segue indisponível neste sandbox (mesma limitação de T-02/T-04, feed licenciado sem GUI/instalador). Passos documentados em `ConfiguracaoVisual.cs` para quando o pacote estiver disponível. Contrato de `Formatadores` (assinaturas/tipos) está pronto e testado — T-42 pode consumir. **Adendo (23/09/2026):** a parte de skin (`UserLookAndFeel.Default.SetSkinStyle("Office 2019 Colorful")`) e a `SvgImageCollection` de ícones 16 px (check/relógio/x) foi entregue junto com T-42 em `ConfiguracaoVisual.cs`, após o pacote DevExpress trial passar a ser instalável via nuget.org (Bloqueio 003). Aparência real segue sem conferência visual. |
| T-42 | `FrmConsulta` (F-1): `GridControl` somente leitura, colunas do UX-SPEC 2.1, ordenação padrão, contador "N vendas", "—" e "Cancelada*" com tooltip/legenda, filtro automático DX desligado, dados via `ConsultaService.Listar` | FE | A | 3h | T-23, T-26, T-41 | — | D-10, ADR-007, ADR-008 | Com seed: lista as 3 vendas, formatos corretos, "—" nos nulos; F5 recarrega; grid sem edição | Concluída — `DevExpress.Win.Grid` 25.1.9 adicionado ao Desktop.csproj (trial, apenas warnings DX1000/DX1001; nenhuma licença commitada). Novos: `ConsultaVendasModelo.cs` (`LinhaConsultaVenda` com "—", "Cancelada*"+tooltip, textos via `Formatadores`; `ModeloConsultaVendas` com contador "N vendas"/"1 venda"; `ConsultaVendasPresenter` que carrega `ConsultaService.Listar` em `Task.Run` — regra 5 — e `CarregadorViaContainer` abrindo escopo Autofac por operação, RT-08), `FrmConsulta.cs` (`GridControl`/`GridView` só com controles DX padrão: `Editable=false`, `ShowAutoFilterRow=false`, `AllowFilter=false`, colunas do UX-SPEC 2.1 com Cancelada em oculta/no column chooser, ordenação padrão Recebida em desc, status com ImageComboBox+SVG, tooltip da linha "Cancelada*", legenda no rodapé, F5 recarrega, `async void Recarregar` sem bloquear UI), `ConfiguracaoVisual` (skin + SVGs, ver adendo em T-41). Program.cs mantido placeholder (T-46). **Verificado por execução real:** 4 testes novos em `FrmConsultaTests.cs` (151 no total, todos passam via `dotnet vstest` fora do OneDrive): presenter contra Firebird embarcado real com seed equivalente a T-06 (3 vendas, ordem, formatos, "—" nos nulos, Cancelada*+tooltip, contador), recarga (F5 = nova chamada do presenter), e configuração real de `GridControl`/`GridView` instanciados headless em thread STA sem `Show()` (funcionou estável, sem hang nem erro de licença): colunas visíveis, `Editable=false`, sem filtro automático, sort Recebida em desc, texto de exibição por célula. **Pendência de conferência manual do usuário (máquina com GUI):** aparência real, alinhamento à direita do valor, ícones SVG e cor da skin renderizados, tooltip aparecendo ao passar o mouse, F5 pelo teclado no form exibido, largura/legibilidade da legenda; nada disso foi visto. Estados vazio/carregando/erro/truncado = T-43 (erro hoje só mostra texto mínimo). |
| T-43 | Estados de F-1: vazio (texto central via `CustomDrawEmptyForeground`), carregando (`ProgressPanel`, UI não congela), erro de banco (+ "Tentar novamente"), aviso "Mostrando as 5.000 mais recentes" | FE | A | 2h | T-42 | T-44 | ADR-008 | Os 4 estados reproduzidos: banco vazio, consulta lenta simulada, `.fdb` indisponível, 5.001 linhas; textos iguais aos do UX-SPEC 4 | Concluída. Nota (23/09/2026): `EstadoConsulta`, `TextosConsulta` (textos literais UX-SPEC 4/2.1), `ModeloConsultaVendas.Estado/AvisoTruncado` em `ConsultaVendasModelo.cs`; `FrmConsulta`: vazio via `GridView.CustomDrawEmptyForeground` (único componente custom, regra 12), carregando via `ProgressPanel` sobreposto (carga em `Task`, UI livre), erro com "Tentar novamente" (mensagem fixa, sem stack; grid esmaecida mantém dados anteriores), aviso das 5.000 no rodapé. **Verificado por execução real** (`FrmConsultaEstadosTests`, 7 testes, Firebird embarcado real): banco vazio, carregando observável com carregador bloqueado e `Task` não concluída (UI thread livre), erro (carregador lançando `BancoIndisponivelException` + `.fdb` em pasta inexistente) e retry refazendo a carga, 5.001 linhas reais -> 5.000 + aviso, textos iguais ao UX-SPEC. **Pendente de conferência visual manual do usuário** (sem display): renderização/posicionamento do texto central do vazio, do `ProgressPanel`, do painel de erro e do aviso no rodapé; clique real no botão (headless chama o mesmo handler, `PerformClick` não dispara sem `Show()`). Suíte: 164 testes; falhas intermitentes observadas em `ApiHostVendas*ControllerTests`/`ConcorrenciaIdempotenciaTests` variam entre execuções (contenção de portas/Firebird com outra instância rodando testes em paralelo), não relacionadas a T-43 |
| T-44 | `FrmDetalheVenda` (F-3, modal): cabeçalho + aba Itens (subtotal, total, texto p/ venda sem itens), `Esc` fecha, foco volta à linha; aba Histórico **não existe** (Tier C); 4 estados | FE | A | 2,5h | T-24, T-42 | T-43 | ADR-007 | Duplo clique/Enter abre; soma dos subtotais = valor da venda (seed); cancelada sem quitação mostra o texto explicativo; falha de itens mostra "Tentar novamente" sem fechar | Concluída — Novos: `DetalheVendaModelo.cs` (`LinhaItemDetalhe` preço N4/subtotal N2, `CabecalhoDetalheVenda` a partir da linha do grid ou do DTO, `ModeloDetalheVenda` com "Total itens: X" e `TextoSemItens` exato do UX-SPEC 2.2, `DetalheVendaPresenter` com `ObterDetalhe` em `Task.Run` e `CarregadorViaContainer` com escopo Autofac por operação) e `FrmDetalheVenda.cs` (`XtraForm` modal, só controles DX padrão: `LabelControl`, `XtraTabControl` com única aba "Itens" — sem aba Histórico, Tier C —, `GridControl` somente leitura, `ProgressPanel`, `SimpleButton` "Tentar novamente"/"Fechar", `CancelButton`=Fechar para Esc; abre com cabeçalho da linha e carrega itens async; falha mostra aviso + "Tentar novamente" sem fechar). Edição mínima em `FrmConsulta.cs`: duplo clique/Enter na grid chama `AbrirDetalhe()` (propriedade `DetalhePresenter` a ser atribuída pelo Program/composition root, que ainda é esqueleto T-04) e devolve foco à linha de origem após `ShowDialog`. **Verificado por execução real** (`FrmDetalheVendaTests`, 6 testes, Firebird embarcado + seed equivalente a T-06, form headless STA sem Show): V-1001 subtotais 300,00+200,50, total 500,50 = valor da venda; V-1003 sem itens sem erro, cabeçalho "Cancelada*"/"—"/motivo, texto explicativo; venda inexistente propaga exceção; grid somente leitura; 1 aba; `CancelButton` = Fechar; estado de erro com aviso + "Tentar novamente" e form não fechado. **PENDENTE de conferência manual do usuário (sem display)**: aparência/layout real (espaçamento, truncamento de labels a 1024x600/DPI), abrir de fato por duplo clique e Enter, Esc fechando com o form aberto, foco visual voltando à linha, `ProgressPanel` animando, clique real em "Tentar novamente" (F-3 fecha-e-reabre sem cache), ligação de `DetalhePresenter` no Program (T-46/T-51). Nota de teste: `Control.Visible` retorna false sem handle, por isso o form guarda estado lógico próprio. |

> **Lote 9 — Validado com ressalvas (Validador, 23/09/2026).** QA: T-41…T-44
> aprovadas contra o critério literal e o UX-SPEC 2.1/2.2/4 (textos dos estados
> idênticos, sem `.Result`/`.Wait()` na UI, escopo Autofac por carga, erro sem
> stack/caminho, aba Histórico ausente); suíte independente **164/164 em 4
> execuções seguidas** (T-20 intermitente não reproduzido; observação, sem
> reprovação). DevSecOps: nenhum achado alto/crítico, nenhuma licença/segredo
> versionado. **Pendência de conferência visual manual do usuário (máquina com
> GUI): nada de aparência foi verificado por ninguém** (layout, ícones SVG,
> skin, tooltip, F5/Enter/Esc com form exibido, foco visual, `ProgressPanel`,
> cliques reais, 1024x600/DPI) — registrada honestamente em T-41…T-44,
> Bloqueio 003 e `QA-REPORT.md`. Dependência conhecida: `DetalhePresenter`/
> presenters só são ligados no composition root (T-46; RL9-02). Checagem
> estrutural: 4 tarefas `Concluída`, sem dependência quebrada nem `Bloqueada`.
> 4 achados simples viraram tarefas em `Refatoração Lote-9` (RL9-01…RL9-04, fim
> da Seção 3).

### Lote 10 — Tela: status e ciclo de vida (Dia 4) [A]

| ID | Tarefa | Dono | Tier | Est. | Dep. | Paralelizável-com | Premissa | Critério de aceite | Status |
|---|---|---|---|---|---|---|---|---|---|
| T-45 | Barra de status (F-6): indicadores independentes API/Banco (ícone+texto), timer 15 s via `IHealthService` em processo, clique abre diálogo (porta, caminho `.fdb`, último erro, **Copiar**) | FE | A | 2,5h | T-22, T-27, T-42 | T-46 | D-04, ADR-004 | Derrubar porta/banco muda o indicador em <= 15 s; texto sempre presente (nunca só cor); "Copiar" coloca o erro na área de transferência | Concluída - lógica e controles implementados e testados (173/173 verdes, isolado): `BarraStatusPresenter` (API via `IFonteEstadoApi`/`ApiHost.Estado`, Banco via `IHealthService` em `Task.Run`, sem empilhar, timer 15 s injetável `ITemporizador`, `IAreaTransferencia` para Copiar), `BarraStatusVisual` (`BarManager` StatusBar + `BarStaticItem` ícone SVG + texto) e `FrmDetalhesStatus` (porta, .fdb, último erro, Copiar); `FrmConsulta.AnexarBarraStatus(presenter)` para T-46 ligar. Verificado por execução: textos/ícones por estado, independência API/Banco, tick de 15 s (timer fake) muda o indicador, não empilha, Copiar chama a abstração, controles headless STA. PENDENTE conferência manual do usuário: aparência real da barra, timer real com o form aberto, clique abrindo o diálogo, Clipboard real, e fiação em Program (T-46) para fechar. **Ligação no app real feita (executor, 23/09/2026, junto com T-47):** `ComposicaoJanelaPrincipal` (novo) cria `BarraStatusPresenter(FonteEstadoApiHost, IHealthService do container, porta, caminho .fdb, AreaTransferenciaWinForms, TemporizadorWinForms)`, chama `FrmConsulta.AnexarBarraStatus` (inicia timer) e é instanciada em `Program.Main` antes de `Application.Run`; barra parada/descartada no encerramento (T-47). Testado (`EncerramentoTests.Montagem_anexa_barra...`, ApiHost + Firebird reais, headless): timer 15 s iniciado, textos "API: Ativa (http://localhost:porta)" e "Banco: OK". Conferência visual/ao vivo (barra real, timer real, diálogo, Clipboard) segue PENDENTE do usuário. |
| T-46 | Inicialização (F-7): splash "Iniciando banco e API...", `DbInitializer` + host, mutex de instância única, falha de banco abre `FrmConsulta` em estado de erro (nunca fecha silencioso), falha de porta mostra diálogo com instruções | FE | A | 2h | T-12, T-27, T-42 | T-45 | D-04 | Segunda instância mostra aviso e sai; `.fdb` bloqueado -> tela em erro; porta ocupada -> diálogo com causa e ação | Concluída — **Nota (executor, 23/09/2026):** lógica em `SequenciaInicializacao` (sem UI; delegates para mutex/banco/API; resultado tipado `ResultadoInicializacao.Decisao`), `InstanciaUnica` (mutex `Global\ERPFinanceiro.Desktop.InstanciaUnica`, constante documentada), `TextosInicializacao` (splash/aviso/diálogo conforme UX-SPEC F-7) e `Program.Main` fino. Decisões: segunda instância sai sem tocar em `.fdb`/porta; a API é tentada mesmo com banco em erro (F-6 mostra "Banco: indisponível"); banco em erro abre `FrmConsulta` normalmente e a carga cai no estado F-1 Erro (T-43); porta ocupada/falha de host -> `XtraMessageBox` Sim/Não com causa + ação (trocar `Api:Porta`, fechar outra instância): Sim = continuar sem API, Não = sair; falha de config antes da sequência mostra mensagem (nunca sai calado). **Verificado por execução real:** 10 testes xUnit em `SequenciaInicializacaoTests` (Firebird embarcado real: `.fdb` ausente cria; `.fdb` bloqueado `FileShare.None` -> banco em erro + log; `HttpListener` na porta -> porta em uso + texto do diálogo; segunda instância não chama banco/API; mutex com nomes únicos entre threads; presenters repassam exceção ao log); suíte completa 183/183 (baseline 164 + 10 deste executor + 9 de outra instância). **Pendente de conferência manual do usuário:** splash real (`SplashScreenManager.ShowDefaultWaitForm`), diálogos reais, mutex entre dois PROCESSOS reais, app de ponta a ponta com `App.config` real. Sem `SetDPIAware`/`AutoScaleMode` (T-51). T-47 não implementada. |
| T-47 | Encerramento: confirmação ao fechar ("Fechar encerra a API; o ERP Vendas não conseguirá enviar vendas.") e `ApiHost.Stop` ordenado | FE | A | 1h | T-46 | — | D-04 | Fechar pede confirmação; "Não" mantém app e API; "Sim" libera a porta e o `.fdb` (processo termina) | Concluída — **Nota (executor, 23/09/2026):** `EncerramentoAplicacao` (lógica sem UI: `PodeFechar(confirmar)`, `Encerrar()` idempotente na ordem barra/timer -> `ApiHost.Stop` -> `container.Dispose` -> libera mutex; falha em um passo não pula os seguintes) e `ComposicaoJanelaPrincipal` (liga `FormClosing`: "Não" => `e.Cancel = true`; `FormClosed` => `Encerrar`; sem edição em `FrmConsulta.cs`). Diálogo `XtraMessageBox` Sim/Não, texto literal do UX-SPEC, título "ERP Financeiro", ícone Warning, **foco padrão em "Não"** (UX-SPEC não define; decisão de detalhe: ação segura). Desligamento do Windows/Gerenciador de Tarefas não pergunta. Processo termina pelo retorno de `Application.Run` + `finally` do `Program`. **Verificado por execução real:** `EncerramentoTests` (5 testes; ApiHost em porta livre + Firebird embarcado real): "Não" mantém `ApiHost.Estado == Ativa`, timer não parado; "Sim" + `Encerrar` 2x => timer parado/descartado, Estado Inativa, novo `HttpListener` sobe na mesma porta, `.fdb` abre com `FileShare.None` e é apagado; suíte completa 188/188 verde 2x. **Pendente de conferência manual do usuário:** aparência/foco do diálogo real, fechar a janela de verdade, processo realmente terminando (Gerenciador de Tarefas) e barra rodando ao vivo. |

> **Lote 10 — Validado com ressalvas (Validador, 23/09/2026).** QA: T-45, T-46
> e T-47 aprovadas contra o critério literal e o UX-SPEC F-6/F-7 (textos dos
> indicadores, splash, aviso de segunda instância e confirmação ao fechar
> idênticos; health check em `Task.Run`, sem `.Result`/`.Wait()`, sem empilhar;
> encerramento na ordem barra -> `ApiHost.Stop` -> container -> mutex,
> idempotente, falha de passo não pula os seguintes; "Não" mantém a API; mutex
> `Global\ERPFinanceiro.Desktop.InstanciaUnica`; segunda instância não toca
> `.fdb`/porta; `ApiHost` "só como fonte de estado" não abre listener nem
> `DbContext`). Suíte independente **188/188 em 6 de 7 execuções**; 1 falha
> intermitente em `ApiHostVendasStatusControllerTests` (NotFound em vez de OK,
> Lote 8, não relacionada a este lote; RL10-06). **Pendência de conferência
> visual/ao vivo do usuário (máquina com GUI): nada de aparência/comportamento
> real foi verificado por ninguém** (barra, timer real, diálogo de detalhes,
> Clipboard, splash, diálogos reais, foco visual do "Não", fechar a janela de
> verdade, dois processos reais). RL9-02, RL9-03 e RL9-04 conferidas e
> resolvidas (`Concluída`); RL9-01 segue `Em andamento` até a conferência
> visual (SVG `class="Black"` resolvido pela paleta da skin: plausível pela API
> DevExpress, sem risco de segurança). Checagem estrutural: 3 tarefas
> `Concluída`, sem dependência quebrada nem `Bloqueada`; 6 achados simples
> viraram tarefas em `Refatoração Lote-10` (RL10-01…RL10-06, fim da Seção 3).

### Lote 11 — Relatório (Dia 5) [A]

| ID | Tarefa | Dono | Tier | Est. | Dep. | Paralelizável-com | Premissa | Critério de aceite | Status |
|---|---|---|---|---|---|---|---|---|---|
| T-48 | `RelatorioDataSource` (Reports): mesmo `ConsultaService` e mesmo `FiltroVendas`, **sem limite de 5.000**, "Total listado" calculado da mesma lista, nulos como 0 + contagem para nota de rodapé | BE | A | 2h | T-23 | T-49 (após DTO `LinhaRelatorio`) | D-10, ADR-008 | Com seed: Total listado = soma documentada em T-06 (CA-07.3); lista vazia -> total 0,00 | Concluída — Decisão de design (SDD 2.1, Reports referencia só Application, nunca EF direto; `database/01-schema.sql` inalterado): adicionado `IVendaConsultaLeitura.ListarTodas()` (nova, ao lado de `ListarMaisRecentes` de T-23) — mesma ordenação `DataRecebimento` desc, sem tracking, mas **sem** `Take`, implementada em `VendaRepository.ListarTodas()` (`src/ERPFinanceiro.Infrastructure/Persistencia/VendaRepository.cs`). `ConsultaService.ListarParaRelatorio(FiltroVendas)` novo (`src/ERPFinanceiro.Application/Consultas/ConsultaService.cs`), ao lado de `Listar` (T-23): usa `ListarTodas` (sem limite de 5.000, regra 15), mapeia para o novo DTO `LinhaRelatorio` (`src/ERPFinanceiro.Application/Consultas/LinhaRelatorio.cs` — tipo próprio, não reaproveita `VendaListagemDto`, para T-49/`.frx` poder evoluir independente da grade F-1) e devolve `ResultadoRelatorioVendas` (`.../ResultadoRelatorioVendas.cs`) com `TotalListado` (soma de `ValorTotal ?? 0m` **da própria lista mapeada**, nunca segunda consulta) e `QuantidadeSemValor` (contagem de `ValorTotal == null`, insumo da nota de rodapé UX-SPEC 2.3: "Vendas com valor nulo... entram como 0 e são contadas"). `RelatorioDataSource` (`src/ERPFinanceiro.Reports/RelatorioDataSource.cs`, projeto T-04, refs só `Application`) é camada fina: recebe `ConsultaService` (mesma instância da tela, ADR-008, em processo sem HTTP) e expõe `Obter(FiltroVendas)` chamando `ListarParaRelatorio`. `filtro` continua deliberadamente ignorado por ora (mesma decisão já tomada por `Listar`/T-23 — aplicação real é T-57, Tier B); documentado nos comentários XML de todos os métodos novos. Testes: unitário (Moq, sem banco) em `src/ERPFinanceiro.Tests/Application/ConsultaServiceListarParaRelatorioTests.cs` (3 casos: massa equivalente ao seed T-06 com Total listado 951,00 e QuantidadeSemValor=1; lista vazia -> 0,00; sem `IVendaConsultaLeitura` -> `InvalidOperationException`); integração real contra Firebird embarcado em `src/ERPFinanceiro.Tests/Reports/RelatorioDataSourceTests.cs` (3 casos: massa idêntica ao `database/02-seed.sql` de T-06 via `Venda.CriarPorQuitacao`/`CriarPendente`/`CriarCancelada` -> Total listado = **951,00** confere CA-07.3, ordenação `DataRecebimento` desc preservada, QuantidadeSemValor=1 para V-1003; banco vazio -> `Linhas` vazia e Total listado 0,00; massa de 5.005 vendas -> `RelatorioDataSource` devolve as 5.005 linhas e Total listado 5.005,00, confirmando a ausência do limite de 5.000 de `ConsultaService.Listar`/T-23). `src/ERPFinanceiro.Tests/ERPFinanceiro.Tests.csproj` ganhou `ProjectReference` a `ERPFinanceiro.Reports` (só para o teste de integração). **Execução real confirmada**: `dotnet build src/ERPFinanceiro.Tests` — 0 avisos/0 erros (solução inteira compila, incluindo `Reports`). Contorno do Bloqueio 001 (mesmo de T-09/T-11/T-12/T-23: pasta OneDrive recusa o test adapter net48, HRESULT 0x80131515) aplicado — `bin/Debug/net48` copiado para pasta local fora do OneDrive e `dotnet vstest ERPFinanceiro.Tests.dll` rodado de lá: filtro `FullyQualifiedName~Relatorio` -> **6/6 Aprovado** (3 unitários + 3 integração); suíte completa do projeto Tests -> **143/143 Aprovado, 0 falha** (sem regressão nas tarefas anteriores). Nenhuma alteração em `database/01-schema.sql`/`02-seed.sql`. |
| T-49 | Layout FastReport `.frx` de listagem: cabeçalho (título, filtro aplicado, data de emissão), colunas, "Total listado", nota de nulos, mensagem "Nenhuma venda no período" | FE | A | 3h | T-02, T-48 | — | ADR-006 | Renderiza com seed e com lista vazia; sem cor hardcoded; PDF gerado abre | Concluída (com pendência de conferência visual manual, abaixo) — **Biblioteca:** FastReport Open Source (MIT) 2023.3.13 (já existente) + `FastReport.OpenSource.Export.PdfSimple` 2023.3.13 (MIT, novo em `ERPFinanceiro.Reports.csproj`; Reports segue referenciando só Application). Teste: `PdfPig` 0.1.16 (Apache-2.0, só em `ERPFinanceiro.Tests`). **Verificado por execução (net48, headless):** existem no nuget.org público `FastReport.OpenSource`, `...Export.PdfSimple` (restauram e geram PDF sem GUI, sem marca d'água) e `FastReport.Net.Demo` (demo comercial, `requireLicenseAcceptance`, NÃO adotado nem restaurado; não existe `FastReport.Net` restaurável). **Achados que refinam `docs/licencas.md`/ADR-006/008 (não reescritos; levar ao Coordenador):** (1) o PDF do Open Source é **raster** (cada página = JPEG 300 dpi 2480x3508 dentro do PDF): abre/imprime, mas texto não selecionável/pesquisável e ~250 KB–1 MB por página (fontes embutidas/vetorial só no FastReport .NET comercial); (2) confirmado por reflexão que o Open Source 2023.3.13 NÃO tem `PreviewControl` WinForms (teste `FastReportOpenSource_NaoTemPreviewControlWinForms_ConfirmaLicencasMd`), portanto T-50 no caminho Open Source cai no fluxo "sem preview" (PDF em pasta temporária + abrir/mostrar caminho); preview exigiria FastReport .NET Trial/comercial — decisão registrada, não adotada. **Implementação:** layout `src/ERPFinanceiro.Reports/Layouts/RelatorioListagem.frx` como Embedded Resource (`ERPFinanceiro.Reports.Layouts.RelatorioListagem.frx`; T-53 não precisa empacotar arquivo), A4 retrato: título, "Emitido em", "Filtro: ...", colunas VendaId/ClienteId/Valor/Status/Recebida/Quitada/Cancelada, "Total listado: R$ x (soma dos valores listados)", nota de nulos (só se `QuantidadeSemValor` > 0), "Nenhuma venda no período" (só se lista vazia), "Página n de N". `GeradorRelatorioPdf.Gerar(ResultadoRelatorioVendas, descricaoFiltro, emitidoEmLocal, Stream\|caminho)` e `GerarHtml(...)` (mesma renderização em HTML, usada nos testes). Formatação: `FormatacaoRelatorio` em Reports (moeda N2 pt-BR, data local dd/MM/yyyy HH:mm, status em TEXTO sem ícone, nulo "—"; ClienteId/Valor nulos = "—"/0,00); duplica `Desktop.Formatadores` porque Reports não referencia Desktop, e o teste `FormatacaoRelatorio_IgualAFormatadoresDoDesktop` garante saída idêntica. **"Sem cor hardcoded" (interpretação adotada):** nenhum controle do `.frx` tem cor/fill literal; todos referenciam estilos nomeados (`Texto/Titulo/Cabecalho/Linha/Total`) definidos num único bloco `<Styles>` (impressão neutra: preto sobre branco, cabeçalho cinza `Gainsboro`); teste `Layout_NaoTemCorLiteralForaDoBlocoDeEstilos` varre o `.frx`. **Testes (6, reais, Firebird embarcado com massa idêntica ao seed T-06):** seed -> PDF `%PDF`, >0 bytes, 1 página com imagem (PdfPig), e texto (do HTML da mesma renderização) com título, emissão, filtro, colunas, V-1001/2/3, 500,50, 450,50, "Total listado: R$ 951,00" e nota "1 venda com valor nulo"; lista vazia -> "Nenhuma venda no período", total 0,00, sem nota; 200 linhas -> várias páginas, total no fim, "Página 1 de N". Suíte completa: **194/194** (baseline 188 + 6), sem intermitência nesta execução. Variável opcional `ERP_T49_SAIDA` preserva os PDFs de exemplo. **Evidência extra:** extraí o JPEG da página do PDF do seed e li como imagem: cabeçalho, colunas, linhas, total 951,00 e nota aparecem coerentes (leitura por imagem, não conferência humana). **PENDENTE (usuário):** conferência visual manual de um PDF de exemplo (tipografia, alinhamento, quebra de página em impressão real) — não declarado como verificado visualmente. |
| T-50 | Fluxo "Emitir relatório" (F-5): botão, `WaitForm` cancelável, preview (se T-02 = tem preview) ou PDF em pasta temporária + abrir/ mostrar caminho, erros em `XtraMessageBox`, 4 estados | FE | A | 2,5h | T-42, T-49 | — | ADR-008 | Cenário com preview e cenário sem preview (forçado por configuração) funcionam; falha de arquivo mostra causa + caminho; relatório usa o filtro corrente | Concluída (com pendências de conferência manual e divergência do preview, abaixo) — **Herdado do trabalho parcial (revisado, sem reescrita):** `Desktop/FluxoEmitirRelatorio.cs` (lógica sem UI: `Task.Run` + `CancellationToken`, sem `.Result/.Wait()`; gera em `.tmp` e só renomeia para `.pdf`, cancelamento apaga parcial; falha de arquivo = "Causa: ... Arquivo: <caminho>" sem stack, detalhe vai ao log; PDF em `%TEMP%\ERPFinanceiro\Relatorios\RelatorioVendas_<data>_<guid6>.pdf` sem dado de negócio no nome; limpeza de PDFs > 7 dias em melhor esforço; única ação externa = abrir o PDF gerado via `Process.Start` shell, `Process` descartado; escopo Autofac novo por emissão em `Program.CriarFluxoRelatorio`), `EmissaoRelatorioVisual.cs` (botão `&Emitir relatório` Alt+E, `FrmAguardeRelatorio` "Gerando relatório..." com Cancelar/Esc, `XtraMessageBox` nos erros, "Relatório gerado" na barra de status, botão desabilitado durante a emissão), ligação em `ComposicaoJanelaPrincipal`/`Program`, chave `Relatorio:PreviewDesativado` em `App.config.example`, e `FluxoEmitirRelatorioTests` (26 casos: seed real + gerador real de PDF do T-49, abridor e preview FAKES). Revisão não achou defeito de lógica; nada precisou mudar no código. **Divergência do preview (sem ambiguidade):** ramo com preview implementado como ponto de extensão (`IApresentadorPreviewRelatorio`, ADR-008) e testado com apresentador FAKE; preview real inexistente no FastReport Open Source adotado (`ApresentadorPreviewIndisponivel.Disponivel = false`, ver T-49) — o build atual sempre gera PDF e abre no visualizador do sistema (ou mostra o caminho). NÃO há preview real funcionando. Decisão para o Coordenador/usuário: manter Open Source ou avaliar FastReport .NET comercial. A chave `Relatorio:PreviewDesativado=true` força o modo sem preview (testado com fake disponível). **Filtro corrente:** em Tier A a grade carrega com `new FiltroVendas()` (`ConsultaVendasModelo`) e `Program` passa o mesmo filtro vazio ao relatório (sem limite de 5.000); T-58 (Tier B) deve trocar `filtroCorrente` pelo filtro real do painel. **Causa das falhas de suíte observadas:** não é regressão do T-50. Com `testhost`/`dotnet` órfãos e arquivos de lock do Firebird embarcado deixados em `C:\ProgramData\firebird` (`fb_lock_*`/`fb12_monitor_*`) por hosts encerrados à força, `CreateDatabase` falha com "WaitForSingleObject failed", roda por minutos ou o host cai (visto também em `FinanceiroDbContextTests`, fora do T-50). Reproduzi: na 1a tentativa o filtro `FluxoEmitirRelatorio` travou 7 min com 5 falhas; após matar processos órfãos e limpar os locks, o mesmo filtro passou 26/26. Evite `taskkill` de hosts no meio de rodadas (piora o estado). **Verificado por execução (net48, headless, Firebird embarcado real):** suíte completa **220/220 (194 + 26 novos), 2 execuções seguidas verdes** em cópia isolada, sem intermitência nessas duas; build 0 erros/0 avisos. **PENDENTE (usuário, conferência manual — sem display, não declarado verificado visualmente):** botão/atalho Alt+E reais, `FrmAguardeRelatorio` aparecendo/cancelando de verdade, `XtraMessageBox` de erro, PDF abrindo no leitor do sistema, posição do painel do botão na janela (ordem de docking com grade/barra de status), e mensagem na barra de status. |

### Lote 12 — Acessibilidade e verificação (Dia 5) [A]

| ID | Tarefa | Dono | Tier | Est. | Dep. | Paralelizável-com | Premissa | Critério de aceite | Status |
|---|---|---|---|---|---|---|---|---|---|
| T-51 | Acessibilidade da tela (UX-SPEC 5): `TabIndex` explícito, `AccessibleName/Description`, `AccessKey`, atalhos F5/Enter/Esc, `AutoScaleMode=Dpi` + `SetDPIAware`, tamanho mínimo 1024x600/640x420 | FE | A | 2h | T-42, T-44, T-45, T-46, T-47, T-50 | — | UX-SPEC 5/6 | Navegação completa só por teclado; DPI 125/150% sem corte; contraste da skin conferido (>= 4,5:1) ou variante de alto contraste aplicada | Concluída (parte verificável por código; conferência visual/ao vivo pendente, ver T-52) — `FrmConsulta`, `FrmDetalheVenda`, `FrmDetalhesStatus` e `FrmAguardeRelatorio` com `AutoScaleMode=Dpi`; `ConfiguracaoVisual.AplicarAcessibilidadeDpi()` (`WindowsFormsSettings.SetDPIAware()`) chamado como 1ª linha de `Program.Main`, antes de qualquer janela. Tamanhos mínimos: principal 1024x600; `FrmDetalheVenda` 640x420 (já existia); `FrmDetalhesStatus` passou de 560x240 para 640x420 (+`MinimumSize`); `FrmAguardeRelatorio` (espera pequena, `FixedDialog`, só Cancelar) **não** recebeu mínimo 640x420 por decisão de detalhe (seria absurdo para um diálogo de 300x110; só `AutoScaleMode=Dpi`). `TabIndex` explícito e `TabStop=false` em painéis/rótulos: F-1 = grid, "Tentar novamente", "Emitir relatório" (após a grid), rodapé; F-3 = itens, "Tentar novamente", "Fechar"; diálogo de status = memo, "Copiar", "Fechar". Foco inicial na grid (F-1/F-3) e em "Copiar" (status). `AccessibleName`/`AccessibleDescription` em grid, botões, abas, avisos, contador e legenda ("Lista de vendas", "Itens da venda" etc.). `AccessKey`: `&Emitir relatório`, `&Tentar novamente`, `&Fechar`, `&Copiar`, `&Cancelar` (já existiam, agora testados). Atalhos: F5 (atualiza), Enter na grid (abre F-3), Esc (`CancelButton` em F-3/status/espera) — lógica extraída para `FrmConsulta.TratarTecla`/`TratarTeclaGrid`, testável. **RL10-01 incluída aqui**: F2 na `FrmConsulta` abre o diálogo de detalhes da barra de status (`BarraStatusVisual.AoClicar`), documentado no `AccessibleDescription` da grid. Testes novos: `src/ERPFinanceiro.Tests/Desktop/AcessibilidadeTests.cs` (10: mínimos/AutoScale, nomes/descrições, ordem de Tab efetiva, foco inicial, F5, Enter, F2 abrindo o diálogo real, F2 sem barra, Esc/`CancelButton`, contraste). Contraste calculado por código (razão WCAG) com as cores da paleta da skin "Office 2019 Colorful" (`CommonSkins`: WindowText/Window, ControlText/Control) e `SystemColors.GrayText` (texto do estado vazio) sobre Window: todos >= 4,5:1 (teste falha se algum cair abaixo); é cálculo pela paleta, **não** contraste renderizado (cores de cabeçalho/seleção da grid, ícones e desabilitados não medidos). Suíte completa: 234/234 (224 + 10 novos), no isolamento FIREBIRD/FIREBIRD_LOCK. Nota de ambiente: a worktree nova não tinha as DLLs nativas do Firebird nem o `App.config` do Tests; recriados só na pasta temporária (fora do repo), removida ao final. **Pendências de conferência manual (sem display aqui):** DPI 125/150% real sem cortes (labels de largura fixa em `FrmDetalheVenda` com `AutoScaleMode=Dpi`), Tab/Shift+Tab ao vivo, foco visível, contraste renderizado/alto contraste do Windows, leitura no Narrador (T-52). |
| T-52 | Verificação manual com Narrador: checklist de 8 itens do UX-SPEC 5, resultado registrado em `docs/acessibilidade.md` | QA | A | 1h | T-51 | — | UX-SPEC 5 | 8 itens marcados ok/nok com evidência; nok crítico vira correção antes do Dia 6 | Concluída (dispensada pelo usuário, 23/09/2026) — **a verificação manual com Narrador NÃO foi realizada**: o usuário instruiu ignorar o Lote 12 e considerá-lo concluído (Bloqueio 005). `docs/acessibilidade.md` não foi criado; nenhum dos 8 itens tem evidência. Pendências manuais de T-51 (DPI 125/150% real, Tab ao vivo, contraste renderizado, Narrador) permanecem em aberto como risco aceito. |

### Lote 13 — Entrega O-12 (Dia 5 preparação; 26-27/09 conclusão; **sem código novo**) [A]

| ID | Tarefa | Dono | Tier | Est. | Dep. | Paralelizável-com | Premissa | Critério de aceite | Status |
|---|---|---|---|---|---|---|---|---|---|
| T-53 | **Redefinida (entrega só do código-fonte, decisão do usuário 24/09/2026)** Manter apenas o script de empacotamento opcional `tools/T-53-empacotar` (build Release + pasta de entrega); sem `.fdb` nem pasta de entrega obrigatórios | Build Release + empacotamento: pasta com executável, DLLs Firebird (bitness de T-01), `database/*.sql`, `.fdb` de demonstração (backup do banco), `App.config.example` | DOC/OPS | A | 2h | T-40, T-50, T-52 | T-54, T-55 | RNF-14 | Script existe e a verificação (exceto `.fdb`) passa; sem `ApiKey` real | Concluída (redefinida) — script `tools/T-53-empacotar` (Empacotar.ps1/Verificar.ps1) entregue e verificado, exceto a geração do `.fdb`, que deixou de ser exigida. A entrega do processo seletivo é só o código-fonte, então o risco de redistribuição DevExpress (evaluation) não se aplica. Bloqueio 006 encerrado |
| T-54 | README: pré-requisitos, instalação dos trials (T-02), configuração (porta, connection string, ApiKey), execução, integração com o Vendas, limitações/dívidas aceitas (SDD 6), versões e licenças | DOC/OPS | A | 2h | T-02, T-03 | T-53, T-55 | D-09 | Um leitor sem contexto executa o passo a passo sem perguntas; lista de versões/licenças completa | Concluída (escopo documental) — `README.md` na raiz: pré-requisitos (incl. DLLs Firebird x64, ADR-009), trials (T-02, com aviso de que não foram instalados de fato), configuração (chaves reais do `App.config.example`), banco, integração Vendas (contrato v1.1), dívidas (SDD 6), versões/licenças de todos os `PackageReference`. Pendências explícitas no README: execução/Desktop (`Program.Main` ainda lança NotImplemented), demais endpoints de vendas, leitura de `Api:Porta`, tela (T-50) (README reatualizado após merge da main com T-35/T-36/T-48/T-49), pacote T-53, evidências T-55, versões DevExpress/FastReport e licenças NuGet a conferir no nuget.org; seções de execução a reconfirmar em T-56. |
| T-55 | Evidências: prints da tela (lista, detalhe, erro, indicador) e PDF de exemplo do relatório em `docs/evidencias/` | DOC/OPS | A | 1h | T-50 | T-53, T-54 | CA-10.2 | Arquivos existem e mostram dados do seed; PDF abre | Concluída (com ressalva) — `docs/evidencias/`: `01-lista.png`, `02-detalhe.png`, `03-erro.png`, `04-indicador-barra-status.png`, `05-indicador-detalhes-status.png`, `relatorio-exemplo.pdf` (%PDF, 1 página, total R$ 951,00 = seed) e `README.md` (índice). Capturas reais das janelas reais e PDF do `FluxoEmitirRelatorio`/`GeradorRelatorioPdf` reais, via harness `tools/T-55-evidencias`. **Ressalva:** dados do seed vieram de repositório em memória (mesmos valores de `02-seed.sql`), não do Firebird (testhost órfão PID 31468 impede o motor embarcado); indicador "API/Banco" e o estado de erro usam stubs. O modo com banco real (`Executar.ps1 -FirebirdDir`) não foi validado; regenerar em máquina limpa (T-56) se desejado. |
| T-56 | Teste em **máquina limpa** (clonar o repositório, compilar e rodar seguindo apenas o README; sem pasta de entrega); registrar resultado; só correção de bug bloqueante (sem funcionalidade nova) | QA | A | 2h | T-54, T-55 | — | CA-10.3 | API, tela e banco sobem na máquina limpa; e cada desvio do README corrigido no README; relatório emitido | A fazer |

### Lote 14 — Tier B (só se sobrar tempo; executa no Dia 5 antes do Lote 13 fechar; **cortar de baixo para cima**) [B]

| ID | Tarefa | Dono | Tier | Est. | Dep. | Paralelizável-com | Premissa | Critério de aceite | Status |
|---|---|---|---|---|---|---|---|---|---|
| T-57 | S-06 regra: aplicar `FiltroVendas` (período, cliente exato/contém, status, combinados) em `ConsultaService`/repositório, com validação período inicial > final | BE | B | 1,5h | T-23 | T-59, T-61, T-63 | D-10 | Com seed: cada filtro isolado e combinado retorna o conjunto esperado; período invertido -> erro de validação | A fazer |
| T-58 | S-06 tela: painel de filtros de F-2 (DateEdit x2, Cliente, Status, Aplicar/Limpar, resumo na barra, estados vazio/erro) | FE | B | 2h | T-57, T-42 | T-60, T-62 | D-10 | Enter aplica; "Nenhuma venda para os filtros informados" + Limpar; período inválido mostra mensagem inline sem consultar; relatório passa a usar o filtro (T-48) | A fazer |
| T-59 | S-07 regra: totais quitado/cancelado (e pendente condicional) no `RelatorioDataSource`, nulos = 0 | BE | B | 1,5h | T-48 | T-57, T-61, T-63 | D-03 (linha pendente) | Com seed, soma por status confere; Total listado = soma dos totais por status (CA-07.3) | A fazer |
| T-60 | S-07 layout: bloco de totais no `.frx` (quitado/cancelado; linha pendente só se D-03 aceita e T-62 concluída) | FE | B | 1h | T-59, T-49 | T-58, T-62 | D-03 | PDF mostra os totais e a contagem por status; linha pendente ausente quando D-03 rejeitada | A fazer |
| T-61 | RF-04 regra: `RegistroService` (registrar Pendente, idempotente com status atual) | BE | B (condicional D-03; **cortar primeiro**) | 1,5h | T-14, T-15, T-16, T-17 | T-57, T-59, T-63 | D-03, P-9 | CA-04.1/04.2 contra `.fdb`; se D-03 rejeitada, tarefa **cancelada** com justificativa (não implementar) | A fazer |
| T-62 | RF-04 endpoint: `POST /api/vendas` (+ alias v1), DTO reutilizando o de quitação | BE | B (condicional D-03) | 1,5h | T-61, T-30 | T-58, T-60 | D-03, D-05, P-9 | curl: 200 `{status:"Pendente"}`; repetição 200 com status atual; venda Pendente depois quitada via T-30 gera histórico de quitação | A fazer |
| T-63 | S-11 reduzido: testes xUnit da máquina de estados do Domain (todas as transições válidas/inválidas/idempotentes, regra de motivo, venda desconhecida) | BE | B | 1,5h | T-09, T-10 | T-57, T-59, T-61 | D-11, RNF-08 | Suíte verde; matriz 3 estados x 2 operações coberta (>= 12 casos) | A fazer |

### Refatoração Lote-1 (achados simples da validação do Lote 1; criada pelo Validador em 22/09/2026)

| ID | Tarefa | Dono | Tier | Est. | Dep. | Paralelizável-com | Premissa | Critério de aceite | Status |
|---|---|---|---|---|---|---|---|---|---|
| RL1-01 | Completar `docs/contrato-v1.1.md`: adicionar >= 1 exemplo JSON de resposta para os códigos de erro `CONFLITO_CONCORRENCIA` (409) e `ERRO_INTERNO` (500), que hoje só aparecem na tabela da Seção 2, sem exemplo em nenhum lugar do documento (achado `QA-REPORT.md`, T-03) | DOC/OPS | A | 0,5h | RL1-01a | RL1-02, RL1-03 | contrato v1.1 | Documento passa a ter >= 1 exemplo JSON por código de erro, todos os 9 códigos da tabela da Seção 2 cobertos; nenhuma mudança de comportamento/endpoint, só conteúdo aditivo do próprio documento | Concluída — dependia de RL1-01a (mapeamento de `ConcorrenciaException` -> 409 `CONFLITO_CONCORRENCIA`), já `Concluída`. Adicionados em `docs/contrato-v1.1.md` Seção 2, logo após a tabela de códigos, dois exemplos JSON aditivos: 409 `CONFLITO_CONCORRENCIA` (mensagem "Conflito de concorrência ao processar a operação; tente novamente.") e 500 `ERRO_INTERNO` (mensagem "Ocorreu um erro interno inesperado. Consulte o suporte informando o horário da operação."), ambos copiados literalmente das constantes `MensagemConflitoConcorrencia`/`MensagemGenericaErroInterno` em `ExceptionParaRespostaMapper.cs`. Os 9 códigos da tabela da Seção 2 agora têm >= 1 exemplo JSON no documento. Nenhuma mudança de código de produção nem de endpoint — só conteúdo aditivo do documento |
| RL1-01a | **(nova, achado do Executor durante RL1-01, 23/09/2026)** Corrigir `ExceptionParaRespostaMapper.Mapear` (`src/ERPFinanceiro.Api/Erros/ExceptionParaRespostaMapper.cs`, T-28): adicionar `case ConcorrenciaException` -> 409 `CONFLITO_CONCORRENCIA` (hoje cai no `default` -> 500 `ERRO_INTERNO`), conforme já previsto pelos próprios comentários de `ExecutorComRetry.cs`/`ConcorrenciaException.cs` e pela regra 9 da Seção 1/contrato v1.1 Seção 2 | BE | A | 0,5h | — | — | ADR-005, regra 9 (Seção 1) | Teste novo em `ExceptionParaRespostaMapperTests.cs` confirma `ConcorrenciaException` -> 409 `CONFLITO_CONCORRENCIA`; suíte completa continua verde, sem regressão nos demais `case` | Concluída — adicionado `case ApplicationExceptions.ConcorrenciaException` em `ExceptionParaRespostaMapper.Mapear` (`src/ERPFinanceiro.Api/Erros/ExceptionParaRespostaMapper.cs`), retornando 409 `CONFLITO_CONCORRENCIA` com mensagem genérica ao cliente ("Conflito de concorrência ao processar a operação; tente novamente.", sem detalhe interno, regra 7); `ConcorrenciaException` deixou de cair no `default`/500 `ERRO_INTERNO`. Teste novo `Mapear_ConcorrenciaException_Retorna409ConflitoConcorrencia` em `src/ERPFinanceiro.Tests/Api/ExceptionParaRespostaMapperTests.cs` (11/11 verdes na classe). Suíte completa executada via `dotnet vstest` a partir de cópia de `bin/Debug/net48` fora do OneDrive (contorno do Bloqueio 001, `BLOCKERS.md`): **147/147 testes verdes**, sem regressão nos demais `case`. Nenhuma outra linha desta tabela alterada; RL1-01 permanece bloqueada até o orquestrador decidir a próxima rodada |
| RL1-02 | Corrigir `spikes/T-01-firebird-spike/FirebirdSpike/Program.cs` (linha ~170, log `CA-3 bitness registrada`): referência a `docs/licencas-bin.md` (arquivo inexistente) deve apontar para `docs/licencas.md` (achado `QA-REPORT.md`, T-01). Cosmético — projeto descartável, sem impacto funcional/segurança | DOC/OPS | A | 0,1h | — | RL1-01, RL1-03 | — | String de log corrigida; reexecutar o console do spike (opcional) só para confirmar a nova mensagem, sem necessidade de refazer os critérios de aceite (já comprovados) | Concluída — string de log em `Program.cs` (linha ~170) corrigida de `docs/licencas-bin.md` para `docs/licencas.md`; sem reexecução do spike (alteração textual isolada, critérios de aceite já comprovados anteriormente) |
| RL1-03 | Confirmar visualmente a instalação real do DevExpress WinForms trial e do FastReport (.NET Trial ou Open Source) num ambiente com GUI Windows disponível: watermark, splash de trial, existência de preview "In Application"; ajustar `docs/licencas.md` se algo divergir da pesquisa documental (achado `QA-REPORT.md`, T-02 — limitação de ambiente sandbox sem GUI, já declarada no próprio documento) | DOC/OPS | A | 0,5h | — | RL1-01, RL1-02 | D-09 | Ambos os produtos abertos num projeto WinForms vazio, com print/evidência; `docs/licencas.md` atualizado (ou confirmado sem mudança) com nota de verificação visual concluída; **prazo: antes de T-49/T-50 (Lote 11) começarem**, gate já natural do fluxo | A fazer |

### Refatoração Lote-4 (achados simples da validação do Lote 4; criada pelo Validador em 22/09/2026)

| ID | Tarefa | Dono | Tier | Est. | Dep. | Paralelizável-com | Premissa | Critério de aceite | Status |
|---|---|---|---|---|---|---|---|---|---|
| RL4-01 | `UnitOfWork.EhViolacaoDeUniqueVendaId` (`src/ERPFinanceiro.Infrastructure/Persistencia/UnitOfWork.cs`) só detecta violação de `UNIQUE(VENDA_ID)` casando o **texto em inglês** da mensagem da `FbException`. Trocar/complementar por sinal locale-independente: `FbException.ErrorCode` e/ou `SQLSTATE` (confirmados publicamente expostos pela API do provider 10.3.4, sem constante nomeada em `IscCodes` mas com valor numérico estável), mantendo o texto como fallback se necessário. Achado `SECURITY-REVIEW.md`/`QA-REPORT.md`, T-15, severidade baixa | BE | A | 0,5h | — | RL4-02 | ADR-005 | Mesmos 3 testes de `UnitOfWorkTests.cs` continuam verdes; novo teste (ou ajuste dos existentes) confirma que a detecção não depende só do texto em inglês da mensagem (ex.: usar `ErrorCode`/`SQLSTATE` como sinal primário); nenhuma mudança de comportamento observável para o caso já coberto | Concluída — `UnitOfWork.EhViolacaoDeUniqueVendaId` agora usa `FbException.ErrorCode` como sinal primário (constante `GdsCodeViolacaoDeChaveUnica = 335544665`, o gds-code `isc_unique_key_violation` do Firebird), com o texto em inglês mantido como fallback só quando o `ErrorCode` não bate. Valor descoberto empiricamente (não documentado como constante pública em `IscCodes` na versão 10.3.4): instrumentado `EhViolacaoDeUniqueVendaId` temporariamente para imprimir `ex.ErrorCode`/`ex.SQLSTATE` e rodado `SalvarAlteracoes_ViolacaoDeUniqueVendaId_LancaConcorrenciaException` real contra Firebird embarcado — `ErrorCode=335544665`, `SQLSTATE=23000`. Achado adicional relevante: `SQLSTATE` sozinho **não** é específico o bastante — a violação de CHECK (`CK_FIN_VENDA_HIST_OPER`, mesmo teste de rollback) também retorna `SQLSTATE=23000` (mesma classe "integrity constraint violation"), mas com `ErrorCode=335544558` diferente; por isso o sinal primário implementado é o `ErrorCode`, não o `SQLSTATE`. Testes: os 3 originais de `UnitOfWorkTests.cs` continuam verdes sem alteração de asserts; +3 novos testes unitários isolam `EhViolacaoDeUniqueVendaId` via reflexão (método privado) contra `FbException` reais "fake" (sem tocar servidor — `IscException.ForErrorCode` + construtor interno `FbException(string, Exception)`, técnica descoberta por decompilação do getter `ErrorCode` via `MethodBody.GetILAsByteArray`): (1) `ErrorCodeBateSemTextoEmIngles_DetectaViaErrorCode` — mensagem sem nenhum termo em inglês, detecta só pelo `ErrorCode`; (2) `ErrorCodeAusente_CaiNoFallbackDeTextoEmIngles` — `ErrorCode=0`, mensagem em inglês real capturada em execução, fallback funciona; (3) `ErrorCodeDeOutraViolacao_NaoDetectaMesmoComMesmoSqlstate` — `ErrorCode` de CHECK constraint (mesmo SQLSTATE) não é tratado como UNIQUE(VENDA_ID). Execução real (contorno do Bloqueio 001, bin copiado para fora do OneDrive): `UnitOfWorkTests` 6/6 verde; suíte completa `ERPFinanceiro.Tests` 146/146 verde em 2 rodadas seguidas, sem falha intermitente |
| RL4-02 | Criar `src/ERPFinanceiro.Tests/xunit.runner.json` (ou `.runsettings` equivalente, copiado para `bin/` via `.csproj`) com paralelização de assembly/coleção **desligada**, conforme já decidido e validado (mas nunca commitado) por T-23. Achado `QA-REPORT.md`, ponto de atenção sobre T-23 — ausência confirmada por busca no repositório e reproduzida (2/2 falhas por `AccessViolationException` sem o arquivo; 2/2 execuções limpas com ele) | BE | A | 0,3h | — | RL4-01 | — | Arquivo existe, é copiado para o `bin/` de saída do `ERPFinanceiro.Tests` (`CopyToOutputDirectory`), e a suíte completa roda 2x seguidas sem falha intermitente de `AccessViolationException`; **prazo: antes de T-20 (Lote 5) iniciar** | Resolvida — `src/ERPFinanceiro.Tests/xunit.runner.json` criado (`parallelizeAssembly: false`, `parallelizeTestCollections: false`, schema xUnit 2.x) e registrado em `ERPFinanceiro.Tests.csproj` como `<None Include="xunit.runner.json" CopyToOutputDirectory="PreserveNewest" />`; confirmado copiado para `bin/Debug/net48/xunit.runner.json` após `dotnet build`. Verificação real (não só existência do arquivo): bin copiado para fora do OneDrive (contorno do Bloqueio 001) e `dotnet vstest` rodado **2x seguidas** — 109/109 Aprovado nas duas rodadas (108 pré-existentes + o novo harness de T-20), sem nenhuma `AccessViolationException`, confirmando que o runner realmente lê o arquivo. |

### Refatoração Lote-5 (achado da validação do Lote 5; criada pelo Validador em 22/09/2026)

| ID | Tarefa | Dono | Tier | Est. | Dep. | Paralelizável-com | Premissa | Critério de aceite | Status |
|---|---|---|---|---|---|---|---|---|---|
| RL5-01 | Corrigir o risco de entidade "zumbi" sob concorrência documentado no `<remarks>` de `QuitacaoService.cs` (T-18) e presente pela mesma causa no ramo "desconhecida" de `CancelamentoService.CancelarInterno` (T-19): `ExecutorComRetry` (T-16) reaproveita a mesma instância de `IVendaRepository`/`IUnitOfWork` (logo o mesmo `DbContext`) entre as até 3 tentativas de retry; se a 1ª tentativa de **criar** a venda falhar por violação de `UNIQUE(VENDA_ID)` (corrida de criação concorrente), a entidade `Added` zumbi permanece rastreada e a 2ª tentativa pode esgotar as 3 tentativas com um falso 409 `CONFLITO_CONCORRENCIA` em vez do 200/resultado idempotente correto. Correção: dar a cada tentativa de retry um escopo de `DbContext` próprio (fábrica de `DbContext`/repositório por tentativa), dentro do mesmo escopo de operação já definido pela regra 5 da Seção 1/RT-08 (nunca compartilhado entre requisições diferentes — só entre tentativas da mesma operação). Ponto certo de implementar: junto com o composition root de **T-26** (Autofac, ainda não iniciado), que é quem vai decidir como o escopo de `DbContext` por operação é de fato construído. Achado `QA-REPORT.md`/`SECURITY-REVIEW.md` (classificado como achado de correção funcional sob concorrência, não de segurança), T-18/T-19/T-20, severidade/prioridade alta (não reproduzido em >= 26 execuções; não crítico; não bloqueia o Lote 5) | BE | A | 1,5h | T-26 | — | RT-08, regra 5 da Seção 1 | Novo teste (ou extensão de `ConcorrenciaIdempotenciaTests`/T-20) força deliberadamente a 1ª tentativa a colidir por `UNIQUE(VENDA_ID)` (ex.: pré-inserindo a linha concorrente antes da 2ª tentativa rodar) e confirma que o resultado final é sempre sucesso idempotente (200), nunca `ConcorrenciaException` esgotada por causa da entidade zumbi; os 20 casos de `ConcorrenciaIdempotenciaTests` (T-20) continuam verdes; mesma correção cobre `QuitacaoService` e `CancelamentoService` (reaproveitando o mesmo mecanismo de `ExecutorComRetry`/T-26); **prazo: antes de T-26 (Lote 6) fechar** | Resolvida — implementada junto com T-26 (composition root), conforme decisão de composição prevista na própria linha. Abordagem (a): `QuitacaoService`/`CancelamentoService` (`src/ERPFinanceiro.Application/Servicos/`) trocaram a dependência direta de `IVendaRepository`/`IUnitOfWork` por uma nova `IFabricaEscopoOperacao` (`Application/Interfaces/IFabricaEscopoOperacao.cs`+`IEscopoOperacao.cs`); dentro do callback passado a `ExecutorComRetry.Executar`, cada tentativa chama `_fabricaEscopo.Abrir()` (dentro de um `using`) e só usa o `IVendaRepository`/`IUnitOfWork` daquele escopo — nunca reaproveita entre tentativas. Implementação real em `src/ERPFinanceiro.Infrastructure/Persistencia/FabricaEscopoOperacaoEf.cs`: abre uma `FbConnection`+`FinanceiroDbContext` novos a cada `Abrir()` (mesmo padrão "conexão curta própria" de `HealthService`/T-22); registrada `SingleInstance` no composition root de T-26 (ela mesma não guarda nenhum `DbContext`, só sabe criar um novo por chamada). Teste determinístico do critério de aceite (não apenas estatístico) em `src/ERPFinanceiro.Tests/Infrastructure/Persistencia/ConcorrenciaIdempotenciaTests.cs` (`Executar_ColisaoDeUniqueForcadaNaPrimeiraTentativa_SegundaTentativaComEscopoNovoResolveIdempotente`): um `FabricaComColisaoNaPrimeiraTentativa` decora a fábrica real e, no exato momento em que a 1ª tentativa chamaria `SalvarAlteracoes()` (depois de já ter lido "venda não existe" e decidido criar — a janela de corrida exata do achado), insere e comita — numa conexão/contexto totalmente separados, simulando uma operação concorrente vencedora — uma venda já Quitada com o mesmo `VendaId`; a 1ª tentativa colide de verdade contra `UNIQUE(VENDA_ID)` (T-05) e vira `ConcorrenciaException` (T-15), disparando o retry; a 2ª tentativa abre um escopo/`DbContext` novo (não decorado) e resolve pelo ramo idempotente (200, sem duplicata, sem tentar inserir de novo) — confirmado por asserts sobre o `.fdb` real (1 único histórico de `Quitacao`, com o `correlationId` da "concorrente", não da tentativa original). Os 20+1+5 casos originais de `ConcorrenciaIdempotenciaTests` (T-20) continuam verdes, agora usando a mesma `FabricaEscopoOperacaoEf` real (réplica fiel da composição de produção) em vez de um único `FinanceiroDbContext` manual por chamada. 9 pontos de construção de `QuitacaoService`/`CancelamentoService` em `QuitacaoServiceTests.cs`/`CancelamentoServiceTests.cs`/`ConcorrenciaIdempotenciaTests.cs` adaptados para `IFabricaEscopoOperacao`, sem perda de cobertura (mesmos cenários, mesmas asserções). Execução real (contorno de rotina do Bloqueio 001): suíte completa **114/114 Aprovado em 2 rodadas seguidas**; teste determinístico de RL5-01 isolado **3x seguidas sem falha**. |

### Refatoração Lote-9 (achados simples da validação do Lote 9; criada pelo Validador em 23/09/2026)

| ID | Tarefa | Dono | Tier | Est. | Dep. | Paralelizável-com | Premissa | Critério de aceite | Status |
|---|---|---|---|---|---|---|---|---|---|
| RL9-01 | Remover as cores literais `#555555` dos 3 SVGs de `ConfiguracaoVisual.cs` (linhas ~33-36) usando cor da skin/paleta DevExpress (ex. `SvgImage` com `SvgPalette`/`WindowText`) e corrigir o comentário da classe | FE | A | 0,5h | T-41 | RL9-02, RL9-03, RL9-04 | T-41 "nenhuma cor hardcoded" | `grep '#[0-9A-Fa-f]\{6\}' src/ERPFinanceiro.Desktop` sem ocorrência; ícones seguem a skin (conferência visual manual) | Em andamento — **Nota (executor):** os 3 SVGs viraram formas preenchidas (`polygon`/`path`, sem `stroke`) com `class="Black"` (constante `ConfiguracaoVisual.ClassePaleta`), classe resolvida pela paleta SVG da skin DevExpress; comentário da classe corrigido. `grep -rE '#[0-9A-Fa-f]{6}' src/ERPFinanceiro.Desktop --include=*.cs` sem ocorrência (executado); coleção monta 3 itens (teste). **Pendente:** conferência visual manual de que os ícones renderizam na cor da skin (não foi possível verificar renderização sem display; se aparecerem pretos/invisíveis, trocar para `SvgPalette` explícita). Fecha como `Concluída` após essa conferência. |
| RL9-02 | Registrar em T-46 (e T-51) a fiação do composition root: instanciar `ConsultaVendasPresenter`/`DetalheVendaPresenter` via `CarregadorViaContainer` e atribuir `FrmConsulta.DetalhePresenter`; chamar `ConfiguracaoVisual.AplicarSkin()` antes do primeiro Form; logar a exceção hoje descartada nos `catch` de F-1/F-3 | FE | A | 0,3h (registro) | T-42, T-44 | RL9-01, RL9-03 | Achado QA Lote 9 | Linhas de T-46/T-51 citam a atribuição; duplo clique/Enter abre F-3 no app real; erro de carga vai ao log sem stack na tela. **Prazo: junto com T-46** | Concluída — **Nota (executor):** `Program.Main` cria `ConsultaVendasPresenter`/`DetalheVendaPresenter` com `CarregadorViaContainer` e atribui `FrmConsulta.DetalhePresenter`; `ConfiguracaoVisual.AplicarSkin()` roda antes do splash/primeiro Form. Log: em vez de editar `FrmConsulta.cs`/`FrmDetalheVenda.cs`, os presenters ganharam parâmetro opcional `Action<Exception> aoFalhar` (em `ConsultaVendasModelo.cs`/`DetalheVendaModelo.cs`), chamado no `CarregarAsync` antes de a exceção propagar; o composition root liga a `IAppLogger.Registrar` (contexto + exceção completa, só no log; tela segue sem stack). Nenhum formulário foi editado. **Verificado por execução real:** teste de que ambos os presenters repassam a exceção ao callback e propagam (compatível com os catch existentes). **Pendente manual:** duplo clique/Enter abrindo F-3 no app real e linha de erro no arquivo de log com o app rodando. |
| RL9-03 | Alinhar `FrmConsulta` à Diretriz 12/UX-SPEC 3: `XtraForm` no lugar de `Form`; `LabelControl`/`PanelControl` no lugar de `Label`/`Panel` (contador, legenda, aviso, painel de erro, host) | FE | A | 0,7h | T-42, T-43 | RL9-01 | Diretriz 12 | Testes `FrmConsulta*` continuam verdes (164); nenhum `System.Windows.Forms.Label/Panel` no form | Concluída - `FrmConsulta` agora `XtraForm`; contador/legenda/aviso/erro = `LabelControl`, rodapé/painel de erro/host = `PanelControl`; contratos internos (`CarregandoVisivel`, `ErroVisivel`, `TextoErro`...) preservados; sem `Label/Panel` WinForms no arquivo (grep). Testes FrmConsulta* verdes (173 na suíte). PENDENTE conferência visual manual: alinhamento/aparência dos rótulos (VAlignment/HAlignment em vez de TextAlign). |
| RL9-04 | Atualizar `docs/licencas.md`: DevExpress vem do nuget.org público em trial de 30 dias (relógio do trial via NuGet não confirmado); em evaluation "Redistribution prohibited" (aviso DX1000) — proibida a entrega do build trial; splash/marca d'água persistem; ligar ao checklist de T-53 (licença válida antes de empacotar) | Doc | A | 0,3h | T-02, T-41 | RL9-01…RL9-03 | Achado DevSecOps Lote 9; Bloqueio 003. **Prazo: antes de T-53** | Concluída — `docs/licencas.md` atualizado (situação 23/09/2026, DX1000/DX1001, pendências: relógio do trial e visual RL1-03; checklist T-53; nota ao Gestor sobre G-2) |

### Refatoração Lote-10 (achados simples da validação do Lote 10; criada pelo Validador em 23/09/2026)

| ID | Tarefa | Dono | Tier | Est. | Dep. | Paralelizável-com | Premissa | Critério de aceite | Status |
|---|---|---|---|---|---|---|---|---|---|
| RL10-01 | Caminho por teclado para o diálogo de detalhes da barra de status: `BarStaticItem` não recebe foco/Tab, então "Detalhes/Copiar" só abre com mouse (UX-SPEC 5: "nenhuma ação exclusiva de mouse"). Adicionar atalho (ex. F2/Ctrl+D em `FrmConsulta`) ou botão "&Detalhes" acessível | FE | A | 0,5h | T-45 | T-51 | UX-SPEC 2.4/5 | Diálogo abre só por teclado; entra na checklist de T-51/T-52. **Prazo: junto com T-51** | Concluída — F2 na `FrmConsulta` abre `FrmDetalhesStatus` (via `TratarTecla`, mesmo caminho do clique na barra); teste `F2_AbreODialogoDeDetalhesDoStatus_RL10_01` abre o diálogo real headless e `F2_SemBarraDeStatus_NaoQuebra`. Ver T-51. Confirmação ao vivo entra na checklist de T-52. |
| RL10-02 | Robustez do ciclo de vida: (a) `Program.Main` sem `Application.ThreadException`/`AppDomain.UnhandledException` -> registrar handlers (log via `IAppLogger` + mensagem sem stack); (b) `Encerrar` descarta o container (que descarta o `TraceLogger`) antes do passo do mutex, então falhas dos passos seguintes são perdidas -> logar/armazenar falhas antes de descartar; (c) `AtualizarAsync` disparado com `var _ =` fora de handler: garantir que exceção de `Alterado` (ex. UI já descartada após fechar) seja observada e que nada atualize a UI após `FormClosed` | FE | A | 0,7h | T-45, T-46, T-47 | RL10-03 | Observação do Validador (Diretrizes 5/8) | Exceção não tratada de UI/AppDomain gera log + mensagem sem stack; falha de passo do encerramento aparece no log; sem exceção não observada após fechar | A fazer |
| RL10-03 | `InstanciaUnica.TentarAdquirir`: com mutex `Global\` criado por OUTRO usuário/sessão (RDP, usuário padrão), `new Mutex` pode lançar `UnauthorizedAccessException` (Program mostra "Não foi possível iniciar" e sai com 2, em vez do aviso "Já existe uma instância em execução"). Tratar como segunda instância (ou `MutexSecurity` permissiva) | FE | A | 0,5h | T-46 | RL10-02 | Regra 11 / UX-SPEC F-7 | Teste com exceção simulada devolve `null` (segunda instância) e o aviso correto é exibido | A fazer |
| RL10-04 | "Último erro" do diálogo da barra/Copiar usa `Exception.Message` cru (Firebird/HttpListener) sem filtro: hoje não há segredo comprovado (senha não é ecoada pelo FbClient), mas é texto copiável para a área de transferência. Defesa em profundidade: sanitizar/limitar (sem connection string, tamanho máximo) + teste com mensagem contendo `Password=`; opcional: atualizar o texto do diálogo já aberto | FE | A | 0,5h | T-45 | RL10-01 | GUARDRAILS G-6 | Mensagem com `Password=...`/`PWD=` sai mascarada no texto copiado; teste automatizado | A fazer |
| RL10-05 | Documentação: (a) registrar no UX-SPEC (Coordenador) o foco padrão "Não" no diálogo de encerramento (decisão de detalhe do Executor, hoje só no código/nota de T-47); (b) nota de T-46 ainda diz "T-47 não implementada" (desatualizada) | Doc | A | 0,2h | T-46, T-47 | qualquer | Achado QA Lote 10 | UX-SPEC 2.7/F-7 cita foco em "Não"; nota de T-46 corrigida | A fazer |
| RL10-06 | Investigar intermitência da suíte completa: `ApiHostVendasStatusControllerTests.GetStatus_PipelineHttpCompletoComContainerRealEFirebirdReal_CobreOsCenariosDoContratoV11` falhou 1x em 7 execuções (linha 139: esperado OK, obtido NotFound, mesmo após seed direto no `.fdb`); junto com `Harness_ConcorrenciaEIdempotencia_T20` (RL4-02). Gravar saída do `vstest` em arquivo; verificar visibilidade do seed entre conexões Firebird embarcadas (seed fechado antes do container?) | QA/BE | A | 1h | T-31, RL4-02 | qualquer | Achado QA Lote 10 | Causa identificada e corrigida ou documentada; 10 execuções seguidas sem falha | A fazer |

### Tier C — estacionado (sem tarefa; não decompor nesta rodada)
S-03 aba/tela de histórico (histórico **continua gravado** em T-18/T-19; dados via T-24 disponíveis) · S-10 Serilog/log JSON estruturado (T-21 mantém a abstração) · S-12 endpoint de histórico · S-13 estorno formal · testes unitários de `QuitacaoService`/`CancelamentoService` com Moq (resto de S-11) · rodapé de totais na grid · persistência de layout do grid.

### Resumo de contagem
**63 tarefas** = 56 Tier A (Lotes 1-13) + 7 Tier B (Lote 14). **14 lotes.** Esforço somado Tier A: dev (Lotes 1-12) ≈ 96,5 h-pessoa; entrega (Lote 13) 7 h; Tier B 10,5 h. Ver Seção 5 (capacidade).
**+ 3 tarefas em `Refatoração Lote-1`** (RL1-01…RL1-03, ~1,1h somadas), criadas pelo Validador em 22/09/2026 a partir de achados simples da validação do Lote 1 — fora da contagem original de 63, não redecompõem nenhum lote existente.
**+ 2 tarefas em `Refatoração Lote-4`** (RL4-01…RL4-02, ~0,8h somadas), criadas pelo Validador em 22/09/2026 a partir de achados simples da validação do Lote 4 — fora da contagem original de 63, não redecompõem nenhum lote existente.
**+ 1 tarefa em `Refatoração Lote-5`** (RL5-01, ~1,5h), criada pelo Validador em 22/09/2026 a partir de um achado de correção funcional sob concorrência da validação do Lote 5 — fora da contagem original de 63, não redecompõe nenhum lote existente.

---

## 4. Dependências e Ordem de Execução

### 4.1 Paralelismo por lote (instâncias do Executor por rodada)

"Rodada" = conjunto de tarefas cujas dependências já estão `Concluída`. Tarefas de lotes diferentes podem ser misturadas na mesma rodada real se as dependências permitirem (ex.: T-05 roda junto de T-04).

| Lote | Rodadas (tarefas em paralelo) | Máx. paralelo |
|---|---|---|
| 1 Spike e preparação | R1: T-01 ‖ T-02 ‖ T-03 | **3** |
| 2 Esqueleto e Domínio | R1: T-04 · R2: T-07 · R3: T-08 · R4: T-09 ‖ T-10 | **2** (cadeia curta; compensar com Lote 3) |
| 3 Banco | R1: T-05 · R2: T-06 ‖ T-11 ‖ T-12 | **3** |
| 4 Persistência/Application base | R1: T-13 · R2: T-14 ‖ T-17 ‖ T-21 · R3: T-15 · R4: T-16 | **3** |
| 5 Serviços de negócio | R1: T-18 ‖ T-19 ‖ T-22 ‖ T-23 ‖ T-24 · R2: T-20 | **5** |
| 6 API base/hospedagem | R1: T-25 · R2: T-26 ‖ T-28 ‖ T-29 · R3: T-27 | **3** |
| 7 Endpoints | R1: T-30 ‖ T-31 ‖ T-32 · R2: T-33 ‖ T-34 | **3** |
| 8 Segurança e integração | R1: T-35 ‖ T-36 · R2: T-37 ‖ T-38 · R3: T-39 · R4: T-40 | **2** |
| 9 Tela grid/detalhe | R1: T-41 · R2: T-42 · R3: T-43 ‖ T-44 | **2** |
| 10 Tela status/ciclo de vida | R1: T-45 ‖ T-46 · R2: T-47 | **2** |
| 11 Relatório | R1: T-48 · R2: T-49 · R3: T-50 (T-48/T-49 em paralelo se `LinhaRelatorio` for publicada primeiro) | **1** (2 com o acordo de DTO) |
| 12 Acessibilidade | R1: T-51 · R2: T-52 | **1** |
| 13 Entrega O-12 | R1: T-53 ‖ T-54 ‖ T-55 · R2: T-56 | **3** |
| 14 Tier B | R1: T-57 ‖ T-59 ‖ T-61 ‖ T-63 · R2: T-58 ‖ T-60 ‖ T-62 | **4** |
| Refatoração Lote-1 | R1: RL1-01 ‖ RL1-02 ‖ RL1-03 (sem dependência entre si; RL1-03 sujeita ao prazo "antes de T-49/T-50") | **3** |
| Refatoração Lote-4 | R1: RL4-01 ‖ RL4-02 (sem dependência real entre si; RL4-02 sujeita ao prazo "antes de T-20") | **2** |
| Refatoração Lote-5 | R1: RL5-01 (depende de T-26, Lote 6 — só roda depois; prazo "antes de T-26 fechar") | **1** |
| Refatoração Lote-9 | R1: RL9-01 ‖ RL9-03 ‖ RL9-04 (independentes; RL9-04 prazo "antes de T-53"); RL9-02 junto com T-46 | **3** |
| Refatoração Lote-10 | R1: RL10-01 (junto com T-51) ‖ RL10-02 ‖ RL10-03 ‖ RL10-04 ‖ RL10-05 ‖ RL10-06 (independentes entre si, exceto arquivos comuns de RL10-02/03) | **4** |

Independentes **entre lotes** (paralelismo adicional): Lote 9 (T-41 em diante) roda em paralelo aos Lotes 6-8 assim que T-23 existir; Lote 8 (T-35/T-36/T-37) roda em paralelo ao Lote 9/10.

### 4.2 Ordem por dia e caminho crítico

| Dia | Data | Lotes / marcos |
|---|---|---|
| 1 | 21/09 | L1 (**T-01 primeiro, bloqueante**), L2, L3; início L4 (T-13) |
| 2 | 22/09 | L4, L5 (T-18/T-19 + S-04) |
| 3 | 23/09 | L6, L7. **Marco: API utilizável pelo Vendas no fim do Dia 3** (T-30/T-31 no meio do dia; T-34 fecha o marco) |
| 4 | 24/09 | L8 (S-08, S-09, **O-11 T-38…T-40 até o fim do dia**) ‖ L9 + L10 |
| 5 | 25/09 | L11, L12; L14 (Tier B) se sobrar tempo; preparação de L13 (T-54/T-55, sem código). **Fim do desenvolvimento** |
| 6-7 | 26-27/09 | L13 (T-53, T-56 e ajustes de README); **sem código novo**, só correção bloqueante |

**Caminho crítico (Tier A, ~24 h em série):** T-01 -> T-04 -> T-07 -> T-08 -> T-10 -> T-19 -> T-32 -> T-38…T-40 (integração) e, em paralelo, T-05 -> T-11 -> T-14 -> T-15 -> T-16 -> T-18 -> T-30 -> T-38. Qualquer atraso em T-01 ou T-14…T-16 desloca o marco do Dia 3.

### 4.3 Pontos "A VALIDAR" -> tarefas afetadas (premissas explícitas)

| Ponto | Premissa assumida | Tarefas que dependem | Se rejeitado |
|---|---|---|---|
| D-03 / P-9 (Pendente) | `POST /api/vendas` existe | T-18 (ramo Pendente), T-59/T-60 (linha pendente), T-61, T-62 | Cancelar T-61/T-62; remover linha pendente; registrar justificativa |
| D-04 / ADR-004 (OWIN no Desktop) | API dentro do app | T-25, T-26, T-27, T-45, T-46, T-47 | Muda hospedagem: Coordenador reabre (novo ADR) antes de T-27; T-45…T-47 refeitas |
| D-05 / P-8 (alias `/api/v1`) | Alias ativo | T-33, T-62 | Desativar rota alias (1 linha) |
| D-06 (motivo p/ Quitada) | Obrigatório | T-09, T-19, T-32, T-39 | Ajustar T-09 e critérios; sem impacto estrutural |
| D-08 / ADR-007 (cancelar desconhecida) | Cria Cancelada, colunas NULL | T-05, T-07, T-10, T-11, T-19, T-42, T-44 | Voltar colunas a NOT NULL e `Cancelar` desconhecida -> 404; T-10 sem uso |
| D-10 (cliente por ID) | Sem nome | T-23, T-42, T-48, T-57, T-58 | Campo `clienteNome` aditivo: coluna nova, sem redesenho |
| D-11 (xUnit + Moq) | xUnit | T-04, T-20, T-63 | Trocar framework, sem impacto em código de produção |
| Contrato v1.1 (P-1…P-9) | Aceito pelo Vendas | T-03, T-28, T-30…T-33, T-35…T-40 | Manter v1.0; mudanças só aditivas; bloqueio em `BLOCKERS.md`; T-38 vira teste por Postman se sem acesso ao Vendas — **exercido em 23/09/2026** (ADR-010): sem aceite do contrato **e** sem acesso ao Delphi real, T-38/T-39 redefinidas para suíte de integração simulada (harness HTTP); integração real com o Vendas fica pendência externa explícita (Seção 6, `BLOCKERS.md` Bloqueio 002) |

---

## 5. Riscos de Prazo

| # | Risco | Sev. | Ação |
|---|---|---|---|
| RP-1 | **Capacidade:** soma de esforço Tier A dev ≈ 96,5 h-pessoa contra ~49 h do VISAO 6.1. A diferença é overhead de granularidade fina e testes por tarefa, **não** escopo novo; só cabe em 5 dias com **execução paralela real** (Seção 4.1) e caminho crítico ~24 h. Com um único executor sequencial não cabe | Alta | Confirmar com o usuário quantas instâncias paralelas estão disponíveis; se < 3, cortar Tier B inteiro e simplificar T-43/T-51 |
| RP-2 | T-01 no-go: +1h de extensão e refaz T-05/T-11/T-12 (~7 h) | Alta | Timebox rígido; fallback SQL Server já decidido (ADR-002) |
| RP-3 | Contrato v1.1 não aceito / sem acesso ao código do Vendas (T-38/T-39) | Alta | Enviar T-03 no Dia 1; T-34 como contrato executável; fallback v1.0 |
| RP-4 | Trial DevExpress/FastReport (marca d'água, sem preview) | Média | T-02 no Dia 1; fluxo PDF já previsto (T-50) |
| RP-5 | OWIN/porta/firewall (T-27) | Média | Porta configurável; erro visível na barra de status |
| RP-6 | O-11 concentra dependência externa no Dia 4 | Alta | T-40 é reserva de 2 h; nenhum Tier B antes dela |
| RP-7 | Dia 5 cheio (L11+L12) sem folga real para Tier B | Média | Tier B é oportunista, cortar sem discussão |

---

## 6. Lacunas Sinalizadas e Decisões de Detalhe

**Lacunas estruturais no SDD/UX-SPEC:** nenhuma que exija novo ADR ou alteração de SDD/UX-SPEC nesta rodada.

**Decisões de detalhe tomadas na decomposição (documentadas, sem impacto de custo/prazo):**
1. **ADR-009 (resultado do spike):** ADR-002 é imutável e está "Aceito (go/no-go pendente)". O resultado do spike entra como ADR-009 novo (rascunho do Executor em T-01; ratificação do Coordenador). Se for no-go, ADR-009 supersede ADR-002 no que trata do banco.
2. **`ICorrelationContext`** em Application, implementado na Api (T-13/T-29), para o histórico gravar `CORRELATION_ID` sem Application conhecer OWIN.
3. **`TraceLogger` em Tier A (T-21)**, Serilog Tier C (SDD 2.6): mantém a abstração `IAppLogger`.
4. **Health com `banco`** e 503 em falha (SDD 2.6) fica em T-36; a tela usa `IHealthService` em processo (T-45), não o endpoint.
5. **Tests project** referencia só Domain/Application (SDD 2.1). Testes de integração com Firebird (T-20) são harness com o app **fechado** (regra de único processo dono do `.fdb`).
6. **Relatório sem limite de 5.000** e tela com aviso de truncamento (UX-SPEC 7) implementados em T-48/T-43.
7. **Tarefas inseparáveis (misturas justificadas):** nenhuma. T-46 (F-7) agrega splash + mutex + falha de banco/porta por ser um único fluxo de tela; T-28 agrega handler + tabela de códigos por ser uma única unidade (um handler).
8. **T-63 (S-11 reduzido)** cobre só o Domain; os testes dos serviços com Moq são Tier C.
9. **Datas:** estimativas não incluem o registro da linha v1.1 em VISAO 2.4, que só ocorre com aceite do Vendas (fora das tarefas; ação do usuário).
10. **T-38/T-39 redefinidas (23/09/2026, ADR-010, `BLOCKERS.md` Bloqueio
    002):** integração real com o Vendas (Delphi) inviável neste sandbox
    (sem aceite do contrato e sem acesso ao sistema Delphi). Redefinidas
    como suíte de integração **simulada** via harness HTTP real
    (reaproveita T-34/T-37), cobrindo o mesmo critério técnico original.
    **Não é decisão de detalhe silenciosa**: é mudança de escopo do que o
    marco "O-11"/Dia 4 entrega (deixa de ser "integração real confirmada" e
    passa a ser "confiança técnica via simulação; integração real
    pendente") — sinalizada ao usuário (orquestrador) para aprovação
    explícita antes de o Executor rodar os novos T-38/T-39. A pendência de
    integração real com o Vendas permanece **aberta** (não fechável nesta
    sessão/ambiente): deve ser retomada em ambiente com acesso ao sistema
    Delphi real e aceite formal do time Vendas, e documentada como
    limitação conhecida em T-54 (README) e no SDD (riscos/dívida técnica
    aceita) antes da entrega final (Lote 13).

**Autocheck de granularidade — divisões realizadas (antes -> depois):**

| Item original (VISAO) | Problema no autocheck | Resultado |
|---|---|---|
| O-01 (4h) | Mistura spike + trials + contrato | T-01, T-02, T-03 |
| O-03 (5h) | Mistura SQL + entidades + regra de estado + EF + inicializador | T-05, T-06 (SQL); T-07, T-08, T-09, T-10 (domínio, incl. regras S-05 e D-08 separadas); T-11 (EF); T-12 |
| O-04 (3h) | Mistura interfaces, repositório, transação, retry | T-13, T-14, T-15, T-16 |
| O-05 (4h) + S-04 | Serviço + validação + concorrência | T-17, T-18, T-16/T-15, T-20 |
| O-06 (3h) + S-05 | Serviço + regra de negócio distinta | T-19, T-09, T-10 |
| O-07 (5h) + S-02 | 3 endpoints + envelope + alias | T-25, T-28, T-29, T-30, T-31, T-32, T-33 |
| O-08 (2h) | Composition root + host | T-26, T-27 |
| O-09 (5h) | 4 fluxos de tela | T-41, T-42, T-43, T-44, T-45, T-46, T-47 |
| O-10 (5h) | Dados + layout + fluxo de tela | T-48, T-49, T-50 |
| O-11 (5h) | Cenários distintos + correção | T-38, T-39, T-40 (+ T-37 contrato) |
| O-12 (4h) | Build + docs + evidência + teste | T-53, T-54, T-55, T-56 |
| S-06, S-07 (Tier B) | Regra + tela/layout | T-57/T-58; T-59/T-60 |
| S-08, S-09 | Cada um já é 1 endpoint/handler | T-35, T-36 |

**Canário de ~300 mil tokens:** nenhuma tarefa prevista acima disso; as maiores (T-18, T-42, T-49) tocam 3-5 arquivos e um serviço/tela cada.
