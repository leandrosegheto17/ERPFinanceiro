/**
 * Placeholder de `core/*` (TASK-001): confirma que a pasta existe como módulo
 * TypeScript puro, sem dependência de React/DOM/`features/*`. Módulos reais de
 * domínio (corpus, reference, storage, sync, content) entram nas tarefas do
 * Lote 1 em diante.
 */
export function appName(): string {
  return "Estudo Bíblico";
}
