# Evidência de execução real — T-37 (22-23/09/2026)
#
# App local real: ApiHost + Startup + CompositionRoot.Construir() (mesmo caminho de produção
# de T-26/T-27/T-30/T-31/T-32/T-35/T-36) subido por tools/T-34-smoke-harness/ (harness
# descartável, reaproveitado sem alteração, ver docs/postman/README.md) contra Firebird 3.0
# embarcado real (mesmas DLLs nativas já usadas pelos testes de integração de T-30/T-31/T-32
# nesta worktree — fbclient.dll/fbembed.dll/security3.fdb/plugins//intl/, copiadas de
# src/ERPFinanceiro.Tests/bin/Debug/net48). Build do harness (dotnet build) feito de dentro do
# worktree (OneDrive) sem erro; execução do binário (T34SmokeHarness.exe) feita fora do
# OneDrive (C:\temp_erp_t37\net48), mesmo contorno do Bloqueio 001 (BLOCKERS.md). Porta 5034,
# banco dedicado C:\ERPFinanceiro\data\financeiro_t34_smoke.fdb (apagado e recriado entre
# reinícios do harness durante esta sessão, para produzir estado limpo por cenário).
#
# Parte 1 — Validação byte a byte de cada exemplo JSON do contrato-v1.1.md (Seções 2, 3.1,
# 3.2, 3.3, 3.5), incluindo cenários não cobertos pelo smoke-vendas.sh (VALOR_TOTAL_DIVERGENTE,
# DADOS_DIVERGENTES, GET /api/health, alias /api/v1/vendas/...) e os dois casos de 401
# (chave ausente / chave inválida). Requests feitos via curl direto contra
# http://localhost:5034, X-Api-Key: CHAVE_LOCAL_SMOKE_T34 quando aplicável (mesmo valor do
# App.config do harness). Resultado usado para os ajustes registrados em
# docs/contrato-v1.1.md Seção 3.7.

== GET /api/health (sem autenticação, P-5) ==
$ curl http://localhost:5034/api/health
{"status":"ok","banco":"ok"}
HTTP 200
-> bate 100% com o exemplo do contrato Seção 3.5 (200 — banco acessível).

== POST /api/vendas/quitacao sem X-Api-Key (401, chave ausente) ==
{"erro":{"codigo":"NAO_AUTORIZADO","mensagem":"Credencial de acesso ausente ou inválida."}}
HTTP 401
-> bate 100% com o exemplo do contrato Seção 3.1 (401).

== POST /api/vendas/quitacao com X-Api-Key inválida (401, chave inválida) ==
{"erro":{"codigo":"NAO_AUTORIZADO","mensagem":"Credencial de acesso ausente ou inválida."}}
HTTP 401
-> idêntica ao caso "chave ausente" acima, como o contrato exige (mesma resposta nos dois
   casos). Bate 100% com o exemplo do contrato.

== 1. Quitação — caminho feliz (venda nova, cria+quita) ==
{"status":"Quitada","dataQuitacao":"2026-09-23T02:12:57.5214287Z"}
HTTP 200
-> campos batem; dataQuitacao é valor dinâmico (excluído da comparação byte a byte por
   definição do critério de aceite).

== 2. Quitação — 400 PAYLOAD_INVALIDO (itens vazio) ==
{"erro":{"codigo":"PAYLOAD_INVALIDO","mensagem":"O campo 'itens' não pode ser vazio."}}
HTTP 400
-> bate 100% com o exemplo do contrato Seção 3.1.

== 3. Quitação — 400 VALOR_TOTAL_DIVERGENTE ==
{"erro":{"codigo":"VALOR_TOTAL_DIVERGENTE","mensagem":"valorTotal (1250,50) diverge da soma dos itens (1240,00) além da tolerância de 0,01."}}
HTTP 400
-> DIVERGÊNCIA DE FORMATO encontrada: a mensagem real usa vírgula como separador decimal
   (culture pt-BR do host), não ponto como no exemplo do contrato. Código/HTTP batem; nota
   explicativa adicionada ao contrato (Seção 3.1, "Nota (T-37, ...)") em vez de reescrever o
   exemplo com vírgula (o separador é não determinístico entre hosts/culturas — ver nota).

== 4. Quitação — 409 VENDA_JA_CANCELADA (venda 'V-T34-CANCELADA', já cancelada) ==
{"erro":{"codigo":"VENDA_JA_CANCELADA","mensagem":"Venda 'V-T34-CANCELADA' já está cancelada; Cancelada é um estado terminal."}}
HTTP 409
-> DIVERGÊNCIA DE TEXTO encontrada e corrigida no contrato (Seção 3.1): mensagem real usa
   aspas simples ao redor do vendaId e frase diferente da proposta original.

== DADOS_DIVERGENTES: quitar Pendente ('V-T34-CANC-PEND') com payload diferente do registrado ==
{"erro":{"codigo":"DADOS_DIVERGENTES","mensagem":"O payload de quitação diverge dos dados registrados na venda Pendente 'V-T34-CANC-PEND'."}}
HTTP 409
-> DIVERGÊNCIA DE TEXTO encontrada e corrigida no contrato (Seção 3.1): mensagem real usa
   aspas simples ao redor do vendaId (contrato original não tinha).

== 5. Cancelamento — caminho feliz (venda Pendente cancelada) ==
{"status":"Cancelada"}
HTTP 200
-> bate 100% com o exemplo do contrato Seção 3.2.

== 6. Cancelamento — 400 PAYLOAD_INVALIDO (vendaId ausente) ==
{"erro":{"codigo":"PAYLOAD_INVALIDO","mensagem":"O campo 'vendaId' é obrigatório."}}
HTTP 400
-> bate 100% com o exemplo do contrato Seção 3.2.

== 7. Cancelamento — 409 MOTIVO_OBRIGATORIO (Quitada sem motivo) ==
{"erro":{"codigo":"MOTIVO_OBRIGATORIO","mensagem":"Motivo do cancelamento é obrigatório."}}
HTTP 409
-> DIVERGÊNCIA DE TEXTO encontrada e corrigida no contrato (Seção 3.2): mensagem real é mais
   curta, não cita o campo 'motivo' nem o vendaId.

== 8. Status — caminho feliz ==
{"vendaId":"V-T37-QUIT-001","status":"Quitada"}
HTTP 200
-> bate 100% com o exemplo do contrato Seção 3.3.

== 9. Status — 404 VENDA_NAO_ENCONTRADA ==
{"erro":{"codigo":"VENDA_NAO_ENCONTRADA","mensagem":"Venda 'V-T34-NAO-EXISTE' não encontrada."}}
HTTP 404
-> DIVERGÊNCIA DE TEXTO encontrada e corrigida no contrato (Seções 3.2 e 3.3): mensagem real
   usa aspas simples ao redor do vendaId (contrato original não tinha).

== 10. Cancelamento — caminho feliz (venda desconhecida, D-08, 200) ==
{"status":"Cancelada"}
HTTP 200
-> bate 100% com o exemplo do contrato Seção 3.2.

== Alias /api/v1/vendas/quitacao ==
{"status":"Quitada","dataQuitacao":"2026-09-23T02:13:59.159429Z"}
HTTP 200
== Alias /api/v1/vendas/{vendaId}/status ==
{"vendaId":"V-T37-ALIAS","status":"Quitada"}
HTTP 200
-> alias confirmado espelhando 100% o comportamento das rotas canônicas (Seção 3.6).
#
# POST /api/vendas (registrar Pendente, Seção 3.4, D-03/P-9): NÃO testado — endpoint ainda
# não implementado (T-62, Tier B condicional). Exemplos dessa seção seguem como proposta,
# sem validação real nesta tarefa.

---

# Parte 2 — smoke-vendas.sh revisado (T-37: + X-Api-Key nas chamadas de sucesso, +2 cenários
# 401), 9 cenários, rodado 2 vezes seguidas contra a mesma instância real (banco recriado
# antes desta rodada), ambas 9/9 verde.
#
# Comando: BASE_URL=http://localhost:5034/api/vendas API_KEY=CHAVE_LOCAL_SMOKE_T34 \
#   bash docs/postman/smoke-vendas.sh

== Execução 1/2 ==
== Coleção de fumaça T-34 — BASE_URL=http://localhost:5034/api/vendas ==

[OK]    1. Quitação — caminho feliz (200 Quitada) — HTTP 200
        corpo: {"status":"Quitada","dataQuitacao":"2026-09-23T02:15:30.892442Z"}
[OK]    2. Quitação — 400 PAYLOAD_INVALIDO (itens vazio) — HTTP 400
        corpo: {"erro":{"codigo":"PAYLOAD_INVALIDO","mensagem":"O campo 'itens' não pode ser vazio."}}
[OK]    3. Quitação — 409 VENDA_JA_CANCELADA — HTTP 409
        corpo: {"erro":{"codigo":"VENDA_JA_CANCELADA","mensagem":"Venda 'V-SMOKE-JACANC-1790129730' já está cancelada; Cancelada é um estado terminal."}}
[OK]    4. Cancelamento — caminho feliz (200 Cancelada, D-08) — HTTP 200
        corpo: {"status":"Cancelada"}
[OK]    5. Cancelamento — 409 MOTIVO_OBRIGATORIO (Quitada sem motivo) — HTTP 409
        corpo: {"erro":{"codigo":"MOTIVO_OBRIGATORIO","mensagem":"Motivo do cancelamento é obrigatório."}}
[OK]    6. Status — caminho feliz (200 Quitada) — HTTP 200
        corpo: {"vendaId":"V-SMOKE-QUIT-1790129730","status":"Quitada"}
[OK]    7. Status — 404 VENDA_NAO_ENCONTRADA — HTTP 404
        corpo: {"erro":{"codigo":"VENDA_NAO_ENCONTRADA","mensagem":"Venda 'V-SMOKE-NAO-EXISTE-1790129730' não encontrada."}}
[OK]    8. Quitação — 401 NAO_AUTORIZADO (X-Api-Key ausente) — HTTP 401
        corpo: {"erro":{"codigo":"NAO_AUTORIZADO","mensagem":"Credencial de acesso ausente ou inválida."}}
[OK]    9. Quitação — 401 NAO_AUTORIZADO (X-Api-Key inválida) — HTTP 401
        corpo: {"erro":{"codigo":"NAO_AUTORIZADO","mensagem":"Credencial de acesso ausente ou inválida."}}

== Resultado: 9/9 cenários verdes ==
100% verde.

== Execução 2/2 (mesma instância, vendaId novo por timestamp) ==
== Coleção de fumaça T-34 — BASE_URL=http://localhost:5034/api/vendas ==

[OK]    1. Quitação — caminho feliz (200 Quitada) — HTTP 200
        corpo: {"status":"Quitada","dataQuitacao":"2026-09-23T02:15:38.0369113Z"}
[OK]    2. Quitação — 400 PAYLOAD_INVALIDO (itens vazio) — HTTP 400
        corpo: {"erro":{"codigo":"PAYLOAD_INVALIDO","mensagem":"O campo 'itens' não pode ser vazio."}}
[OK]    3. Quitação — 409 VENDA_JA_CANCELADA — HTTP 409
        corpo: {"erro":{"codigo":"VENDA_JA_CANCELADA","mensagem":"Venda 'V-SMOKE-JACANC-1790129737' já está cancelada; Cancelada é um estado terminal."}}
[OK]    4. Cancelamento — caminho feliz (200 Cancelada, D-08) — HTTP 200
        corpo: {"status":"Cancelada"}
[OK]    5. Cancelamento — 409 MOTIVO_OBRIGATORIO (Quitada sem motivo) — HTTP 409
        corpo: {"erro":{"codigo":"MOTIVO_OBRIGATORIO","mensagem":"Motivo do cancelamento é obrigatório."}}
[OK]    6. Status — caminho feliz (200 Quitada) — HTTP 200
        corpo: {"vendaId":"V-SMOKE-QUIT-1790129737","status":"Quitada"}
[OK]    7. Status — 404 VENDA_NAO_ENCONTRADA — HTTP 404
        corpo: {"erro":{"codigo":"VENDA_NAO_ENCONTRADA","mensagem":"Venda 'V-SMOKE-NAO-EXISTE-1790129737' não encontrada."}}
[OK]    8. Quitação — 401 NAO_AUTORIZADO (X-Api-Key ausente) — HTTP 401
        corpo: {"erro":{"codigo":"NAO_AUTORIZADO","mensagem":"Credencial de acesso ausente ou inválida."}}
[OK]    9. Quitação — 401 NAO_AUTORIZADO (X-Api-Key inválida) — HTTP 401
        corpo: {"erro":{"codigo":"NAO_AUTORIZADO","mensagem":"Credencial de acesso ausente ou inválida."}}

== Resultado: 9/9 cenários verdes ==
100% verde.

---

# Nota de log (confirmação de que a chave nunca é logada, ApiKeyHandler/T-35): saída do
# harness durante os cenários 401 acima mostrou só mensagem genérica, sem o valor do header
# recebido nem a chave configurada:
#   [<correlationId>] Requisição rejeitada: X-Api-Key ausente ou inválida.

# Pasta temporária C:\temp_erp_t37 (build copiado para fora do OneDrive, contorno do
# Bloqueio 001) removida ao final desta sessão. Banco C:\ERPFinanceiro\data\financeiro_t34_smoke.fdb
# removido ao final.
