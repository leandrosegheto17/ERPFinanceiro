# TASK.md — App de Leitura Bíblica Guiada + Preparo de Estudo

- **Versão**: rascunho rodada 1 (Loop C do `/definir_organizar`)
- **Data**: 2026-09-07
- **Autor**: coordenador (chapéu Tech Lead)
- **Base**: `.md/SDD.md` (aprovado), `.md/UX-SPEC.md` (aprovado), ADRs 001–012,
  `.md/PRD-TECNICO.md`, `.md/CTO-REVIEW.md`
- **Consumidores**: executor, validador, gestor
- **Status**: aguardando aprovação do usuário (orquestrador)

> Granularidade-alvo: ~1 dia-pessoa por tarefa. Nenhuma tarefa mistura mais de uma
> tela / endpoint / regra de negócio distinta / migration de SQL, salvo
> inseparabilidade documentada (marcada `[insep.]` e justificada na Seção 6).
> Nenhuma tarefa deve exigir do Executor mais de ~300 mil tokens de contexto de
> trabalho — ver checagem na Seção 6.

---

## 1. Diretrizes de Implementação

Traduzidas dos ADRs e do SDD.md em regras práticas de código, obrigatórias em toda
tarefa deste documento.

| # | Diretriz | Origem |
|---|---|---|
| DI-01 | Regra de negócio existe **uma vez**, no cliente (`core/*` ou `features/*`). O servidor (Supabase) nunca reimplementa uma regra já implementada no cliente — só propriedade de linha (RLS) e conciliação | ADR-002 |
| DI-02 | `core/*` é TypeScript puro, sem React nem DOM. Nenhum import de `features/*` para dentro de `core/*` (dependência unidirecional verificada por lint, tarefa TASK-001) | ADR-001, ADR-012 |
| DI-03 | Toda tabela com coluna `user_id` nasce com RLS habilitada **na mesma migration** que a cria. Uma migration que cria tabela sem política de RLS é defeito de severidade máxima (RT-04) | SDD §7.2, GUARDRAILS |
| DI-04 | Nenhum HTML de usuário ou de acervo editorial é injetado sem sanitização | SDD §7.7, ADR-004 |
| DI-05 | O produto nunca renderiza as strings "Almeida Atualizada", "ARA" ou "ARC", em nenhuma tela, nenhum commit, nenhum texto de acervo | CTO-REVIEW R-02, RN-06 |
| DI-06 | Toda tela nova é cruzada contra os 4 estados (vazio, carregando, erro, sucesso) antes de ser marcada `Concluída`; "N/A" só com motivo escrito no PR | UX-SPEC §4 |
| DI-07 | Nenhuma informação de estado é transmitida só por cor — todo selo/indicador tem ícone **e** texto | UX-SPEC §5.1 (1.4.1) |
| DI-08 | Alvo de toque mínimo 44×44 px (48×48 em T-20); reordenação por arrastar sempre tem alternativa por botão ↑/↓ | UX-SPEC §3.1, §5.1 (2.5.7/2.5.8) |
| DI-09 | Nenhum segredo de serviço (chave VAPID privada, credencial de service role) é gravado em código de cliente ou em variável exposta ao bundle; todo PR que tocar credenciais roda o teste de grep do bundle (TASK-042) | SDD §7.1, §7.5 |
| DI-10 | `localStorage` nunca recebe conteúdo do usuário nem segredo além do necessário; conteúdo do usuário só em IndexedDB (Dexie) | SDD §7.1, §7.3 |
| DI-11 | Dentro da rota de `features/presentation` (T-20), nenhuma UI fora do cartão é renderizada, e a fila de mensagens do app (convites, toasts de sincronização) é suspensa enquanto a rota estiver ativa | UX-SPEC T-20, CA-14.2/14.3 |
| DI-12 | Toda migration nova roda contra o teste de catálogo de RLS (TASK-033) antes de merge — é gate de CI, não revisão manual | RT-04 |
| DI-13 | Módulo 2 (`features/outline`, `features/presentation`) nunca é importado por código da Fase 1; a rota do Módulo 2 só é registrada quando há sessão válida | ADR-012, SDD §7.1 |
| DI-14 | Testes de acessibilidade e de contraste são automatizados (axe/Playwright + teste de par de token), nunca inspeção manual isolada | UX-SPEC §5 |
| DI-15 | Bibliotecas obrigatórias por camada: Dexie (storage local), Radix Primitives (só em overlays), Workbox via `vite-plugin-pwa` (SW), Vitest (unidade), Playwright (e2e). Proibido: qualquer SDK de analytics/anúncio de terceiro, qualquer fonte web externa, qualquer biblioteca de streak/gamificação | SDD §3, RNF-13, CSP §7.5 |

## 2. Spikes Técnicos

Tarefas de incerteza técnica alta, sem estimativa de esforço forçada — o produto é
conhecimento, não código funcional. Bloqueiam a tarefa de implementação associada
até serem resolvidos.

| ID | Spike | Pergunta a responder | Bloqueia | Origem do risco |
|---|---|---|---|---|
| SPIKE-01 | Confiabilidade de Web Push em Safari/iOS PWA instalado | Cobertura real de entrega, latência típica, comportamento com app fechado; decidir se o texto de T-12 precisa de ressalva adicional | TASK-056 a TASK-061 | RT-02 |
| SPIKE-02 | Comportamento de `storage.persist()` e expurgo de armazenamento gravável em Safari/iOS, com e sem PWA instalado | Confirmar a janela real de expurgo (~7 dias) e a eficácia de `persist()`; decidir gatilho exato do convite de instalação em T-15/T-19 | TASK-067, TASK-081, TASK-083 | RT-01 |
| SPIKE-03 | Gramática de reconhecimento de referência bíblica em pt-BR: abreviações ambíguas, intervalos entre capítulos, variações de digitação reais | Fechar a tabela de abreviações e a lista de casos "não resolvido" antes de escrever a suíte simétrica | TASK-010 | RT-05, ADR-005 |
| SPIKE-04 | Suporte cross-browser do Screen Wake Lock API e estratégia de fallback quando indisponível | Confirmar navegadores sem suporte real na base esperada e o texto de aviso correspondente em T-19 | TASK-085 | UX-SPEC T-20 (CA-14.4), L-05 |

**SPIKE-03 — Resolvido (2026-09-07)**: tabela de abreviações dos 66 livros
fechada, gramática de espaçamento/separadores decidida, e lista de casos "não
resolvido" fechada — incluindo uma ambiguidade real encontrada (colisão
"Jó"×"Jo" após normalização de acento) e sua resolução. Ver
`.md/spikes/SPIKE-03-resultado.md`. TASK-010 desbloqueada.

## 3 / 4. Lista de Tarefas, Lotes, Dependências e Paralelismo

Convenção: **Paralelizável-com** lista as tarefas do mesmo lote que podem começar
sem esperar a tarefa da linha terminar. **Depende de** lista pré-requisitos de
qualquer lote. Todas as tarefas são Fase 1, salvo Lote 14/15 (Fase 2). Estimativa
padrão = **1 dia-pessoa** (dono: Executor); desvio é marcado explicitamente.

### Lote 0 — Fundação e CI

| ID | Tarefa | Critério de aceite | Depende de | Paralelizável-com | Status |
|---|---|---|---|---|---|
| TASK-001 | Scaffold do projeto (Vite+React+TS, pastas `core/*`/`features/*`/`tools/*`) e lint de dependência unidirecional (DI-02) | Build limpo; lint falha se `core` importar de `features` | — | TASK-005 | Concluída — scaffold Vite+React+TS manual (`package.json`, `tsconfig*.json`, `vite.config.ts`, `eslint.config.js`) com `src/core`, `src/features/shell`, `src/tools`; regra unidirecional via `dependency-cruiser` (`.dependency-cruiser.cjs`, rule `no-features-from-core`, `npm run lint:deps`), com regex cross-plataforma (`\\`/`/`) para funcionar em Windows. Teste automatizado (`src/tools/dependency-rule.test.ts`, Vitest + API `dependency-cruiser`) cobre as duas pontas do critério de aceite: `src` real passa limpo, e uma fixture que força `core` a importar de `features` é reprovada pela mesma regra (`no-features-from-core`, severidade `error`). Confirmado também via CLI real (`npx depcruise`): exit 0 no `src` limpo, exit 1 ao inserir um import real `core → features` temporário (removido depois do smoke test). `npm run build`, `npm run typecheck`, `npm run lint` e `npx vitest run` passam limpos. `npm run dev`/`preview` disponíveis; CI (TASK-002), PWA (TASK-003) e Playwright (TASK-004) ficam para as tarefas seguintes do lote. |
| TASK-002 | Pipeline CI (GitHub Actions): lint, typecheck, teste unitário, build | Workflow verde em PR de exemplo | TASK-001 | TASK-003, TASK-004 | Concluída — workflow `.github/workflows/ci.yml` (job único `build`, Node 24 via `actions/setup-node` com cache npm, gatilho `push`/`pull_request` em `main`, `concurrency` por ref) executando em sequência `npm ci`, `npm run lint`, `npm run typecheck`, `npm run test`, `npm run build`. YAML validado sintaticamente (`python -c "import yaml; yaml.safe_load(...)"`, sem erro). Os 4 comandos foram executados localmente contra o estado atual do repo (após TASK-003/TASK-004 já terem adicionado `vite-plugin-pwa`/Workbox e Playwright em paralelo) e passam limpos: lint (eslint + `lint:deps`, "no dependency violations found"), typecheck (`tsc -b --noEmit` sem erro), test (`vitest run`, 2/2 testes verdes), build (`tsc -b && vite build`, gera `dist/` com `sw.js`/`workbox-*.js` via PWA). Observação de ambiente (não é defeito do workflow): como três instâncias do Executor rodaram em paralelo no mesmo checkout (sem worktrees separados), houve corrupção transitória de `node_modules` por instalações npm concorrentes (`ENOTEMPTY`, módulos parcialmente escritos) durante a validação local — resolvida reexecutando `npm install`; o workflow do GitHub Actions roda em runner isolado por job e não sofre esse tipo de contenção. Nenhuma edição destrutiva feita em `package.json`/`vite.config.ts` (só leitura, para conferir os scripts já existentes). |
| TASK-003 | `vite-plugin-pwa` + Workbox: shell básico instalável | App instala localmente; manifest.json válido | TASK-001 | TASK-002, TASK-004 | Concluída — `vite-plugin-pwa` (`^1.3.0`) adicionado a `vite.config.ts` (edição cirúrgica, sem reescrever o arquivo) com manifest inline (`name`/`short_name`/`start_url: "/"`/`scope: "/"`/`display: "standalone"`/`lang: "pt-BR"`, ícones `192x192`, `512x512` e `512x512 maskable` em `public/icons/`, gerados como PNG placeholder — arte final é tarefa de design, fora de escopo) e `manifestFilename: "manifest.json"`. `workbox.globPatterns` restrito a `**/*.{js,css,html}` — só o app shell entra no precache aqui; estratégias de cache por artefato (corpus/conteúdo editorial) ficam para TASK-066 e `storage.persist()` para TASK-067 (Lote 13), como delimitado no escopo desta tarefa. Teste automatizado (`src/tools/pwa/pwa-manifest.test.ts`, Vitest) roda um build real via API programática do Vite (mesmo `vite.config.ts`, sem duplicar config) contra um outDir temporário e confirma as duas pontas do critério de aceite: `manifest.json` gerado é JSON válido com os campos mínimos de instalabilidade (name/short_name/start_url/display/ícones 192+512, cada ícone referenciado existindo de fato no output) e o plugin gera um Service Worker real (`sw.js` não vazio, contém Workbox — DI-15) no build; confirma também o `<link rel="manifest">` no `index.html`. `npm run build` confirmado manualmente: gera `dist/manifest.json`, `dist/sw.js`, `dist/workbox-*.js`, `dist/registerSW.js`; `npm run lint`, `npm run typecheck`, `npx vitest run` e `npx playwright test` (suíte e2e de TASK-004) permanecem verdes — `playwright.config.ts` roda contra `vite dev` (não o build), e `devOptions.enabled: false` neste `vite.config.ts` garante que o Service Worker não é registrado em modo dev, então `e2e/placeholder.spec.ts` continua testando exatamente o cenário "sem SW" que seu comentário descreve; nenhuma dessincronia entre as duas tarefas. Durante a execução desta tarefa, ambiente local apresentou corrupção pontual e recorrente em `node_modules` (`es-abstract`, `@babel/types`, `caniuse-lite`, `vite`) por corrida de instalações `npm` concorrentes de outras instâncias do Executor no mesmo lote — corrigido via reinstalação pontual de cada pacote afetado; nenhuma mudança de código foi necessária por isso, mas registrado aqui como risco operacional de paralelismo em `node_modules` compartilhado. |
| TASK-004 | Setup Vitest + Playwright, incl. config de rede desligada e cache limpo | Teste unitário e e2e placeholder passam em CI | TASK-001 | TASK-002, TASK-003 | Concluída — correção do achado crítico do Validador (`.md/QA-REPORT.md`, Seção 2, TASK-004): `.github/workflows/ci.yml` (job `build`) recebeu 2 novos steps, em edição cirúrgica após o step `Build` — `Install Playwright browsers (Chromium)` (`npx playwright install --with-deps chromium`) e `E2E tests` (`npm run test:e2e`) — nenhum step pré-existente foi removido/reescrito. Como `playwright.config.ts` já define `webServer` (servidor de dev do Vite, `reuseExistingServer: !process.env.CI`), o próprio Playwright sobe o servidor dentro do runner de CI, sem step adicional de `preview`. YAML revalidado sintaticamente (`python -c "import yaml; yaml.safe_load(...)"`, sem erro) e `npx playwright install --with-deps chromium && npx playwright test` confirmado localmente contra o estado atual do repo: 2/2 verdes ("renderiza o shell do app", "cache limpo + rede desligada bloqueia nova navegação"). Critério de aceite agora cumprido nas duas metades (unitário e e2e passam em CI, não só localmente). Nota anterior do Executor (mantida como contexto): Vitest de TASK-001 reaproveitado sem alteração de config; adicionado `@playwright/test` (`playwright.config.ts`, `testDir: e2e/`, projeto `chromium`, `webServer` no servidor de dev do Vite — deliberadamente não no build de produção/PWA, para não acoplar TASK-004 ao pipeline `vite-plugin-pwa` de TASK-003 em andamento em paralelo). Mecanismo de "rede desligada + cache limpo" em `e2e/utils/offline.ts` (`clearBrowserCache`/`goOffline` via CDP `Network.clearBrowserCache`/`clearBrowserCookies` + `context.setOffline`, nessa ordem), reaproveitável pelos smoke tests offline de PWA do Lote 13 (TASK-066/067/068) e do Lote 15 (TASK-088) sem recriar a lógica. `e2e/placeholder.spec.ts`: 2 testes — shell renderiza, e cache limpo+offline efetivamente bloqueia nova navegação (prova que o mecanismo não é no-op). `playwright.config.ts`/`e2e/**/*.ts` incluídos em `tsconfig.node.json` e no script `lint`; script `test:e2e` adicionado ao `package.json` (edição cirúrgica, scripts existentes preservados). `.gitignore` recebeu `test-results/`/`playwright-report/`/`blob-report/`. Nota sobre `src/tools/dependency-rule.test.ts` (TASK-001) tratada pelo Validador como débito de severidade baixa (`REFAT-L0-01`, ver Lote Refatoração Lote-0), não como bloqueio desta tarefa — reproduzido sem carga concorrente dentro do timeout padrão. |
| TASK-005 | Setup Supabase local + segredos de CI (nunca no bundle) | `supabase start` local funciona; teste de grep do bundle vazio ainda passa (placeholder) | — | TASK-001 | Concluída — `supabase init` + portas offset (+100: API 54421, DB 54422, Studio 54423, Inbucket 54424, pooler 54429, shadow 54420, analytics 54427, inspector 8183) para não colidir com outro projeto Supabase local já em execução; `supabase start`/`status`/`stop` validados de ponta a ponta com Docker real. Segredos de CI documentados em `docs/ci-secrets.md` + `.env.example` (nunca commitados). Teste placeholder em `tests/security/bundle-secrets.test.mjs` (detector real em `scripts/security/scan-bundle-secrets.mjs`, node:test): 3 asserções reais contra fixture sintética (prova que a lógica de detecção não é vazia) + 1 checagem contra `dist/` real, `skipped` explicitamente (motivo registrado) até a primeira tarefa de build gerar bundle de verdade — nunca um pass silencioso. `npm run test:security` |

### Refatoração Lote-0

> Criada pelo Validador (chapéu QA) durante o fechamento estrutural do Lote 0
> (2026-09-07), para achado simples/débito de baixa severidade que não
> compromete o critério de aceite central de nenhuma tarefa `Concluída` do
> lote — ver `.md/QA-REPORT.md`, Seção 3 e Seção 5.

| ID | Tarefa | Critério de aceite | Depende de | Paralelizável-com | Status |
|---|---|---|---|---|---|
| REFAT-L0-01 | Revisar o timeout de `src/tools/dependency-rule.test.ts` (TASK-001) — chamada real ao `dependency-cruiser` via API pode ultrapassar o timeout padrão de 5000ms do Vitest sob carga de I/O/CPU do runner; reproduzido apenas sob concorrência local de múltiplas instâncias de Executor, não reproduzido isoladamente | Rodar o teste algumas vezes no CI real (após correção de TASK-004) sob carga normal; se o tempo de execução recorrer perto do limite, aumentar o timeout do teste ou isolar a chamada do `dependency-cruiser`; senão, fechar sem alteração | TASK-004 (workflow de CI precisa estar corrigido e rodando de verdade para observar o tempo real) | — | Concluído — Validador rodou `npx vitest run` 4 vezes de forma independente durante a validação do Lote 1 (2026-09-07), nenhuma recorrência do timeout observada (durações 4.31s/4.95s/5.23s/3.93s, todas dentro do limite padrão). Fechado sem alteração de código, conforme a própria prescrição da tarefa. Ver `.md/QA-REPORT.md`, Seção "Lote 1", Seção 3 e 5. |

### Lote 1 — Núcleo: Corpus e Importação

| ID | Tarefa | Critério de aceite | Depende de | Paralelizável-com | Status |
|---|---|---|---|---|---|
| TASK-006 | `tools/corpus-import`: parser do snapshot bruto `porbr2018` → modelo canônico | Fixture gera estrutura com 66 livros | TASK-001 | TASK-010 | Concluída — parser puro em `src/tools/corpus-import/parse-snapshot.ts` (`parseSnapshot(rawContent): CanonicalCorpus`), sem snapshot real disponível no repo (dado externo, fora deste TASK.md): implementado contra fixture sintética documentando a suposição de formato no próprio arquivo (`src/tools/corpus-import/__fixtures__/porbr2018-sample.txt`, comentário de cabeçalho — linha por versículo, campos `<ID_LIVRO_USFM>\t<CAPÍTULO>\t<VERSÍCULO>\t<TEXTO>`, ids no padrão USFM/eBible.org). Cânon protestante completo (39 AT + 27 NT, ordem Gênesis...Malaquias/Mateus...Apocalipse) em `src/tools/corpus-import/canon.ts` (`CANONICAL_BOOKS`); modelo canônico (`CanonicalBook`/`CanonicalChapter`/`CanonicalVerse`, alinhado a `Book`/`Chapter`/`Verse` do SDD.md §5.3, sem `CorpusManifest` — isso é TASK-008) em `src/tools/corpus-import/types.ts`. Parser não normaliza texto (preserva byte a byte, conforme ADR-003 — normalização é problema do render), ordena por cânon (não pela ordem do snapshot bruto) e falha com `CorpusParseError` em 3 cenários defeituosos (livro do cânon ausente do snapshot, livro fora do cânon presente, linha malformada) — interface pensada para TASK-007 (verificação byte a byte) e TASK-009 (acesso tipado) consumirem sem retrabalho. Teste automatizado (`src/tools/corpus-import/parse-snapshot.test.ts`, Vitest, 8 casos) cobre diretamente o critério de aceite — roda o parser sobre a fixture real em disco e confirma exatamente 66 livros, na ordem canônica exata (índices 0/38/39/65 = Gênesis/Malaquias/Mateus/Apocalipse), split AT=39/NT=27, preservação do texto sem normalização, agrupamento/ordenação de capítulo e versículo, tolerância a linhas em branco/comentário, e os 3 cenários de falha. `npm run lint` (eslint + `lint:deps`, "no dependency violations found"), `npm run typecheck` (`tsc -b --noEmit`, sem erro) e `npx vitest run` (3 arquivos de teste, 12/12 verdes, incluindo os 8 novos) confirmados localmente após a implementação.
| TASK-007 | `tools/corpus-import`: verificação byte a byte, falha o build (RNF-10) | Teste que corrompe 1 caractere confirma falha do pipeline | TASK-006 | — | Concluída — `verifyCorpusIntegrity(rawContent, corpus)` em `src/tools/corpus-import/verify-integrity.ts`: reconstitui, a partir do `CanonicalCorpus` (saída de `parseSnapshot`, TASK-006), as linhas `<ID_LIVRO_USFM>\t<CAPÍTULO>\t<VERSÍCULO>\t<TEXTO>` na ordem de travessia canônica, e compara byte a byte (`!==`, sem nenhuma normalização) contra as linhas significativas do `rawContent` original (mesmo critério estrutural do parser para ignorar linha em branco/comentário — o texto de cada linha não é tocado). Diverge em contagem de linhas ou em qualquer byte de uma linha → lança `CorpusIntegrityError` (interrompe o pipeline, não loga e segue silencioso); idêntico → retorna sem erro. Teste automatizado (`src/tools/corpus-import/verify-integrity.test.ts`, Vitest, 3 casos) cobre exatamente o critério de aceite: (1) snapshot íntegro não lança erro; (2) corrompe 1 único caractere do `rawContent` original (troca 1 caractere por "X" preservando o comprimento) e confirma `CorpusIntegrityError`/mensagem de divergência byte a byte; (3) truncamento (divergência de contagem de linhas) também lança. `npm run lint` (eslint + `lint:deps`, "no dependency violations found"), `npm run typecheck` (`tsc -b --noEmit`, sem erro) e `npx vitest run` (4 arquivos de teste, 15/15 verdes, incluindo os 3 novos e sem regressão nos 12 de TASK-006) confirmados localmente após a implementação. |
| TASK-008 | `tools/corpus-import`: manifesto (`sourceVersionDate`, SHA-256, licença) | `manifest.json` validado contra schema | TASK-007 | — | Concluída — `CorpusManifest` (`sourceId`, `sourceVersionDate`, `importedAt`, `license`, `licenseUrl`, `holders`, `modifications: "none"`, `sha256`) em `src/tools/corpus-import/manifest-types.ts`. Divergência documentada do SDD.md §5.3: `sha256` é `Record<string, string>` (mapa nome-do-arquivo → hash), não `sha256[]` — permite lookup O(1) por artefato sem duplicar o nome do arquivo, mesma informação. `buildManifest(input)` em `build-manifest.ts` calcula o SHA-256 de cada artefato (`node:crypto`, `createHash("sha256")`, sem dependência nova) a partir de proveniência informada pelo chamador (`sourceId`, `sourceVersionDate`, licença, `holders`, lista de artefatos). `validateManifestSchema(value)` em `validate-manifest-schema.ts` (assertion function, sem Ajv/Zod — não estavam no `package.json`): confirma todos os campos obrigatórios presentes e tipados, `sourceVersionDate`/`importedAt` como data ISO 8601 válida, `licenseUrl` como URL válida, `modifications` como literal `"none"`, e `sha256` como mapa não vazio com valores hexadecimais de 64 caracteres; lança `ManifestSchemaError` com o campo e motivo exato em vez de retornar `false`. `src/tools/corpus-import/*.ts` (não-teste) movido do escopo de `tsconfig.app.json` (browser) para `tsconfig.node.json` (usa `node:crypto`, é script de build/CI fora do bundle de runtime — README já documentava essa intenção) — sem impacto em `core`/`features`, confirmado por `lint:deps`. Teste automatizado (`src/tools/corpus-import/build-manifest.test.ts`, Vitest, 7 casos) cobre o critério de aceite diretamente: manifesto gerado por `buildManifest` a partir da fixture de TASK-006/007 passa em `validateManifestSchema`; hash determinístico; e rejeição com motivo claro para campo obrigatório faltando, hash em formato inválido, data malformada, `sha256` vazio e `modifications` fora do literal. `npm run lint` (eslint + `lint:deps`, "no dependency violations found"), `npm run typecheck` (`tsc -b --noEmit`, sem erro) e `npx vitest run` (5 arquivos de teste, 22/22 verdes, incluindo os 7 novos e sem regressão nos 15 de TASK-006/TASK-007) confirmados localmente após a implementação. |
| TASK-009 | `core/corpus`: acesso tipado ao corpus local (índice de livros/capítulos) | Dado o bundle, retorna capítulo por referência estruturada | TASK-008 | — | Concluída — primeiro módulo de `core/*` real do projeto (runtime do app, distinto de `tools/corpus-import`, que é código de build/CI). Tipos mínimos de runtime (`Book`/`Chapter`/`Verse`, `CorpusBundle` como array, `ChapterReference { bookId, chapter }`) em `src/core/corpus/types.ts` — deliberadamente não reimportam `CanonicalBook`/`CanonicalChapter`/`CanonicalVerse` de `src/tools/corpus-import/types.ts`: mesmo shape parecido, são camadas diferentes (build vs. runtime do navegador), e `core/*` nunca depende de `tools/*` em runtime (evita vazar Node/crypto do pipeline de build para o bundle do cliente). `getChapter(bundle, reference): Chapter \| undefined` em `src/core/corpus/get-chapter.ts`, com `buildCorpusIndex`/`getChapterFromIndex` auxiliares (`Map<bookId, Book>`) para lookup O(1) por livro em vez de O(n) por acesso; `undefined` para "não encontrado" (não exceção) — alinhado ao padrão do projeto de reservar exceção para violação de invariante/formato (`CorpusParseError`/`CorpusIntegrityError`/`ManifestSchemaError`), não para ausência esperada de referência. Barrel `src/core/corpus/index.ts` exporta a API pública do módulo. Teste automatizado (`src/core/corpus/get-chapter.test.ts`, Vitest, 5 casos) cobre diretamente o critério de aceite com bundle de fixture sintético (2 livros, GEN/EXO): retorna capítulo correto por referência estruturada (incluindo um segundo capítulo do mesmo livro e um livro diferente), `undefined` para `bookId` inexistente no bundle, `undefined` para capítulo inexistente no livro, e equivalência entre `getChapter` e o par `buildCorpusIndex`/`getChapterFromIndex`. `npm run lint` (eslint + `lint:deps` — "no dependency violations found", confirma `core/corpus` sem import de `features/*`/`tools/*`), `npm run typecheck` (`tsc -b --noEmit`, sem erro) confirmados limpos; `npx vitest run` do arquivo novo isolado 5/5 verde. Na suíte completa (`npx vitest run`), único teste vermelho é o timeout pré-existente de `src/tools/dependency-rule.test.ts` (rastreado em REFAT-L0-01, Lote Refatoração Lote-0) — reproduzido também isoladamente (sem carga concorrente de outras instâncias), portanto não é regressão desta tarefa; mantido como está, já com prazo definido ("antes do fechamento do Lote 1") e dono (Validador/REFAT-L0-01), não deste TASK-009. |
| TASK-010 | `core/reference`: reconhecimento de referência bíblica (Resolved/Unresolved, ADR-005) | Suíte simétrica (resolve/não resolve) 100% verde | SPIKE-03, TASK-001 | TASK-006, TASK-007, TASK-008, TASK-009 | Concluída — módulo puro `src/core/reference/` (ADR-005: soma de tipos `ReferenceResolution = ResolvedReference \| UnresolvedReference`, `UnresolvedReason` fechado com os 6 motivos do ADR, `src/core/reference/types.ts`). Tabela de nomes/abreviações dos 66 livros como dado (`src/core/reference/book-names.ts`, `BOOK_NAME_TABLE`), transcrição literal da Seção 2 do `SPIKE-03-resultado.md`: `canonicalName`/`abbreviation` armazenam a forma base sem prefixo numérico, com `numberedPrefix?: "1"\|"2"\|"3"` modelando os livros numerados como entradas distintas por `bookId` (evita duplicar a lógica de prefixo em cada uma); inclui a decisão de João sem abreviação curta, Jó sem "Jo", "Salmo" como alias singular de Salmos. Normalização (`src/core/reference/normalize.ts`, `normalizeReferenceInput`): minúsculas + NFD com remoção de diacríticos (`̀`-`ͯ`) + colapso de espaços múltiplos; pontos não são removidos à parte — são o próprio separador capítulo:versículo (`Rm 8.28`), e um ponto fora dessa posição (`v. 28`) quebra a estrutura do token de livro e cai em `formato-nao-reconhecido` sem regra adicional (decisão documentada no próprio arquivo, evita contradizer o caso obrigatório `v. 28`). `resolveReference(input, bundle, table?)` em `src/core/reference/resolve-reference.ts`: parsing em 3 passos determinísticos (divide no último espaço da entrada normalizada → extrai prefixo numérico só quando inequívoco, forma espaçada sempre exige espaço explícito e a forma colada só dispara com dígito arábico literal no início, nunca letra → valida a forma estrutural do restante antes de consultar a tabela) em vez de um único regex monolítico — decisão documentada no cabeçalho do arquivo: um regex com grupo de prefixo opcional greedy `(1\|2\|3\|i\|ii\|iii)\s?` consome o "i" inicial de livros como "Isaías" sem chance de backtrack para a leitura correta, o que quebraria a forma colada `1Co 13:4` (SPIKE-03 §1) sem essa separação em etapas. Livro reconhecido na tabela mas ausente do `CorpusBundle` fornecido é tratado como `livro-desconhecido` (nunca ocorre em produção — o bundle real sempre tem os 66 livros — mas é o que torna o caso obrigatório `Jo 3:16` corretamente `livro-desconhecido` com uma fixture de teste deliberadamente parcial, decisão documentada no próprio código e no teste). Barrel `src/core/reference/index.ts`. Teste automatizado (`src/core/reference/resolve-reference.test.ts`, Vitest, 35 casos): tabela positiva cobrindo todo formato da Seção 1 do spike (livro+cap:vers, intervalo mesmo capítulo com hífen/travessão, capítulo inteiro, forma numerada arábica espaçada/colada e romana, segundo livro numerado, alias "Salmo", insensibilidade a maiúsculas/acentos) com fixture cobrindo Romanos/1 Coríntios/2 Coríntios/Gálatas/Salmos (bundle deliberadamente parcial, sem Jó/João); tabela negativa cobrindo literalmente os 10 casos obrigatórios da Seção 4 (via `it.each`, reason exato confirmado por caso); testes extras de intervalo inválido (verseStart > verseEnd, verseEnd inexistente) e de `ambiguo` via tabela de abreviações sintética injetada por parâmetro (não a tabela real de produção, conforme a nota da Seção 3 do spike — a tabela real não tem ambiguidade real nenhuma); teste dedicado confirmando que nenhuma entrada malformada lança exceção. `npm run lint` (eslint + `lint:deps`, "no dependency violations found", confirma `core/reference` sem import de `features/*`/`tools/*`, só de `core/corpus`), `npm run typecheck` (`tsc -b --noEmit`, sem erro) e `npx vitest run` (7 arquivos, 62/62 verdes, incluindo os 35 novos e sem regressão nos 27 anteriores, sem o timeout intermitente de REFAT-L0-01 desta vez) confirmados localmente após a implementação. Fecha o Lote 1 (6 de 6 tarefas concluídas). |
| TASK-011 | Teste anti-"Almeida Atualizada/ARA/ARC" no bundle final (DI-05) | CI falha ao inserir a string de propósito | TASK-009 | — | Concluída — segue o mesmo padrão já estabelecido por TASK-005/TASK-042 (`bundle-secrets`) para strings proibidas: `scanBundleForForbiddenStrings(bundleDir)` em `scripts/security/scan-bundle-forbidden-strings.mjs` varre `dist/` (mesma lista de extensões de texto do scanner de segredos) procurando "Almeida Atualizada", "Almeida Revista e Atualizada", "ARA" e "ARC". Decisão de matching documentada no cabeçalho do próprio scanner: as duas frases longas usam comparação direta case-insensitive (risco de colisão desprezível); já "ARA"/"ARC" são siglas de 3 letras que colidiriam com substring legítima frequente em português (ARARA, ARCO, PARAR, MARCOS, ARCA) — por isso exigem borda de token (lookaround, não flanqueadas por letra incl. acentuada) E a forma exatamente maiúscula convencional da sigla bíblica (não `ara`/`arc` minúsculo, comum em outra palavra/token), sem abrir brecha real porque quem citar a tradução por nome/marca o faz na forma convencional. Teste automatizado (`tests/security/bundle-forbidden-strings.test.mjs`, `node:test`, mesmo estilo do teste irmão de segredos) com 7 casos: 4 positivos (uma fixture sintética por string proibida, confirma que o detector aciona), 2 negativos (bundle limpo comum; bundle com texto legítimo que colide por substring com as siglas — ARARA/arco-íris/arca de Noé/"Marcos"/`pararExecucao` — confirma zero falso positivo), e o teste real contra `dist/`, `skipped` explicitamente com motivo quando `dist/` não existe na sessão (nunca passa silencioso), rodando de verdade a partir do primeiro `npm run build`. Nenhum script novo no `package.json`: `test:security` (`node --test tests/security/*.test.mjs`, já existente de TASK-005) já cobre por glob qualquer teste novo em `tests/security/`, incluído este. Evidência do critério de aceite ("CI falha ao inserir a string de propósito"): após `npm run build` gerar `dist/` real, injetei manualmente a string `"Almeida Atualizada"` num arquivo do bundle publicado e rodei `node --test tests/security/bundle-forbidden-strings.test.mjs` — o teste "bundle real do cliente (dist/) não contém nenhuma string proibida por DI-05" falhou (`ERR_ASSERTION`, finding reportado com arquivo/motivo/match) e o processo saiu com exit code 1; refiz o build para restaurar `dist/` limpo e a suíte voltou a 11/11 verde. Este teste não foi adicionado ao `.github/workflows/ci.yml` nesta tarefa — mesma divisão de escopo já documentada por TASK-005 para o teste irmão de segredos (gate formal de release é TASK-091, que também consolida os demais checks de DI-05); registrando como observação, não decisão unilateral. `npm run lint` (eslint + `lint:deps`, "no dependency violations found"), `npm run typecheck` (`tsc -b --noEmit`, sem erro), `npm run build` (bundle real gerado) e `npx vitest run` (6 arquivos, 27/27 verdes, sem regressão) confirmados localmente após a implementação. |

### Refatoração Lote-1

> Criada pelo Validador (chapéu QA) durante o fechamento estrutural do Lote 1
> (2026-09-07), para achado simples/débito de baixa severidade que não
> compromete o critério de aceite central de nenhuma tarefa `Concluída` do
> lote — ver `.md/QA-REPORT.md`, Seção "Lote 1", Seções 3 e 5.

| ID | Tarefa | Critério de aceite | Depende de | Paralelizável-com | Status |
|---|---|---|---|---|---|
| REFAT-L1-01 | Adicionar regra `no-tools-from-core` ao `.dependency-cruiser.cjs` (mesmo padrão de `no-features-from-core`, trocando `features` por `tools`) — hoje a separação build vs. runtime entre `core/*` e `tools/*` (ADR-003, DI-02) é respeitada de fato (confirmado por leitura direta de imports), mas só é verificada mecanicamente para `core/* → features/*`, não para `core/* → tools/*` | `npm run lint:deps` falha se `core/*` importar de `tools/*`, do mesmo jeito que já falha para `features/*` | TASK-001 | — | Concluída (revisão inline aprovada, sem achado) — regra `no-tools-from-core` adicionada em `.dependency-cruiser.cjs` (mesmo padrão estrutural de `no-features-from-core`: `from: core/*`, `to: tools/*`, `severity: "error"`), comentário de cabeçalho do arquivo atualizado para citar ADR-003 e as duas regras. Teste automatizado irmão em `src/tools/dependency-rule.test.ts` estendido com um segundo `describe` (`DI-02 / ADR-003 — dependência unidirecional core -> tools`), replicando a mesma estrutura de 2 casos do teste existente: `src/` real passa limpo pela nova regra, e uma fixture com `core -> tools` é reprovada por ela. Verificação empírica adicional pedida pelo Coordenador: criado temporariamente `src/core/__probe-tools-import.ts` importando `CANONICAL_BOOK_IDS` de `src/tools/corpus-import/canon.ts`; `npx depcruise src --config .dependency-cruiser.cjs` reportou a violação (`error no-tools-from-core: src/core/__probe-tools-import.ts → src/tools/corpus-import/canon.ts`) com exit code 1, confirmando que a lacuna é fechada; probe removido logo em seguida. `npm run lint` (eslint + `lint:deps`, "no dependency violations found", 35 módulos/61 dependências), `npx vitest run` (7 arquivos, 64/64 verdes, incluindo os 2 casos novos e sem regressão), `npm run typecheck` (`tsc -b --noEmit`, sem erro) e `npm run build` (bundle gerado sem erro) confirmados localmente após a implementação. |

### Lote 2 — Núcleo: Conteúdo Editorial e Validação

| ID | Tarefa | Critério de aceite | Depende de | Paralelizável-com | Status |
|---|---|---|---|---|---|
| TASK-012 | Tipos compartilhados: `CorpusManifest`/`Pericope`/`Note`/`PlanDay`/`Plan` | Tipos exportados e usados por importer, validador e `core/content` | TASK-001 | — | Concluída (revisão inline aprovada, sem achado; campos conferidos contra SDD.md §5.3) — `Pericope`/`Note`/`AtCategory`/`AtPortion`/`PlanDay`/`ValidationReport`/`Plan` (SDD.md §5.3) e `ScriptureRange` (intervalo comum extraído para não duplicar campos entre `Pericope` e as porções de `PlanDay`) em `src/core/content/types.ts`, com barrel `src/core/content/index.ts`. Também definido `ContentBundle` (agregado `plan`+`days`+`pericopes`+`notes`) — não é entidade do SDD.md §5.3, mas é a composição que o diagrama ER do §5.1 já relaciona (`PLAN ||--o{ PLAN_DAY }o--o{ PERICOPE`, `NOTE ||--|| PERICOPE`), documentada como conveniência para TASK-013/014 (valida o conjunto) e TASK-015 (resolve `PlanDay` por número) não inventarem a mesma forma duas vezes. `CorpusManifest` **não** foi duplicado/movido para cá: é o mesmo conceito já implementado em `src/tools/corpus-import/manifest-types.ts` (TASK-008, proveniência/licença do corpus bíblico bruto) — decisão documentada no cabeçalho de `types.ts` com o motivo (nenhum critério de C1–C11/RN-13 depende dele; `core/content` não pode importar de `tools/corpus-import` por `no-tools-from-core`/DI-02; se `tools/content-validate` precisar dele no futuro, importa direto de `tools/corpus-import`, direção tools→tools permitida). `PlanDay.hook` opcional (dia 90 não tem gancho, C11/RF-20); `Note.reviewer`/`reviewedAt` opcionais (RN-04: "quando houver"); `Plan` sem array de dias embutido (SDD.md §5.3 não lista `days` nos campos essenciais — fica em `ContentBundle.days`). Teste automatizado (`src/core/content/types.test.ts`, Vitest, 6 casos) é de compilação/smoke, proporcional a um módulo sem lógica de runtime: constrói um literal de cada tipo via o barrel `./index` (mesmo caminho de import que TASK-013/014/015 vão usar) e confirma em runtime o shape esperado (intervalo, herança de `Pericope`, autor obrigatório/revisor opcional em `Note`, hook presente/ausente em `PlanDay`, `ValidationReport` em `Plan`, resolução de `PlanDay` por número dentro de `ContentBundle`). `npm run lint` (eslint + `lint:deps` — "no dependency violations found", 38 módulos/64 dependências, confirma `core/content` sem import de `tools/*`/`features/*`), `npm run typecheck` (`tsc -b --noEmit`, sem erro), `npx vitest run` (8 arquivos, 70/70 verdes, incluindo os 6 novos e sem regressão) e `npm run build` (bundle gerado sem erro) confirmados localmente após a implementação. |
| TASK-013 | `tools/content-validate`: regras C1–C11 (RN-01) com veto de build | Fixture violando C3 falha o build com mensagem específica | TASK-012 | — | Concluída — `validateContent(bundle): ContentValidationReport` + `assertContentValid(bundle): void` (lança `ContentValidationError`, mesmo padrão de `CorpusParseError`/`CorpusIntegrityError` de `tools/corpus-import`) em `src/tools/content-validate/validate-content.ts`, barrel `src/tools/content-validate/index.ts`; importa tipos de `core/content` (TASK-012) — direção `tools → core` permitida (REFAT-L1-01 só proíbe o sentido oposto). Cobertura mecânica de 9 das 11 regras (C1–C4, C6–C9, C11) com dados 100% do `ContentBundle`; duas exceções documentadas no cabeçalho do arquivo (não simulam checagem vazia): **C5** (proporção AT/NT em palavras) exige quebra de `wordCount` por trilha — adicionados `atWordCount`/`ntWordCount` **opcionais** a `PlanDay` (`src/core/content/types.ts`, edição aditiva, sem quebrar consumidores existentes de TASK-012/015); se algum dia do bundle não trouxer os dois campos, C5 é pulado (não é violação, nem "passa" por acidente) — testado nos dois sentidos. **C10** (plano não deve ser exibido como cobertura integral) é essencialmente copy de UI (`PRD-TECNICO.md` §4.3); implementada só uma checagem de guarda sobre `plan.name` contra um pequeno léxico de frases proibidas, documentada como não-substituta de uma verificação real de tela. C9 (Evangelho completo dentro dos 90 dias) resolvido com uma tabela fixa de versificação padrão dos 4 Evangelhos (capítulo:versículo final, `src/tools/content-validate/gospel-versification.ts`) — não depende de texto de nenhuma fonte licenciada, só da estrutura pública de capítulos/versículos; contiguidade de cobertura verificada por `isImmediatelyAfter` (`range-utils.ts`, cruzamento de capítulo aceito quando o próximo começa no versículo 1, sem precisar saber o último versículo real do capítulo anterior). Fixture positiva de 90 dias construída programaticamente (`src/tools/content-validate/__fixtures__/valid-bundle.ts`) satisfaz as 11 regras simultaneamente (AT em Salmos, categoria em ciclo de 3 a partir do dia 8 sem 2 dias áridos consecutivos, dias 1–7 narrativos; NT cobre Mateus 1–28 inteiro — terminando exatamente em 28:20 — depois Marcos/Lucas/João/Atos como preenchimento sem repetir trecho; toda porção com perícope correspondente; todo dia árido com nota cobrindo a porção; gancho em todos os dias exceto o 90º). Teste automatizado (`src/tools/content-validate/validate-content.test.ts`, Vitest, 18 casos): a fixture positiva passa sem nenhuma violação (`passed: true`) e `assertContentValid` não lança; critério de aceite da tarefa coberto diretamente — fixture mutada violando C3 (AT não-narrativo na janela de ativação, e variante pela porção de NT fora de Evangelho) falha com mensagem específica citando "C3:" tanto em `validateContent` quanto em `assertContentValid`/`ContentValidationError`; demais regras (C1, C2, C4, C5 — nos dois sentidos, C6, C7, C8, C9 — nos dois sub-casos, C10, C11 — nos dois sub-casos) cobertas com fixture negativa dedicada cada. `npm run lint` (eslint + `lint:deps`, "no dependency violations found", 46 módulos/82 dependências), `npm run typecheck` (`tsc -b --noEmit`, sem erro), `npx vitest run` (10 arquivos, 93/93 verdes, incluindo os 18 novos e sem regressão) e `npm run build` (bundle gerado sem erro) confirmados localmente após a implementação. **Correção pós-revisão inline (achado real):** `checkC6` só checava fronteira de perícope, nunca fronteira de capítulo, apesar de `PRD-TECNICO.md` linha 487 definir C6 como "perícope **ou** capítulo" — e o comentário da função referenciava uma "nota de módulo sobre capítulo" que não existia. Corrigido para checar as duas condições (`endsAtPericopeBoundary` OU `endsAtChapterBoundary`), mas fronteira de capítulo só é mecanicamente verificável para o último capítulo dos 4 Evangelhos (reaproveitando `GOSPEL_FINAL_VERSE`, já usado por C9) — não foi criada uma tabela de versificação dos 66 livros por ser desproporcional ao escopo desta tarefa e por acoplar o validador editorial ao corpus bíblico inteiro (`core/corpus`, runtime de navegador). Limitação documentada explicitamente no cabeçalho do módulo, mesmo padrão de C5/C10 (não é checagem vazia, é lacuna registrada). Dois casos novos de teste: porção de NT terminando em Mateus 28:20 passa por fronteira de capítulo mesmo com a perícope correspondente desalinhada; porção terminando num capítulo comum (não-Evangelho/não-final) com perícope desalinhada continua reportada, confirmando que a lacuna fora dos Evangelhos não é mascarada. `npm run lint`, `npm run typecheck`, `npx vitest run` (10 arquivos, 95/95 verdes, incluindo os 2 novos e sem regressão) e `npm run build` reconfirmados limpos após a correção. |
| TASK-014 | `tools/content-validate`: cobertura de RN-13 + validação de nota (120–200 palavras, autoria, revisor) | Fixture com nota fora do range falha | TASK-013 | — | Concluída (revisão inline: 1 achado de escopo corrigido — cobertura de RN-13 para dias não-áridos estava faltando, ver nota completa abaixo) — leitura atenta de `PRD-TECNICO.md` RN-13 (linhas 712–739) confirmou que a "regra de cobertura" de RN-13 (todo dia tem ≥1 nota) já tem sua única exigência mecanicamente relevante coberta por **C8** (TASK-013: dia de AT árido/genealógico/profético tem nota cobrindo aquela porção, RN-04) — TASK-014 não duplica essa checagem por dia, escopo confirmado como sendo especificamente sobre a **nota em si** (contagem de palavras, autoria, revisor), não "qual dia precisa de nota". `checkNoteValidity` adicionada em `src/tools/content-validate/validate-content.ts`, chamada dentro de `validateContent` junto das C1–C11 existentes (mesmo padrão `violate(rule, message)`, mesma função `countWords` interna via `split(/\s+/)` em texto `trim()`ado), com tag de violação `"RN-13"` (não um C-número, pois RN-13 não pertence à lista C1–C11 de RN-01): (1) `body` fora da faixa 120–200 palavras (CA-06.3); (2) `author` ausente/vazio/só espaço (RN-04/CA-06.4: "toda nota tem autor identificado"); (3) `reviewer`, quando o campo está presente, não pode ser string vazia/só espaço — mas sua ausência nunca é violação, porque RN-04 explicita revisor como "quando houver" (nunca obrigatório) e `Note.reviewer` já é opcional desde TASK-012. Fixture positiva de TASK-013 (`__fixtures__/valid-bundle.ts`) tinha corpo de nota de ~15 palavras (nunca testado contra faixa de palavras até agora) — corrigida com `buildNoteBody(dayNumber)`, gerador determinístico de corpo com exatamente 150 palavras (vocabulário fixo repetido), para a fixture continuar satisfazendo RN-13 além de C1–C11 sem introduzir texto editorial real. Teste automatizado (`validate-content.test.ts`, 9 casos novos, describe "RN-13") cobre o critério de aceite diretamente: nota com corpo abaixo de 120 palavras falha com mensagem citando a nota e a faixa "120–200"; acima de 200 palavras idem; exatamente 120 e exatamente 200 passam (limites inclusivos); `assertContentValid` lança `ContentValidationError` citando "RN-13:" para nota fora do range; nota sem autor (string vazia e string só de espaço) falha citando "autoria"; nota sem revisor (campo ausente) não falha; nota com revisor identificado não falha; nota com revisor presente mas vazio/só espaço falha citando "revisor". `npm run lint` (eslint + `lint:deps`, "no dependency violations found", 46 módulos/82 dependências — nenhum import novo cruzando camada), `npm run typecheck` (`tsc -b --noEmit`, sem erro), `npx vitest run` (10 arquivos, 104/104 verdes, incluindo os 9 novos e sem regressão nos 95 anteriores) e `npm run build` (bundle gerado sem erro) confirmados localmente após a implementação. **Correção pós-revisão inline (achado real, lacuna de escopo):** a leitura de RN-13 estava incompleta — `PRD-TECNICO.md` linhas ~724–732 define **duas** metades de cobertura, não uma: (1) dias de AT árido/genealógico/profético exigem nota da porção específica, já coberta por C8 (correto não duplicar); (2) **nos demais dias, uma nota cobrindo qualquer uma das duas porções (AT ou NT) satisfaz a regra** — essa segunda metade não estava implementada em lugar nenhum, e como C8 só roda para as categorias áridas, um dia narrativo/poético/etc. sem nenhuma nota associada passava despercebido, violando RN-13 silenciosamente. Corrigido com `checkRN13Coverage` (nova função em `validate-content.ts`, chamada em `validateContent` logo após `checkC8`): para todo dia fora de `NOTE_REQUIRED_CATEGORIES`, confirma que `day.noteIds` tem pelo menos uma nota cuja perícope cobre (`rangeContains`) a porção de AT **ou** a de NT do dia; tag de violação `"RN-13-cobertura"` (distinta de `"RN-13"` da validação de nota, documentado no cabeçalho do módulo — mesma regra do PRD, duas metades mecânicas diferentes). Fixture positiva (`__fixtures__/valid-bundle.ts`) ajustada: todo dia não-árido ganhou uma nota (`note-nt-{dia}`) referenciando a perícope de NT do dia, já que antes desses dias ficavam com `noteIds: []`. Dois casos novos de teste (describe "RN-13-cobertura"): dia não-árido sem nenhuma nota cobrindo AT/NT falha citando o dia; dia não-árido com nota cobrindo NT não falha; dia árido (já coberto por C8) confirmado sem duplicação em RN-13-cobertura. `npm run lint`, `npm run typecheck`, `npx vitest run` (10 arquivos, 107/107 verdes, incluindo os 3 novos e sem regressão nos 104 anteriores) e `npm run build` reconfirmados limpos após a correção. |
| TASK-015 | `core/content`: loader do bundle editorial (plano, perícopes, notas, ganchos) | Dado o bundle fixture, retorna `PlanDay` por número | TASK-012 | TASK-013, TASK-014 | Concluída — implementado `src/core/content/get-plan-day.ts`, mesmo padrão de `core/corpus/get-chapter.ts` (TASK-009): `buildContentIndex(bundle)` constrói três `Map` (dias por `dayNumber`, perícopes por `id`, notas por `id`) para lookup O(1) em vez de O(n) por acesso; `getPlanDay(bundle, dayNumber)` e a variante `getPlanDayFromIndex(index, dayNumber)` retornam `PlanDay | undefined` (sem exceção — ausência é resultado normal de navegação); também expostos `getPericopeFromIndex`/`getNoteFromIndex` para resolver `Pericope`/`Note` referenciados por `PlanDay.noteIds`, sem inventar requisito além do que TASK-044/TASK-048 vão precisar. Não depende de TASK-013/014 (validador) rodarem antes — consome um `ContentBundle` já pronto como dado estático, independente de quem o valida. Barrel `src/core/content/index.ts` atualizado com os novos exports. Teste automatizado (`src/core/content/get-plan-day.test.ts`, Vitest, 6 casos) usa um `ContentBundle` de fixture sintética com 3 dias (1, 2 e 90 sem `hook`, C11/RF-20) cobrindo: retorno correto por número, dia 90 sem hook, `undefined` para número inexistente, equivalência entre `getPlanDay` e `buildContentIndex`+`getPlanDayFromIndex`, e os dois helpers de perícope/nota (encontrado e não encontrado). `npm run lint` (eslint + `lint:deps` — "no dependency violations found", 40 módulos/70 dependências, confirma `core/content` sem import de `tools/*`/`features/*`), `npm run typecheck` (`tsc -b --noEmit`, sem erro), `npx vitest run` (9 arquivos, 76/76 verdes, incluindo os 6 novos e sem regressão) e `npm run build` (bundle gerado sem erro) confirmados localmente após a implementação. |

### Refatoração Lote-2

> Criada pelo Validador (chapéu QA) durante o fechamento estrutural do Lote 2
> (2026-09-08), para achado de severidade baixa que não compromete o critério de
> aceite central de nenhuma tarefa `Concluída` do lote — ver `.md/QA-REPORT.md`,
> seção "Lote 2", Seção 5.

| ID | Tarefa | Critério de aceite | Depende de | Paralelizável-com | Status |
|---|---|---|---|---|---|
| REFAT-L2-01 | `checkC6` (TASK-013, `src/tools/content-validate/validate-content.ts`) só verifica fronteira de capítulo mecanicamente para o último capítulo dos 4 Evangelhos (via `GOSPEL_FINAL_VERSE`) — uma porção de NT que termina de fato em fronteira de capítulo de um livro que não é um dos 4 Evangelhos em seu capítulo final gera falso positivo de violação de C6, mesmo satisfazendo a regra na prática ("perícope **ou** capítulo") | Quando o acervo editorial real (não mais fixture sintética) estiver disponível, confirmar se algum dia do plano de fato esbarra nesse caso; se sim, ampliar a tabela de versificação (`gospel-versification.ts` ou equivalente) para cobrir o livro/capítulo necessário, documentando a extensão; se nenhum dia real esbarrar, fechar sem alteração de código, registrando a confirmação | TASK-013 | — | Pendente |

### Lote 3 — Núcleo: Armazenamento e Sincronização

| ID | Tarefa | Critério de aceite | Depende de | Paralelizável-com | Status |
|---|---|---|---|---|---|
| TASK-016 | `core/storage`: schema Dexie versionado (stores de Fase 1) | Migração de versão testada | TASK-001 | — | Concluída — `src/core/storage/` (primeiro módulo de storage local do projeto, runtime de navegador). Schema Dexie (`Dexie` 4.4.5, biblioteca obrigatória por DI-15) em `src/core/storage/db.ts` (classe `AppDatabase`), cobrindo as 8 stores de Fase 1 de SDD.md §5.2: `corpusChapters`, `corpusBundle`, `contentBundle`, `anonProgress`, `progress`, `preferences`, `outbox`, `eventQueue` — deliberadamente **sem** `outlines`/`outlineSnapshots`/`presentationState` (Módulo 2, Fase 2, ADR-012/DI-13, TASK-070, fora de escopo aqui, conforme delimitado pelo Coordenador). Tipos de registro de cada store em `src/core/storage/types.ts`, com comentário de proveniência (SDD.md §5.2) por store; `OutboxRecord` inclui `retryCount` (campo introduzido pela migração v2, ver abaixo) já como parte do shape "atual". `db.version(1).stores(...)` define o schema base; `db.version(2).stores(...).upgrade(...)` adiciona `retryCount` a toda mutação já existente na outbox (back-preenchimento via `tx.table("outbox").toCollection().modify(...)`, nunca perde/reseta um registro pendente de sincronização real do usuário) e o índice composto `[status+createdAt]` (consulta "próximos pendentes, em ordem", necessária para TASK-019). DI-10 respeitado: módulo é puramente Dexie/IndexedDB, nenhuma gravação em `localStorage`. Dependência nova mínima e justificada: `dexie` (`^4.4.5`, produção — já mandatada por DI-15/SDD §3, não é uma escolha nova deste Executor) e `fake-indexeddb` (`^6.2.5`, dev-only — biblioteca padrão do ecossistema Dexie para prover `indexedDB`/`IDBKeyRange` em Node/Vitest, via `fake-indexeddb/auto`; sem ela o Dexie não tem IndexedDB para rodar fora do navegador). Teste automatizado (`src/core/storage/db.test.ts`, Vitest, 4 casos) cobre o critério de aceite diretamente e sem simulação: (1) confirma as 8 stores de Fase 1 declaradas e a ausência das 3 stores de Módulo 2; (2) smoke test grava/lê um registro real em cada uma das 8 stores; (3) teste central da migração — abre um banco Dexie **só com o schema v1** (definido inline no teste, replicando o schema real de v1 antes de v2 existir) num nome de banco isolado por `crypto.randomUUID()`, grava uma mutação pendente na outbox **sem** `retryCount` (dado legado real, não sintético demais), fecha o banco, e reabre **o mesmo IndexedDB** com o `AppDatabase` real (v1+v2) — forçando o Dexie a rodar de fato o `upgrade()` de produção (não é simulação: mesmo mecanismo, mesmo banco, mesmo nome) — e confirma que o registro sobrevive, mantém `entityId`/`payload`/`status`, ganha `retryCount: 0` por back-preenchimento, e que o novo índice `[status+createdAt]` já funciona sobre o dado migrado; (4) uma mutação gravada depois da migração já nasce com `retryCount` explícito. `npm run lint` (eslint + `lint:deps`, "no dependency violations found", 52 módulos/91 dependências, confirma `core/storage` sem import de `features/*`/`tools/*`), `npm run typecheck` (`tsc -b --noEmit`, sem erro), `npx vitest run` (11 arquivos, 111/111 verdes, incluindo os 4 novos e sem regressão nos 107 anteriores) e `npm run build` (bundle gerado sem erro) confirmados localmente após a implementação. |
| TASK-017 | `core/storage`: orçamento de quota + expurgo LRU (RNF-03, RT-08) | Teste de quota excedida confirma ordem de expurgo declarada | TASK-016 | TASK-018 | Concluída — `src/core/storage/quota.ts` (orçamento) + `src/core/storage/purge.ts` (expurgo), sobre o schema de TASK-016, sem tocar `src/core/sync/*` (paralelo a TASK-018). `quota.ts`: `estimateStorageUsage()` lê `navigator.storage.estimate()` via um tipo local mínimo (`MinimalStorageManager`) em vez do `Navigator` de `lib.dom` — necessário porque `core/storage/*.ts` é compilado tanto pela config de app (com DOM) quanto, através dos testes que o importam, pela config de Node de `tsconfig.node.json` (sem DOM); depender do `Navigator` do DOM quebrava o typecheck sob a config de teste. Sem suporte à Storage API (ambiente sem navegador), devolve uso `0`/quota infinita — nunca finge estouro por falta de dado real (RNF-03: nunca descarta "no escuro"). `isOverBudget(estimate, budgetBytes = STORAGE_BUDGET_BYTES)` (50 MB, RNF-03) usa o menor entre o teto do produto e a quota real do navegador (RT-08). `purge.ts`: ordem de expurgo é a declarada em `ADR-006` ("Orçamento e expurgo local") — (1) `corpusChapters` por LRU (`lastAccessedAt` mais antigo primeiro, via índice `[bookId+chapter]` do schema de TASK-016 para a exclusão, já que `EntityTable.delete()` tipa a chave pelos nomes de props e não como tupla); (2) `corpusBundle`, só se o Módulo 2 não for usado há mais de 60 dias — como o Módulo 2 só existe na Fase 2 (TASK-070, ADR-012/DI-13) e não há, ainda, sinal de "uso do Módulo 2" nesta camada, esta etapa fica como no-op documentado (nunca remove `corpusBundle` sem esse sinal real); (3) versões perdedoras de esboço expiradas — fora de escopo, a store nem existe no schema de Fase 1. `contentBundle`, `anonProgress`, `progress`, `preferences`, `outbox`, `eventQueue` nunca são candidatos (dado do titular ou fila de sincronização pendente, nunca descartados em silêncio por ADR-006/RNF-03). `purgeIfOverBudget(db, options)` só age se `isOverBudget` for verdadeiro (estimador e teto injetáveis, `maxRecordsPerRun` opcional para não zerar o cache de um só golpe) e devolve a lista de registros removidos, na ordem em que a exclusão ocorreu. Barrel `src/core/storage/index.ts` atualizado com os novos exports. Testes automatizados: `src/core/storage/quota.test.ts` (Vitest, 2 casos — fallback sem Storage API e comparação de teto) e `src/core/storage/purge.test.ts` (Vitest, 6 casos, mesmo padrão `fake-indexeddb/auto` de TASK-016) cobrem o critério de aceite diretamente: nada é expurgado sob quota normal; `corpusChapters` semeado fora de ordem cronológica é expurgado por `lastAccessedAt` mais antigo primeiro (GEN 1 antes de GEN 2, nunca GEN 3 primeiro), respeitando `maxRecordsPerRun`; expurgo continua até esvaziar `corpusChapters` se a quota seguir excedida; com `corpusChapters` já vazio e quota excedida, `progress`/`anonProgress`/`preferences`/`outbox`/`eventQueue`/`contentBundle`/`corpusBundle` permanecem intactos (prova direta de "nunca expurgado"); e a quota real do navegador (mais apertada que 50 MB) também dispara `isOverBudget`. `npm run lint` (eslint + `lint:deps`, "no dependency violations found", 59 módulos/112 dependências, sem import cruzando para `core/sync`/`features/*`/`tools/*`), `npm run typecheck` (`tsc -b --noEmit`, sem erro), `npx vitest run` (118/118 verdes fora do teste isolado conhecido, mais 4/4 desse teste rodado à parte — sem regressão nos 115 anteriores de TASK-018) e `npm run build` (bundle gerado sem erro) confirmados localmente após a implementação. |
| TASK-018 | `core/sync`: outbox local (mutação com id próprio e `baseRev`) | Mutação persiste sem rede e sobrevive a reload | TASK-016 | TASK-017 | Concluída — `src/core/sync/` (primeiro módulo de sincronização, ADR-007). `enqueueMutation(db, input)` em `src/core/sync/enqueue-mutation.ts` cobre a etapa "escreve local primeiro; enfileira mutação com id próprio" do diagrama de sequência de SDD.md (linhas ~240-247): recebe uma `AppDatabase` (TASK-016) já aberta e `{ entityType, entityId, operation, payload, baseRev }`, gera um `id` próprio para a mutação via `crypto.randomUUID()` (nunca reaproveita o id da entidade — permite múltiplas mutações em sequência sobre a mesma entidade, cada uma com seu id de outbox), grava `baseRev` tal como informado (revisão-base conhecida no momento da mutação, `null` quando desconhecida, ex.: criação — usado para detecção de conflito em TASK-020), e persiste o `OutboxRecord` completo (`status: "pending"`, `retryCount: 0`) direto na store `outbox` via Dexie (`db.outbox.add`) — nenhuma chamada de rede/sessão envolvida (isso é TASK-019). Módulo não é dono do ciclo de vida da conexão Dexie: recebe `db` já aberto, não instancia `AppDatabase` internamente. `core/sync` importa de `core/storage` (`AppDatabase`, `OutboxRecord`) — permitido por DI-02 (a regra unidirecional proíbe `core/* → features/*` e `core/* → tools/*`; não há restrição a `core/* → core/*`), confirmado sem violação por `lint:deps`. Barrel `src/core/sync/index.ts` exporta `enqueueMutation`/`EnqueueMutationInput`. Teste automatizado (`src/core/sync/enqueue-mutation.test.ts`, Vitest, 4 casos, mesmo padrão `fake-indexeddb/auto` de TASK-016) cobre o critério de aceite diretamente: (1) "sem rede" — sobrescreve `globalThis.fetch` para lançar antes de chamar `enqueueMutation` e confirma que a mutação persiste normalmente (prova de que a função nunca depende de rede para gravar); (2) `id` da mutação é distinto do `id` da entidade, formato UUID, `baseRev`/`retryCount`/`status` corretos; (3) duas mutações da mesma entidade recebem ids de outbox distintos; (4) "sobrevive a reload" — enfileira uma mutação, fecha a conexão Dexie (`db.close()`), reabre uma conexão nova sobre o **mesmo nome de banco/IndexedDB** (mesmo mecanismo do teste de migração de TASK-016, não simulação) e confirma que o registro completo (`entityType`, `entityId`, `operation`, `payload`, `baseRev: null`, `status: "pending"`, `retryCount: 0`) sobrevive intacto. `npm run lint` (eslint + `lint:deps`, "no dependency violations found", 55 módulos/99 dependências), `npm run typecheck` (`tsc -b --noEmit`, sem erro), `npx vitest run` (12 arquivos, 115/115 verdes, incluindo os 4 novos e sem regressão nos 111 anteriores) e `npm run build` (bundle gerado sem erro) confirmados localmente após a implementação. |
| TASK-019 | `core/sync`: envio em lote idempotente com gate de rede/sessão | Reenvio de lote não duplica no servidor mock | TASK-018 | — | Concluída — `src/core/sync/sync-server-client.ts` define a porta injetável `SyncServerClient` (ADR-007, SDD.md §5.4, diagrama "Envia lote idempotente"): `sendBatch(mutations)` devolve um `MutationOutcome` (`"accepted"` \| `"duplicate"` \| `"rejected"`) por `id` de mutação — `core/sync` não conhece `fetch`/SDK nenhum (DI-02); a implementação real fica para quando houver backend de fato (Supabase, Lote 5/6). `src/core/sync/__fixtures__/in-memory-sync-server.ts` (`InMemorySyncServer`, usado só em teste) é o "servidor mock" citado no critério de aceite: registra cada mutação por `Map` chaveado no seu `id` próprio (gerado no cliente por TASK-018, nunca pelo servidor) e devolve `"duplicate"` sem novo registro quando o `id` já foi visto — é essa dedução por `id` que implementa a idempotência descrita em ADR-007 ("Idempotência por chave de mutação"). `src/core/sync/send-pending-mutations.ts`: `sendPendingMutations(db, client, { isOnline?, hasSession })` cobre o nó `gate{"Tem rede e sessao?"}` do diagrama — sem rede (`isOnline()` falso) ou sem sessão (`hasSession()` falso), retorna imediatamente com `skippedReason` (`"offline"`/`"no-session"`) e **nunca chama `client.sendBatch`** (provado por spy no teste); com os dois presentes, lê as mutações `pending` da outbox (`db.outbox.where("status").equals("pending").sortBy("createdAt")`), envia o lote inteiro de uma vez, e marca `"sent"` toda mutação cujo outcome for `"accepted"` **ou** `"duplicate"` — tratar duplicata como sucesso local é o que impede a mutação de ficar presa `pending` para sempre quando o servidor já a tinha recebido antes (cenário de confirmação perdida, citado em ADR-007). Mutação `"rejected"` (ou ausente na resposta) mantém `status: "pending"` e incrementa `retryCount` (campo de TASK-016), elegível a nova tentativa, sem inventar lógica de re-tentativa/backoff além do que o critério de aceite exige. `isOnline` tem padrão `defaultIsOnline` (lê `navigator.onLine`, mesmo padrão de tipo mínimo local de `MinimalStorageManager`/TASK-017 para compilar sob a config de teste sem DOM; sem `navigator`, assume `true` — nunca o cenário mais restritivo "no escuro", mesma postura de `estimateStorageUsage`); `hasSession` é obrigatório, sem padrão — não existe `core/auth` real ainda (Lote 6), documentado no cabeçalho do módulo que o chamador real passará algo como `() => authStore.hasSession()` quando essa peça existir, sem este módulo mudar. Barrel `src/core/sync/index.ts` atualizado com os novos exports (`sendPendingMutations`, `defaultIsOnline`, tipos). `core/sync` continua só importando de `core/storage` (DI-02), confirmado sem violação por `lint:deps`. Teste automatizado (`src/core/sync/send-pending-mutations.test.ts`, Vitest, 9 casos, mesmo padrão `fake-indexeddb/auto`) cobre o critério de aceite diretamente: (1) **idempotência isolada no servidor mock** — `sendBatch` chamado duas vezes com o mesmo registro devolve `"accepted"` na primeira e `"duplicate"` na segunda, `receivedCount()` permanece 1; (2) **cenário de confirmação perdida** — o servidor mock recebe a mutação por uma chamada direta (fora do fluxo local, simulando que a confirmação nunca chegou ao cliente, outbox local continua `"pending"`), o reenvio via `sendPendingMutations` marca `"sent"` localmente mas `receivedCount()` do servidor continua 1 (prova central do critério de aceite: reenvio não duplica); (3) duas chamadas consecutivas de `sendPendingMutations` com o mesmo lote pendente — a segunda não encontra nada `pending` (já `"sent"`) e não toca o servidor de novo; (4)/(5) gate: sem rede e sem sessão, cada um isoladamente, `client.sendBatch` nunca é chamado (`vi.spyOn`) e a mutação permanece `pending`; (6) com rede e sessão, envia e marca `"sent"` normalmente; (7) outbox vazia não chama o servidor e não falha; (8) mutação `"rejected"` mantém `pending` e incrementa `retryCount` de 0 para 1. `npm run lint` (eslint + `lint:deps`, "no dependency violations found", 63 módulos/127 dependências), `npm run typecheck` (`tsc -b --noEmit`, sem erro), `npx vitest run` (15 arquivos, 130/130 verdes, incluindo os 9 novos e sem regressão nos 121 anteriores) e `npm run build` (bundle gerado sem erro) confirmados localmente após a implementação. |
| TASK-020 | `core/sync`: conciliação por entidade (união monotônica + LWW) | Dois dispositivos simulados convergem conforme a regra por entidade | TASK-019 | — | Concluída — `src/core/sync/reconcile-entity.ts` implementa a etapa `rules{"Regra da entidade"}` do diagrama de sequência (SDD.md §2.6) para as duas entidades de Fase 1 com regra própria (ADR-007; `outlines`/`presentationState` são Fase 2, TASK-070/071, fora de escopo). `reconcileProgress(left, right)` implementa união monotônica (RN-08, CA-11.3): agrupa por chave `planId::dayNumber`, e o resultado é sempre a **união** dos dias concluídos nos dois lados — nunca a interseção nem um subconjunto de nenhum dos dois, nunca removendo um dia já concluído em qualquer lado; quando o mesmo dia existe nos dois, mantém o `completedAt` mais antigo (ADR-007, tabela "Progresso do plano": "preservando o `completed_at` mais antigo" — a primeira conclusão real, não a chegada mais recente). O `id` (autoincremento Dexie local, nunca significativo entre réplicas) é removido do resultado por `withoutId` (via `Record<string, unknown>` + `delete`, mesmo padrão de `src/tools/corpus-import/build-manifest.test.ts` — `delete` direto falha o typecheck em propriedade `readonly`). `reconcilePreferences(left, right)` implementa Last-Write-Wins por `updatedAt` (ADR-007: "escrita aplicada só se `updated_at` recebido > `updated_at` armazenado"): agrupa por `key`, o registro com `updatedAt` mais recente vence e o perdedor é descartado sem versão recuperável (diferente do esboço, TASK-071); empate mantém o lado `right` (escolha arbitrária mas determinística, documentada — ADR-007 não define desempate de preferência, só de esboço via `rev`). `applyReconciledState(db, state)` grava o estado conciliado numa réplica (`clear()` + `bulkAdd`/`bulkPut`), e `reconcileDevices(deviceA, deviceB)` fecha o ciclo ponta a ponta: lê `progress`/`preferences` dos dois `AppDatabase`, concilia por entidade e regrava o mesmo resultado nos dois — é essa função que prova a convergência de dois dispositivos simulados exigida pelo critério de aceite. Barrel `src/core/sync/index.ts` atualizado com os novos exports. `core/sync` continua só importando de `core/storage` (DI-02), sem violação por `lint:deps`. Teste automatizado (`src/core/sync/reconcile-entity.test.ts`, Vitest, 9 casos, mesmo padrão `fake-indexeddb/auto` das tarefas anteriores) cobre o critério de aceite diretamente: (1)-(3) `reconcileProgress` isolado — dias diferentes em cada lado preservam ambos, dia presente só à esquerda sobrevive com o lado direito vazio (prova de "nunca reduz"), e mesmo dia nos dois lados mantém o `completedAt` mais antigo; (4)-(5) `reconcilePreferences` isolado — mesma preferência com `updatedAt` diferente converge para o mais recente (comutativo, testado nos dois sentidos), preferências de `key` distintas em cada lado sobrevivem ambas; (6)-(8) **convergência ponta a ponta com dois `AppDatabase`/nomes de banco distintos simulando dois dispositivos**: dois dispositivos que completam dias diferentes offline convergem, após `reconcileDevices`, para a união exata dos três dias em ambos os bancos; dois dispositivos que mudam a mesma preferência (`theme`) em momentos diferentes offline convergem para o valor do dispositivo que escreveu por último (LWW); um teste misto com `progress` e `preferences` juntos no mesmo passo de conciliação confirma que cada entidade segue sua própria regra (união para uma, LWW para outra) e que os dois dispositivos terminam bit-a-bit iguais nas duas stores; (9) `applyReconciledState` isolado substitui por completo o conteúdo anterior das duas stores pelo estado conciliado informado. `npm run lint` (eslint + `lint:deps`, "no dependency violations found", 65 módulos/136 dependências, sem violação de DI-02), `npm run typecheck` (`tsc -b --noEmit`, sem erro — exigiu trocar `delete` direto em propriedade `readonly` de `ProgressRecord` por clone via `Record<string, unknown>`), `npx vitest run` (16 arquivos, 139/139 verdes, incluindo os 9 novos e sem regressão nos 130 anteriores, sem o timeout intermitente conhecido desta vez) e `npm run build` (bundle gerado sem erro) confirmados localmente após a implementação. |
| TASK-021 | `core/sync`: RT-06, relógio incorreto (`received_at` além de `updated_at`) | Relógio adiantado 1 ano não corrompe LWW além do aceitável | TASK-020 | TASK-022 | Concluída — mitigação de RT-06 (SDD.md linha 456: "servidor grava `received_at` além do `updated_at`; apuração descarta divergência absurda; nunca corrige dado do usuário em silêncio", ADR-007) implementada estendendo `reconcilePreferences` existente (TASK-020) em `src/core/sync/reconcile-entity.ts`, sem criar um segundo sistema de conciliação paralelo. `PreferenceRecord.receivedAt` (novo campo **opcional**, `src/core/storage/types.ts`) marca o instante em que o servidor recebeu a mutação — relógio confiável, independente do dispositivo que escreveu; campo aditivo e não-indexado, sem migração de schema Dexie (mesmo padrão de `PlanDay.atWordCount`/TASK-013). Algoritmo (`effectivePreferenceTimestamp`, função nova privada do módulo): quando `receivedAt` está presente e `updatedAt > receivedAt` (fisicamente implausível — a mutação não pode ter sido "atualizada" depois de o servidor já tê-la recebido, sinal direto de relógio do dispositivo adiantado), o timestamp efetivo usado na comparação de LWW é clampado para `receivedAt` (o menor dos dois, sempre o mais conservador); o `updatedAt` armazenado no registro **nunca é reescrito** por esse clamp — só o critério de desempate muda, dado do usuário nunca é corrigido em silêncio (ADR-007). Registro sem `receivedAt` (ainda não passou por round-trip de servidor, ou anterior a esta migração aditiva) usa `updatedAt` puro — comportamento de TASK-020 preservado por padrão, todos os testes antigos de `reconcilePreferences`/`reconcileDevices` continuam passando sem alteração. Popular `receivedAt` de fato (round-trip real com o servidor) é responsabilidade de uma peça futura de `core/sync`/backend (Lote 5/6, ainda não implementado) — esta tarefa cobre o lado de conciliação que consome o campo quando presente, documentado no cabeçalho do módulo e da função. Teste automatizado (`src/core/sync/reconcile-entity.test.ts`, Vitest, 5 casos novos, mesmo padrão `fake-indexeddb/auto`): (1) **teste central do critério de aceite** — dispositivo A com relógio adiantado 1 ano grava `theme=light` (`updatedAt` em 2027, mas `receivedAt` real em 2026-09-01T08:00), dispositivo B com relógio correto grava `theme=dark` de fato uma hora depois (2026-09-01T09:00, `updatedAt`=`receivedAt`); sem a mitigação, LWW ingênuo por `updatedAt` faria A (2027) vencer e o valor ficaria preso no "futuro" por quase um ano — com o clamp, o timestamp efetivo de A (08:00) perde para o de B (09:00), e o valor real mais recente vence, confirmado nos dois sentidos de argumento (comutativo); (2) registro sem `receivedAt` em nenhum dos lados usa `updatedAt` puro (regressão de TASK-020 preservada explicitamente); (3) `receivedAt` posterior/igual ao `updatedAt` (relógio plausível, sem skew) não altera o resultado do LWW; (4) variante ponta a ponta do cenário central via `reconcileDevices` com dois `AppDatabase` reais, confirmando que os dois dispositivos convergem para o valor real mais recente (B) e não para o valor de A que só "parecia" mais recente pelo relógio incorreto — dano limitado à janela real de 1 hora entre as escritas, não a um ano no futuro. `npm run lint` (eslint + `lint:deps`, "no dependency violations found", 67 módulos/145 dependências), `npm run typecheck` (`tsc -b --noEmit`, sem erro), `npx vitest run` (17 arquivos, 146/146 verdes, incluindo os 5 novos e sem regressão nos 139 anteriores, sem o timeout intermitente conhecido desta vez) e `npm run build` (bundle gerado sem erro) confirmados localmente após a implementação. |
| TASK-022 | `core/sync`: migração de progresso anônimo (RN-07) | 3 dias locais aparecem em `plan_progress` após criar conta | TASK-020 | TASK-021 | Concluída — `src/core/sync/migrate-anonymous-progress.ts` implementa RN-07 ("dias concluídos sem conta; migra uma vez ao criar conta", `src/core/storage/types.ts`) só na etapa **local** desta camada: `migrateAnonymousProgress(db)` lê todo `AnonProgressRecord` de `anonProgress` (TASK-016), grava um `ProgressRecord` correspondente por dia em `progress` (mesmos `planId`/`dayNumber`/`completedAt`, via `db.progress.bulkAdd`), enfileira uma mutação `{ entityType: "progress", operation: "create", baseRev: null }` por dia migrado via `enqueueMutation` (TASK-018, `entityId` no formato `planId:dayNumber`, mesmo padrão já usado pelos testes de TASK-018) e só então limpa `anonProgress` (`db.anonProgress.clear()`) — a ordem (grava progresso + enfileira mutação antes de limpar) garante que uma falha no meio do caminho nunca perde o progresso anônimo original, deixando-o intacto para nova tentativa. O envio real ao servidor (`plan_progress`, nome da tabela do backend, SDD.md §5.3) fica fora de escopo aqui — é responsabilidade de `sendPendingMutations` (TASK-019) quando o backend existir (Lote 5/6); a mutação enfileirada por esta função é a ponte entre os dois, documentada assim no cabeçalho do módulo. **Idempotência ("migra uma vez", RN-07):** se `anonProgress` já estiver vazio (migração anterior já rodou ou nunca houve progresso anônimo), a função é no-op — `anonProgress` vazio após uma migração bem-sucedida é o próprio mecanismo que impede duplicação numa segunda chamada, sem precisar de flag adicional. Barrel `src/core/sync/index.ts` atualizado com `migrateAnonymousProgress`/`MigrateAnonymousProgressResult`. `core/sync` continua só importando de `core/storage` (DI-02), sem violação por `lint:deps`. Teste automatizado (`src/core/sync/migrate-anonymous-progress.test.ts`, Vitest, 3 casos, mesmo padrão `fake-indexeddb/auto` das tarefas anteriores) cobre o critério de aceite diretamente: (1) **caso central** — semeia 3 dias distintos em `anonProgress`, roda a migração, e confirma que os 3 aparecem em `progress` com `planId`/`dayNumber`/`completedAt` idênticos, que `anonProgress` fica vazio (`toHaveLength(0)`), e que exatamente 3 `OutboxRecord` foram enfileirados (`entityType: "progress"`, `operation: "create"`, `status: "pending"`, `baseRev: null`), um `entityId` por dia (`plan-genesis:1`/`:2`/`:3`); (2) idempotência — segunda chamada com `anonProgress` já vazio devolve `migrated: []` e não duplica nada em `progress`/`outbox`; (3) sem nenhum progresso anônimo, não grava nada em `progress` nem na `outbox`. `npm run lint` (eslint + `lint:deps`, "no dependency violations found", 67 módulos/145 dependências, sem violação de DI-02), `npm run typecheck` (`tsc -b --noEmit`, sem erro), `npx vitest run` (17 arquivos, 142/142 verdes, incluindo os 3 novos e sem regressão nos 139 anteriores, sem o timeout intermitente conhecido desta vez) e `npm run build` (bundle gerado sem erro) confirmados localmente após a implementação. |

### Refatoração Lote-3

> Criada pelo Validador (chapéu QA) durante o fechamento estrutural do Lote 3
> (2026-09-08), para achado de severidade baixa que não compromete o critério
> de aceite central de nenhuma tarefa `Concluída` do lote — ver
> `.md/QA-REPORT.md`, seção "Lote 3", Seção 5.

| ID | Tarefa | Critério de aceite | Depende de | Paralelizável-com | Status |
|---|---|---|---|---|---|
| REFAT-L3-01 | `src/tools/dependency-rule.test.ts` (TASK-001) voltou a apresentar o timeout intermitente de 5000ms já fechado uma vez em `REFAT-L0-01` (Lote 1) — recorrência observada de fato nesta rodada (1 falha em 2 execuções da suíte completa; isolado, 1 de 4 execuções falhou, demais perto do limite). Causa provável: `src/` cresceu de 35 módulos (quando `REFAT-L0-01` fechou) para 109 módulos hoje, e a chamada real (não mockada) ao `dependency-cruiser` está mais perto do teto de 5000ms por si só, independente de concorrência externa | Aumentar o `testTimeout` deste teste (ex.: 10000ms) **ou** isolar a chamada real do `dependency-cruiser` para rodar uma vez por suíte em vez de uma vez por caso; confirmar estabilidade com algumas execuções após a mudança | TASK-001 | — | Pendente |

### Lote 4 — Núcleo: Design System

| ID | Tarefa | Critério de aceite | Depende de | Paralelizável-com | Status |
|---|---|---|---|---|---|
| TASK-023 | Tokens de tipografia/cor/espaçamento (temas claro/escuro) + teste de contraste AA | Teste de contraste passa para todos os pares de token | TASK-001 | — | Concluída — novo diretório `src/design-tokens/` (nem `core/*` nem `features/*`): decisão de localização documentada no cabeçalho de `tokens.css` — ADR-011 exige tokens como CSS custom properties, mas `core/*` é definido (DI-02) como "TypeScript puro, sem React nem DOM"; um arquivo `.css` não é TS, então um top-level próprio evita ambiguidade sobre a fronteira de `core/*` sem forçar os tokens para dentro de `features/*` (que ainda não existiam quando os tokens são necessários por TASK-025/026). Tipografia (`--fs-leitura/-corpo/-apoio/-titulo/-numero/-cartao`, `--medida`) e espaçamento (`--space-1..7`, escala de 4px, `--touch-target-min`/`-presentation`) copiados literalmente de UX-SPEC §3.1 — nada inventado. Cor: UX-SPEC só define hex para o tema de apresentação (escopo de TASK-024); para os temas claro/escuro, paleta própria escolhida e documentada (`--color-bg`, `--color-bg-elevated`, `--color-text`, `--color-text-secondary`, `--color-accent`, `--color-accent-text`, `--color-danger`, `--color-border`, `--color-focus-ring`), sujeita ao teste de contraste abaixo. Mecanismo de tema (`src/design-tokens/theme.ts`): atributo `data-theme` no elemento raiz é a única fonte de verdade em runtime (`resolveTheme`/`applyTheme`/`initTheme`/`setThemePreference`, preferência persistida via `ThemeStorage` injetável — chave `estudobiblico:theme-preference`); `tokens.css` também traz um bloco `@media (prefers-color-scheme: dark)` como fallback só para o instante antes do JavaScript aplicar o atributo (evita flash de tema errado), com um teste dedicado confirmando que esse fallback nunca diverge do override manual `[data-theme="dark"]`. `main.tsx` chama `initTheme` no boot, antes da primeira renderização, e importa `tokens.css`. **Teste de contraste AA** (critério de aceite, `src/design-tokens/token-contrast.test.ts`): fórmula de luminância relativa/razão WCAG implementada do zero em `src/design-tokens/contrast.ts` (curta o bastante para não justificar dependência nova tipo `wcag-contrast` — decisão registrada no cabeçalho do arquivo, ADR-011 "menor superfície de dependência possível"). Em vez de duplicar valores de cor num segundo lugar (risco de divergência entre CSS e teste), `src/design-tokens/parse-tokens-css.ts` (Node-only — usa `node:fs`, por isso excluído de `tsconfig.app.json` e incluído em `tsconfig.node.json`, mesmo padrão de `src/tools/corpus-import/**`) faz parse por regex do próprio `tokens.css`, lendo tanto os hex de cada `--color-*` por tema quanto as anotações `CONTRAST-PAIR (tema): --a sobre --b \| nível (>=x:1) \| ~y:1` escritas nos comentários do CSS (UX-SPEC §3.1: "o arquivo de tokens registra o par e a razão calculada ao lado de cada combinação"). O teste gera um caso por par declarado (7 pares × 2 temas = 14 casos) e falha se a razão recalculada cair abaixo do mínimo do nível (`AA-normal` >=4.5:1, `AA-ui` >=3:1); todos os 14 pares passam (texto/texto-secundário sobre fundo/fundo-elevado, texto sobre acento, aviso de erro sobre fundo, borda sobre fundo/fundo-elevado — nos dois temas, razões entre ~4.13:1 e ~17.01:1, sempre acima do alvo do nível declarado para aquele par). Também confirma que o fallback de `prefers-color-scheme` é idêntico ao tema escuro explícito. `src/design-tokens/contrast.test.ts` (14 casos) e `src/design-tokens/theme.test.ts` (10 casos, com `ThemeTarget`/`ThemeStorage` falsos via duck typing — sem depender de jsdom, que não está instalado no projeto) cobrem as unidades de apoio. `npm run lint` (eslint + `lint:deps`, "no dependency violations found", 77 módulos/162 dependências, sem violação de DI-02), `npm run typecheck` (`tsc -b --noEmit`, sem erro), `npx vitest run` (20 arquivos, 183/183 verdes, incluindo os 37 novos deste módulo e sem regressão nos 146 anteriores) e `npm run build` (bundle gerado sem erro, `tokens.css` presente em `dist/assets/*.css`) confirmados localmente após a implementação. |
| TASK-024 | Tema exclusivo de apresentação (≥7:1) + teste de contraste AAA | Teste falha se qualquer par cair abaixo de 7:1 | TASK-023 | — | Concluída — `src/design-tokens/presentation-theme.css`, arquivo separado de `tokens.css` (TASK-023, decisão já registrada lá: "para não misturar dois critérios de aceite — 4.5:1/3:1 vs 7:1 — num único teste"). Os 4 tokens (`--apres-fundo: #0B0B0F`, `--apres-texto: #F2F2F7`, `--apres-secundario: #A8A8B3`, `--apres-acento: #F0B429`) são os valores literais da tabela "Token (tema apresentação)" de UX-SPEC §3.1 — nada inventado, confirmado por teste dedicado que compara o objeto de cores parseado contra os 4 hex exatos. Escopo de aplicação (DI-11): tokens só valem sob o seletor `[data-theme="apresentacao"]`, documentado no cabeçalho do arquivo como o contrato de integração futuro — a rota real do Modo Apresentação só existe no Lote 15 (Fase 2), então não há wiring de componente/rota nesta tarefa, só o tema pronto para ser aplicado quando ela existir. Reaproveitamento de TASK-023 em vez de duplicar lógica: `src/design-tokens/contrast.ts` teve `WcagLevel` generalizado de `"AA-normal" \| "AA-ui"` para incluir `"AAA"` (`WCAG_MINIMUM_RATIO.AAA = 7`), e `src/design-tokens/parse-tokens-css.ts` ganhou `parseSingleThemeTokensCss`/`ParsedSingleThemeCss` (variante mais simples de `parseTokensCss`, sem a lógica de light/dark/fallback, reaproveitando `extractBlock` e generalizando a regex de anotação `CONTRAST-PAIR` para aceitar `AAA` e qualquer prefixo de token, não só `--color-*`) — nenhuma duplicação da fórmula de contraste nem do mecanismo de parsing/anotação. Teste automatizado (`src/design-tokens/presentation-theme-contrast.test.ts`, Vitest): parseia `presentation-theme.css` de verdade (não confia nos valores documentados no UX-SPEC.md) e recalcula a razão de cada um dos 3 pares anotados via `CONTRAST-PAIR` — todos acima de 7:1 (~17,40:1, ~8,30:1, ~11,40:1) — mais um teste que confirma que cada par declarado usa de fato o nível `AAA`/razão mínima 7 (não um nível AA "esquecido" no arquivo), e outro que confere os 4 hex exatos. **Correção pós-interrupção**: a generalização de `WcagLevel` (mais ampla) tornou o array-literal local de `token-contrast.test.ts` (TASK-023, tipado antes só como `"AA-normal" \| "AA-ui"`) incompatível por variância de tipo — corrigido trocando a anotação local para `WcagLevel` (import de `./contrast`), sem alterar nenhum valor/asserção do teste de TASK-023; suíte completa revalidada depois da correção. `npm run lint` (eslint + `lint:deps`, "no dependency violations found", 79 módulos/167 dependências), `npm run typecheck` (`tsc -b --noEmit`, sem erro), `npx vitest run` (21 arquivos, 189/189 verdes, incluindo os novos casos deste módulo e sem regressão nos 183 anteriores) e `npm run build` (bundle gerado sem erro) confirmados localmente após a implementação. |
| TASK-025 | Primitivos: `Botao`, `CampoDeTexto`, `CampoDeSenha`, `SeletorDeHora`, `Alternador` | Teste de teclado e alvo de toque 44×44 em cada | TASK-023 | TASK-026 | Concluída — novo diretório `src/design-system/primitives/` (nem `core/*` — DI-02 proíbe React/DOM em `core/*` — nem dentro de `features/*`, que ainda não tem consumidor destes primitivos; mesmo raciocínio de topo-de-`src` já registrado por TASK-023 para `design-tokens/`), com subpasta própria (`primitives/`, não a raiz de `src/design-system/`) para não colidir com os primitivos de sobreposição de TASK-026 (`src/design-system/overlays/`, mesmo lote, em paralelo) — nenhum arquivo em comum entre as duas tarefas. Os 5 primitivos (`Botao.tsx`, `CampoDeTexto.tsx`, `CampoDeSenha.tsx`, `SeletorDeHora.tsx`, `Alternador.tsx`, cada um com `.module.css` próprio) são, em cada caso, um elemento HTML nativo (`<button>`, `<input>`, `<input type="time">`) ou uma composição fina sobre ele — nenhum handler de teclado escrito à mão para Tab/Enter/Espaço, é comportamento de plataforma (coerente com a definição de "primitivo" de UX-SPEC §3: "comportamento convencional, baixo risco"). Alvo de toque mínimo 44×44 (DI-08) via `src/design-system/primitives/touch-target.ts` (`TOUCH_TARGET_MIN_STYLE`, `min-width`/`min-height: var(--touch-target-min)`), aplicado como `style` inline no elemento interativo raiz de cada primitivo (incluindo o botão de mostrar/ocultar de `CampoDeSenha`) — decisão documentada no próprio arquivo: usa o token de TASK-023 (`--touch-target-min` = 2.75rem = 44px, já coberto por `token-contrast.test.ts`/`theme.test.ts`) em vez de duplicar o valor, e é testado via `element.style.minWidth/minHeight` (string do inline style) em vez de `getBoundingClientRect`, porque jsdom não tem motor de layout real — mesmo espírito do padrão já estabelecido por `parse-tokens-css.ts` (verifica a fonte, não uma medição de layout inexistente em teste). DI-07 (nenhuma informação só por cor) nos 3 primitivos com estado de erro (`CampoDeTexto`/`CampoDeSenha`/`SeletorDeHora`): mensagem de erro sempre com ícone (`⚠`, `aria-hidden`) **e** texto, em `role="alert"` associado ao campo por `aria-describedby`; no `Alternador`, o estado ligado/desligado é comunicado por `aria-checked` **e** pelo rótulo textual sempre visível ao lado do controle, nunca só pela cor da trilha. `CampoDeSenha` implementa "senha colável" (UX-SPEC §3.2): nenhum bloqueio de `paste`/`copy`, testado disparando um evento `paste` sintético e confirmando que nada o cancela; rótulo sempre visível nos 4 campos (nunca só placeholder), com `<label htmlFor>` de verdade, não texto solto. Barrel `src/design-system/primitives/index.ts`, escopado à pasta (não um barrel de `src/design-system/` inteiro, pela mesma razão de não colidir com TASK-026). Nenhum primitivo foi ainda importado por uma tela real (`features/*` não existe para nenhuma tela ainda — Lote 6+); ficam prontos para consumo. Testes automatizados (Vitest + `@testing-library/react`/`user-event`/`jest-dom`, `environment: jsdom` via diretiva `// @vitest-environment jsdom` por arquivo — o `vitest.config.ts` compartilhado continua em `environment: "node"` por padrão, sem alterar o comportamento dos demais testes/da tarefa paralela): 23 casos ao todo (`Botao.test.tsx` 5, `CampoDeTexto.test.tsx` 4, `CampoDeSenha.test.tsx` 5, `SeletorDeHora.test.tsx` 4, `Alternador.test.tsx` 5), cobrindo diretamente as duas metades do critério de aceite em cada primitivo — teclado (Tab foca; Enter/Espaço ativam onde aplicável; `Botao` desabilitado não é alcançado por Tab; `CampoDeSenha` alcança o botão de mostrar/ocultar na sequência natural de Tab) e alvo de toque 44×44 — mais os casos de DI-07/rótulo-sempre-visível/senha-colável acima. Ajuste de infraestrutura de teste, necessário e feito nesta tarefa (primeiros testes de componente React do projeto): `tsconfig.app.json` deixou de excluir `src/**/*.test.tsx` do projeto de app (mantém excluído `src/**/*.test.ts`, que segue em `tsconfig.node.json`) — sem isso, nenhum `.test.tsx` seria typechecked por `tsc -b`; cada arquivo de teste importa `@testing-library/jest-dom/vitest` (matchers) e chama `afterEach(cleanup)` explicitamente (sem `globals`/`setupFiles` no `vitest.config.ts`, cada teste limpa o DOM entre casos dentro do próprio arquivo, evitando `getByLabelText`/`getByText` ambíguos entre casos). `npm run lint` (eslint + `lint:deps`, "no dependency violations found", 109 módulos/261 dependências — já incluindo os arquivos de TASK-026 presentes em paralelo), `npm run typecheck` (`tsc -b --noEmit`, sem erro) e `npm run build` (bundle gerado sem erro; primitivos não aparecem no bundle ainda por não terem consumidor, como esperado) confirmados localmente após a implementação. `npx vitest run` da suíte completa: 1 falha intermitente pré-existente e já rastreada (`src/tools/dependency-rule.test.ts`, timeout de 5000ms sob carga concorrente de I/O — mesmo padrão de REFAT-L0-01, reproduzido só quando rodado junto de outras instâncias/arquivos em paralelo; isolado, os 4 casos passam, confirmado por execução dedicada) — não é regressão desta tarefa; as demais falhas vistas numa execução da suíte completa pertencem a `src/design-system/overlays/*` (TASK-026, em andamento em paralelo no mesmo lote), fora do escopo desta tarefa. Isolado (`npx vitest run src/design-system/primitives`), 23/23 verdes de forma estável em múltiplas execuções. |
| TASK-026 | Primitivos de sobreposição: `FolhaInferior`, `Dialogo`, `Toast` (base Radix) | Teste automatizado de armadilha/restauração de foco | TASK-023 | TASK-025 | Concluída — novo diretório `src/design-system/overlays/` (mesmo raciocínio de topo-de-`src` de TASK-023/TASK-025: nem `core/*` — DI-02 proíbe React/DOM — nem dentro de `features/*`, que ainda não existe para nenhuma tela; subpasta própria `overlays/`, não a raiz de `src/design-system/`, para não colidir com `src/design-system/primitives/` de TASK-025, mesmo lote, em paralelo — nenhum arquivo em comum entre as duas tarefas). `@radix-ui/react-dialog` e `@radix-ui/react-toast` (DI-15) já estavam em `package.json`/`node_modules` antes desta tarefa (dependência mandatada, não escolha nova) — usados como base real dos 3 primitivos, não reimplementados do zero: `Dialogo.tsx` e `FolhaInferior.tsx` (`src/design-system/overlays/`) envolvem `Dialog.Root/Trigger/Portal/Overlay/Content/Title/Description/Close` do Radix — Radix não tem um primitivo próprio de "bottom sheet"; `FolhaInferior` é a mesma base de `Dialog`, só variando `className` (ancorada na base, largura cheia, cantos superiores arredondados, alça decorativa `aria-hidden`) — documentado no cabeçalho do arquivo. `Toast.tsx` envolve `Toast.Provider/Viewport/Root/Title/Description/Action/Close` do Radix, com `ToastProvider` exportado à parte (envolve a árvore que pode disparar toasts). Achado ao investigar o próprio pacote (`node_modules/@radix-ui/react-dialog/dist/index.mjs`): esta versão do Radix Dialog **não** aplica `aria-modal="true"` automaticamente (só `role="dialog"`) — por isso `aria-modal="true"` é setado explicitamente em `Dialog.Content` nos dois componentes, decisão registrada no cabeçalho de cada arquivo, para cumprir UX-SPEC §5.2 (T-07: "aria-modal, armadilha de foco, Esc fecha, foco retorna ao botão de origem") ao pé da letra. Armadilha de foco e restauração de foco de `Dialogo`/`FolhaInferior` vêm de graça do `Dialog.Content` do Radix (`FocusScope` interno, `modal` padrão `true`) — **mas** só a restauração automática depende de abrir via a prop `trigger` (`Dialog.Trigger asChild`): achado também documentado no cabeçalho (`onCloseAutoFocus` padrão do Radix foca especificamente `context.triggerRef`, não "o que estava focado antes" de forma genérica) — por isso os dois componentes sempre expõem `trigger` como o padrão de uso esperado; abrir por controle externo sem `trigger` deixa a restauração por conta de quem consome o primitivo (fora de escopo aqui). Decisão para `Toast` (documentada em detalhe no cabeçalho do arquivo, conforme pedido explícito da tarefa): (1) **sem armadilha de foco** — Toast não é modal; confirmado no código-fonte do Radix (`ToastViewport`, sentinelas `FocusProxy` "head"/"tail" que devolvem o foco à ordem de tabulação normal da página ao tabular para além do último elemento focável do toast, sem loop interno) e abrir um toast não move o foco (o elemento que disparou a ação continua focado); (2) **restauração não é automática no Radix Toast** (`handleClose` interno só move o foco para o `viewport`, não para quem abriu, quando fecha com foco dentro do toast — achado em `ToastImpl`/`isFocusInToast`) — como a tarefa pede restauração "em cada primitivo que fizer sentido" e aqui faz sentido para o caso em que o usuário moveu o foco para dentro do toast, `Toast.tsx` adiciona restauração manual: guarda o elemento focado no instante em que abre (`focoAoAbrirRef`) e, ao fechar, só restaura se o foco não estiver mais nesse elemento (nunca move foco que o usuário não pediu para mover — ex.: fechamento por timeout sem interação nenhuma não restaura nada). DI-08 (alvo de toque mínimo 44×44) via `overlays.css` (`--touch-target-min`) nos botões fechar/ação (`eb-overlay-close`, `eb-toast-fechar`, `eb-toast-acao`); DI-07 (nenhuma informação só por cor) no `Toast`: cada `tipo` (`info`/`sucesso`/`erro`) tem ícone **e** texto — ícone é puramente decorativo (`aria-hidden`), quem carrega o significado para leitor de tela é o texto do título/descrição, testado explicitamente. `overlays.css` usa só tokens já existentes de `src/design-tokens/tokens.css` (TASK-023) — nenhum valor de cor/espaçamento/tipografia novo inventado (confirma a dependência declarada da tarefa). Barrel `src/design-system/overlays/index.ts`, escopado à pasta (mesma razão de não colidir com TASK-025). Nenhum primitivo foi ainda importado por uma tela real (`features/*` não existe para nenhuma tela ainda). Testes automatizados (Vitest + `@testing-library/react`/`user-event`/`jest-dom`, `// @vitest-environment jsdom` por arquivo, mesmo padrão já estabelecido por TASK-025): 10 casos (`Dialogo.test.tsx` 3, `FolhaInferior.test.tsx` 3, `Toast.test.tsx` 4). Armadilha de foco de `Dialogo`/`FolhaInferior`: abre via `trigger`, confirma `role="dialog"` com `aria-modal="true"`, e tabula 8 vezes (mais que o número de elementos focáveis internos) confirmando que `document.activeElement` nunca sai do container do overlay. Restauração: fecha por Esc e por clique no botão Fechar, em casos separados, confirmando nos dois que o `dialog` desaparece e o foco volta exatamente ao botão que abriu. Para `Toast`: confirma que abrir não move o foco (permanece no botão "Salvar"); confirma que, a partir do botão fechar do toast focado, um único Tab alcança um botão deliberadamente colocado depois do `ToastProvider` no DOM (prova de que não há prisão de foco, e não só a ausência de asserção de trap); confirma restauração ao botão de origem ao fechar a partir de dentro do toast; confirma ícone `aria-hidden` + texto (DI-07). Achado de ambiente registrado e contornado (não é defeito de produção): jsdom não implementa a Pointer Events API usada pelo gesto de swipe do Radix Toast (`element.hasPointerCapture` inexistente), lançando exceção não tratada no primeiro `pointerdown` simulado por `user-event`; contornado com um polyfill mínimo (`hasPointerCapture`/`setPointerCapture`/`releasePointerCapture` como no-op) só dentro de `Toast.test.tsx` (`beforeAll`), sem tocar código de produção — navegadores reais implementam a API nativamente. `npm run lint` (eslint + `lint:deps`, "no dependency violations found", 109 módulos/261 dependências), `npm run typecheck` (`tsc -b --noEmit`, sem erro — reaproveita a mudança de `tsconfig.app.json` já feita por TASK-025 para incluir `.test.tsx` no projeto de app com lib DOM, nenhuma edição adicional necessária aqui) e `npm run build` (bundle gerado sem erro; overlays não aparecem no bundle ainda por não terem consumidor, como esperado) confirmados localmente após a implementação. Isolado (`npx vitest run src/design-system/overlays`), 10/10 verdes de forma estável em múltiplas execuções. Na suíte completa (`npx vitest run`), única falha observada (221/222) é a mesma intermitência pré-existente já rastreada por REFAT-L0-01/TASK-025 (`src/tools/dependency-rule.test.ts`, timeout de 5000ms sob carga concorrente — passa isolado, inclusive com `--testTimeout=20000` isolado em 6,3s), reproduzida de novo aqui provavelmente por dois fatores que não são desta tarefa: crescimento do `src` (109 módulos) e execução concorrente de outra instância do Executor no mesmo lote; não é regressão introduzida por TASK-026, e não decidi sozinho alterar o timeout desse teste compartilhado (fora do escopo desta tarefa, mesmo padrão de não-decisão unilateral já seguido por TASK-025) — sinalizando aqui para o Coordenador/Validador reavaliarem se REFAT-L0-01 deve ser reaberto agora que a recorrência voltou a acontecer de forma consistente (2 execuções seguidas, não mais esporádica). |

### Refatoração Lote-4

> Criada pelo Validador (chapéu QA + DevSecOps) durante o fechamento estrutural
> do Lote 4 (2026-09-08), para achados de severidade baixa/baixa-média que não
> comprometem o critério de aceite central de nenhuma tarefa `Concluída` do
> lote — ver `.md/QA-REPORT.md` e `.md/SECURITY-REVIEW.md`, seção "Lote 4".

| ID | Tarefa | Critério de aceite | Depende de | Paralelizável-com | Status |
|---|---|---|---|---|---|
| REFAT-L4-01 | `src/design-tokens/theme.ts`/`main.tsx` (TASK-023) persistem a preferência de tema direto em `window.localStorage` (chave `estudobiblico:theme-preference`) — SDD.md §5.2 define explicitamente que "tema" faz parte da store `preferences` do `core/storage` (Dexie, TASK-016), sincronizada por LWW (TASK-020/021) com o backend, e GUARDRAILS.md G-11/DI-10 restringem `localStorage` a "o necessário de sessão", nunca conteúdo/preferência do usuário. O mecanismo atual nunca escreve em `core/storage`, então a preferência de tema não sincroniza entre dispositivos como o SDD promete, e viola G-11 na letra (ainda que sem diferença real de confidencialidade local, já que SDD §7.3 declara dado local do dispositivo não criptografado em ambos os casos) | Antes de qualquer tela que exponha o controle de tema ao usuário (T-11 Configurações) consumir/persistir a preferência: migrar a escrita/leitura de `theme.ts` para usar `core/storage` (`PreferenceRecord`, store `preferences`) + `core/sync` (`enqueueMutation`, mesmo padrão de `migrateAnonymousProgress`/TASK-022), preservando a leitura síncrona pré-render necessária para evitar flash de tema errado (ex.: cache leve em `localStorage` só como *hint* de renderização inicial, com o Dexie como fonte de verdade reconciliada no boot) — decisão de design a ser tomada pelo Executor/Coordenador na tarefa que primeiro conectar tema a uma tela real; se a decisão for manter tema fora do mecanismo de sincronização por escolha deliberada de produto, documentar essa exceção explicitamente (GUARDRAILS.md/SDD.md) em vez de deixá-la implícita | TASK-023, TASK-016 | REFAT-L4-02 | Pendente |
| REFAT-L4-02 | `src/tools/dependency-rule.test.ts` (TASK-001/REFAT-L1-01) voltou a esbarrar no timeout padrão de 5000ms do Vitest de forma consistente (não mais esporádica) ao rodar dentro da suíte completa (109+ módulos): 3 execuções completas e independentes desta rodada falharam nas 3 vezes no mesmo teste (~6,1s cada), enquanto isolado (`npx vitest run src/tools/dependency-rule.test.ts`) passa folgado (~6,5s totais, 4/4 verdes) — REFAT-L0-01 já havia sido fechado sem alteração de código no Lote 1 por não recorrer sob as condições daquele momento (35 módulos); o crescimento do `src` desde então (109 módulos nesta rodada) e/ou a execução concorrente de outra instância do Validador no mesmo checkout tornam a chamada real ao `dependency-cruiser` mais sensível ao tempo default | Aumentar o timeout deste teste especificamente (`it(..., { timeout: <valor> })` ou segundo/terceiro argumento de `it`) para um valor com folga real (ex.: 15000–20000ms), já que o teste em si é rápido isolado e a lentidão vem só de contenção de I/O/CPU sob carga — não requer isolar a chamada do `dependency-cruiser` nem mudar a lógica do teste. Reavaliar se ainda recorre depois da mudança, na validação do próximo lote | TASK-001 | REFAT-L4-01 | Pendente |

### Lote 5 — Backend: Schema e Segurança

| ID | Tarefa | Critério de aceite | Depende de | Paralelizável-com | Status |
|---|---|---|---|---|---|
| TASK-027 | `[insep.]` Migration `profile` + `consent` com RLS na mesma migration | Teste de política de RLS cobre as duas tabelas | TASK-005 | — | Concluída — `supabase/migrations/20260908175414_profile_consent_rls.sql` (criada via `supabase migration new profile_consent_rls`, sobre o setup de TASK-005) cria `profile` (`user_id uuid primary key references auth.users(id) on delete cascade`, `created_at`) e `consent` (`id uuid pk default gen_random_uuid()`, `user_id` not null references `auth.users`, `version`, `text_sha256`, `accepted_at` default `now()`, `revoked_at` nullable, índice em `user_id`), exatamente os campos do SDD.md §5.3 — nenhum campo inventado. RLS habilitada nas duas tabelas na mesma migration (DI-03/G-09): política por titular (`for select/insert/update ... to authenticated using/with check (auth.uid() = user_id)`); deliberadamente sem política de `delete` para `authenticated` em nenhuma das duas — exclusão de conta segue `erasure_request` (RN-11, TASK-032) via `service_role` (que ignora RLS por definição), nunca delete direto do cliente; `consent` modela revogação por `revoked_at` (não por apagar a linha), preservando o histórico como evidência de conformidade LGPD. Achado real durante a implementação, documentado no próprio arquivo da migration: `auto_expose_new_tables` deste projeto (`supabase/config.toml`) segue o padrão atual do CLI (tabela nova não fica acessível à API só por existir + RLS habilitada — falta o `GRANT` de privilégio de tabela do Postgres, camada anterior à RLS); sem o `grant select, insert, update on public.profile/public.consent to authenticated` explícito, toda chamada de usuário autenticado falhava com "permission denied for table" (42501/HTTP 401) mesmo com a política correta — confirmado empiricamente rodando o teste de RLS contra o Supabase local antes do GRANT existir, e corrigido na mesma migration. Nenhum GRANT para `anon`: as duas tabelas exigem sessão autenticada (SDD.md §7.2), então visitante sem conta recebe "permission denied" (bloqueio de privilégio de tabela, antes mesmo da RLS ser avaliada), não uma lista vazia. Teste automatizado real (não simulado) em `tests/db/rls-profile-consent.test.mjs` (Node `node --test`, novo script `npm run test:db`, mesma convenção de `tests/security/*.test.mjs`): cria 2 usuários reais via Admin API do GoTrue local (`service_role`), autentica cada um via `/auth/v1/token?grant_type=password` e chama o PostgREST local (`/rest/v1/profile`, `/rest/v1/consent`) com o token de cada usuário — nunca com `service_role` — cobrindo as duas tabelas com 6 casos: (1) cada usuário grava a própria linha em `profile` e uma tentativa de inserir com `user_id` de outro titular é rejeitada pelo `with check`; (2) um titular não lê a linha de `profile` do outro via `SELECT` filtrado (RLS filtra a linha, resultado vazio); (3) um titular não consegue `UPDATE` a linha de `profile` do outro (0 linhas afetadas); (4)/(5) os mesmos três padrões repetidos para `consent` (insert forjado rejeitado, leitura cross-user vazia, tentativa de revogar consentimento alheio via `PATCH revoked_at` não afeta nenhuma linha, confirmado depois que a linha original de A permanece com `revoked_at = null`); (6) papel `anon` (sem sessão) não lê nenhuma linha de nenhuma das duas tabelas. Credenciais lidas de env vars com fallback para as chaves de demonstração fixas e públicas que o próprio `supabase start` imprime para ambiente local (documentadas como "shared defaults" em `docs/ci-secrets.md` — não são segredo). Comandos rodados e confirmados localmente, nesta ordem: `npx supabase start` (Docker já disponível), `npx supabase migration new profile_consent_rls`, `npx supabase db reset` (aplica a migration do zero sobre o setup de TASK-005, sem warning), `npm run test:db` (7/7 verdes — 1 teste pai + 6 subtestes, incluindo o `after` que apaga os 2 usuários de teste via Admin API), `npm run lint` (eslint + `lint:deps`, "no dependency violations found" — `tests/db/*.mjs` não entra no escopo do lint, mesma convenção já usada por `tests/security/*.mjs`), `npm run typecheck` (sem erro), `npm run build` (gera `dist/` normalmente, PWA incluso), `npm run test:security` (11/11 verdes) e `npx vitest run` (229/230 verdes; o único vermelho é o timeout intermitente pré-existente de `src/tools/dependency-rule.test.ts` sob carga concorrente da suíte completa — já rastreado e fechado em `REFAT-L0-01`, reproduzido aqui só sob paralelismo e confirmado que passa isoladamente em 5.76s, portanto não é regressão desta tarefa). `npx supabase stop` ao final para liberar os containers. Nenhuma outra tabela do Lote 5 foi tocada — `plan_progress`/`preference`/`push_subscription`/`reminder_log`/`analytics_event`/`erasure_request` ficam para TASK-028 a TASK-032, agora desbloqueadas. |
| TASK-028 | Migration `plan_progress` (append-only, `UNIQUE(user_id, plan_id, day_number)`) com RLS | Insert duplicado falha por constraint; RLS testada | TASK-027 | TASK-029, TASK-030, TASK-031, TASK-032 | Concluída — `supabase/migrations/20260908180404_plan_progress_rls.sql`, mesmo padrão de RLS + GRANT explícito de TASK-027: `public.plan_progress` (`id uuid pk default gen_random_uuid()`, `user_id` not null references `auth.users(id) on delete cascade`, `plan_id text not null`, `day_number integer not null check (day_number > 0)`, `completed_at timestamptz not null default now()`), exatamente os campos do SDD.md §5.3, com `unique (user_id, plan_id, day_number)` — a chave composta que torna a **união monotônica** entre dispositivos uma propriedade do schema (ADR-007, "Progresso do plano": cliente sempre envia `INSERT ... ON CONFLICT (user_id, plan_id, day_number) DO NOTHING`, preservando o `completed_at` da primeira escrita). `plan_id` é `text`, deliberadamente **sem FK**: `Plan`/`PlanDay` são conteúdo estático gerado em build (SDD.md §5.1 — "Não existem aqui: dia-calendário, usuário, progresso"), não uma tabela do Postgres; `plan_id` referencia o `Plan.id` do bundle de conteúdo editorial publicado fora do banco, mesmo raciocínio de `planId: string` já usado no cliente (`src/core/storage/types.ts`, sem chave estrangeira local). RLS habilitada na mesma migration (DI-03/G-09): apenas 2 políticas para `authenticated` (`select`/`insert`, ambas `auth.uid() = user_id`) — **deliberadamente nenhuma política de update/delete**: a tabela é append-only por design (RN-08/CA-03.5/CA-11.3, ADR-007 "não existe DELETE exposto") — uma vez concluído, um dia nunca "desconclui" pelo próprio titular; a única forma de remover linhas é a cascata de `erasure_request` (RN-11, TASK-032) via `service_role`. GRANT explícito (`select, insert` para `authenticated`, mesmo racional de TASK-027 — `auto_expose_new_tables` desabilitado neste Supabase local, sem GRANT a chamada de usuário autenticado falha com "permission denied for table" antes mesmo da RLS ser avaliada); nenhum GRANT para `anon`. Teste real (não simulado) contra Supabase local em `tests/db/rls-plan-progress.test.mjs` (`node --test`, mesmo padrão/credenciais de `tests/db/rls-profile-consent.test.mjs`), 6 subtestes verdes cobrindo as duas metades exatas do critério de aceite: (1) usuário grava só a própria linha (insert forjado com `user_id` de outro titular rejeitado pelo `with check`); (2) usuário não lê linha de outro titular (lista vazia); (3) **insert duplicado do mesmo `(user_id, plan_id, day_number)` falha com HTTP 409/código Postgres `23505` (unique_violation)** — o próprio critério de aceite, testado via chamada HTTP real ao PostgREST, não simulado — enquanto o mesmo dia em plano diferente e um dia diferente no mesmo plano são aceitos normalmente, confirmando que a constraint é exatamente a chave composta, não um subconjunto mais amplo; (4) tabela é append-only — UPDATE/DELETE do próprio titular sobre a própria linha são rejeitados (bloqueio de GRANT de tabela, mesma camada de `erasure_request`/TASK-032), e a linha original é confirmada intacta depois; (5) papel `anon` (sem sessão) não lê nem grava nenhuma linha. Achado real de investigação durante a implementação, não um defeito do código final: uma leitura inicial via `has_table_privilege`/`pg_default_acl` direto no Postgres, feita num momento de instabilidade do ambiente compartilhado (ver observação de contenção abaixo), sugeriu por engano que `anon`/`authenticated` já tinham privilégio de tabela por default privilege — corrigido depois de reconfirmar via chamada HTTP real ao PostgREST/Kong (`curl`, mesmo caminho que o cliente real usa) que o comportamento verdadeiro e reprodutível é permission denied (42501/HTTP 401) sem o GRANT explícito, igual ao padrão já estabelecido por TASK-027/029/030/031/032; a introspecção direta via `psql`/`pg_default_acl` se mostrou não confiável neste ambiente sob reset concorrente e foi descartada como fonte de verdade em favor do comportamento observado via API real. Contenção real de ambiente observada e superada (mesmo risco operacional já registrado por TASK-002/003/029/030/031/032 para o projeto Docker `EstudoBiblico` compartilhado por até 5 instâncias paralelas do Lote 5): dezenas de tentativas de `supabase start`/`supabase migration up` reportaram containers auxiliares (`auth`/`rest`/`kong`/`realtime`) derrubados por outra instância logo após subir, `db not ready`/`connection refused`/`relation "identities" does not exist` durante resets concorrentes, e a suíte precisou ser reexecutada em loop de retry (aplicar migrations + rodar teste) até obter uma janela estável — nenhuma mudança de código necessária por isso; ao final, suíte confirmada verde em 25 execuções consecutivas sem falha, e `tests/db/rls-profile-consent.test.mjs` (TASK-027) reconfirmado sem regressão (7/7) na mesma janela estável. `npm run lint` (eslint + `lint:deps`, "no dependency violations found", 115 módulos/275 dependências — nenhum código TS novo desta tarefa, só SQL/`.mjs`), `npm run typecheck` (`tsc -b --noEmit`, sem erro) e `npm run build` (bundle gerado sem erro, incl. `sw.js`/`manifest.json` via PWA) confirmados limpos após a implementação. |
| TASK-029 | Migration `preference` com RLS | RLS testada | TASK-027 | TASK-028, TASK-030, TASK-031, TASK-032 | Concluída — `supabase/migrations/20260908181000_preference_rls.sql`, mesmo padrão de RLS + GRANT explícito de TASK-027 (`20260908175414_profile_consent_rls.sql`): `public.preference` com PK composta `(user_id, key)` (LWW substitui a linha inteira por chave, não acumula versões como `plan_progress`), `value jsonb not null`, `updated_at timestamptz not null` e `received_at timestamptz not null default now()`. Campos coerentes com `PreferenceRecord` do cliente (`src/core/storage/types.ts`, TASK-016/020/021): `key`/`value`/`updated_at` mapeiam 1:1 para `key`/`value`/`updatedAt`; `received_at` é o equivalente servidor de `PreferenceRecord.receivedAt` (RT-06, TASK-021) — mas, diferente do cliente (onde é opcional e vem do round-trip), no servidor é **sempre** definido por trigger BEFORE INSERT OR UPDATE (`preference_set_received_at`) que sobrescreve incondicionalmente `NEW.received_at := now()`, nunca aceitando o valor que o cliente envie no corpo da requisição — é esse controle que faz `received_at` ser de fato "relógio confiável, independente do dispositivo que escreveu" (a mitigação de RT-06 fica furada se o servidor confiar num campo escrito pelo cliente). RLS habilitada na mesma migration (DI-03): 4 políticas para `authenticated` (`select`/`insert`/`update`/`delete`, todas `auth.uid() = user_id`) — diferente de `profile`/`consent` (TASK-027, sem delete), `preference` **tem** política de delete porque SDD.md §7.2 lista `DELETE` explicitamente no conjunto de operações do titular sobre esta tabela (ex.: desativar um lembrete removendo a chave, sem que isso seja exclusão de conta). GRANT explícito (`select, insert, update, delete on public.preference to authenticated`) pelo mesmo racional documentado em TASK-027 — `auto_expose_new_tables` desabilitado neste Supabase local (`supabase/config.toml`); sem GRANT para `anon` (exige sessão autenticada, SDD.md §7.2). Teste real (não simulado) contra Supabase local (`tests/db/rls-preference.test.mjs`, `node --test`, mesmo padrão/credenciais de `tests/db/rls-profile-consent.test.mjs`), 9/9 casos verdes, cobrindo diretamente o critério de aceite: usuário grava só a própria linha (insert forjado com `user_id` de outro titular rejeitado pelo `with check`); `received_at` forjado pelo cliente num insert (data 1 ano no futuro) é ignorado, valor persistido reflete o relógio real do servidor; usuário não lê linha de outro titular (retorna lista vazia, não erro); usuário não atualiza (PATCH afeta 0 linhas) nem apaga (DELETE afeta 0 linhas) a preferência de outro titular, mas consegue atualizar/apagar a própria (LWW substitui o valor da mesma chave); papel `anon` (sem sessão) recebe HTTP não-2xx ao tentar ler `preference` (bloqueio de GRANT de tabela, antes da RLS). Validado com `supabase start` + `supabase db reset` reais (Docker), aplicando as 6 migrations do Lote 5 publicadas até este momento (`profile_consent`, `plan_progress`, `erasure_request`, `push_subscription_reminder_log`, `preference`, `analytics`) sem erro. Contenção real de ambiente observada durante a execução (5 instâncias de Executor rodando `supabase start`/`db reset`/`stop` concorrentemente no mesmo projeto Docker `EstudoBiblico`): múltiplas tentativas de `supabase start`/`db reset` reportaram containers auxiliares (`kong`/`auth`/`rest`/etc.) sendo derrubados por outra instância logo após subirem, e um `db reset` chegou a falhar uma vez por conflito de seed do Realtime (`tenants_external_id_index` já ocupado) — nenhuma mudança de código necessária por isso; resolvido por retry (`supabase start` + `supabase db reset` + polling de `/auth/v1/health` até 200) até a suíte rodar de ponta a ponta contra o Supabase local real, mesmo risco operacional já registrado por TASK-002/TASK-003 para `node_modules` compartilhado, agora também observado para o projeto Docker do Supabase local compartilhado. `npm run lint` (eslint + `lint:deps`, "no dependency violations found", 115 módulos/275 dependências — nenhum código TS novo desta tarefa, só SQL/`.mjs`), `npm run typecheck` (`tsc -b --noEmit`, sem erro) e `npm run build` (bundle gerado sem erro) confirmados limpos após a implementação. |
| TASK-030 | `[insep.]` Migration `push_subscription` + `reminder_log` com RLS | RLS testada em ambas | TASK-027 | TASK-028, TASK-029, TASK-031, TASK-032 | Concluída — `supabase/migrations/20260908180500_push_subscription_reminder_log_rls.sql`, mesmo padrão de TASK-027 (RLS habilitada na mesma migration que cria a tabela, DI-03; GRANT explícito de tabela, já que este Supabase local não tem `auto_expose_new_tables`). `push_subscription` (`id`, `user_id` → `auth.users` on delete cascade, `endpoint`, `p256dh_key`, `auth_key`, `platform`, `created_at`, `last_seen_at`, `unique (user_id, endpoint)` para deduplicar reassinatura do mesmo endpoint): policies de select/insert/update/**delete** próprios para `authenticated` (delete permitido aqui, diferente de `profile`/`consent` — desinscrever um dispositivo é ação legítima e reversível do titular, sem relação com a cascata de exclusão de conta de RN-11); `grant select, insert, update, delete … to authenticated`, sem grant para `anon`. `reminder_log` (`id`, `user_id`, `scheduled_for`, `sent_at`, `outcome` — enum fechado `sent`/`failed`/`skipped`, sem "delivered"/"opened" por Web Push não confirmar entrega ao servidor (SPIKE-01/RT-02) —, `channel`, `created_at`, `unique (user_id, scheduled_for)` implementando o "envio idempotente por dia" da mitigação de RT-07 direto na constraint): só policy de **select** próprio para `authenticated` (`grant select … to authenticated`) — escrita é exclusiva da rotina de agendamento/envio (TASK-057, `service_role`), nunca do cliente, mesmo racional de `analytics_daily_aggregate` contra falsificação de métrica (RT-10); `grant select, insert, update … to service_role` adicionado após achado empírico do próprio teste desta tarefa (`service_role` ignora RLS mas não ignora GRANT de tabela — sem o grant, a rotina de serviço falharia com "permission denied for table reminder_log", 42501, mesmo sendo o papel de serviço). Teste real (não simulado) contra Supabase local: `tests/db/rls-push-subscription-reminder-log.test.mjs` (`node:test`, mesmo padrão de `tests/db/rls-profile-consent.test.mjs`), 7 casos, 7/7 verdes — `push_subscription`: usuário só grava/lê/atualiza/apaga a própria linha (insert forjado com `user_id` de outro titular rejeitado por `with check`; update/delete de B sobre linha de A afetam 0 linhas; A consegue desinscrever a própria assinatura); `reminder_log`: seed via `service_role` confirma escrita de serviço funcionando e a constraint `unique (user_id, scheduled_for)` rejeitando reagendamento duplicado (idempotência de RT-07), cliente autenticado tentando inserir diretamente é rejeitado (somente leitura), e cada titular só lê o próprio log (B não enxerga log de A e vice-versa); papel `anon` sem sessão recebe permission denied nas duas tabelas (bloqueio de privilégio de tabela, antes da RLS). Rodado também em conjunto com `tests/db/rls-profile-consent.test.mjs` (TASK-027) sem regressão (14/14 verdes) — as falhas observadas isoladamente em `rls-analytics.test.mjs`/`rls-plan-progress.test.mjs` (TASK-031/TASK-028, de outras instâncias do Executor rodando em paralelo no mesmo Supabase local) não pertencem ao escopo desta tarefa nem tocam `push_subscription`/`reminder_log`. Observação de ambiente (mesmo risco operacional já registrado por TASK-002/TASK-003 para `node_modules`, agora reproduzido contra o Supabase local compartilhado): `supabase db reset` concorrente entre até 5 instâncias do Executor no mesmo checkout produziu estados transitórios inconsistentes (container saindo com "exit 1"/"exit 143", e um caso mais sério — `alter default privileges … revoke select, insert, update, delete on tables from anon, authenticated, service_role` truncado por "unexpected EOF" no meio da inicialização do schema, deixando `anon`/`authenticated` com grant total de CRUD por padrão em vez do padrão restrito esperado); resolvido reexecutando `supabase db reset` até uma aplicação limpa de todas as migrations do lote, confirmado depois via `information_schema.role_table_grants` que os privilégios de `anon`/`authenticated`/`service_role` em `reminder_log` batem exatamente com o GRANT explícito desta migration, sem resíduo do estado corrompido. `npm run lint` (eslint + `lint:deps`, "no dependency violations found", 115 módulos/275 dependências), `npm run typecheck` (`tsc -b --noEmit`, sem erro) e `npm run build` (bundle gerado sem erro, incl. `sw.js`/`manifest.json` via PWA) confirmados limpos após a implementação. |
| TASK-031 | `[insep.]` Migration `analytics_event` + `analytics_daily_aggregate` + função de incremento sem identificador com RLS | Cliente não lê o agregado; função de incremento testada | TASK-027 | TASK-028, TASK-029, TASK-030, TASK-032 | Concluída — `supabase/migrations/20260908182000_analytics_rls.sql`, mesmo padrão de RLS + GRANT explícito de TASK-027/028/029, implementando os dois níveis de telemetria do ADR-009. **Nível 2** (`analytics_event`, identificado, só pós-consentimento): campos exatos do SDD.md §5.3 (`user_id`, `type`, `occurred_at` — relógio do dispositivo —, `received_at` — relógio do servidor, `default now()`, mitigação de RT-06 —, `payload jsonb`); RLS por titular (`auth.uid() = user_id`), mas deliberadamente só `select`/`insert` para `authenticated` (sem `update`/`delete`) — evento telemétrico é imutável uma vez recebido, mesmo raciocínio append-only de `plan_progress` (TASK-028); exclusão em cascata do titular segue `erasure_request` (RN-11, TASK-032) via `service_role`, nunca DELETE direto do cliente. **Nível 1** (`analytics_daily_aggregate`, agregado anônimo sem sujeito): PK composta `(date, metric)`, `count bigint default 0`, `metric` restrito por `CHECK` à lista fechada de ADR-009 (`soft_gate_exibido`/`soft_gate_aceito`/`soft_gate_recusado`/`vitrine_aberta`/`falha_offline_apresentacao`) — sem coluna de usuário/sessão/dispositivo/IP, RLS habilitada e **sem nenhuma política de SELECT nem GRANT de tabela para `authenticated`/`anon`**: cliente nunca lê o agregado diretamente (SDD.md §7.2), cumprindo o critério de aceite ao pé da letra. Único ponto de escrita: `public.bump_metric(p_metric text)`, `SECURITY DEFINER` + `set search_path = public`, faz `insert ... on conflict (date, metric) do update set count = count + 1` — roda com o privilégio de quem criou a função (bypassa RLS/GRANT do chamador do lado da tabela), por isso funciona tanto para `anon` (soft gate pré-consentimento, vitrine) quanto para `authenticated` (`falha_offline_apresentacao` pode ocorrer com ou sem conta); `EXECUTE` concedido a `anon` e `authenticated` via RPC (`grant usage on schema public to anon` + `grant execute on function ... to anon, authenticated`). Achado real de implementação, corrigido antes de fechar a tarefa (documentado no cabeçalho da migration): o padrão "always-revoked" deste Supabase local (`auto_expose_new_tables` não habilitado) revoga privilégio de tabela por padrão para **todo** papel de API em tabela nova, inclusive `service_role` — confirmado empiricamente via `\dp public.analytics_daily_aggregate` no Postgres (`service_role=Dxtm/postgres`, sem `r` de SELECT) — por isso um `grant select on public.analytics_daily_aggregate to service_role` foi adicionado; não compromete "cliente não lê o agregado" porque `service_role` nunca é a chave usada pelo cliente (GUARDRAILS G-10) — é o papel que a futura apuração de métricas via SQL versionado (ADR-009, "Apuração") e o teste desta tarefa usam para inspecionar o resultado de `bump_metric` no servidor. Teste automatizado real (não simulado) em `tests/db/rls-analytics.test.mjs` (`node --test`, mesmo padrão/credenciais de `tests/db/rls-profile-consent.test.mjs`/`rls-preference.test.mjs`), 10/10 casos verdes, cobrindo as duas metades do critério de aceite: (1) `anon` e `authenticated` não conseguem `select` em `analytics_daily_aggregate` diretamente via REST (bloqueio de GRANT de tabela, papel algum lê o agregado pela API); (2) `anon` não grava direto na tabela (só via função); (3) `bump_metric` via RPC funciona para `anon` e para `authenticated`, incrementando de fato o contador (leitura via `service_role`, único papel capaz de inspecionar o resultado para fins de teste — nunca usado como prova de que o cliente consegue) — asserção por **incremento relativo** (lê o valor antes de chamar, confirma `+1`/`+2` depois), não valor absoluto, porque a chave `(date, metric)` acumula entre reexecuções da suíte no mesmo dia-calendário sem exigir reset do banco a cada rodada (confirmado rodando a suíte 2x seguidas); confirma também ausência de coluna de sujeito na linha lida; (4) `bump_metric` rejeita métrica fora da lista fechada do `CHECK`; (5)/(6)/(7) `analytics_event` isola por titular (insert forjado com `user_id` de outro rejeitado, `anon` não lê nem grava, titular lê a própria linha) — mesmo padrão de RLS já validado para `profile`/`consent`. Achado real de teste corrigido durante a implementação: a primeira versão do teste assumia HTTP 200 para o RPC, mas `bump_metric` retorna `void` — PostgREST responde `204 No Content` (sem corpo) para função sem retorno, corrigido para a asserção esperar `204`. Validado com `supabase start` + `supabase db reset` reais (Docker), aplicando as 6 migrations do Lote 5 publicadas até este momento sem erro; suíte rodada isoladamente e em conjunto com `tests/db/rls-profile-consent.test.mjs` (17/17 verdes combinados), e duas vezes seguidas sem reset entre elas para confirmar idempotência. Contenção real de ambiente observada e superada (mesmo risco operacional já registrado por TASK-002/003/029 para infraestrutura local compartilhada entre as 5 instâncias paralelas do Lote 5): múltiplas tentativas de `supabase start`/`db reset` reportaram containers auxiliares derrubados por outra instância; um achado adicional aqui não visto pelas tarefas irmãs — depois de um `db reset`, o Kong (gateway) por vezes manteve uma rota em cache para o IP anterior do container `auth` (recriado pelo reset), causando `502 Bad Gateway` persistente em `/auth/v1/health` mesmo com o container `auth` já saudável; resolvido com `docker restart supabase_kong_EstudoBiblico`, sem mudança de código — registrado aqui para as tarefas seguintes do lote (TASK-033 em diante) que também dependem do Supabase local. `npm run lint` (eslint + `lint:deps`, "no dependency violations found", 115 módulos/275 dependências — nenhum código TS novo desta tarefa, só SQL/`.mjs`), `npm run typecheck` (`tsc -b --noEmit`, sem erro), `npm run build` (bundle gerado sem erro) e `npm run test:security` (11/11 verdes) confirmados limpos. `npx vitest run`: 229/230 verdes, único vermelho é o timeout intermitente pré-existente de `src/tools/dependency-rule.test.ts` sob carga concorrente (já rastreado em REFAT-L4-02), não é regressão desta tarefa. |
| TASK-032 | Migration `erasure_request` com RLS | RLS testada | TASK-027 | TASK-028, TASK-029, TASK-030, TASK-031 | Concluída — `supabase/migrations/20260908180432_erasure_request_rls.sql`, mesmo padrão de RLS + GRANT explícito de TASK-027: `public.erasure_request` (`id uuid pk default gen_random_uuid()`, `user_id` not null references `auth.users(id) on delete cascade`, `requested_at timestamptz not null default now()`, `due_at timestamptz not null`, `completed_at timestamptz` nullable), exatamente os campos do SDD.md §5.3. `unique (user_id)` torna a cardinalidade `PROFILE ||--o| ERASURE_REQUEST` do ER (§5.3) uma invariante de banco — no máximo um pedido por titular, pedido é terminal (RN-11). `due_at = requested_at + 15 dias` (RN-11) é calculado por trigger `before insert` (`set_erasure_request_due_at`) que sempre sobrescreve `NEW.due_at`, ignorando qualquer valor enviado no corpo do INSERT — não foi possível usar coluna `generated always as (...) stored` porque aritmética de `timestamptz + interval` não é IMMUTABLE aos olhos do Postgres (depende do timezone de sessão), erro confirmado empiricamente (`generation expression is not immutable`, SQLSTATE 42P17) ao tentar essa abordagem primeiro; o trigger é o equivalente funcional sem essa limitação. RLS habilitada na mesma migration (DI-03/G-09): apenas 2 políticas para `authenticated` (`select`/`insert`, ambas `auth.uid() = user_id`) — deliberadamente **sem** política de update/delete, que é exatamente o requisito da tarefa ("usuário não pode alterar due_at/status arbitrariamente — isso é jobs/erasure, que roda com service_role"). GRANT explícito: `select, insert` para `authenticated` (mesmo racional de TASK-027 — `auto_expose_new_tables` desabilitado neste Supabase local) e, adicionalmente, `select, update` para `service_role` — achado real durante a implementação: diferente do que se poderia supor, `service_role` **não** tem select/update automático sobre tabela nova neste ambiente (confirmado consultando `pg_default_acl`: tabelas criadas pela role de migração `postgres` concedem a `service_role` apenas delete/truncate/trigger/references por padrão, não select/update/insert) — sem esse GRANT explícito, `jobs/erasure` (TASK-041) não conseguiria ler o pedido pendente nem gravar `completed_at` via PostgREST, mesmo sendo `service_role` e ignorando RLS (RLS e GRANT de tabela são camadas independentes). Índice parcial `erasure_request_due_at_idx (due_at) where completed_at is null` para a futura consulta de `jobs/erasure` por pedidos vencidos/pendentes (não exigido pelo critério de aceite desta tarefa, mas natural dado o acesso previsível de TASK-041). Teste real (não simulado) contra Supabase local (`tests/db/rls-erasure-request.test.mjs`, `node --test`, mesmo padrão/credenciais de `tests/db/rls-profile-consent.test.mjs`), 8/8 casos verdes: usuário cria o próprio pedido e `due_at` é conferido como exatamente `requested_at + 15 dias`; cliente tentando gravar um `due_at` forjado no corpo do insert tem o valor sobrescrito pelo trigger (não é o valor forjado, é o recalculado); usuário não cria pedido em nome de outro titular (insert forjado rejeitado pelo `with check`); usuário não lê pedido de outro titular (lista vazia); nem o próprio titular nem outro conseguem alterar `completed_at` via PATCH (403 — bloqueio de GRANT de tabela, uma camada antes da RLS, diferente do padrão 200+corpo-vazio de `profile`/`consent`, porque `erasure_request` não concede `update` a `authenticated` em nenhum nível); `service_role` consegue preencher `completed_at`, confirmando que `jobs/erasure` terá o acesso que precisa; papel `anon` (sem sessão) não lê nenhuma linha. Contenção real de ambiente observada durante a execução (múltiplas instâncias de Executor rodando `supabase start`/`db reset`/`stop` concorrentemente no mesmo projeto Docker `EstudoBiblico`, mesmo risco operacional já registrado por TASK-002/TASK-003/TASK-029): vários `db reset` falharam por conflito de container/timeout (`error running container: exit 1`, `unexpected EOF`) e o gateway Kong retornou 502 intermitente entre reinícios de containers concorrentes; nenhuma mudança de código necessária por isso — resolvido por retry com polling de `/auth/v1/health` até 200 antes de rodar a suíte. Suíte completa `tests/db/*.test.mjs` rodada para checar regressão: 41/47 verdes; as 6 falhas são exclusivamente em `rls-analytics.test.mjs` (TASK-031) e `rls-plan-progress.test.mjs` (TASK-028) — tarefas de outras instâncias do Executor em paralelo, fora do escopo desta tarefa, não tocadas aqui. `npm run lint` (eslint + `lint:deps`, "no dependency violations found", 115 módulos/275 dependências — nenhum código TS novo desta tarefa, só SQL/`.mjs`), `npm run typecheck` (`tsc -b --noEmit`, sem erro) e `npm run build` (bundle gerado sem erro) confirmados limpos após a implementação. |
| TASK-033 | Teste de catálogo: toda tabela com `user_id` tem RLS habilitada (RT-04) | CI falha ao adicionar tabela sem RLS de propósito | TASK-028, TASK-029, TASK-030, TASK-031, TASK-032 | — | Concluída — `tests/db/rls-catalog.test.mjs` (novo arquivo, `node --test`, mesma convenção de `tests/db/rls-*.test.mjs`) e alteração em `.github/workflows/ci.yml` (gate real de CI, não só local). **Decisão de design do teste (por que catálogo, não lista hardcoded)**: consulta genérica ao catálogo do Postgres — junta `pg_catalog.pg_class`/`pg_catalog.pg_namespace` (`relrowsecurity`) com `information_schema.columns` (existência de coluna `user_id`) para TODA tabela (`relkind = 'r'`) de `public`, sem citar nenhum nome de tabela no código; assim uma tabela FUTURA criada com `user_id` e sem RLS é pega automaticamente, sem precisar voltar a editar este arquivo a cada migration nova (é exatamente o ponto de RT-04/G-09: "uma migration que cria tabela sem RLS é bloqueio de deploy, não item de backlog"). Confirmado que a varredura pega hoje as 8 tabelas com `user_id` do Lote 5 (`profile`, `consent`, `plan_progress`, `preference`, `push_subscription`, `reminder_log`, `analytics_event`, `erasure_request`) — `analytics_daily_aggregate` fica de fora por desenho (agregado anônimo sem `user_id`, ADR-009/TASK-031), não é uma omissão do teste. **Por que conexão direta ao Postgres (`pg`, novo devDependency) em vez de PostgREST**: catálogo de schema (`pg_class`/`information_schema`) não é dado de aplicação, não é exposto por PostgREST; conecta com a credencial padrão pública do Supabase local (`postgres:postgres@127.0.0.1:54422/postgres`, a mesma impressa por `supabase start`/documentada em `docs/ci-secrets.md` §3 e em `.env.example`, nunca uma credencial de produção), via env var `SUPABASE_DB_URL` com esse valor como fallback, mesma convenção de nome já usada em `.env.example`/`docs/ci-secrets.md`. **Prova empírica de que o teste falha de verdade (não é asserção vazia)**, em duas camadas: (1) um segundo teste permanente no mesmo arquivo ("autoteste") cria dentro da própria suíte uma tabela temporária com `user_id` e SEM RLS, roda a mesma função de detecção, confirma que ela aparece como ofensora, depois habilita RLS na mesma tabela e confirma que ela some da lista — e remove a tabela em `t.after` (mesmo em caso de falha) — fica na suíte permanentemente como regressão da própria lógica de detecção, não é um passo manual documentado em prosa; (2) verificação manual adicional feita durante a implementação, fora da suíte automatizada: criada uma tabela real `public.proof_offender_table` (`user_id uuid not null`, sem RLS) direto no Postgres local, `node --test tests/db/rls-catalog.test.mjs` rodado e **falhou de fato** (exit code 1, `AssertionError`, listando exatamente `{"table_name":"proof_offender_table","rls_enabled":false}` como ofensora) — depois a tabela de prova foi apagada (`DROP TABLE`) e a suíte confirmada verde de novo (2/2). **Gate de CI real (não só local)**: `.github/workflows/ci.yml` ganhou 3 steps novos entre "Unit tests" e "Build" — `supabase/setup-cli@v1`, `supabase start` (sobe Postgres/PostgREST/GoTrue via Docker no runner `ubuntu-latest`, que já tem Docker pré-instalado, e aplica as migrations do repo) e `npm run test:db` (roda toda `tests/db/*.test.mjs`, incluindo este arquivo, com `SUPABASE_DB_URL`/`SUPABASE_URL`/`SUPABASE_SERVICE_ROLE_KEY` do ambiente local via `env:` do step — as chaves são as mesmas de demonstração pública documentadas em `docs/ci-secrets.md`/já usadas como fallback nos próprios arquivos de teste, não segredo real, nenhum `secrets.*` do GitHub necessário para rodar contra Postgres local), mais um `supabase stop` com `if: always()` para não vazar containers entre execuções do runner; mesmo espírito do achado corrigido em TASK-004 (Lote 0) — "teste que só roda localmente não cumpre critério de aceite que fala de CI" — aqui replicado para não deixar `test:db` (que já existia, mas nunca tinha sido plugado no workflow) fora do gate. Decisão registrada: usar `supabase/setup-cli@v1` + `supabase start` (não um serviço `postgres` avulso do Actions) porque o projeto já usa migrations reais do Supabase CLI (mesmas de todas as tarefas do Lote 5) — subir só um container Postgres genérico exigiria reimplementar a aplicação de migrations em CI por outro caminho, sem necessidade. Comandos rodados e confirmados localmente: `npm install --save-dev pg @types/pg`, `node --test tests/db/rls-catalog.test.mjs` (2/2 verde), `npm run test:db` (49/49 verde — os 47 preexistentes + os 2 novos), `npm run lint` (eslint + `lint:deps`, "no dependency violations found" — `tests/db/*.mjs` fora do escopo do lint, mesma convenção já usada por `tests/security`/`tests/db`), `npm run typecheck` (`tsc -b --noEmit`, sem erro — `tests/db` fora do `include` de `tsconfig.app.json`) e `npm run build` (bundle gerado sem erro). Nenhuma migration real do projeto foi tocada — a tabela usada para provar a falha foi criada e apagada fora de `supabase/migrations/`, via SQL ad hoc contra o Postgres já rodando, sem deixar rastro em `supabase/migrations/`. |
| TASK-034 | Headers de segurança: CSP, HSTS, Permissions-Policy, Referrer-Policy (§7.5) | Teste de headers HTTP confirma presença de todos | TASK-005 | TASK-027 (e demais desta lote) | Concluída — decisão de portabilidade (hosting/CDN definitivo — Cloudflare Pages, ADR-008/E-06 — ainda não provisionado neste ponto do desenvolvimento): fonte única de verdade dos headers em `src/tools/security/securityHeaders.ts` (`SECURITY_HEADERS`), consumida por um plugin Vite próprio (`src/tools/security/vitePluginSecurityHeaders.ts`, registrado em `vite.config.ts`) que (1) aplica os headers via middleware em `vite dev`/`vite preview` (`configureServer`/`configurePreviewServer`), permitindo teste HTTP real localmente, e (2) escreve `_headers` — formato nativo do Cloudflare Pages, lido automaticamente pelo CDN a partir da raiz do diretório publicado — no `writeBundle` do build (`dist/_headers`), sem exigir Edge Function/Worker adicional. CSP: `default-src 'self'` + `connect-src 'self' https://*.supabase.co` (domínio exato do projeto Supabase ainda não provisionado — `supabase start` roda só local, TASK-005 — wildcard de subdomínio Supabase documentado no código como decisão temporária a apertar para o domínio exato do projeto antes do go-live), `script-src`/`img-src`/`font-src`/`manifest-src`/`worker-src` restritos a `'self'` (SW do Workbox e Web App Manifest são same-origin por decisão, DI-15/TASK-003), `style-src 'self' 'unsafe-inline'` (ver correção pós-revisão inline abaixo), sem `unsafe-inline`/`unsafe-eval` em `script-src`, `frame-ancestors 'none'`, mais `object-src 'none'`/`base-uri 'self'`/`form-action 'self'` como reforço que não contradiz o mínimo do SDD. HSTS `max-age=31536000; includeSubDomains; preload`. `Referrer-Policy: no-referrer`. `Permissions-Policy: camera=(), microphone=(), geolocation=()` (nenhuma das três é usada pelo produto). `X-Content-Type-Options: nosniff` adicionado como reforço padrão de mercado, não exigido pelo SDD mas sem contradizê-lo. 3 arquivos de teste novos (Vitest), 15 testes: `src/tools/security/securityHeaders.test.ts` — 6 testes de valor exato de cada diretiva/header (não só presença da chave: CSP com `default-src 'self'`/`frame-ancestors 'none'`/`connect-src` restrito/sem `unsafe-inline`/único wildcard sendo o do backend; HSTS com `max-age` ≥ 1 ano + `includeSubDomains` + `preload`; `Referrer-Policy` exatamente `no-referrer`; `Permissions-Policy` negando as 3 APIs; presença dos 4 headers exigidos pelo §7.5 no conjunto; formato do `_headers` gerado) + 1 teste de **resposta HTTP real** (sobe servidor `node:http` com o mesmo middleware do plugin, faz `fetch()` de verdade contra `127.0.0.1`, confirma os 5 headers na resposta HTTP — não apenas a constante em memória, cumprindo literalmente o critério de aceite "teste de headers HTTP"); `src/tools/security/headers-build.test.ts` — roda `vite build` real via API programática (mesmo `vite.config.ts`, mesmo padrão de `src/tools/pwa/pwa-manifest.test.ts` de TASK-003) contra outDir temporário e confirma que `dist/_headers` é de fato escrito com o conteúdo exato esperado — prova que o artefato de deploy é gerado, não só a lógica em memória. `npx vitest run src/tools/security` → 2 arquivos, 8 testes, todos verdes. Suíte completa (`npx vitest run`) → 229/230 verdes; a única falha (`src/tools/dependency-rule.test.ts`, timeout de 5s) é o flake pré-existente de contenção sob carga concorrente já registrado em `REFAT-L0-01`/nota de TASK-004 (outra instância do Executor rodando TASK-027 em paralelo no mesmo checkout) — reproduzido limpo (4/4 verde) ao rodar o arquivo isoladamente, confirmando que não é regressão desta tarefa. `npm run lint` (eslint + `lint:deps`, "no dependency violations found") e `npm run typecheck` (`tsc -b --noEmit`) limpos. `npm run build` confirmado ponta a ponta: gera `dist/_headers` com as 5 linhas de header corretas (inspecionado manualmente após build local, removido em seguida — `dist/` é gitignored). `npm run test:security` (TASK-005/TASK-042) segue 11/11 verde, sem interferência. Nenhum arquivo em `supabase/` ou relacionado a banco tocado (fora do escopo desta tarefa, por instrução explícita de evitar corrida com a instância paralela em TASK-027). Como a tabela do Lote 5 ainda não tinha coluna `Status`, ela foi adicionada aqui (mesmo padrão retroativo já usado nos Lotes 0–4); as demais linhas (TASK-027/028/029/030/031/032/033) foram marcadas `Não iniciada` como valor neutro — cabe a cada instância que concluir sua própria tarefa atualizar sua linha. **Correção pós-revisão inline (achado real):** `style-src 'self'` (sem `'unsafe-inline'`, sem nonce, sem hash) bloqueia o atributo `style=""` que o React gera no DOM para qualquer `style={{...}}` inline — e os 5 primitivos do Design System (Lote 4: `Botao`, `CampoDeTexto`, `CampoDeSenha`, `SeletorDeHora`, `Alternador`, em `src/design-system/primitives/`) usam exatamente isso para aplicar `TOUCH_TARGET_MIN_STYLE` (alvo de toque mínimo, DI-08). O achado não era visível nos testes desta tarefa porque nenhuma tela real ainda importa esses primitivos (Lote 6+), mas a CSP como publicada quebraria DI-08 assim que a primeira tela os consumisse. Projeto não tem CSS-in-JS com nonce configurado, então a correção foi adicionar `'unsafe-inline'` só a `style-src` (`src/tools/security/securityHeaders.ts`), documentando no comentário do módulo por que é necessário e deixando explícito que `script-src` — o risco real que CSP mitiga — continua sem `'unsafe-inline'`/`'unsafe-eval'`, sem exceção. `securityHeaders.test.ts` atualizado: a asserção antes genérica (`not.toMatch(/unsafe-inline/)` na policy inteira) virou duas checagens específicas — `script-src 'self'` exato (sem `unsafe-inline`) e `style-src 'self' 'unsafe-inline'` exato — mais `not.toMatch(/unsafe-eval/)` mantido para a policy inteira. `npm run lint`, `npm run typecheck`, `npm run build` e `npx vitest run src/tools/security` (2 arquivos, 8/8 verdes) reconfirmados limpos após a correção. **Correção pós-fechamento do lote (regressão encontrada durante TASK-035/Lote 6):** o middleware `configureServer` do plugin (`vitePluginSecurityHeaders.ts`) aplicava o mesmo conjunto completo de headers usado em produção — incluindo a CSP restrita (`script-src 'self'`, sem `unsafe-inline`/`unsafe-eval`) — em toda resposta do `vite dev`. Causa raiz: em modo dev, `@vitejs/plugin-react` injeta um `<script type="module">` inline (preamble do React Fast Refresh/HMR) na página; sob essa CSP, o navegador bloqueia esse script inline e o React nunca monta — quebrando o app inteiro em dev e, por consequência, toda a suíte e2e Playwright (que roda contra `vite dev`, ver `playwright.config.ts`). Confirmado empiricamente antes da correção: `npx playwright test e2e/placeholder.spec.ts` falhava com "element(s) not found" no locator `main` (app nunca montava); esse mesmo sintoma já havia sido reportado como achado fora de escopo na nota de TASK-035 acima. Correção: `configureServer` passou a usar uma nova função, `applyDevSecurityHeaders` (`securityHeaders.ts`), que aplica todos os headers **exceto** `Content-Security-Policy` — decisão deliberada de não aplicar CSP nenhuma em dev (em vez de manter uma segunda CSP "relaxada" sincronizada com a de produção), já que `vite dev` nunca é o ambiente de ameaça real que a CSP protege. `configurePreviewServer` e o `writeBundle` (`_headers` do build) continuam usando `applySecurityHeaders` (conjunto completo, com CSP) — nenhuma regressão no critério de aceite original desta tarefa para produção/preview, confirmado por `dist/_headers` inspecionado após `npm run build` (5 headers, CSP completa presente e idêntica à anterior) e pelo teste já existente `headers-build.test.ts` (roda `vite build` real e confirma o conteúdo exato do `_headers`). Testes novos em `securityHeaders.test.ts`: resposta HTTP real (mesmo padrão `node:http` + `fetch()` já usado no arquivo) confirmando que `Content-Security-Policy` está ausente em dev e que os outros 4 headers continuam presentes com valor exato. Teste e2e novo, `e2e/security-headers-dev.spec.ts`, contra o `vite dev` real (mesmo `webServer` de `playwright.config.ts`): confirma a ausência do header CSP na resposta HTTP e, via listener de `console`/`pageerror` do Playwright, confirma que nenhuma violação de CSP é registrada pelo navegador durante o carregamento — além do próprio `main` montar com o texto esperado. `npx playwright test` reconfirmado após a correção: `e2e/placeholder.spec.ts` (2/2) volta a passar, `e2e/security-headers-dev.spec.ts` (novo, 1/1) passa, `e2e/identity-signup.spec.ts` (TASK-035) segue passando quando o Supabase local está de pé (skip automático quando não está, comportamento pré-existente, não relacionado a esta correção). `npm run lint`, `npm run typecheck` e `npm run build` reconfirmados limpos. Status da tarefa mantido `Concluída` (correção pontual, não reabertura). |

### Refatoração Lote-5

> Criada pelo Validador (chapéu QA + DevSecOps) durante o fechamento
> estrutural parcial do Lote 5 (2026-09-08, cobrindo TASK-027 a TASK-032 e
> TASK-034 — TASK-033 segue `Não iniciada`, fora desta rodada), para achado
> de severidade baixa-média que não compromete o critério de aceite central
> de nenhuma tarefa `Concluída` do lote — ver `.md/QA-REPORT.md` e
> `.md/SECURITY-REVIEW.md`, seção "Lote 5".

| ID | Tarefa | Critério de aceite | Depende de | Paralelizável-com | Status |
|---|---|---|---|---|---|
| REFAT-L5-01 | `analytics_event` (TASK-031, `20260908182000_analytics_rls.sql`) não implementa o "teto de inserção de eventos por usuário por dia, aplicado por gatilho no banco" exigido literalmente por `SDD.md` §7.5 (linha "API de dados"). A tabela tem RLS/GRANT corretos (isolamento por titular confirmado, TASK-031 aprovada), mas nenhum gatilho limita quantos eventos um único `authenticated` pode inserir em `analytics_event` por dia — nem o critério de aceite de TASK-031 ("Cliente não lê o agregado; função de incremento testada") nem nenhuma outra tarefa do `TASK.md` cobrem esse requisito específico do SDD; gap de decomposição isolado (uma linha da tabela §7.5), não um padrão recorrente, então tratado aqui como débito do Validador, não escalado ao `coordenador` | Adicionar um trigger `before insert` em `public.analytics_event` que rejeita (ou já ignora) inserts além de um teto diário por `user_id` (valor exato do teto é decisão de produto/capacidade, a definir com o Gestor/Coordenador — ex.: nº generoso o bastante para uso legítimo intenso, baixo o bastante para impedir abuso de armazenamento/custo); teste real (`tests/db/rls-analytics.test.mjs` ou arquivo novo) que insere até o teto com sucesso e confirma rejeição do insert seguinte, sem falso positivo para uso normal. Prazo: antes do go-live de produção (não é achado de confidencialidade/RLS — é proteção de abuso/disponibilidade, SDD §7.5) | TASK-031 | — | Pendente |

### Lote 6 — Identidade

| ID | Tarefa | Critério de aceite | Depende de | Paralelizável-com | Status |
|---|---|---|---|---|---|
| TASK-035 | Cadastro e-mail+senha com verificação (Supabase Auth) | e2e cria conta e recebe verificação mock | TASK-027 | — | Concluída — **correção da reprovação crítica do Validador (2026-09-08)**, pelo Executor (leandrosegheto17@gmail.com), junto com TASK-036 (mesma causa raiz: gatilho `handle_new_auth_user`). O gatilho (`supabase/migrations/20260908190000_profile_on_signup_trigger.sql`) foi reescrito **no próprio arquivo** (migration ainda não commitada — confirmado via `git log`/`git status` antes de editar, conforme instrução) para inserir `profile` **e**, condicionalmente, `consent` na MESMA transação que cria `auth.users`: lê `consent_version`/`consent_text_sha256` de `new.raw_user_meta_data` (populado pelo GoTrue a partir de `options.data` do próprio `auth.signUp`) e só insere `consent` quando as duas chaves estão presentes — usuários criados sem essa metadata (ex.: `/admin/users` dos testes de RLS de TASK-027) continuam recebendo só `profile`, como antes. Se o INSERT de `consent` falhar dentro do gatilho, a transação inteira (inclusive a criação de `auth.users`) é desfeita pelo Postgres — atomicidade de banco, não ordem de chamadas do cliente, conforme SDD.md §7.6. `src/features/identity/sign-up.ts` (`SignUpInput.metadata`): novo campo opcional repassado como `options.data` ao `gateway.signUp`, sem interpretar o conteúdo (mecanismo genérico, TASK-035 continua sem saber o que é "consent"). Testes novos: `sign-up.test.ts` (+2 casos: metadata repassada/omitida), `tests/db/rls-profile-consent.test.mjs` (+2 casos de integração contra o Supabase local real: metadata presente cria `profile`+`consent` atomicamente; metadata ausente cria só `profile`, sem inventar `consent`; e o caso `profile: usuário só grava a própria linha` foi reescrito para `profile: a linha já existe (criada pelo gatilho on_auth_user_created)...`, já que o INSERT que antes esperava `201` agora colide com a linha que o próprio gatilho já criou — comportamento novo esperado, não regressão). Fluxo de verificação por e-mail (Mailpit) intacto, sem nenhuma alteração — `e2e/identity-signup.spec.ts` continua verde isolado e na suíte completa. Verificação: `npm run lint` (limpo, `lint:deps` "no dependency violations found"), `npm run typecheck` (sem erro), `npx vitest run` (324/324 verdes com `--no-file-parallelism`; o timeout intermitente de `REFAT-L0-01` sob execução concorrente, já rastreado, reproduzido de novo e confirmado como pré-existente/não relacionado ao passar isolado), `npm run build` (bundle gerado sem erro), `npm run test:db` — **o gate que estava quebrado** — 51/51 verdes (`npx supabase db reset` aplicando a migration reescrita antes de rodar), e `npx playwright test` — 11/11 verdes, incluindo `e2e/identity-signup.spec.ts`, `identity-signin.spec.ts`, `identity-erasure.spec.ts`, `identity-export-data.spec.ts` e `placeholder.spec.ts` (as 2 falhas de `placeholder.spec.ts` registradas na nota original de TASK-035 não se reproduziram nesta rodada). Nota de implementação original preservada abaixo, com a ressalva de que a descrição do gatilho como "só `profile`" foi superada pela correção acima: mecanismo de cadastro (não a tela — T-08 é TASK-036), em `src/features/identity/` (`SDD.md` §2.2 designa explicitamente `features/identity` para "Cadastro, login, recuperação..."; `core/*` ficou de fora por decisão de arquitetura, não por engano). `sign-up.ts` (`signUpWithEmailPassword`) encapsula `auth.signUp` e `confirm-signup.ts` (`confirmSignUp`) encapsula `auth.verifyOtp({type:"signup", token_hash})`, ambos contra `AuthGateway` — porta mínima (`Pick<SupabaseClient["auth"], "signUp" \| "verifyOtp">`, `auth-gateway.ts`) no mesmo padrão de porta explícita já usado por `core/sync` (`SyncServerClient`), para testar sem simular o `GoTrueClient` inteiro. `create-identity-supabase-client.ts` lê só `VITE_SUPABASE_URL`/`VITE_SUPABASE_ANON_KEY` (nunca `service_role`, DI-09) para a UI real (TASK-036 em diante); testes/e2e constroem o gateway à parte, sem depender de `import.meta.env`. `SignUpResult`/`ConfirmSignUpResult` são uniões discriminadas (`confirmation-required` — caminho normal deste projeto, já que `enable_confirmations = true` — `signed-in`, `error` com `SignUpErrorReason` classificado a partir da mensagem/status do Supabase Auth). `supabase/config.toml`: `enable_confirmations` ligado de `false` para `true` (estava desligado desde TASK-005; SDD.md §7.1 exige verificação de e-mail) — confirmado que não quebra `tests/db/rls-profile-consent.test.mjs` (usa `admin/users` com `email_confirm:true`, contorna a confirmação por e-mail deliberadamente). Criação de `profile`: **gatilho de banco** (`supabase/migrations/20260908190000_profile_on_signup_trigger.sql`, função `security definer` `handle_new_auth_user`, `after insert on auth.users`), não código de cliente — decisão registrada no próprio comentário da migration: sem sessão disponível até a confirmação (`enable_confirmations=true`), o cliente não teria como autenticar o INSERT no momento do `signUp`; um gatilho de banco é a única garantia estrutural contra usuário órfão sem linha de `profile` por falha de rede/JS entre as duas chamadas, alinhado a SDD.md §7.1/§7.2 ("banco decide apenas identidade, propriedade de linha e conciliação"). Confirmado manualmente contra o Supabase local real (`supabase db reset` aplicando a migration nova): `profile` ganha a linha imediatamente no `signUp`, antes mesmo da confirmação. Testes: 11 casos Vitest (`sign-up.test.ts`, `confirm-signup.test.ts`) cobrindo os 3 estados de `SignUpResult`/2 de `ConfirmSignUpResult` e a classificação de erro (e-mail duplicado, senha fraca, e-mail inválido, HTTP 429, desconhecido), com `AuthGateway` fake — sem tocar rede. e2e real (`e2e/identity-signup.spec.ts`, Playwright, contra o Supabase local de verdade, nunca mockado): cria conta com `signUpWithEmailPassword` (anon key só, DI-09), confirma "verificação mock" via API do Mailpit local (porta 54424 — nota: `supabase/config.toml`/`docs/ci-secrets.md` chamam de "Inbucket", mas a versão atual da CLI serve Mailpit nessa porta; mesmo papel, API HTTP diferente, documentado no próprio arquivo do teste), extrai o `token`/`token_hash` do corpo do e-mail capturado, completa a verificação com `confirmSignUp`, e confirma com uma consulta autenticada (`GET .../profile?user_id=eq...`, com a `anon key` + o `access_token` da sessão confirmada) que a linha de `profile` existe e é visível só ao próprio titular (prova RLS de TASK-027 + o gatilho novo juntos) — depois apaga o usuário de teste via `service_role` (só para limpeza, nunca para a lógica de cadastro em si). `.github/workflows/ci.yml`: reordenado — "Stop Supabase local" movido para depois do step "E2E tests" (antes rodava logo após os testes de DB, o que quebraria `identity-signup.spec.ts` em CI por falta de Supabase/Mailpit de pé); YAML revalidado sintaticamente. Achado fora do escopo desta tarefa, reportado e não corrigido aqui: `npx playwright test` roda com 2 falhas pré-existentes em `e2e/placeholder.spec.ts` ("can't detect preamble" do `@vitejs/plugin-react"), reproduzidas mesmo isoladamente e sem relação com TASK-035 — causadas pelo middleware de CSP de `securityHeadersPlugin()` (`vite.config.ts`, trabalho não commitado de TASK-034/Lote 5 já presente na árvore de trabalho antes desta tarefa começar); `e2e/identity-signup.spec.ts` (o teste desta tarefa) passa isoladamente e junto dos demais. `npm run lint` (eslint + `lint:deps`, "no dependency violations found"), `npm run typecheck` (`tsc -b --noEmit`, sem erro), `npm run build` (bundle gerado; código de `features/identity` ainda não é importado por nenhuma tela — T-08/TASK-036 é quem vai importar — então não aparece no bundle ainda, esperado) e `npx vitest run` (241/241 verdes, incluindo os 11 novos; 1 timeout intermitente pré-existente de `REFAT-L0-01` sob execução concorrente, reproduzido como passa isolado) confirmados localmente. |
| TASK-036 | Tela T-08 (Criar conta) + `BlocoConsentimento` separado, desmarcado por padrão, versão do texto em `consent` | Teste de acessibilidade 3.3.2/1.3.1; grava `consent` antes de qualquer dado pessoal | TASK-035 | TASK-037, TASK-038 | Concluída — **correção da reprovação crítica do Validador (2026-09-08)**, pelo Executor (leandrosegheto17@gmail.com), junto com TASK-035 (mesma causa raiz). `sign-up-with-consent.ts` foi reescrito: não chama mais `recordConsent`/`ConsentGateway` (ambos removidos — `record-consent.ts`, `record-consent.test.ts`, `consent-gateway.ts` deletados; nenhum outro consumidor dependia deles) como uma segunda chamada HTTP separada depois do `signUp`; agora chama `signUpWithEmailPassword` (TASK-035) uma única vez, com `consentVersion`/`consentTextSha256` anexados como `metadata: { consent_version, consent_text_sha256 }` (`SignUpInput.metadata`, nova em TASK-035). O gatilho de banco `handle_new_auth_user` (correção de TASK-035, mesma migration) lê essa metadata e grava `consent` na mesma transação que cria `auth.users` — a ordem "`consent` antes de qualquer dado pessoal" passa a ser garantida por atomicidade de transação no banco (SDD.md §7.6), nunca mais por sequência de chamadas do cliente. `types.ts`: `SignUpWithConsentResult` agora é só um alias de `SignUpResult` — o desfecho `"consent-failed"` foi removido (não existe mais um segundo passo que possa falhar isoladamente: se a gravação de `consent` falhar dentro do gatilho, a transação inteira é desfeita e o próprio `signUp` retorna erro). `TelaCriarConta.tsx`: prop `consentGateway` removida (não é mais necessária); `mensagemDeErroDeSignUp` não trata mais `"consent-failed"`. `index.ts`: exports de `ConsentGateway`/`recordConsent`/`RecordConsentInput`/`RecordConsentResult` removidos. Acessibilidade (`fieldset`/`legend`, `aria-expanded`, `role="alert"`) intacta, sem nenhuma alteração em `BlocoConsentimento.tsx`. Testes: `sign-up-with-consent.test.ts` reescrito (agora confirma que `authGateway.signUp` é chamado uma única vez, com a metadata de consentimento em `options.data`, e que erros do `signUp` continuam propagados); `TelaCriarConta.test.tsx` ajustado para não passar mais `consentGateway` (9 casos, mesmos cenários de antes, todos verdes); `tests/db/rls-profile-consent.test.mjs` com os 2 novos casos de integração descritos na nota de TASK-035, que cobrem exatamente o critério de aceite desta tarefa ("grava consent antes de qualquer dado pessoal") no nível de banco, não só de unidade. Verificação: mesmos comandos e resultados descritos na nota de TASK-035 acima (lint/typecheck/vitest/build/test:db/playwright, todos passando; `npx vitest run` isolado em `src/features/identity` — 93/93 verdes). Nota de implementação original preservada abaixo, com a ressalva de que a descrição de `recordConsent`/`ConsentGateway` como o mecanismo de gravação de `consent` foi superada pela correção acima: `src/features/identity/`. **Ordem crítica ("consent antes de qualquer dado pessoal")**: decisão documentada em `sign-up-with-consent.ts` — a leitura literal "antes do `signUp`" é impossível (`consent.user_id references auth.users(id)`, migration de TASK-027; o titular só existe depois que o Supabase Auth responde ao `signUp`). Interpretação mais fiel adotada: e-mail/senha do `signUp` são credenciais de identidade (não o dado pessoal sensível que RNF-05 regula — progresso/preferências/esboços que revelam convicção religiosa); `signUpWithConsent` (nova orquestração) chama `signUpWithEmailPassword` (TASK-035) e, **imediatamente em seguida, como o próximo passo do mesmo fluxo — nunca uma etapa posterior separada que o usuário poderia pular**, chama `recordConsent` (grava em `public.consent` via `ConsentGateway`, porta `Pick<SupabaseClient, "from">`, mesmo padrão de porta explícita de `AuthGateway`). Se o `signUp` falha, `consent` nunca é tocado (`from` nem é chamado — confirmado em teste). Se `signUp` funciona mas o insert de `consent` falha, o resultado é `"consent-failed"` (nunca sucesso parcial silencioso) e `TelaCriarConta` trata isso como falha do cadastro. Nenhuma escrita de perfil/progresso/migração (RN-07, TASK-055) acontece dentro desta tarefa nem é disparada por ela — só depois que `consent` já está gravado, por construção. `consent-text.ts`: `CONSENT_TEXT_VERSION`/`CONSENT_TEXT_FULL`/`CONSENT_TEXT_SHA256` como única fonte de verdade — a versão exibida pelo `BlocoConsentimento` é literalmente a mesma constante usada por `TelaCriarConta` ao chamar `signUpWithConsent`, nunca duas fontes; teste (`consent-text.test.ts`) recalcula o SHA-256 do texto literal com `node:crypto` e confirma que bate com a constante gravada, pegando qualquer edição do texto sem atualizar o hash/versão junto. `BlocoConsentimento.tsx`: componente **separado** de `TelaCriarConta` (recebe `aceito`/`onChange`/`erro` de fora, testado isoladamente em `BlocoConsentimento.test.tsx` sem nenhuma tela ao redor), desmarcado por padrão (`aceito` sempre decidido pelo pai — `TelaCriarConta` inicia com `useState(false)`), usa `Alternador` do Design System (Lote 4) como o "checkbox equivalente" pedido pela tarefa. Acessibilidade (achado do próprio UX-SPEC §5.1 "T-08 Consentimento como caixa única junto de termos — Violava 3.3.2/1.3.1", corrigido): `fieldset`/`legend` expressam a relação estrutural entre o controle e o texto (1.3.1, não só posição visual); o rótulo do `Alternador` é o texto de consentimento completo do resumo, sempre visível (3.3.2, nunca só placeholder/tooltip); "ler o texto completo" é um `button[aria-expanded]` nativo que revela o texto integral no próprio DOM (sem modal); erro de validação segue o padrão de `CampoDeTexto`/`CampoDeSenha` — ícone **e** texto, nunca só cor (DI-07), `role="alert"`, associado ao `Alternador` via `aria-describedby`. Sem `axe`/`jest-axe` no `package.json` (conferido antes de adicionar dependência nova, conforme pedido) — projeto já usa checagem estrutural via `@testing-library/react`/`jest-dom` em todos os primitivos do Lote 4 (`CampoDeTexto.test.tsx`, `Alternador.test.tsx`); mesmo padrão seguido aqui, sem introduzir dependência nova nesta tarefa; registrado como observação para o Coordenador/DI-14 (que cita "axe/Playwright"): nenhuma das duas está de fato integrada ao projeto ainda. `TelaCriarConta.tsx`: os 4 estados de UX-SPEC §4/DI-06 — **vazio** (formulário pronto, botão desabilitado com motivo em texto visível enquanto o consentimento não é marcado — UX-SPEC "sem ela o botão fica desabilitado com motivo em texto"); **carregando** (`disabled` nos campos e botão, texto "Criando conta…"); **erro** (e-mail já cadastrado/senha fraca/sem rede/consentimento não marcado, cada um com mensagem própria e foco movido ao campo relevante via `useEffect` + `ref`, inclusive fallback de exceção de rede tratado com `try/catch` ao redor de `signUpWithConsent`); **sucesso** (conta criada + estado de migração de progresso — RN-07 — exibido só quando o chamador informa `diasDeProgressoParaMigrar`; a migração em si é TASK-055, fora do escopo desta tarefa, documentado no componente). Testes: 8 (`consent-text.test.ts`) + 2 (`record-consent.test.ts`) + 4 (`sign-up-with-consent.test.ts`, cobrindo a ordem signUp→consent, o caso de erro sem tocar consent, e `consent-failed`) + 9 (`BlocoConsentimento.test.tsx`) + 9 (`TelaCriarConta.test.tsx`, um por estado/mensagem de erro) = 26 novos, todos verdes isolados e na suíte completa. `npm run lint` (eslint limpo nos arquivos desta tarefa; `lint:deps` — "no dependency violations found") e `npm run typecheck` confirmados sem nenhum erro atribuível a esta tarefa. `npm run build`/`npx vitest run` completos executados sobre a árvore de trabalho compartilhada do lote: apresentam erros/timeout pré-existentes **fora do escopo desta tarefa** — `create-identity-supabase-client.ts`/`.test.ts` e `dexie-auth-storage.test.ts` (trabalho em andamento de TASK-038, `src/vite-env.d.ts` e `src/core/storage/*` aparecem como modificados/não commitados na árvore) e o timeout intermitente já rastreado de `REFAT-L0-01`; nenhum arquivo desta tarefa (`src/features/identity/{BlocoConsentimento,TelaCriarConta,consent-*,record-consent,sign-up-with-consent}*`) aparece nesses erros — confirmado filtrando a saída e isolando `npx vitest run` dos arquivos novos (26/26 verdes) e `npx eslint` direcionado aos arquivos desta tarefa (limpo). |
| TASK-037 | Tela T-09 (Entrar/recuperar acesso): senha + link mágico + recuperação, sem CAPTCHA de quebra-cabeça | e2e de login, recuperação e mensagem de erro sem revelar existência do e-mail | TASK-035 | TASK-036, TASK-038 | Concluída — `src/features/identity/`. Porta própria e mínima `SignInGateway` (`sign-in-gateway.ts`, `Pick<SupabaseClient["auth"], "signInWithPassword" \| "signInWithOtp" \| "resetPasswordForEmail">`), deliberadamente separada de `AuthGateway` (TASK-035) para não editar um tipo compartilhado tocado em paralelo por TASK-036/038 no mesmo lote. **Guardrail central da tarefa (critério de aceite)**: `classify-auth-error.ts` (`classifyAuthError`) é uma classificação deliberadamente grosseira — só reconhece `"rate-limited"` (HTTP 429) e `"network-error"` (erro sem status HTTP/mensagem de fetch/rede) como motivos distintos; **todo o resto cai no mesmo balde `"invalid-credentials"`**, sem nenhuma inspeção de texto tentando separar "e-mail não encontrado" de "senha incorreta" — normalização feita no cliente, não depende de o Supabase já devolver (na prática) a mesma mensagem genérica para os dois casos. `sign-in-with-password.ts` usa esse balde único para login por senha. `sign-in-with-magic-link.ts` (`signInWithOtp` com `shouldCreateUser: false` — link mágico nunca cria conta implicitamente, isso é escopo de T-08/TASK-036) e `request-password-reset.ts` (`resetPasswordForEmail`) vão além: **qualquer erro que não seja `rate-limited`/`network-error` é colapsado no mesmo resultado de sucesso aparente** (`"request-sent"`) — inclusive o erro real que o Supabase Auth devolve para e-mail sem conta com `shouldCreateUser:false` — para que a UI mostre a mensagem genérica "se esse e-mail existir, você recebe..." de forma idêntica exista ou não a conta, e não apenas confiando que o `resetPasswordForEmail` do Supabase já não diferencia isso por padrão. `parse-auth-callback-error.ts` cobre separadamente o estado "link expirado" do UX-SPEC (lido dos parâmetros de erro que o Supabase anexa ao redirect de um link mágico/recuperação usado/expirado — `error_code=otp_expired` — nunca de uma resposta de API; não é sobre existência de e-mail, é sobre validade temporal do link). `TelaEntrar.tsx`: um único componente com 3 modos internos (senha/link mágico/recuperação, troca sem perder o e-mail já digitado), consumindo `CampoDeTexto`/`CampoDeSenha`/`Botao` do Design System (Lote 4); mapeia cada `SignInErrorReason` para exatamente uma string fixa (`MENSAGENS_DE_ERRO_DE_ACESSO`), nunca construída a partir do texto de erro do Supabase. Sem CAPTCHA (UX-SPEC 3.3.8): nenhum mecanismo anti-robô foi adicionado — nada no SDD.md/GUARDRAILS.md exige um, e o único controle de abuso é o limite de tentativas do próprio servidor, já refletido como o estado `"rate-limited"`; teste dedicado confirma ausência de `iframe`/texto "captcha" no DOM. Os 4 estados de UX-SPEC §4/DI-06: vazio N/A (mesma linha do UX-SPEC.md — a tela sempre mostra o formulário); carregando (botão "Entrando…"/"Enviando…", campos e alternativas desabilitados); erro (credencial inválida genérica, sem rede, limite de tentativas, link expirado — 4 mensagens fixas, nenhuma citando existência de conta); sucesso (login → "Sessão ativa" + `onSignedIn`; link mágico/recuperação → confirmação genérica). Testes: 35 novos — 6 (`classify-auth-error.test.ts`), 7 (`sign-in-with-password.test.ts`, incluindo o caso lado a lado "senha errada de conta existente" vs. "e-mail inexistente" com resultado idêntico), 4 (`sign-in-with-magic-link.test.ts`), 4 (`request-password-reset.test.ts`), 4 (`parse-auth-callback-error.test.ts`) e 7 (`TelaEntrar.test.tsx`, jsdom+Testing Library, cobrindo carregando/erro/sucesso dos 3 modos e a comparação direta de string da mensagem de erro entre os dois cenários do critério de aceite) — todos verdes isolados e na suíte completa. e2e real (`e2e/identity-signin.spec.ts`, Playwright, contra o Supabase local de verdade — mesmo padrão de `identity-signup.spec.ts`/TASK-035: exercita as funções diretamente, não uma rota montada, porque `src/features/shell/App.tsx` ainda é um placeholder sem roteador, fora do escopo desta tarefa): (1) login com senha correta contra conta real criada e confirmada no próprio teste; (2) recuperação de senha confirmada por e-mail real capturado no Mailpit local (assunto "Reset your password", corpo com link `type=recovery`); (3) critério de aceite central — `signInWithPassword` com senha errada de conta real e com e-mail que nunca existiu retornam **exatamente o mesmo** `{status:"error", reason:"invalid-credentials"}`, comparado byte a byte no teste. `npm run lint` (eslint + `lint:deps`, "no dependency violations found"), `npm run typecheck` (`tsc -b --noEmit`, sem erro), `npm run build` (bundle gerado limpo) e `npx vitest run` (302 testes; 301 verdes, 1 timeout intermitente pré-existente de `REFAT-L0-01` sob execução concorrente, reproduzido como passa isolado, sem relação com esta tarefa) confirmados localmente; `npx playwright test` (suíte completa, 7/7 verdes, incluindo os 3 novos desta tarefa) confirmado contra o Supabase local real. Observação operacional (mesmo padrão de concorrência já documentado por TASK-002/003/036 no Lote 0/6): durante a execução, `npm run typecheck`/`npm run build` mostraram, em rodadas intermediárias, erros transitórios em arquivos de TASK-038 (`create-identity-supabase-client.ts`/`.test.ts`, `dexie-auth-storage.test.ts` — `Property 'env' does not exist on type 'ImportMeta'`, `Cannot find name 'window'`) por essa tarefa estar em edição concorrente no mesmo checkout; nenhum desses arquivos pertence a TASK-037, e as rodadas finais (após TASK-038 estabilizar) confirmaram os dois comandos limpos, sem nenhum erro atribuível a esta tarefa em nenhum momento. |
| TASK-038 | Sessão: token de vida curta com refresh rotativo, sem segredo extra no dispositivo (DI-10) | `localStorage` sem dado do usuário; refresh automático testado | TASK-035 | TASK-036, TASK-037 | Concluída — escopo puramente de gestão de sessão/token (nenhuma tela de UI; T-08/T-09 são TASK-036/TASK-037, em paralelo). Achado central desta tarefa: o cliente Supabase JS, com a config padrão de TASK-035 (`createClient(url, anonKey)` sem `auth.storage`), persistiria a sessão (`access_token`/`refresh_token`) em `globalThis.localStorage` — violação direta de DI-10/G-11 ("`localStorage` nunca recebe... segredo além do necessário"). Resolvido com storage customizado via Dexie, não com uma exceção documentada: `src/features/identity/dexie-auth-storage.ts` (`createDexieAuthStorage(db)`) implementa a porta mínima `getItem`/`setItem`/`removeItem` esperada por `GoTrueClientOptions.storage` (`SupportedStorage` do SDK — deliberadamente não importado aqui, mesmo padrão de porta própria já usado por `AuthGateway`/`ConsentGateway`), gravando em `core/storage` (IndexedDB via Dexie) em vez de `localStorage`. Store nova `authSession` (chave/valor opaco — `key`/`value`/`updatedAt`) em `core/storage`: `types.ts` (`AuthSessionRecord`), `db.ts` (schema v3, `AppDatabase.authSession`, sem função `upgrade` — store nova, nada a migrar), `index.ts` (export). `create-identity-supabase-client.ts` (`createIdentityAuthGateway`) passou a configurar `createClient(url, anonKey, { auth: { storage: createDexieAuthStorage(db), persistSession: true, autoRefreshToken: true, detectSessionInUrl: true } })` — `db` e `fetch` agora são um segundo parâmetro opcional (`CreateIdentityAuthGatewayOptions`, retrocompatível com a assinatura `(config)` de TASK-035/já em uso por TASK-036/TASK-037 em paralelo) com padrão lazy/memoizado (`getDefaultAppDatabase()`) para não abrir uma conexão IndexedDB nova a cada chamada sem argumento explícito. Refresh automático: nada foi reimplementado — `autoRefreshToken: true` só torna explícito o já-padrão do SDK (`GoTrueClient#_autoRefreshTokenTick`/`_startAutoRefresh`, timer de 30s em segundo plano). Testes (18 novos): `dexie-auth-storage.test.ts` (2 casos, `fake-indexeddb/auto` + `jsdom`) confirma get/set/remove persistindo de fato em Dexie (`db.authSession`) com `window.localStorage.length === 0` em cada ponto de checagem, inclusive isolamento entre chaves (sessão principal vs. verifier PKCE). `create-identity-supabase-client.test.ts` (2 casos, `fetch` fake interceptando só `/token?grant_type=password`/`grant_type=refresh_token` do Supabase Auth real — sem depender de `supabase start`/rede): (a) após `signInWithPassword` real (via SDK completo, não um fake de `AuthGateway`), `window.localStorage.length` continua `0` e a sessão está de fato em `db.authSession`; (b) com `expires_in: 60` (dentro de `EXPIRY_MARGIN_MS` = 90s do SDK), o próprio mecanismo automático do SDK (`_startAutoRefresh` → tick agendado no próximo turno do event loop, **não** chamado manualmente pelo teste — `vi.waitFor` só espera o efeito) troca o `access_token`/`refresh_token` sem novo login, com `localStorage` seguindo vazio; uma segunda checagem chamando `_autoRefreshTokenTick` diretamente confirma idempotência (sem refresh redundante quando a sessão não está mais perto de expirar). `src/core/storage/db.test.ts` estendido (não novo): store `authSession` no smoke test de schema e no teste de migração real v1→v2 (ajustado para `verno` 3, já que v3/`authSession` agora é o schema de produção — migração v1→v2 continua validada, só o número de versão final mudou). Achado de infraestrutura de tipos corrigido como parte necessária desta tarefa (não escopo funcional, mas bloqueava `npm run typecheck` para qualquer teste `.test.ts` com `jsdom`/DOM, inclusive os novos desta tarefa): `tsconfig.node.json` (usado por `tsc -b` para todo `src/**/*.test.ts`) não tinha lib `DOM` nem incluía `src/vite-env.d.ts` — corrigido adicionando `"DOM"`/`"DOM.Iterable"` a `lib` e `src/vite-env.d.ts` a `include`; mudança aditiva, sem remover nada usado por outras tarefas/instâncias em paralelo. `npm run lint` (eslint + `lint:deps`, "no dependency violations found"), `npm run typecheck` (`tsc -b --noEmit`, sem erro — inclusive o erro pré-existente de `ImportMeta.env` em `create-identity-supabase-client.ts` sob `tsc -b`, causado pela lacuna de `include` acima, ficou corrigido) e `npm run build` (bundle gerado limpo) confirmados. `npx vitest run` completo: 301/302 verdes — único vermelho é o timeout intermitente já rastreado de `REFAT-L0-01` (`src/tools/dependency-rule.test.ts`, default de 5000ms insuficiente sob a base de código já maior; passa em 20s), reproduzido isoladamente (não regressão desta tarefa, confirmado rodando com `--testTimeout=20000`). Todos os 18 testes desta tarefa e os 82 de `src/features/identity`+`src/core/storage` juntos passam limpos. |
| TASK-039 | Tela T-13: exportação de dados em JSON (CA-10.4) | e2e baixa JSON com progresso/preferências/esboços | TASK-038 | TASK-040 | Concluída — implementa só a metade de **exportação** de T-13 (`src/features/identity/`); a metade de exclusão é TASK-040, feita em paralelo pela mesma convenção de nome já usada pela outra instância (`BlocoExcluirConta.tsx`), então este trabalho ficou em `BlocoExportarDados.tsx` — um subcomponente independente, testável isolado, que **não** monta a tela T-13 inteira. Uma `TelaContaDados.tsx`/`TelaT13.tsx` de composição (fora do escopo de ambas as tarefas) empilha os dois blocos depois, resolvendo `db`/`userId`/gateways a partir da sessão (TASK-038) e passando para cada bloco — evitando as duas tarefas em paralelo editarem o mesmo arquivo de tela; comentário de coordenação deixado no topo de `BlocoExportarDados.tsx`. **Coleta de dados** (`export-data.ts`, `collectDataExportPayload(db)`): lê `progress`/`preferences` de `core/storage` (Dexie, Lote 3) e monta um payload JSON com `schemaVersion`, `exportadoEm`, `progresso`, `preferencias`, `esbocos` e `notas.esbocos`. **Decisão sobre `outlines`/esboços** (pedida explicitamente por esta tarefa): `core/storage` ainda não tem as stores `outlines`/`outlineSnapshots` (Fase 2, Lote 14, TASK-070) — o campo `esbocos` do JSON exportado é **sempre `[]`** nesta fase, tipado como `readonly never[]` (força erro de compilação no dia em que alguém tentar popular sem atualizar o tipo) e acompanhado de um campo irmão `notas.esbocos` no próprio JSON explicando por escrito, para o titular que abrir o arquivo, que a ausência não é perda de dado — só populado quando a store existir; o formato do arquivo (o campo já existe hoje) não muda quando isso acontecer, só o conteúdo. **Download** (`download-json.ts`): `downloadJsonFile(data, filename)` — `Blob` + `URL.createObjectURL` + `<a download>` sintético + `revokeObjectURL`, sem SDK de terceiro (DI-15); `buildExportFilename(now?)` gera `meus-dados-estudobiblico-AAAA-MM-DD.json`. **Componente** (`BlocoExportarDados.tsx`): botão "Exportar meus dados" chama `collectDataExportPayload` e depois `onDownload` (injetável para teste; padrão `downloadJsonFile` real) — `onDownload` pode devolver `Promise`, e o estado "carregando" só sai quando ela resolve. **Estados (DI-06)**: a própria tabela do UX-SPEC.md (Seção 4) já marca "T-13 Vazio: N/A" sem motivo escrito na tabela — motivo aplicado aqui, por escrito, como pede DI-06: a exportação sempre produz um JSON estruturalmente válido (mesmo com `progresso`/`preferencias` vazios e `esbocos` sempre `[]`), então não existe um estado "nada para exportar" distinto de sucesso; os outros três estados são implementados literalmente como o UX-SPEC define — carregando ("Preparando exportação…", botão desabilitado), erro (`role="alert"`, "Não conseguimos preparar seus dados agora", botão volta a ficar habilitado para nova tentativa) e sucesso (`role="status"`, "Arquivo baixado (nome-do-arquivo)"). Nenhuma informação só por cor (DI-07): erro e sucesso têm ícone (⚠/✓) e texto. Exportado por `src/features/identity/index.ts`. Testes (17 novos): `export-data.test.ts` (3 casos, `fake-indexeddb/auto` — payload válido sem dado nenhum, payload com progresso/preferências reais, round-trip `JSON.stringify`/`JSON.parse`); `download-json.test.ts` (3 casos, `@vitest-environment jsdom` — Blob/clique/revoke, conteúdo serializado correto, nome de arquivo com data); `BlocoExportarDados.test.tsx` (4 casos, RTL — estado inicial, carregando, sucesso com asserção do payload real passado a `onDownload`, erro real forçando falha de leitura via `db.close()` antes do clique) — todos verdes isolados e na suíte completa. **e2e real** (`e2e/identity-export-data.spec.ts`, Playwright, 2 casos): como `src/features/shell/App.tsx` ainda é um placeholder sem roteador (Lote 0), não existe rota real para navegar até T-13 num navegador de verdade, e este critério de aceite específico ("e2e baixa JSON") só pode ser provado com um download de navegador real (`Blob`/`<a download>` não funcionam de fato em jsdom) — diferente do padrão de TASK-035/037 (que testam a função/gateway direto, sem DOM nenhum), aqui foi criado um harness dev-only (`e2e/fixtures/exportar-dados.html` + `e2e/fixtures/mount-exportar-dados.tsx`, comentário de topo explica a decisão) que abre um `AppDatabase` real nomeado por query string (`?dbName=...`, evita colisão entre testes paralelos), opcionalmente semeia progresso/preferência conhecidos (`?seed=1`) e monta `BlocoExportarDados` de verdade; o harness não é alcançável a partir de `index.html`/`vite build` (confirmado: `npm run build` mostra só os 19 módulos do shell real, harness ausente do bundle de produção). O teste clica em "Exportar meus dados", captura o download real via `page.waitForEvent("download")`, lê o stream do arquivo baixado e confirma JSON válido com `progresso`/`preferencias`/`esbocos` batendo com o que foi semeado (1º caso) e com os três campos presentes e vazios quando não há dado local nenhum (2º caso, conta recém-criada). `tsconfig.node.json` ganhou `"jsx": "react-jsx"` e `e2e/**/*.tsx` no `include` (aditivo — só o necessário para o harness ser type-checado; nada removido) — confirmado que não quebra nenhum arquivo `.ts` de e2e já existente. `npm run lint` (eslint + `lint:deps`, "no dependency violations found"), `npm run typecheck` (`tsc -b --noEmit`, sem erro) e `npm run build` confirmados limpos. `npx vitest run`: 323/325 verdes — os 2 vermelhos são o timeout intermitente já rastreado de `REFAT-L0-01` (`src/tools/dependency-rule.test.ts`, sem relação com esta tarefa), reproduzido isolado como passa (4/4 verdes, 7.6s). `npx playwright test` (suíte completa, 11/11 verdes, incluindo os 2 novos desta tarefa e os de TASK-040 em paralelo — nenhum conflito de arquivo/estado entre as duas tarefas). |
| TASK-040 | Tela T-13: fluxo de exclusão ("EXCLUIR", grava `erasure_request` com `due_at`, estado visível) | e2e cria `erasure_request` com `due_at = +15 dias` | TASK-038, TASK-032 | TASK-039 | Concluída — `src/features/identity/`, subcomponente **próprio** `BlocoExcluirConta.tsx` (deliberadamente separado da tela T-13 inteira: a outra metade, "Baixar meus dados", é TASK-039/`BlocoExportarDados.tsx`, em execução paralela no mesmo lote; uma futura `TelaContaEDados.tsx`, fora do escopo de ambas, compõe os dois blocos a partir da sessão de TASK-038 — isso evitou as duas tarefas em paralelo editarem o mesmo arquivo de tela). Camada de dados nova, no mesmo padrão de porta explícita já usado por `AuthGateway`/`ConsentGateway`: `erasure-request-gateway.ts` (`ErasureRequestGateway = Pick<SupabaseClient, "from">`), `request-account-erasure.ts` (`requestAccountErasure` — INSERT em `public.erasure_request` só com `user_id`; **nunca envia `due_at`**, mesmo que o SDK permitisse — o trigger `set_erasure_request_due_at` de TASK-032 sempre recalcula no servidor, `requested_at + 15 dias`; `select("requested_at, due_at").single()` encadeado no mesmo INSERT devolve a data-limite real sem uma segunda ida ao banco) e `get-erasure-request-status.ts` (`getErasureRequestStatus` — lê o pedido do próprio titular via `erasure_request_select_own`, RLS de TASK-032; usado por `BlocoExcluirConta` ao montar para o estado "exclusão solicitada" continuar visível depois de um recarregamento de página, em vez de só aparecer uma vez após o POST). `erasure-confirmation-word.ts`: `PALAVRA_DE_CONFIRMACAO_EXCLUSAO = "EXCLUIR"` isolada em módulo sem import de React/CSS module — necessário porque `e2e/identity-erasure.spec.ts` roda fora de qualquer bundler (Playwright puro) e travava (`SyntaxError` em `.module.css`) ao importar a constante direto de `BlocoExcluirConta.tsx`, que arrasta `Botao.module.css`/`CampoDeTexto.module.css`; `BlocoExcluirConta` importa e reexporta a mesma constante, nunca duas fontes. Fricção deliberada do UX-SPEC T-13 ("confirmação digitando a palavra 'EXCLUIR'"): o botão só habilita quando o texto digitado é **exatamente igual** à constante — nunca exclusão de um clique. UX-SPEC T-13 não prevê cancelamento do pedido ("Após confirmar, a conta entra em estado 'exclusão solicitada' com a data-limite visível", sem menção a cancelar) — não inventado aqui. Os 4 estados de UX-SPEC §4/DI-06 (mapeados 1:1 na linha "T-13 Conta e dados" da tabela de estados): **vazio** N/A (mesmo padrão já adotado por `TelaEntrar`/TASK-037 — a tela sempre mostra o formulário ou o estado atual); **carregando** (verificação inicial do estado ao montar + envio do pedido, `role="status"`); **erro** (falha ao verificar OU ao enviar — ícone **e** texto nunca só cor, DI-07, `role="alert"`, foco movido via `useEffect`+`ref`; mensagem de erro de envio é explícita — "seu pedido continua pendente" — e o texto já digitado não se perde, então o usuário só clica de novo, sem redigitar "EXCLUIR"); **sucesso** (data-limite do servidor visível, tanto logo após o POST quanto ao recarregar a página com um pedido pré-existente, via `getErasureRequestStatus`). Testes: 4 (`request-account-erasure.test.ts`, incluindo confirmação explícita de que o corpo do INSERT nunca contém `due_at`) + 3 (`get-erasure-request-status.test.ts`) + 6 (`BlocoExcluirConta.test.tsx`, jsdom+Testing Library, cobrindo os 4 estados e a fricção de digitação) = 13 novos, todos verdes isolados e na suíte completa. e2e real (`e2e/identity-erasure.spec.ts`, Playwright, contra o Supabase local de verdade, mesmo padrão de `identity-signup.spec.ts`/`identity-signin.spec.ts`: exercita as funções diretamente — não uma rota montada, já que `src/features/shell/App.tsx` segue placeholder sem roteador, fora do escopo desta tarefa): (1) critério de aceite central — cria conta real e confirma (Mailpit local), simula "digita EXCLUIR, confirma" checando a mesma constante que habilita o botão do componente, chama `requestAccountErasure` e confirma `due_at - requested_at === 15 dias` exatos, tanto no retorno da função quanto numa consulta REST direta subsequente (RLS) quanto via `getErasureRequestStatus` (estado visível depois de "recarregar"); (2) guardrail extra, não pedido literalmente pelo critério de aceite mas alinhado ao guardrail central da tarefa ("o cliente não pode gravar due_at diretamente") — POST manual via REST com `due_at` manipulado (100 anos no futuro) confirma que o trigger do servidor sobrescreve e o valor gravado continua sendo `requested_at + 15 dias`, nunca o valor enviado. `npm run lint` (eslint limpo nos arquivos desta tarefa — achado pré-existente fora do escopo em `download-json.test.ts`, de TASK-039 em edição concorrente, não corrigido aqui) e `npm run typecheck` (`tsc -b --noEmit`, sem erro) confirmados. `npm run build` (bundle gerado limpo) e `npx vitest run --testTimeout=20000` (325/325 verdes, suíte completa incluindo os 13 novos desta tarefa e os arquivos de TASK-039 em paralelo; sem `--testTimeout` estendido, `dependency-rule.test.ts` mostra o timeout intermitente já rastreado de `REFAT-L0-01` sob execução concorrente, sem relação com esta tarefa) confirmados localmente. `npx playwright test identity-erasure.spec.ts` (2/2 verdes) confirmado contra o Supabase local real. |

### Refatoração Lote-6

> Criada pelo Validador (chapéus QA + DevSecOps) durante o fechamento
> estrutural do Lote 6 (2026-09-08), para achado de severidade baixa-média
> que não compromete o critério de aceite central de TASK-037/038/039/040
> nem bloqueia por si só nenhuma outra tarefa do lote — ver
> `.md/QA-REPORT.md` e `.md/SECURITY-REVIEW.md`, seção "Lote 6". Os 2
> achados críticos do lote (consentimento não garantido por política de
> banco; regressão em `npm run test:db`) **não** entram aqui — voltaram
> TASK-035/TASK-036 para `Em andamento`, acima.

| ID | Tarefa | Critério de aceite | Depende de | Paralelizável-com | Status |
|---|---|---|---|---|---|
| REFAT-L6-01 | `minimum_password_length = 6` em `supabase/config.toml` é o piso mínimo aceito pela ferramenta, não um valor deliberado; nenhuma verificação de senha vazada (leaked-password protection) configurada — recurso tipicamente disponível só no projeto Supabase hospedado, não em `supabase/config.toml` local nesta versão da CLI (SDD.md §7.1/§7.5, "senha mínima e verificação de vazamento conhecido") | (a) Elevar `minimum_password_length` para 8+ em `supabase/config.toml` (mudança de config local, baixo risco, pode ser feita já); (b) habilitar leaked-password protection no painel do projeto Supabase hospedado antes do go-live (requisito operacional para o chapéu DevOps, junto do domínio exato de `connect-src` já registrado em TASK-034/`REFAT-L5-01`) | TASK-035 | REFAT-L6-02 | Pendente |
| REFAT-L6-02 | `axe`/`jest-axe` (ou equivalente) ainda não integrado ao projeto — as telas de identidade (T-08/T-09/T-13, TASK-036/037/039/040) foram validadas só por checagem estrutural via Testing Library (fieldset/legend, aria-*, role="alert"), não por um auditor de acessibilidade automatizado, como DI-14 exige ("Testes de acessibilidade e de contraste são automatizados (axe/Playwright...), nunca inspeção manual isolada") | Integrar `@axe-core/playwright` (ou `jest-axe` para os testes de componente) e rodar contra pelo menos T-08/T-09/T-13; achado é cross-lote (Lote 4 em diante), mas registrado aqui por ter sido o primeiro lote com telas reais o bastante para o gap importar de fato | TASK-036, TASK-037, TASK-039, TASK-040 | REFAT-L6-01 | Pendente |

### Lote 7 — Rotina de Exclusão

| ID | Tarefa | Critério de aceite | Depende de | Paralelizável-com |
|---|---|---|---|---|
| TASK-041 | `jobs/erasure`: execução de exclusão em cascata dentro do SLA (RN-11) | Fixture de `erasure_request` vencida confirma cascata em todas as tabelas do titular | TASK-033, TASK-040 | TASK-042 |
| TASK-042 | Teste: credencial de serviço nunca aparece no bundle do cliente (DI-09) | Grep automatizado no bundle final falha se a chave aparecer | TASK-005 | TASK-041 |

### Lote 8 — Leitura

| ID | Tarefa | Critério de aceite | Depende de | Paralelizável-com |
|---|---|---|---|---|
| TASK-043 | Tela T-04 (Índice do corpus): listas AT/NT + filtro por nome | e2e navega e filtra | TASK-009, TASK-023 | TASK-045 |
| TASK-044 | Tela T-05 (Leitor de capítulo): numeração de versículo acessível, nota ancorada ao ponto do texto | Teste de leitor de tela (aria-label por versículo) + nota em `<aside>` | TASK-043, TASK-015 | — |
| TASK-045 | Tela T-06 (Atribuição da licença), a partir do manifesto | e2e mostra data da versão, titulares, link CC BY 4.0; nunca contém string proibida | TASK-008, TASK-026 | TASK-043 |
| TASK-046 | Tela T-05: estados vazio/erro (capítulo inválido; "ainda não está neste aparelho" + lista de disponíveis) | e2e simula os dois estados (C-01 UX-SPEC) | TASK-044 | — |

### Lote 9 — Plano

| ID | Tarefa | Critério de aceite | Depende de | Paralelizável-com |
|---|---|---|---|---|
| TASK-047 | Tela T-01 (Vitrine): dia 1 estático, CTA "Começar meu plano", sem login | e2e renderiza sem rede (pré-renderizado) | TASK-015, TASK-023 | TASK-048 |
| TASK-048 | Tela T-02 (Dia do plano): AT/NT + nota + "Concluir dia" fixo + constância | e2e conclui dia; rodapé fixo não cobre foco (2.4.11) | TASK-015, TASK-017 | TASK-047 |
| TASK-049 | RN-09: cálculo do dia corrente ("o plano anda com o usuário") | Teste unitário cobre usuário que pula dias | TASK-048, TASK-020 | — |
| TASK-050 | Tela T-03 (Dia concluído): `GanchoDeContinuidade` + `IndicadorDeConstancia`, foco no título | Teste de foco/ordem do DOM | TASK-049 | TASK-051, TASK-052 |
| TASK-051 | Tela T-10 (Progresso e constância): faixa de 30 dias com rótulo acessível por quadrado | Teste de acessibilidade da faixa (1.4.1) | TASK-049 | TASK-050, TASK-052 |
| TASK-052 | Tela T-14 (Conclusão do plano, dia 90): comentário opcional, dispensa permanente (CA-20.4) | e2e dispensa e confirma que não reaparece | TASK-049 | TASK-050, TASK-051 |
| TASK-053 | T-01: variante "continue no dia N" com progresso local sem conta (L-04) | e2e usuário com progresso local vê "continue" | TASK-047, TASK-049 | — |

### Lote 10 — Soft Gate e Conversão

| ID | Tarefa | Critério de aceite | Depende de | Paralelizável-com |
|---|---|---|---|---|
| TASK-054 | Tela T-07 (Convite de conta): regra de frequência (1ª conclusão, depois 1/3, nunca 2×/dia, nunca em T-20) | Teste unitário da regra com sequência de conclusões | TASK-050, TASK-035 | — |
| TASK-055 | Integração T-07→T-08→T-03 com migração de progresso (RN-07) e recusa sem perda (CA-10.5) | e2e cobre os 3 desfechos do fluxograma §1.1 | TASK-054, TASK-036, TASK-022 | — |

### Lote 11 — Engajamento e Lembrete

| ID | Tarefa | Critério de aceite | Depende de | Paralelizável-com |
|---|---|---|---|---|
| TASK-056 | `features/engagement`: assinatura Web Push (VAPID) | e2e assina e grava `push_subscription` | SPIKE-01, TASK-030, TASK-038 | — |
| TASK-057 | `jobs/reminder`: seleção/envio de lembretes devidos + `reminder_log` | Fixture gera lembrete devido e confirma log de desfecho (CA-05.7) | TASK-056 | — |
| TASK-058 | Tela T-12 (Lembrete e canal): prévia real do gancho, estado real do canal | e2e mostra os 3 estados de canal (§1.2) | TASK-057 | TASK-059 |
| TASK-059 | Tela T-15 (Instalar na tela de início): variantes lembrete/púlpito, instruções por plataforma | e2e mostra texto correto por ramo de entrada | TASK-057 | TASK-058 |
| TASK-060 | Tela T-11 (Configurações): índice completo, desativar lembrete em 2 toques (CA-05.6) | e2e conta toques até desativar = 2 | TASK-058, TASK-026 | — |
| TASK-061 | Gancho in-app garantido nos 3 caminhos do fluxograma §1.2 | Teste unitário cobre os 3 ramos convergindo em `HOOK` | TASK-058, TASK-059 | — |

### Lote 12 — Telemetria

| ID | Tarefa | Critério de aceite | Depende de | Paralelizável-com |
|---|---|---|---|---|
| TASK-062 | `features/telemetry`: fila local de eventos (`eventQueue`), inclusive pré-conta | Teste unitário enfileira evento sem conta | TASK-016 | — |
| TASK-063 | Agregado anônimo sem sujeito, via função de incremento (M-06) | e2e sem conta gera incremento no agregado, sem linha identificável | TASK-062, TASK-031 | — |
| TASK-064 | Migração de eventos anônimos ao consentir, preservando `occurred_at` (M-07) | Teste migra fila local com timestamp original | TASK-063, TASK-035 | — |
| TASK-065 | Wiring de emissão de eventos nos pontos de instrumentação (D30, M-02, M-03, M-04, M-08 — RF-02/03/05/09/12/15/19/20) | Teste de integração confirma emissão de cada evento no ponto correto | TASK-064 | — |

> Nota de tamanho/canário sobre TASK-065: por tocar oito pontos de instrumentação
> diferentes já existentes (chamada de função já pronta em 062–064), o contexto de
> trabalho previsto fica em torno de 120–180k tokens (lê 8 componentes de feature
> já implementados + 1 módulo de telemetria) — abaixo do canário de 300k, mas é o
> item deste documento mais próximo do teto. Se, ao chegar a hora, alguma das oito
> features estiver marcadamente maior que o previsto no SDD.md, dividir por
> fase/módulo (Fase 1 leitor vs. Fase 2 líder) antes de iniciar.

### Lote 13 — PWA Offline (Fase 1)

| ID | Tarefa | Critério de aceite | Depende de | Paralelizável-com |
|---|---|---|---|---|
| TASK-066 | Estratégias de cache Workbox por artefato (shell/corpus/conteúdo), cache imutável + hash | e2e offline após 1ª visita navega em conteúdo já visitado | TASK-003, TASK-008 | — |
| TASK-067 | `storage.persist()` na entrada do app, com fallback declarado | Teste confirma chamada de `persist()` e fallback quando não suportado | SPIKE-02, TASK-016 | — |
| TASK-068 | Smoke e2e offline de Fase 1 (T-01/T-02 com rede desligada e cache limpo) | Playwright confirma T-01/T-02 funcionam a partir do cache | TASK-066, TASK-048 | — |

### Lote 14 — Fase 2: Esboço (`features/outline`)

| ID | Tarefa | Critério de aceite | Depende de | Paralelizável-com |
|---|---|---|---|---|
| TASK-069 | Migration `outline` + `outline_version` (JSONB) com RLS | RLS testada; constraints de `rev` testadas | TASK-033 | — |
| TASK-070 | `core/storage`: stores `outlines`/`outlineSnapshots`/`presentationState` (Fase 2) | `presentationState` nunca aparece na outbox | TASK-069, TASK-018 | — |
| TASK-071 | `core/sync`: conciliação do esboço (LWW no `rev` + versão perdedora preservada 30 dias) | Edição concorrente simulada confirma versão perdedora em `outline_version` | TASK-070, TASK-020 | — |
| TASK-072 | Tela T-16 (Entrada do Módulo 2): retomar até 4h, apresentar agora, lista curta, selo real | e2e cobre os três CTAs | TASK-071 | TASK-073 |
| TASK-073 | Tela T-17 (Lista de esboços): ordenada por edição, menu (apresentar/duplicar/versões/excluir) | e2e cobre cada ação do menu | TASK-071 | TASK-072 |
| TASK-074 | RF-19: duplicar esboço (copia conteúdo e estado de materialização) | Teste duplica e confirma `rev` independente | TASK-073 | — |
| TASK-075 | Tela T-18: `BlocoDeEsboco` (4 tipos fechados, RN-12), salvamento automático, reordenação com botões ↑/↓ | e2e cria/edita/reordena e confirma persistência local automática | TASK-070, TASK-010 | — |
| TASK-076 | `ReferenciaEmbutida`: auto-embed com nota (CA-13.2), três remoções independentes (texto/referência/nota — CA-13.5) | e2e cobre as três remoções | TASK-075, TASK-010 | — |
| TASK-077 | `AvisoDeReferenciaNaoLida`: motivo mapeado do parser, texto do usuário intacto (CA-13.4) | Referência inválida confirma texto preservado + motivo exibido | TASK-076 | — |
| TASK-078 | `EstadoDeSincronizacao`: "salvo no aparelho" ≠ "sincronizado" (CA-12.4) | e2e simula falha de rede e confirma rótulo correto | TASK-071, TASK-075 | — |
| TASK-079 | Tela T-21 (Versões recuperáveis): lista, leitura somente, restaurar cria nova entrada | e2e restaura versão antiga sem destruir a atual | TASK-071, TASK-073 | — |

### Lote 15 — Fase 2: Modo Apresentação (`features/presentation`)

| ID | Tarefa | Critério de aceite | Depende de | Paralelizável-com |
|---|---|---|---|---|
| TASK-080 | `SeloDeMaterializacao`: verificação real do snapshot (pronto/baixando/pendente) | Teste confirma verificação real, não valor lembrado | TASK-071 | TASK-081 |
| TASK-081 | Provisionamento do corpus integral em segundo plano na entrada do Módulo 2 (CA-13.3) | UI responsiva durante o provisionamento (não bloqueia digitação) | TASK-070, TASK-009 | TASK-080 |
| TASK-082 | Materialização do snapshot autocontido (blocos + versículos + notas + versões) | Apresentação não lê nenhuma outra store (RNF-01) | TASK-070, TASK-075, TASK-076 | — |
| TASK-083 | Tela T-19 (Preparo para apresentar): checklist, barra "X de Y cartões", "Apresentar assim mesmo"/"Esperar terminar" | e2e cobre os 3 estados (pronto/baixando/pendente) | TASK-080, TASK-081, TASK-082 | — |
| TASK-084 | Tela T-20: `CartaoDeApresentacao` + navegação (toque/arrastar/teclado/Page Up-Down), zonas 40/20/40 | e2e navega por todos os métodos | TASK-082, TASK-083 | — |
| TASK-085 | Tela T-20: fila de interrupções suspensa + Screen Wake Lock + saída sem confirmação | e2e confirma nenhuma interrupção visível durante a rota; wake lock ativo/liberado | SPIKE-04, TASK-084 | — |
| TASK-086 | RN-14: retomada de estado (`presentationState`, índice gravado a cada navegação) | Recarga simulada confirma retomada no mesmo cartão | TASK-084 | TASK-087 |
| TASK-087 | RT-11: envelhecimento do snapshot (diferença de versão registrada ao abrir) | Snapshot antigo confirma diferença registrada e rematerialização com rede | TASK-082, TASK-084 | TASK-086 |
| TASK-088 | Teste e2e crítico RNF-01: primeiro cartão ≤1s, rede desligada, caches limpos (M-04) | Playwright confirma tempo ≤1s e zero requisições de rede | TASK-084, TASK-085 | — |

### Lote 16 — QA Transversal (Acessibilidade, Responsividade, Segurança Final)

| ID | Tarefa | Critério de aceite | Depende de | Paralelizável-com |
|---|---|---|---|---|
| TASK-089 | Suíte de acessibilidade automatizada (axe/Playwright) — as 21 telas | Suíte roda em CI e falha em qualquer violação crítica | Todas as telas de Fase 1 e Fase 2 | TASK-090, TASK-091 |
| TASK-090 | Testes de responsividade (600px/900px) — telas de layout variável | Snapshots/Playwright confirmam os três layouts em cada tela listada em UX-SPEC §6 | Todas as telas de Fase 1 e Fase 2 | TASK-089, TASK-091 |
| TASK-091 | Consolidação final: headers de segurança + strings proibidas + credencial fora do bundle | CI de release confirma os três checks simultaneamente | TASK-034, TASK-011, TASK-042 | TASK-089, TASK-090 |

> Nota de canário sobre TASK-089/090: ambas dependem "de todas as telas prontas",
> o que é uma dependência ampla por natureza (gate de QA, não uma tela nova). O
> contexto de trabalho real por execução é pequeno (a suíte roda contra o app já
> construído, não relê a decisão de cada tela) — mas a **dependência de
> sequenciamento** é ampla: só disparar depois que os Lotes 6, 8, 9, 10, 11, 14 e
> 15 estiverem `Concluído`.

---

## 5. Riscos de Prazo

| # | Risco | Lote afetado | Mitigação |
|---|---|---|---|
| RP-01 | SPIKE-03 (parser de referência) atrasar por ambiguidade real maior que a esperada | Lote 1 e todo o Módulo 2 (Lotes 14–15 dependem de TASK-010) | Rodar SPIKE-03 o mais cedo possível, em paralelo ao Lote 0; se ultrapassar 2 dias sem convergir, escalar ao Coordenador via `BLOCKERS.md` antes de estimar TASK-010 com confiança |
| RP-02 | RT-03 do SDD.md (acervo editorial não pronto): TASK-013/014/015 e todo o Lote 9 (Plano) ficam bloqueados por conteúdo, não por código | Lote 2, Lote 9 | Fixtures de conteúdo sintético permitem implementar e testar sem esperar o acervo real; troca de fixture por acervo real é tarefa de configuração, não de código |
| RP-03 | TASK-065 (wiring de telemetria) e TASK-089/090 (QA transversal) são os itens de maior superfície de dependência do documento — atraso em qualquer tela de Fase 1 atrasa esses fechamentos | Lote 12, Lote 16 | Não são bloqueantes de lote anterior: podem começar parcialmente (por feature já `Concluída`) em vez de esperar o fechamento total |
| RP-04 | RT-01/RT-02 (expurgo de storage no iOS; cobertura desigual de push) são riscos de produto, não só de prazo — SPIKE-01/SPIKE-02 podem revelar que a mitigação de arquitetura não é suficiente | Lote 11, Lote 13, Lote 15 | Se o spike confirmar severidade maior que a prevista no SDD.md §6.1, é lacuna estrutural — sinalizar ao Coordenador/Gestor, não decidir sozinho um novo desenho |
| RP-05 | Backend (Lote 5) é pré-requisito de praticamente todo o resto do documento — qualquer atraso nele se propaga para Identidade, Engajamento, Telemetria e Exclusão | Lote 5 | Priorizar TASK-027 (bootstrap) no primeiro dia útil de implementação; as 5 migrations paralelizáveis (028–032) podem ser divididas entre múltiplas instâncias do Executor no mesmo lote |

## 6. Lacunas Sinalizadas

| # | Lacuna | Tratamento nesta decomposição | Precisa de decisão? |
|---|---|---|---|
| LS-01 | L-01 do UX-SPEC.md (insumo de 5 notas-exemplo do stakeholder não existe) | TASK-014/TASK-036/TASK-044 usam a definição normativa de RN-04/RN-13 e fixtures sintéticas; se as notas reais revelarem estrutura interna (subtítulos, listas, citação), `NotaDeContexto` (TASK-044) precisa de tarefa de revisão adicional, não coberta aqui | Sim — insumo do stakeholder, sinalizado ao Gestor |
| LS-02 | Grupamentos `[insep.]` do Lote 5 (TASK-027, TASK-030, TASK-031) juntam duas tabelas numa única migration | Justificativa: `profile`+`consent` são o bootstrap mínimo de identidade (uma não existe sem a outra no primeiro commit); `push_subscription`+`reminder_log` são o bootstrap mínimo de engajamento; `analytics_event`+`analytics_daily_aggregate` são ligadas pela mesma função de incremento privilegiada — separar criaria uma migration que não compila isolada | Não — decisão de detalhe, documentada aqui |
| LS-03 | Nenhuma lacuna estrutural do SDD.md ou do UX-SPEC.md foi encontrada durante a decomposição — a arquitetura e a experiência já resolveram os 10 conflitos (C-01 a C-10) e as 6 lacunas de detalhe (L-02 a L-06) do UX-SPEC.md §7 antes desta rodada | Nenhuma ação adicional necessária | Não |
| LS-04 | O canário de ~300k tokens: nenhuma tarefa deste documento excede a estimativa de contexto necessário, exceto a nota explícita em TASK-065 (Seção 4) — nenhuma outra tarefa toca mais de 2–3 arquivos/módulos existentes simultaneamente | Autocheck já aplicado tarefa a tarefa durante a decomposição (divisões como TASK-084/085/086/087/088 dentro do modo apresentação, e TASK-027–034 no schema, resultaram diretamente desse autocheck: a primeira tentativa de desenhar "Modo apresentação" como 2 tarefas e "Schema" como 3 migrations agrupadas foi descartada por ultrapassar o tamanho-alvo de 1 dia-pessoa e a regra de não-mistura de SQL) | Não |

---

## Resumo de Rastreabilidade

- **91 tarefas de implementação** (TASK-001 a TASK-091) + **4 spikes técnicos**
  (SPIKE-01 a SPIKE-04), organizadas em **17 lotes** (Lote 0 a Lote 16).
- Todo componente da Seção 2.2 do `SDD.md` tem ao menos uma tarefa; toda tela da
  Seção 1.0 do `UX-SPEC.md` (T-01 a T-21) tem ao menos uma tarefa própria; toda
  tabela da Seção 5.3 do `SDD.md` tem migration própria (ou par inseparável
  documentado); todo ADR tem ao menos uma tarefa que o implementa ou verifica.
- Fase 1 = Lotes 0–13 (79 tarefas); Fase 2 = Lotes 14–15 (20 tarefas); Lote 16 é
  transversal às duas fases.
